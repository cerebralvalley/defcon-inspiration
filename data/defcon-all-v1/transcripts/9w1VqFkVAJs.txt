- Welcome to the five o'clock track three. Unfortunately, Orange Tsai cannot be here today. However, we are going to try to stream, I'm sorry, play his presentation for you guys. Alright, enjoy. - Hi, sorry, I couldn't be on the stage because of the issue of the visa. I hope one day we can meet face to face in Vegas. About 10 years ago, I failed my algorithm course at university, but I'm now here to talk about a Hash Table. This is a big achievement for me. I'm Orange and today my topic is, Let's Dance in the Cache, Destabilizing Hash Table on Microsoft IIS. Before the talk, I would like to give you a short case, if there is the super secret area, creator is protected by a super secure password. You cannot login, only if you know this long password. However, under the design of IIS, all of this passwords are valid and can log into the system. Amazing, right? I guess you might have several questions in your mind. This is one of the attacks I will present today, and you will learn the details later. Hi, I'm Orange and now the principal security researcher at DEVCORE. I am a zero day researcher and focusing on web and vacation security. My job is to discover the most severe bugs, in the tech service, that can impact the world ahead of the bad guys. My research has also received several awards, such as the best server-side bug of the Pwine awards, and the champion of Pwn2Own. If you are interested, you are welcome to follow my Twitter or chat with me later. This is the outline today. First we will introduce several essential concepts. Those concepts are important to let you into Microsoft IIS internals and our topic today, after having a good understanding of IIS internal and a cache mechanism, we will elaborate on our research, followed by three different types of vulnerabilities. Also, we will demonstrate how we apply a text to Microsoft exchange server, as a showcase. Of course, we will provide mitigations, recommendations and future works to those components. So first one is the hash table. Hash table is the most fundamental data structure in computer science. And it's purposes is to store data. No matter what the data is, hash table can hold a data in memory very well. Hash table is especially good at handling the key value pair and can operate data with the high performance. Due to those benefits hash table is used extensively in computer infrastructures, such as the operating system, programming language, database or web server. Here is an example of the hash table usage in programming language. Programming language uses the hash table widely, however you don't know because they write the hash table into a more high level structure such as the array, dictionary or object. So if you are a developer, you must have used hash table. As the internal, I assume that everyone here is a good student at university, so I skipped the hash table internal this time. As for what is the hash-flooding attack. Also, we are not going to talk about this attack only. We still give it a slot because I believe that the hash-flooding attack is the best case to learn the mindset of how an attacker abuses the hash table. The idea of hash-flooding attack is to drop all the records into the same bucket, since the hash algorithm is public and attacker can predict its hash and make all records for it into the same bucket. At the time, the attacker degenerate the hash table, which can store numbers, records, into a single link list. For example, the attacker has crafted several malicious records on the left side. The hash function in the middle will process and convert the record into an index. The first record is convert to the index four and put into the corresponding bucket. The second is also put into the same bucket. The third, the fourth, the fifth, and the attacker can drop all records into the same bucket and the table now becomes a single link list. So the hash-flooding attack can make the table always sit on the worst case. That means for inserting and records, the time complexity comes to n squared. We should all agree that n squared is in a terrible performance. Okay, we have a good understanding of the hash table, let's discuss the relationship between the hash table and IIS. First, IIS loves hash table. As the well known and only web server in windows, IIS uses the hash table extensively in it's architecture to store information such as HTTP headers, server variables, configuration, and cache. Due to the massive use of this data structure, Microsoft has designed its own hash table implementation from a very early stage. The following two are older implementation that will be used in IIS. One is called Tree Hash Table, and the other is called LKR Hash Table. The tree hash table should be the most common, like the simple code in your textbooks and use the linked list to resolve the collision and rehash the table, when it's in an unhealthy state. The rehash is a series of reviewing process. It first enlarges the table, recovers all records and reposition them to their new index. As for what hash function is used, we will have a future discussion later. The other table is the LKR hash. An implementation that aims to build a scalable and high concurrent hash table in a multicore environment. The name LKR is an acronym for the name of the creators. This implementation was also patented by Microsoft in 1999. The LKRHash is the successor of linear hashing, which is used another better algorithm to resolve the expansive rehash, to enhance the performance. The creators put a lot of effort to make this implementation portable, flexible, and usable. The application can define its own table related functions to adapt to multiple products across Microsoft. It's interesting to note that part of the creators are also the IIS developer, which should be the reason why we find less of this implementation in IIS. Now we have a complex understanding of the hash table, it's time to go deep into our research. In this section, we will show the IIS internals and define the scope we really care about. We will first explain our mindset and idea to uncover bugs. We are mainly focusing on the hash table implementation and its usage. Since most of the hash tables that are used in IIS are cache related, we also study and look into it's mechanism. Because Microsoft has design its own hash table without open source. The implementation should be an explored area to discover bugs. So in this context we find, not only for memory corruption, but also logic bugs. Here I would like to highlight the bug CVE-2006-3017, it's a logic bug in the unset function of the PHP. The hash table implementation didn't distinguish the key type of an array so that an attacker can delete arbitrary elements with an index number. When I saw this bug it amazed me and I believe this is just the bug I'm looking for. Of course, we also look for the algorithm complexity problems, such as the hash-flooding attack. However, since it's already a 20 years old attack, most of the lower hanging fruit should be disappeared. So in this context, we put more effort into the implementation. As for the usage, we mentioned that the LKRHash is decided to be a portable, flexible, and easy to use algorithm that can be applied to any situation. The convention requires the application to configure its own table related function during initialization. This kind of extensibility gives us more opportunities When I saw this, I was curious that is the particular function good? Is the key calculation good? Or how does the function select the correct record when a collision happens? These types of questions all interested me. So in this context, we care more about the relationship between the record, the key and the function. When an HTTP request is coming, the kernel HTTP assist will first handle the connection and dispatch the request. The kernel checks, if there is an active IIS worker, if not it knocks the IIS service to spawn a new process. The WAS, Windows Activation Service, first raise the configuration to note several specific caches for the process initialization and spawn the worker process. Several IIS components are loaded here. The worker then loads their modules, which are defined in the configuration. IIS, by default, loads several modules for different purposes, such as the compression, redirection, authentication and the cache. Among all modules, the following four are the global cache provider to manage the cache between modules. Each module is responsible for handling a different cache, such as the cache for static file, the cache for configuration and the cache for windows token. Once the modules are loaded, the worker enters the request life cycle. The life cycle is an event loop, that process all the notifications from the request. There are several events in a life cycle, the module can subscribe to its interesting events to compare it's logic. There are also global events that the module can subscribe to. For example, the cache provider subscribes to the event cache clean up and the cache operation So our research is not only focusing on the cache between the request life cycle, but also the global cache providers, especially those four, our golden targets. The following section is about our research and the bugs. In this section, we will first elaborate on our idea and the direction we are trying to fight bugs. We will attach three different types of vulnerabilities to prove our idea works. Our bugs include a hash flooding DoS, a cache poisoning, and authentication bypass. We will also talk about the exploitation of how we maximize the severity to make this bugs impactful. Of course, a real world authentication bypass on Microsoft exchange server will be detailed and damage checked. Okay, the first case we would like to share is a hash flooding DoS. First, we would like to give you a spoiler that all hash tables implemented by Microsoft are affected by hash flooding attack. The tree hash table is vulnerable by default and the LKR hash is vulnerable only, if a poor hash function is configured, However, vulnerable is not equal to exploitable. Besides finding the entry point, we still have several obstacles to overcome. We find that URI cache module seems to be a good target. The purpose of this module is to cache configuration for URLs because rate configuration is costly, so cache is the best strategy. In this module, every HTTP access triggers the cache operation and the most important thing is that the module uses tree hash table as its cache storage. Well an accessible by default tree hash table sounds good to us. This is the time chart of every thousand new records in tree hash table. Because it's not open sourced, we must reverse the structure and code the table dynamically to get the data set. The line in blue is the time of inserting random records and the line in orange is the records with collisions. You should be able to figure out there's the huge difference between the two lines. However, the jitters along 35,000 and 75,000 are weird. What's the jitter? Theoretically, the lines growth in orange should be in linear time. So what actually makes the time increase unsmoothly? The answer is rehash. This is the part of the insert operation. The implementation first traverse the linked list to ensure no duplicates and do it's internal job. However, after the internal add operation, there is a code to the function, rehash table is needed. The function checks the number of records and rehash the table if the number is greater than two times of the threshold, The threshold is selected from a list of prime numbers and once rehash it, the function enlarges the table by the next prime number and remaps all records to their corresponding bucket. This is a huge and expansive operation and that's the reason why the chart increased unsmoothly. To exploit that, there are still several questions to be solved. For example, how much of the hash key we can control? Or how to construct the collision padlock? There are several elements in the cache key, and the only element we can control is the URL pass, which reflected at the end of the cache key. The implementation will first convert all the text to uppercase and send a key to the hash function. simply multiplies each byte of the cache key by 101 and just adds them up. The function also uses an error to scramble the result. is this hash function good? This is the answer from Alech and Zeri in their awesome talk at 28c3. This multiply then add method is a variant of DJB hash. And this reason algorithm has been proven that collidable by equivalent sub strings. When two keys have the same hash such as the PS and Q2, they must be equal no matter what you add before or after that. For example, after you add the letter A, the hash of PSA and the Q2A are still equal. By knowing this feature it's not difficult to understand that two equivalent sub strings can be combined to form different case, such as the PSPS, PSQ2, Q2PS and Q2Q2, they all share the same hash. Since you can repeat the string many times, constructing the payload is easy. You'll just need to find a few strings with the same hash and arrange them in combinations. So all of these share the same hash value, this is handy for our exploitation. However, there is a fatal floor in this attack, that it's too weak. To get a notice about slowdown, you must have sent about 35,000 requests at least. For our one request at one record exploit, it's too slow. And the (indistinct) is that there is a cache scavenger to recycle records regularly. The scavenger is a thread to delete unused records every 30 seconds. Those two obstacles makes our attack not so practical. To overcome this, we have to dive into the implementation more. During our investigation, we find an interesting behavior that can rescue our attack. The implementation will scan the key recursively. It trace all sub directories as new records and adds all of them to the table. For example, in our imagination, the URL will only trigger one search and one insert operation. However, each sub directory will also be checked as a new record so that the URL will trigger multiple insert operations. What's more interesting is that all new keys will be packed with the newer record. The scavenger is nice to the null records and only they list them one by one, instead of killing all of them in a batch. So the only question left is, How to construct the collision in this new context? Because all sub directories are traded as in independent keys, we have to construct the payload that satisfied all following equations. It may be a little hard to imagine how to make this happen, so it's our previous equivalent feature. But if we make all the results to be zero, then this comes much easier. Zero multiply anything is zero and zero plus zero is also zero. So we can satisfy all the equations, if we match the hash of each sub directory to zero. So we can prepare a set of zero hashes and amplify the attack 10 times, at least, by a slight modification. The result is that we can make a server unresponsive with about 30 connections per second. This is not an unreasonable number because IIS can handle sources of connections easily and concurrent. Because this bot affect the windows by default, we also award a good amount of bounty. Let's check out the demo. We first double check and ensure the server has eight cores and 32 gigabytes RAM. The window in the lower left is the script that monitors the status of the IIS, it's a while loop and checks the server every second. Okay, we first run the random test. We send random test to the IIS. You can see the loading is less than 10%. Okay, we now want the collision mode. As you can see the CPU is now under high loading. Okay, 100% CPU load. Okay, as you can see the monitor enters the time out, time out loop. Here, we speed a little bit up. Okay, as you can see the server is unavailable and can't handle any request anymore. Okay, the second case is a cache poisoning attack. There are two types of response cache in IIS, one is the static cache support by the Kernel, such as the picture, the CSS and the JavaScript resources. The other is dynamic cache, which is usually used to cache responses that rarely change. For example, the product information of an online shop or the news announcement of SDN. The benefit of the dynamic cache is that you can reduce the number of the database assets. Dynamic cache is handled by the HTTP cache module, and you can configure the cache by the output caching component. To use the cache, you must first set out your own rule. The rule can be based on several conductions such as the file extension, the correlation or the HTTP behavior. Here, we set out a rule for the extension of ASPX and cache the result based on the ID. So the root cause of the cache poisoning is that, the module used a bad parser to handle the query string. But then that you have already set out a rule to cache a specific parameter, an inconsistency between the module and the backend may lead IIS to cache the wrong result. In the real world, the most common backend is ASP.net. And a simple HTTP parameter pollution can rule them all. So the key duplicates in the query string, the module only uses the first occurrence of the cache key. However, the APS.net concatenate all together. These inconsistent positive behaviors cause the IIS to cache the wrong response. if a patch just prints out your name based on the ID, the attacker can poison the result by repeating the ID on the URL. The IIS only recognize the orange of the cache key. However, the ASP.net will concatenate both values and show them up. So the result is that the next time the user answers the patch he got hacked. Okay, our last task is the authentication bypass. Back to our opening, a super secret area, which is protected by a super strong password, under the design of the IIS. All of this passwords are valid. and be thinking what's the root cause? Or how do I get those passwords? Or suspect this must be a hash case, what kind of the scenario is vulnerable? First logon is an expensive operation. To not reduce the performance, IIS caches all tokens for password based authentications by default. The implementation uses the LKRHash instead of the tree hash table and configures a scavenger to delete and use the record every 15 minutes. As we mentioned, LKRHash is highly customized. During initialization, the module define several functions, such as the hash function, the logic of how to extracting the key from the record and how to decide which one is the correct record when the collision happened. And this is the hash function defined by the token cache module. It simply uses the DJB to hash the username and the password, and then makes them together by XO. And this is the function used to decide which one is the correct record when a collision happens. The function first checks whether the local method of both record are equals or not and then compare the username and compare the username again. You may be wondering, why did this function compare the user name twice? I guess the original intent of IIS was to compare the password, however, the developer copy and paste the code, but forgot to replace the name to the password. It's the big fail. The failure could add an inconsistency between the hash calculation and the cache comparison. The hash calculation involve both the username and the password. However, when a collision occurs, the table only compare the username to get the correct entry. Since we can adjust the field of the password to change the hash, if the hash hits a record that is already in the table, the module will return that to it's directory. you can reuse another users log in token with random passwords. However, they are still a few prerequisites. First, each password attempt only has the success rate of 42 billion because the hash is a 32 bit integer. The other prerequisite is that, there must be a successful login before our attack, but once the log in is done and the token is cached in the memory, you have unlimited attacks during the 15 minutes time window. Both of the above prerequisites makes this bug unusable. It is just like playing the lottery in the space of the 32 bit integer. So to make this bug a more severe vulnerability, we have developed several ways to win the lottery. The first enhancement is to increase the probability of the collision. A vulnerability that requires user interaction is too late. So for the second, we must find a way to exploit without user interaction. The last is to defeat the restriction of the 15 minutes time window, we figure out a way to cache the token forever. Okay, the first is to increase the probability. As we mentioned that the four billion password records are a ridiculous number. The LKRhash even use an LCG to make the result more random, but just because of this LCG we can lower the key space because the LCG is not one to one mapping under the key space of 32 bit integer. So there must be results that will never appear. So we can precompute a dictionary that excludes the password whos hash is not in the results. This development can reduce a number of the key space. Also, there are still billions of password records, we have increased our success rate by 13% at the least. The second enhancement is to regain the initiative. There's a feature called "Connect As" that is usually used in the virtual hosting because the vendor has to separate IIS process for their customers. With this feature, each IIS process can be under a different user context. Under the feature, IIS will auto logon the user you specify while spawning the new process. This token will also be cached, that means we can reuse the customer's identity and no longer to wait for user interaction. In this case, we have regained the initiative. To prove it works, we did a test in our lab environment. Windows server can handle about 1800 login attempts per second. Because every attack cost nothing, you can run this all day and the success spread is about 4.2%. This is already higher than the SSR in your Gacha games. And you can also run it for longer, five days for 20%, 12 days for 50% and the success rate of running for 24 days can get 100%. We have reduced in our lab environment and can get a password in about five days. The last enhancement is to defeat the time window. Our idea is simple, in modern software architecture it's common to see the patent of a background daemon, monitors the system's health or several cron-jobs assess the internal APIs regularly. So we can assume a situation that a credential is attached with the cron-job and the gap between each access is less than 15 minutes. In this assumption, the token will be cached forever. Okay, I know it's very ideal, Is there any real case? Sure, let's talk about the exchange server again. There is a service called active monitoring, which is enabled by default and responsible for monitoring all services, HX to OWA and active sync service every 10 minutes with the test credential. That's for the health check, the token of the credential will be cached forever. So you can try as many as you like until you get a successful login and the password is also usable to log into the OWA. The account for the health check has its own mailbox too, this is useful for further exploitations, such as phishing or training another post office RCE together, that's our last case. Let's talk about the mitigations and future works. For the design of the hash table, it's recommended to use the pseudo random function such as the SipHash or HighwayHash that can reduce the collision and make the attack more custom. For the design of the cache, inconsistency is still the key, just like our case today, the cache poisoning due to the different password behaviors and those indication bypass due to the unkeyed password. Since the hash-flooding attack is by the side attack, there are several workarounds to mitigate the problem such as the input size limitation or a state to randomize the hash. Each solution has its limitation, so the last recommendation is to learn attack and defense from the history. There are still several cool future works in my mind, such as using timeless timing attack to reduce the key space. However, since I am lazy, welcome to pick them up if you are interested, this is the end of my presentation, if you have any questions, here is my contact information. Thank you again for being here, thanks.
