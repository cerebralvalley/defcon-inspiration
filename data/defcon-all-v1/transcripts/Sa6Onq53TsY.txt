Hello everyone, thanks for coming. I am very honored to be here to give a presentation. So today our topic is found observant the shan s of democracy in Kafka. in Kafka. First of all let me introduce my team. We are security engineers from Alibaba cloud and I'm Zangi. Then let me introduce the agenda. First I will introduce something about Kafka. Then we compare previous research with our new findings. Next discuss the journey of hunting bus in Kafka ecologic. After that we will uncover the sham abilities in Kafka broker and finally explore the inspe and differences. differences. Okay let's officially begin our presentation. presentation. So what's Kafka? According to the official documentation, Apachi Kafka is an open-source distributed event streaming platform widely adapted by the enterprises for building high performance data pipelines, real time analysis, data integration systems and critical application architectures. At its core, Kafkar has three have three main plants producers, consumers and the Kafka cluster. Consumers are like the data sources. They send messages into the system. Consumers read those messages and add on them like analyzing or storing data. And Kafka cluster is a backbone. She handles messages storage reputations and ensure everything runs smoothly at scale. Inside the Kafka cluster, the functions are steed into two main plus. the data prim and the control p. The control p is in charge of ch managing all the metadata like topics, brokers and reputation. reputation. Meanwhile, the data is where the real action happens is handled by the brokers which are responsible for reading from and writing to cafka topics. In the control plane of cafka, there are two different implementations of the kiss and civil protocol. The older one based on cuff car and the newer one known as K raft control. As for the data pre request fall into two capies produce requestes and f requestes produce request is used to write a batch of data to a specific topic. A fetch request on the other hand is used to write data from cafka topics. These operations are all limited with the topic make it making making it the central component in Kafka's architecture. architecture. In addition, Kafka has a powerful ecology. The Kafka ecology is built around several crow components. Cafka crow cuff classes streams cafka connect rest procre rest procre and queso. and queso. So are there any security releases? That's a good question. Let's now look at the nonfindings and see how September potentials might be virus in repetitious. repetitious. It is a remote code execution ability in Kafka clients identif. The key is here is that if an attacker can control the conence stream used to connect the connect to cuff car they might be able to trigger GMD injection and that will lead all the way to remote code execution. code execution. She is pro the proof of concept. The program s as a cafka client and uses an attack controlled connection stream to connect the cuff caster. The payload contains an LDAP which is the cable component. In this export as the program it ends our tickering jm cover here on the right. You can clearly see see the code stack leading to the injection point. This is the clover of the ability. the ability. Let's focus on the login contest login process is general central to understanding how the sport was. You will initialize subject and construct JMD login model and analyze with subject callback handler options. And the final level call the f locking method. method. JMD locking model locking requestication method. Then the low cover method of initial method is code. Before we move on with the next item, I would like to briefly introduce the local method here is a common thing like runtime xC. runtime xC. And it will look every untrustive address and lead to you can use a diagram to show how the attack process. First the attacker set an evil JMD server. Second the attack constructs the e connection stream and pass it to the cafka client. Third, the Cafka client passes the connection stream construct the e login config passes it to the login contest and close the login method of locking contest. Then the login contest in the JMD login mod to authenticate the user. Next the JMDI login model looks over the U JMDI server and the server returns something evil payload. Finally the server is taken over. The key point is the controllable login config. Kafka client allows the users to control the login config. So the user can use an evil login config to achieve RC in the version 3.4.1 the renter introduced a back list mechanism and by default it includes JMD locking module locking module in other words the JMDI login module is disabled out of the box. If we we run the P again, we will see the programmer now exception. The error message clearly says that JMDI locking model is not allowed. We can use a diagram to show how the patch work was. When attack construct the e connection stream and passes it to the cafka client, the cafka client will check the login config. If the login config contains a U login module memory JMD login module, the Kafka client will raise an exception and can we bypass. After understanding BR stability and nor how the patch work was, we came up with an idea. What if we find another locking module that can be abused in the similar way? If we can identify a new malicious login module such some much like JMD login module we might be able to achieve RC once again. So what are the registrations on the locking modules that we are looking for? First improvement locking module. Second is this in popular Java lips. Third can take RC attribute f right attribute fry read. Thanks to this idea we found several interesting login modules. The first one is poly locking module provided by J boss. As the name suggests it delegates an author authentication ask to another agent or module. Specifically in this in analyze method it constructs an agent based on the specified M locking module name and then analyze it during the login phrase. The PY login module passes the assertification process to the agent. Now you might has already guessed what where I am going with this. Yes, we can use process locking module to bypass the back list. Here is the perk. We configure the login post login module and set is login name to JMDI login method. method. Even though is on the back list. When we run the pro, you can see that the login method of a JMDI login model is actually invoked. invoked. If we check the evil jmd server, we can find that we have a bypass the back list and a tri again. Just how the bypass work was posing module delegates to JMD login module allowing us to be bypass the back list and tickle JMDI injection leading to us here. Another interesting state of lo modules is provided by web sphere. Due to time constraints due due to time constraints I won't be a I won't go into full details but just the proof of concept. Imagine a web server deployed on real sphere that allow users to control cafka connection stream. stream. Could we take over? Of course we can. The idea is simple. We prepare a cons consu consum login configuration file. Then we send a first payload to instruct the web server to use our login config. After that we send a second payload which will trigger an error. As a result, the server writes the the error message now containing malicious content to the file we specified. Finally, we will get a rep shell and a tri game. tri game. In addition, there's an interesting login module named the LDAP login module. It's built into the JDK. is login method call terror authentication method. When skating the user the login model will first obtain name and password we are call back handler. Then you will check whether the password is blank. If no blank it will utilize the JMDI LDAP lock contest which is a tiger JMDI injection brief briefly. If we can find a call break handler that can provide a password we can trigger JMDI injection and achieve RC. So our goal is to find a callback handler for the LDAP login module. There are three registrations on the callback handles that we are looking for. First, you should be able to handle name callback and password callback otherwise you will receive unsupportive callback exception. callback exception. Second, you should be able to obtain a password which is not blank. Most importantly, it should be improvement authenticated callback handler which is an interface provided by cuff cover otherwise we will receive a class cluster exception. cluster exception. Be honest is really hard to sh all three requirements at the same time especially the last one which greatly limited the range. range. So which ranks implemented the interfaces of Kafka? After some revenu stationation we found that confl is a great candidate. Confront is the creators of Kafka and the co custodians of Kafka and Confront has their release versions. Conference primarily offers two versions. The community versions of Kafka normally as CP cafka and the commercial version called CP server server as a strong sheer dability exist in CP server but not in CP cafka. We test this on November 2024 or on version 7.7.1 7.7.1 and it was confirmed to be support for this this issues has seem been fixed in the new versions. We discovered a callback handle named filebased dynamic blank callback handle in conference caf which read the username and password from the local file. Here is a simplified code of fire callback handler. We can control the file name the keys of name and password. For example, if there is a fry named the user pawn confer contains one or more username, password, pair, separative private uh s semis con. This correct handle means almost the resistance but the file named user pawn conf is created by us. Can we find more one more common one? common one? Of course the etc password fier fits our needs perfectly. This means we no we no longer need to upload a consumer user point conf. We just need to set a file name to etc password and the key to an existing user for example root. She is the poke. In the end, we can use the use this callback handover to trigger JMDI injection and achieve RC as shown in the diagram. Here's how the entire attack processed. In the third step, we can use the the firebased dynamic blank login callback handler to bypass the login config track and uh this callback handler will obtain the username and password to the LDAP login module. login module. And finally, the LDAP login module will look and JMDI server and finally will be taken over. As you can see on this slide, we are now entering a new phase of our research. Not just sporting a single mobility, but hunting flow blocks around the entire Kafka ecologic. Kafka ecologic. In the previous chapter, we successfully trigger JMDI injection in the C Kafka clients of the commercial version of Kafka. Kafka. Can this be impat 25194? Let's take it step by step. First, can cafka connect be exported? After our testing, the answer is yes, it can be supported. You can follow the official conference caf documentations to install and set up a conference platform. platform. Is a proof of concept is pro CV 2023 in c apache cafka connectet. is our new pro for confet. We just need to convert it into approach format and then we replaced it with the previously mentioned by plus payload and you will finally trigger JMD injection and achieve as game something else. something else. Yes, let's start a journey to take over confl confl server. server. When we deploy the confluent platform, there are many components in the website. We can see three obser components. When I try to use it, I find the queso DB is something like DBMS. Since DBMS also has a lot of features, just choose choose this to analyze. And then I read documents to know about this. this. And when I read the documents, I find a interesting issue in the GitHub. It says the producer is closer to wait a moment. What does the producer exist here? exist here? And I just try to try it out in our local environment. local environment. We can see the producer configure in logs. logs. What does this mean? Through the insert operation queso DB can produce data. Can we modify the producer config? We can see that the configuration is obtained from session config. So what is the session config since there is no other place to set it our shar? Let's try and let's give a simple try unexpectedly. We successfully modified the configuration and then we modify it to use our pro and we finally r sure let's watch the video demo. Okay, let's do a brief summary. Using the bypass technology we successfully bypass confer bypass confer and achieve on queso DB we have taken over the conf products of multiplier cloud ranks. So if we can we has a attack control cafka client configuration and using the bypass tech bypass tech we can achieve RC. So next let's uncover hidden vulnerabilities in Kafka broker and in previous section and we already have a basic understanding of the Kafka ecosystem and it is clear that the Kafka broker is the core component of the Kafka ecosystem Kafka ecosystem and also prior to this Kafka broker had no RC vulnerability and so if someone can find a vulnerability in such a critical and unbreakable system and it is definitely something worth celebrating celebrating and so you may be curious about how I found this vulnerability and to be honest just by accident so initially I didn't ever think that I could achieve RC in capa broker and as mentioned earlier I was actually focusing on finding other issues or potential bypass method in the capka client and at that time I was reading the official document looking for some suspicious configuration configuration and we read the document I find a very interesting feature and that is the listeners can be added or removed dynamically dynamically and also what's more the GS configuration of the listener can also be set dynamically so this means that the Kafka broker also supports JS and here let's take some time to introduce this interesting feature so first this is a very normal request and the user Just send a add listener request to the Kafka broker and the broker receive the request and add a new listener and after that the other Kafka client can connect to this new listener for producing or consuming message and but if there is a hacker and he can just send an evil request and the broker will receive the ad listener request and try to add a new listener. However, the server will also trigger the GS process and we are send the agenda request to the Evo Gendia server and finally achieve. achieve. So first let's set up the exploit environment and we can download the binary from the official website and then run below command to start the Kafka server. And now let's exploit it step by step. So first we can use the Kafka config tool to retrieve the broker's configuration. As you can see here there are a lot of configuration and here we should pay special attention to the broker ID and here it is one and here we only need to focus on the configurations related to listener. So we use grab command and you can see that now it has two port and the one is the standard 9092 port and the protocol is PL test and the other is managed by craft and called controller listener and the port is 9093 and then we can use net state command to check the locally listener ports and confirm the existence of these two ports and so now we will try to add a new listener and we just copy the previous configuration and add a new listener listen on the 91 94 port and as you can see that after we send this request and the Kafka server will receive the request and update it configuration and we can use the nested command to check the newly added ports and you may have noticed that the newly added listener configuration is SAS plant. So what is that? And it is essentially the communication protocol for brokers. And there are different types such as PL text and it is an encrypted and SSL and it is authenticate and also SSL. SSL is a encrypted protocol and but in fact after we execute the command to add a new listener essentially when we specify the new listener use the SAS SL protocol and it will throw an exception on the broker set and if we look carefully the error message we can see that it indicates the exist the absence of J simulated configuration and this is because that tcast authentication mechanism is based on GS and therefore when we add listeners that requests authentication and the system will look for corresponding GS configuration and if we properly configure the settings and we can trigger the GS process and finally achieve RCE. achieve RCE. But the question is how can we set the EVO JS configuration and after reviewing the cafa document again and I just found the answer. We can config it through the configuration key and you can see the key is listener name listener name and SASL mechanism SASL.gI GS config and so now we can we need to set the corresponding configuration and since by default the SASL mechanism is G SS API so we can name it in this listener lame dot SL plan this is our listen name and GSS API this is our SASL mechanism and SAG config and so we already succeeded in one shoot one shoot and but it didn't work and it threw a new exception but not bad since we at least we successfully configured our JS and but said not equal solution and code equal clues and it just review the code and you can see that this is our error message and so here you can see we need to set either the GSS server name or the config service name and here I choose to set this config service name and you that get from our configuration slame. So here we need to add this parameter. So now you can see that we just add this configuration key and we can add a test and finally you can see that we receive the LD request. So yeah we finally made it and there exist another solution. So we can simply change our SAS SL mechanism to play and don't forget to modify the name and we can also receive the out request and since now all we can just utilize the server send a GDI request and the only thing we need to do is deploying an evil geni server and then waiting for the shell. So now I have discovered the first ever RC vulnerability in KKA broker and no one has thought of it in this way and if you look carefully and we just exploit it in a old version of Kapka and so at that time I just wonder that this exploit also works in the latest version and then I just to exploit in the version 3.4.0 0 and the exact version fixed the CV 2023 and 25194 and but when I try to exploit it and but unfortunately it just threw an exception and has the same exception as the Kafka clan and I was very confused I don't know why the patch method related to the Kafka clan also affects Kafka server and I reviewed the source code and finally I got the answer and the reason is that you can say the Kafka client use a load clan net function to load the GS configuration and it will finally call the load function with specify we are client and however our Kcasta server will also use the load server contest function as it will also call the load function in the end and will specify our type in server and but our cut patch method is adding a restriction in the load function. So this means that it will influence both the server side and the client side. So Kafka just accidentally fixed a crucial annoy server side vulnerability. So what the hell? hell? But since the fixed way is the same as the client and if you remember in the previous section and we have a bypass method in the confluent version and so does this mean that we can bypass and achieve RCA in the latest version of Confluent Kafka broker and so first we also need to set up the environment and just follow the guide. And now we need to add a port mapping to help with our testing. And then we can use the Kafka config tool to quickly verify if our server runs normally. And here we first just want to test with our previous code. Just want to verify whether the confluence broker has patched the vulnerability. And we started by fetching the current listener configuration. And this is now and slightly different with previous one. and then slightly modify the previous poke. But however it throws an exception indicating the absence of the security protocol not defined and uh I just find the reason and the reason is that we missed this configuration and you can see that the security protocol of each listeners must be defined in this configuration and in our previous Kafka broker vulnerability and since by default it has already defined this this map and You can see it has the SS SL plan test. So now so we we we will not encounter the error but however here this is our now map and you can see it only had controller plan test plan test hold don't have the SL plan test. So this is why we field so we need to add this parameter. And now you can see that we just modify the listener security protocol map and we add this mapping. And now we can use the listeners as a as our plan test. You can see that yes and we can trigger the patch logic. So now we need to just bypass it using our previous method. And just looking back at our previous method and you can see that the the key of the bypass method is SSL.loc callback.handle.class callback.handle.class parameter. parameter. But now here is a question. How can we set the call back handler on the broker side? So after reviewing the official document again and I discovered that it can also be configured dynamically and the format of the key is similar to the key format used by the listener and called listener.name dot listener.name listener name dos SL.log callback.handerclass handle the class and now we can just edit the callback handler and the name is listener name SSL.plats.lo.comclass and we modify the GS configuration to our previous bypass method and finally we can also receive the junia request. So this means we successfully bypass it. And now here comes to the video. And so first we just run our EVO GDI server and we get the listeners configuration. configuration. And we just run our so first we started the listening port waiting for the share and we run our exploit payload and update configuration. update configuration and finally we receive the shell and you can see that we are inside our container and this is the confluent Kafka broker. So after that we promptly reported our findings to the Kafka official team and finally Kafka has acknowledged this vulnerability and this is the first ever RC vulnerability affecting Kafka broker and we also reported this vulnerability to Confluent and we were rewarded the highest ever bug bounting Confluent and also here is the patch and it is very easy to understand and it just gi module and LDAP login module by default and finally is some defense recommendation. recommendation. So first if you still use the affected version of Kafka and please update now and then authentication and authorization must be enabled to prevent unauthorized sides and what's more the component in the Kafka ecosystem are very important and usually don't need to be exposed to public internet and therefore it is best not to expose them publicly. publicly. So, is there any questions? And thanks again for your listening. And if you have any questions, please feel free to contact us.
