Good afternoon, Defcon. Uh, our next talk is up zero trust total bust. Please give a big welcome to our next speakers, Dave and Rich. Hey everyone, thanks for coming to our talk uh about zero trust network access. Um, I'm Dave, this is Rich. Uh we both work at Amberwolf which is a UK based consultancy specializ in red teaming and and bone research. We're covering a lot of bugs in this 45 minutes. Uh but all of the technical details will be on our blog uh within the next few weeks, some of them straight after this talk. So everyone's heard of zero trust. The theory is that no user device or request is inherently trusted. Everything must be continuously evaluated against a set of access controllers. So even if you've got valid creds, you won't get access unless all the criteria are met. There's some terminology that we'll be using in the presentation. If you think of SAS as like the gatekeeper, checking whether the user and device are permitted to access a resource, um acting like a traffic controller. And then you've got steering which is routing traffic based on uh conditions. So like ACL's identity providers feature in this. It's not their fault. And we've included a definition of privacy just as a reference. a reference. So the number one question we got asked after our last presentation was what about Zcala about Zcala and we've all seen adverts similar to this saying that legacy VPN are dead need to move to the cloud zero trust network access. We also routinely see these products on red team engagements. So we thought we'd better take a look at whether they live up to their claims. So a picture of the scene everyone's panicking over the latest Avanti bug and they want to move to ZTNA. So what happened is that people just replaced their VPN with a Z10A product. Um but then just used it just like a VPN. So no granular access controls, just brokering connections into the internal network. And actually even worse, we've seen a lot of this trusted corporate LAN concept used in the ZTNA world. Um so but instead this time um you're sharing an IP space with every other customer of that ZNA product. So we see conditional access policies like this uh where there's an exclusion um for MFA from the ZTNA public IP ranges to reduce authentication friction. Uh and this means that we can just bypass MFA by coming from those ranges. A lot of vendors do have a concept of dedicated IPs. Uh so if you do have to do this then uh maybe look at that. But enough of the preamble. Let's get into some actual bugs. So Checkpoint said an offering is called Harmony. We cracked open the Windows client in DNS Spy immediately saw a reference to an encrypted private key within an upload to SFTP function. Interesting. This function is called whenever a customer chooses to upload log files to the checkpoint support team. team. And that decode resource function you saw um that just decrypts a resource from within the binary with a hard-coded AES key and IV. And the result is your standard OpenSH private key. The log files were also encrypted before being uploaded to the SFTP server, but the encryption key was predictable and brute forceable. The log files contained JWTs that lived for 30 days. So the impact is that any customer that had uploaded logs um was could just be compromised because those JWTs can be used or straight to the uh ZTNA service. um read access to the SFTP file system was removed immediately upon reporting to checkpoint. It's pretty quick. Um we've got some more bugs in this product as well, but they are still in disclosure at this point. When we started this research, we made like a little wish list of uh what are the best bugs we could possibly get uh in all of these products. So, we're going to keep this table updated as we go through the presentation. So next we're going to take a look at Zcaler which is a very popular ZCNA product. U we see this a lot in red teams and we'll start from a completely unauthenticated perspective. So the first thing that happens when you launch a Zcala client is you put in your email address. Soon as you do that the client's going to start making requests to the mobile API. So the first request it makes is uh it's encrypted here. And um we don't have time to go through reversing the exact encryption routine here, but to summarize, essentially it generates a random AS key. It combines that with a hard-coded key and then it encrypts that with RSA and adds it to a token header and then that blob that you see there in the post request is encrypted with AES. So we'll just decrypt that now. So you can see here it sends a device type and a user email and then it uses that to fetch the cloud endpoint host name and then the next request is this get a we just decrypt this uh and so before you put in a password it pulls down this policy uh pre-off and it's called the pre-off configuration for the tenant. So what does a pre-off config contain? So this can include configuration details such as the authentication settings such as whether the tenant is using IDP or it can contain things like the cloud features that are enabled whether using ZPA uh Zcala private access or just ZIA uh and what features in the ZCC client are enabled. So things that we might be able to potentially abuse once we get a shelf for things like privilege escalation. So, this has been fixed now, but the preoth config also used to contain a URL for the pack file. And this is something that we'd commonly see on red teams. So, um they usually look something like this one here with lots of m misconfigurations in. So, it might be things such as like misconfigured wild cards in domains, expired domains, or abusable services like Azure. And what we're looking for here is domains that we can use to bypass inspection and we can use those potentially for C2. So another way that Zcala can be configured without the ZCC client is in this explicit proxy mode. So the idea here is that users traffic is steered to Zcala's cloud proxies either by explicitly setting a proxy in their browser or via a pack file. And what happens here is the user's HTTP requests get pushed through the Zscala cloud proxy. Uh the proxy then checks for a gateway cookie. If no cookie is found, the user is redirected to their IDP to do or the user is then sent back through the proxy and then the proxy checks that gateway cookie and strips that from the request and forwards it. So, it might not be a surprise if we can steal the gateway cookie from a user's device, then we can also authenticate to the Zcala gateway and make our traffic appear as if it's coming from a Zcaler IP. But what about if we don't have a gateway cookie? gateway cookie? Well, unfortunately, we can't show you the next slide cuz Zcala asks us to remove it while they fix it. But the good news is we've got a different bug to show you, a better bug. Uh so to pick up where you left off on the Zcala client flow, if a tenant's configured for Samaloth, the client makes a request to the CL star endpoint. This get request includes the domain parameter, a code challenge which is used for PKC. So this is essentially a client chosen SH 256 hash which the user has the the plain text for and then the server responds with a SL form with a SL request relay state parameter fairly standard SL flow. This is then submitted by the client browser and redirected to the IDP. the IDP. So once the user signed into their identity provider, the SL response and the relay state are posted back to this SFC SSO endpoint. This then responds with a 302 redirect redirecting the client back to the mobile endpoints test.html test.html uh endpoint containing a JWT in the redirected URLs CK parameter. This JWT can then be used to register a device. And that's summarizes a typical SML login flow. But let's take a step back and have a look at that last step again. So the client sends back this SL response to the ZCA service provider. The server should then decode the SL response, extract the user details from the SL assertion and validate that the signature is valid for that assertion uh for the one that's been configured by the admin in the portal. All fairly standard. standard. However, what we found is that the signature on the SL assertion was only checked for presence and it wasn't validated against the IDP's public key. So, we could just sign it with any key we wanted or even one with an invalid signature. So this gives us an orth bypass for any Zcala tenant using SAML for authentication. for authentication. So we reported this to Zcala on the 18th of July and they were very quick fixed it within about 4 hours. Um on Tuesday the 22nd of July uh we tried the pug again and it it worked. Uh so we reported this regression to Zcala uh and they confirmed that they had to roll back the patch due to a more critical issue that was introduced. So this has now been fixed and there's a CV. So it's a CV 2025 54982. Uh and it's fully resolved. Doesn't require any uh interaction by Zcala clients. clients. So we're going to show a demo of this now. now. So what we're going to do is we're going to run our script which is going to forge the SAL assertion. And this contains now we've got our JWT back and we've got our our code challenge which we need to redeem the JWT and then we're going to use our PIC scaler tool which is just going to take that JWT and then register a a fake machine in Zcala. So you can see now in the admin portal, this is our machine that we've registered using that um JWT and you can see the the hardware ID that we chose to register with them. So let's take a look at that wish list again. We can takeick off or off bypass for Zcala. So moving on, another way that um Zcala can be configured is with this authentication mode called device token authentication. So, this isn't enabled by default, but it enables admins to allow um a token to be distributed to machines or mobile devices that use that can use Zcala as the identity provider. This is documented in Zcala's docs. So on Windows, you simply specify the token and user as MSI arguments during install. Uh but the problem here is that it only validates that the token is ver is valid for that tenant. it there's no association with a specific user. So if an attacker or a malicious user is able to extract that token from an endpoint they can then register as a machine with any username any username or simply by changing the username on an existing device they can potentially bypass steering policies by simply changing the username and registering as anyone they want. So where is the device token to stored? Uh so when installing um with the MSI arguments, this actually ends up in the registry. So there's a there's a device token that's stored in the registry in clear text and a username parameter. For mobile devices, this is typically deployed by a MDM config. So we're going to show a demo now uh of device token extraction and replay. So the US this is on the victim's machine. So, we're going to extract that device token from the registry. And then on our attacker VM, we're now going to import that token. So, we're going to pause Zcala. We've then imported those tokens and username. kills edgar. So it picks up that new token. So you see now we're logged in as user one with just a token. Now if we try and access the internet, we can't get to the internet because this user doesn't have internet access. Uh so let's try a different user. So again, we'll suspend it and we'll just go in and change the username in the registry. the registry. >> So we'll try admin one. They've probably got better access. >> Registers as upin one. You can see now we've got the network admin policy and we can access the internet. So we move on to Netscape. We came across this on a red team engagement and it seemed quite architecturally similar to Zcala. Um there's a couple of specific bits of terminology to uh to talk about here. So we've got an or key um and that's uh assigned to your tenant. It's immutable. Uh so it's always going to be the same one. And we've got a user key which is assigned to the user. Um also immutable. So always going to be the same for that user not revocable. Recon again very similar to Zcala. the tenant name sent uh and if the tenants's configured to hand off to another identity provider, a SL request is returned uh and that contains the or key. Um so, you know, we we've already kind of leaked that at that point. Um if they're not configured to use SSO, um then there's a high chance their kind of customer admin portal bit is. Uh so if you go to like customer.gocope.com, there's the same kind of SL request flow. Uh, and again it leaks the or key. So I think we've established at this point that the or key isn't really a secret value. Recon again very similar to Zcalery. You can use this or key to pull parts of the tenant configuration or or the pack file. And this is before you supply a password. Um, password. Um, there's actually more information that's leaked through Netscape than there is through Zcala. uh you can see some of the end points there and you can kind of get the steering config and everything before you've even supplied creds. We're going to go through the flow of a publicly disclosed authentication bypass and net scope uh to set the scene for kind of what comes afterwards. So here we start with our first request exchanging our knowledge of the the tenant name um for a SL request and remember that SL request contains our our key our key as convention. The next step sends the SL request to the identity provider uh and the SL response is received. So it's all fairly normal so far. That sle response is exchanged for a JWT. Again, all fairly standard. Then the or key and the email address are exchanged for a user key. Um, and that as we said that user key is uh just a long immutable string that's that's tied to the user. Uh, but hang on, this request has got no authentication at all. We can just see that we're supplying the or key that we have already leaked uh and an email address. So in actual fact, we can skip that whole SL IDP stage and go straight to pretending that we're whoever we want, providing we know the or key and the valid email address. So we're m lover. lover. The authentication flow then continues by exchanging the or key and that user key for a configuration and finally ends with exchanging that same information for a certificate that's used to set up the tunnel. And remember this is for Netscape's private access solution which in the scenarios we came across was uh equivalent to VPN access to the target network. So we got our exploit working exploited the bug on the red team network access um reported the issue to Netscape point out that it had already been reported fixed and even had a CV advisory. Uh so firstly big props to Sander the wit for being the original discover of this issue. Um, we're a bit confused because if it had been fixed and had a CVE, why were we still exploiting this a year on? Um, but I suppose the important takeaway is that Net Scope's recommendations here are use secure enrollment uh and make sure you use sorry, make sure you use device compliance um so that rogue devices can't connect. So we'll just talk through the fix for that quickly. So uh according to Net Scope's advisory, this issue was first reported to Netscope by the original discoverer on the 18th of April 2024. Uh they released an advisory on August the 26th 2024 and assigned CV 20247401. Uh so this describes a new enrollment method called secure enrollment and this was first introduced in release 11610. So, interestingly, in this advisory that they released, it also states that the issue was exploited in the wild by bug bounty hunters. Uh, so despite a CBN advisory or still running this mode in um February 2025 when they made secure enrollment the default um and even now in August 2025, we're still finding organizations who don't have secure enrollment enabled. So, what about secure enrollment? How does that work? So, this uses a secure enrollment token and this has to be deployed via MDM to users devices before registration. Uh, as a side note, we're not really sure why they didn't go for just validating the JWT that you could get in in the off flow, but we'll go through it now anyway. So, we can see they've added an authorization header here. And this authorization header contains a JWT. And we're still specifying an all key in the URL, but we've now got this sign JWT in the authorization header. So how is this JWT made? It's signed with the secure enrollment token and it contains the UPN and the user key uh along with the org key and um in the JWT as a claims uh which you can see on the right here. As mentioned this JWT is signed using secure enrollment token with HS256. But there's also an all key in the URL. So which one will the server use? The one in the JWT or the one in the URL? There's only one way to find out. That's right. Get parameter wins. The server only checked the JWT assigned correctly for the OR key within the JWT itself, but actually uses the OR key in the URL when it enrolls into the tenant. So if we supply a valid JT for any net scope tenant and then we can tamper with the or key in the URL and begin secure enrollment process for any other tenant. This negates the need to know there's cure enrollment token for the target org as long as you have an enrollment token for an entirely different org and we can specify the target user in sign JWT. We even found leaked enrollment tokens online. So you don't necessarily need to be a net scope customer to exploit this. So what about the fix? We reported this cross tenant bug to Net Scope on the 14th of March 2025. Uh they released a serverside fix in version 126 on May the 12th. This fix now checks that the key in the URL matches the one in the JWT. Unfortunately, there's no CV for this vulnerability. Um, despite Net Scope being a CNA, they don't issue CVS for server side fixes. So, we're going to show a demo of the cross tenant uh exploit now. So attacker is going to run a script and they're going to provide the target tenant details, the uh EPM for the target tenant and the or key for the target tenant which remember can be leaked uh pre uh they're going to provide the or key and the secure token for the source or and then this gets back aert and a branding file. Then on the attackers VM they can then just import this the branding file and the So now we just restart net scope. And now we have MPA access. So that's net scope or bypass completed. So we've seen how secure enrollment can be bypassed. But how should it work? As Rich said, um it's usually kind of rolled out via uh MDM like an intune install argument or SECM or something. The token's then stored in the registry in this location. uh and it's encrypted uh using DP API uh but it's encrypted using uh flag 4 um which equates to equates to local machine which in fact means that any user on the machine can decrypt it uh it does have a little bit of entropy uh and that's hardcoded as this is global entropy string and yeah that really is the the entropy um we'll just show a quick demo just to show you that yeah you can actually uh extract that string as a a lab user. Uh and what you end up with is the the decrypted um enrollment key. That's what an enrollment key looks like. So what does all this mean? Um, if you're a Nexcape customer and you're still using IDP mode, there's still a complete orth bypass for enrollment. Um, in secure enrollment mode, uh, this has been fixed. Um, but if your secure enrollment token is leaked, for example, from a someone compromising your your Intune environment and and getting the install arguments, then you can still authors any user uh, and bypass steering restrictions. Um, and it's still possible if you've got an or key, which is very easy to obtain to to do loads of recon on a target environment. So, we cover the orth bypasses. What if you've already got a shell? If we're up against EDR, we might want to spend as little time as possible on a moniitored endpoint. Uh, and actually a good option is to steal the ZTNA config, move it to our machine, and then we can access the internal network from a non-monitored endpoint. So both Zcaler and Netscape can store their configs as DPAPI encrypted blobs. Only Zcaler actually does this by default. Um but in theory zero trust should stop you from stealing a config. The posture check should fail. Um the hardware ID should mismatch. Uh but as we'll show all these uh checks can be spoofed. So we'll have a quick primer on the Zcala config. Um it's stored in a a file and uh it's encrypted um well it's exorded with a fixed key uh after some kind of convoluted exor shuffle. Um but to dump the config you do require system uh and that means we need at least admin privileges to read it. Um luckily we've got two prives in uh Zcaler as well. Um one of which we reported in 2023 and still isn't fixed. Um Um we could actually do like a whole other talk on uh on the prives. Um but we don't have time to like cover all the technical details uh in this bit. Um but we do have some demos uh to show you in a second um after we've explained how they kind of roughly work. But if we go back to our table here uh we can tick off config theft. So about these privves, all of the ZTNA uh products we looked at shared a very similar architecture. They've got a low privilege system tray uh and a highprivileged service or broker. Um and those two kind of communicate um via IPC that might be TCP socket, a name pipe, RPC. Uh but they all had one thing in common. They failed to properly validate um the calling process. Uh there's some attempts to do this by checking signing or uh pathbased checks. Uh but you can bypass that by injecting into a relevant process. Um and there's also some kind of tamper proof stuff, but we bypass that too. Um but just cuz you can call IPC methods, so it doesn't automatically give you a system shell. Uh but luckily for us, uh every product we looked at had a vulnerable IPC method that we could exploit to to run code system. whether that was directly via um like process starting or via file writing. So I'll show a couple of quick demos. Here's our Netscape LP. Um we can see the attackers using a custom IPC client injects a TCP proxy into a legitimate process to bypass the path signature check. Uh and then after this um the client actually enrolls nachevpn style if you've seen that into a malicious server uh and it gets delivered an MSI update uh and that MSI update gets run as system and ping uh we got system shell. So next up we got Zcala. Um this is version 4.7 which I think is the the latest. Uh again using process injection to to bypass the signer check and then trigger a vulnerable RPC method to load some malicious code. Uh and then once we've got our system shell we can dump the users config as we previously discussed. Um and then finally uh we use uh a script to dump the various source values needed to spoof the hardware ID which will be important later on when we discuss um hardware ID spoofing. So, back to the wish list. We can takeick off Prevesca. And this is starting to look quite scary now, but let's carry on. Cool. So, you've stolen config and you've enrolled. Now, what? So, we mentioned at the start of the talk that zero trust involves verification of the device as well as the user. Uh, and one of the ways this is achieved is through proster checks. Uh so these have been around for decades as in SSLVPNs as well and they're really no different in um in the ZCNA space. Uh so the specific checks that are carried out vary vendor to vendor but um they typically involve the ones we've listed here. Uh in the ZTNA products that we assessed the poster checks were retrieved over HTTP from the ZTNA server before being processed locally via the ZTNA client and then the results returned back to the over HTTP to the server for Net Scope. Bypassing these checks was really simple. We just had to uh put an intercepting proxy between the client and the server and change status equals false to status equals true and we could bypass the posture checks. Whilst this was mildly amusing, in practice having an intercepting proxy between the client and the server is impractical due to things like DTLS and mutual TLS. Uh so we took a different approach here and instead of we injected a DL into the client and then used API hooking to spoof the checks at a Windows API level. API level. So another piece of information that we had to fake was the hardware ID. The hardware ID is essentially a hash of information that's derived from various components in the operating system and hardware. Um and then this is sent during registration and later on during keepive or device assessment requests. Uh it's supposed to be unique per user and per machine. Um, and if the hardware ID mismatches, the server might refuse the connection or class it as a new device and force re-registration. Uh, in reality, all of the values that make up a hardware ID can be spoofed and we can generate the correct expected hardware ID. Uh, in the game world, this would be called a spoofer and it's really no different for ZTNA. Uh so we quickly realized this would make a great generic tool for tricking software that evaluated these various operating system components to to trick these compliance checks. Uh so we made this configurable DL that could be injected into the ZTNA process and allow us to hook these API calls. Uh for hooking the Windows API methods, we mainly use minhook cuz it's a simple library and it's widely used. Um for DL injection we mainly use the app app init DLS um method for DL injection but we also have the option to use a sign driver. driver. So for most checks a simple Windows API hook was all that was needed. Uh however to boost some other checks things are slightly harder. Um so one of the things that normally makes up the the uh hardware ID check is CPU ID. So they use this as a way to get a unique fingerprint of the hardware. And the way this works is they'll execute the CPU ID instruction to extract the processor info and the feature bits um by saying EAX to one and then executing the CPU ID instruction. Uh so we can't hook this because it's an instruction. It's not a um a method. Uh so instead what we do is we use a page guard hook. So the way this works is we search through the the the memory looking for these CPU ID instructions and then we find the the page that they're in. We set the page as page guard and then when that instruction gets executed, it triggers an exception which we can catch with a custom exception handler and then we can swap the the register values out just after the CPU ID instruction gets executed. executed. The downside to this is it is slightly slower than a inline hook. Uh another option would be to use a custom hypervisor to spoof this but in reality we didn't need to do this. So we then created this small guey application to control the hook settings allow us to spoof all these various values dynamically. Um and this allows us to import the config and it will automatically adjust the spoofing settings to the posture profile inside the config. Um although some vendors offer the ability to perform cryptographically verified compliance checks or serverside checks, they're not turned on by default and some vendors don't actually support this feature at all, meaning that compliance checks can always be bypassed in this manner. Um, yep. Uh, also we got a side note here that, um, we called it red scaler, but it makes more sense in British English than American English. Otherwise, I guess we would have called it rescaler. Uh, we're going to do a demo of this. Now, this picks up where we left off. So, we we provest, we stole the config, and this is what the config looks like in plain text. So, you can see it contains all the the posture checks. So now we're going to import that config into our red scaler guy. Just going to re-encrypt the config and put it in the right place. You can see here it's picked up some of the posture checks and asks if we want to apply those automatically. those automatically. Then it restarts scaler. And we're logged in, but we can't get to the internet. Oh, it's okay. We forgot to spoof Bit Lucker. So, we'll just spoof that just to show the posture checks are working. You sunk my battleship. That's a full house for all the products we looked at by the uh checkpoint side quest. So, let's have a couple of minutes on this privacy thing. So due to the way these products work, they have to install uh a receate in order to inspect TLS traffic and provide you some of the security benefits that that they offer. Um they're quite long lived here like 2065 is quite a long time for a set. Um the way these are pushed down are typically either with the policies or they're installed with the client. It's not really particularly difficult different to a traditional uh VPN appliance or or a web proxy. Um but the difference here is that the zero trust engine is is very much an unknown. Your traffic enters there, it's decrypted and processed and it leaves again. You're trusting that it's rooted correctly. Um that nothing else happens. Um and we're sure this hasn't happened, but the compromise as ZTN provider would allow some additional processing to be performed on the decrypted traffic. Um and you would never know. So we'll talk through a couple of uh defense and mitigations here. So the most important one is if you are an Xcode customer and you're not using secure enrollment, please enable it. Now um also ensure that clients are always updated. Even if there's no CV published, just because there's no CV doesn't mean there's no bug. So make sure you're always up to date. Enable cryptographically secure checks or serverside validated checks where they're supported. Um, as far as we know, there's nothing for Net Scope, but um, Zcala do support this since version 4.4. Uh, we've also shared some general hardening tips here and some mitigations to help limit the impact of config theft, for example, requiring reauthentication with a a small reauthentication period, and to minimize the attack surface for privilege escalation, disable client side access to features such as debugging and verbose logging. Um and finally, if you do use app-based or domain based steering bypasses, ensure that these are appropriate for and narrowly scoped and they're reviewed on a regular basis. To detect malicious behavior, you can look at vendor provided logs for newly registered devices, devices that perhaps quickly switch from passing failures and fa passing posture checks and failing posture checks, and make sure that you're ingesting these logs into your scene. scene. You might also see devices coming from unexpected locations or um from unexpected operating systems. So for example, if a user normally uses a Mac and then suddenly enrolls a Windows device. Uh you can also use EDR to detect activities on endpoints such as reading sensitive registry keys or files. Uh and some of the techniques we've discussed require process injection for example bypassing those signature checks. Uh equally some vulnerabilities might cause a ZTNA process to spawn an unexpected child process. So using uh EDR to detect on unexpected child processes can be useful too. too. So there's a couple of techniques that are good for detecting token and config theft. Uh Google have this brilliant blog that uh explains how to configure auditing from for DPappy events. Uh this is actually aimed at detecting browser cookie theft but also applies here. So if the data is uh flagged as audit auditable during encryption then the the 4693 event will be raised during decryption or when an attacker attempts to decrypt the value on the on the machine. But unfortunately none of the vendors actually flagged the data as auditable when it's created. Uh so we can't use this 4693 event type. However, we can use the very very memorably named Microsoft Windows crypto dappy log if we turn on debug locking. And this is also detailed in the in the Google blog. And then this gives us a new event event ID 16385. And we can use this to correlate the data description and the caller process ID to identify any potential misuse. Uh so in the case of Zcala you can see here uh when we stole the Zcala config earlier uh you can see the data description is client configuration uh which is the description that's set by zcala during encryption. Uh another option for audit auditing access to sensitive registry keys and config files is to configure sackles uh which will raise uh another event in the event security log. So this is event ID 4663. Uh and you can see here the event that was raised when we exported the secure token from um from the registry for net scope. So in conclusion, well it turns out there are no magic ZT CNA beans. We've got the same old bug classes reimagined for a new technology stack rather than zero trust. We're actually putting a lot of trust into these vendors and to process our data securely. And we expect them to clearly and effectively communicate when they do fix vulnerabilities. Even if those bugs are server side so that clients can make informed decisions regarding risk profile. When the bugs exist only in one place though, at least patching them is efficient. But even in that case, if the patch still requires you to change a setting, we think that this could be communicated more effectively. vendors are still putting far too much trust into client side components as we saw with authentication bypass and posture profile bypasses which doesn't really fit the zero trust principle. In summary, we found that in practice rather than being never trust, always verify, we found it was more always trust, never verify. So, thanks for watching our talk. We hope you enjoyed it. We've got a link here to our blog. We'll be publishing writeups over the next few weeks and days. Um, and you can come up and speak to us and we'll take some questions.
