- Welcome to the 12 o'clock talk, on All Roads Lead to JKE. Without any ado, Billy, are you starting off? - Yes. - All right. (audience applauds) - Hi, I'm Billy, and thanks for coming. Here is our title, "All Roads Leads to GKE" Google, Google net engine host. Hosts refer to the Google GKE node. Forward to escape. We will use four different Linux kernel vulnerability, including one day and zero day we found, to escape from the container, to take over the node in GKE. Today's presentation will be presented by me, Billy, and my partner Ramdhan. Before going to the topic, let's introduce ourself. My name is Billy, a security researcher in the STAR lab. My partner Ramdhan, also works in the STAR lab. We usually work together to do research on the Linux kernel and Hypervisor. Like Virtualbox parallels for bug hunting and exploitation. We successfully Pwned Ubuntu, in Pwn2own 2021 and 2022. And Virtualbox in Pwn2own 2022. We also identified various vulnerability in Linux, like CVE-2021-3491 It is a Linux kernel privilege escalation vulnerability in the io_uring subsystem. We also combined with CVE-2021-2221 with CVE-2021-2250, We achieved via escape, from the Virtualbox. We also achieved the parallel loss via escape, from the CVE-2021-34854. This CVE shows we have some bug in Linux kernel and Hypervisor. Let's start today's outline. We will give you some background information, introduce a bit about what is Google Container-Optimize OS And explain kCTF VRP Challenge, a Google bug hunting program, programming focused on the container escaping exploitation. And then we will start showing our details on our first submission and it's exploitation we used to transform a limited primitive to escape from the container. And finally, through the conclusion. Google Container-Optimized OS base is a Linux OS base optimized for running Docker containers. Which is default node always in Google Kubernetes Engine. No packaging manager, unable to install third-party kernel nodules. Read-only root file system. Every time when you put, you always verify the file system tracer to measure the file system is not malformed or backdoor. Usually the Linux kernel configure are stored in the etc folder. In the Google Container-Optimized OS, this folder is writeable but stateless. Which means, every time it put, it will start from a clear slate. And configure is set up at run time. For attack install, a persistent backdoor is hard. If we want to escape from a container, there are some limits here. There are some obstacles when we exploit attack surface it uses some common exploit technique in the container. It disallowed unprivileged user to use eBPF, and that includes the userfaultfd system call. It disallowed to access internal networking. So we are unable to access Kubernetes API server. Also limit on device node access. Unable to spray tty_structure and unable to use fuse. Okay, there are some attack surface we can use in a container. First, we can create a new user namespace to explore more attack surface. Like Cgroup, a Linux kernel feature limited account for an isolate the resource usage. Or the Filesystem context functionality, or packet circuit, or traffic control configuration. In io_uring, is a Linux kernel system code, interface code for asynchronous IO operation. And there were some CVEs in this subsystem before. And it is reachable in our container. We think this is great attack surface. Let's see what our kCTF VRP environment architecture looks like. Basically it is a GKE cluster, has multiple nodes. The node you can see it is a vn and each node has multiple parts. A part you can see is a container, in a part you will launch a kCTF challenge. kCTF has two challenges. They are in different parts. Your goal is to get two flags. One flag is called KCTF flag It's place in the same part when you're connecting to the challenge and this challenge will give you nsjail bash shell. And another flag is called Full-Chain flag. It's placed on the other part. The node OS and the Kubernetes version are upgraded automatically, but there is a time gap between the bug build interface and upgrade. So it is allowed to use one day vulnerability, and we introduce before. All nodes are running on the Container-Optimized OS, Pod egress access only. So you can download the exploit from the internet and launch. There are two attack scenarios. One is breaking out of the nsjail sandbox and get the flag on the same part. Another attack scenario is to break the isolation that Kubernetes provides and access the flag from other challenge. But with the kernel vulnerability, we can literally escape privileges and take over the whole node. So you can get a whole frame from a single kernel vulnerability. And next let's show our first submission. And let Ramdhan to show the first two submissions. - Hello everyone, I'm Ramdhan. I will talk about our first submission. is using using CVE-2021-4154. This bug is, we used to exploit kCTF container. This bug is use-after-free bug in cgroup1_parse_param function in cgroup kernel subsystem. So basically this bug is caused by lack of check in this function. There are source key that always need value, that has string type, but before dispatch file, there's no check if the value is string or not. So we can pass a value with another type. So using the fs config sys call, we passed our value with the file type. So we pass a file descriptor as a value. Then param string will contain file strap address. Then it will start in fc source. We can reach this bug by calling a fsconfig sys call, but value set the key as source, and we set the value with the file descriptor using fsconfig set after. So... the put fscontext will- if it calls the file descriptor, the put fscontext will call and it will free the fc->source that contain the file struct. But the file descriptor that associated with the freed file struct, still exists in the process. So we have use-after-free in file struct object. And... So using this bug, we can we can create a bunch of fc_context pointing to the same struct file. So there are multiple fc->source So if we close one of them, and we can close another to have double free. Okay. So.. To.. We can free another fc_context that associated with it. We can perform double free because we have the fc_context still pointed to the freed struct file. To... So how to exploit this bug? We need to build our exploit strategy. In use-after-free scenario usually we overwrite some object with another type of object, but we have another problem, exploiting this bug because the file struct is- first problem is the file struct is allocated in their own slab cache. So we cannot override with another type of object in a common way. And the second problem, what type of object that we can use to extend the limit primitive of this bug. So, I'll present you the solution of the first problem. To overwrite some object with another type of object, that reside in different slab cache, we used the technique, that's called cross cache technique. This is the common technique to exploit use-after-free in Linux kernel. So there's two type of cross cache techniques. The first is cross cache use-after-free. And the second one is cross cache heap overflow. So in cross cache use-after-free, first we slap page freed to buddy allocator. So first we fill the whole slab page with some object. So for example, there's slab of object a, so we allocate all of object a to fill the whole slab page. Then we free them all. So the slab page will go to the buddy or page allocator. Then we spray another type of object and it will coming from the page allocator, and now the old freed slab cache of object a owned by object x slab cache. So using this technique we can solve the first problem. But I will present to you the another cross cache that's called the cross cache heap overflow. In cross cache heap upper flow, We make the target slab cache adjacent, next to another slab cache, the object that we want. So for example, in the right side image that we made, we can override the object a from the object x, but they are in two different slab cache. But it requires the sprayable object for both object x and object a. But don't expect it will be reliable most of the time because, we also need to trigger the heap overflow bound multiple times. And we can also make it more reliable if we have the capability to gain some information, if we already provided the object or not. So I already present you the first problem is that we can actually to override the file struct in our slab cache with another of object using the cross cache. And for the second problem, to extend the limited primitive. for the second problem I- as some of you already know- this is the msg_msg object and msg_msg_seg. This is the kernel object for exploiting Linux kernel bugs. For the future, I will refer the msg_msg as msg object, and msg_msgseg as msgseg object. this is the properties of msg object. So, this is the object that, already known for exploiting Linux kernel bug, because of,. especially the heap bugs, because of its flexibility of it's size, and this is the sprayable object. And there are the second object called the msgseg it's used to store extra message if the total of the message followed by the header with the size of hex 30. So if the total of object of the message is more than 4k, the msgseg object will use and it pointed by the next pointer of the msg object. So this is the msg object is a powerful object that can be used for many things, for example, arbitrary write arbitrary free, arbitrary read. We will go deeper with some of this. And it's accessible via msg get for a lot of thing, msg queue and the msg send function to insert the message to the queue, and msg retrieve, to retrieve the message from the queue. So, there are- To allocate the msg we can use the msgget. This is the indirect set image. There's a definition of some of functions for interface that we are going to use. So as you can see, there are the msgp parameter in the function interface, but it's actually defined as the msgbuf struct and the msg type is used for selection process. When we retrieve a message from the queue and the msg flag, there are some useful flag that we can use. For example, the msg_copy, this flag is, we can use if we want to retrieve the messages from the queue without deleting them from the queue. So there's the image that shows how the message links to the queue. For example, we allocate the message queue, and then we send the message with the mtype one and with the 4k bytes of message. We send to the queue and we have the first message insert to the msg queue. And there are the msgseg because of the total of encountered the size of msg object with the header is more than 4k. And if we try to add another msg object with the mtype two, and with the C of hex 200, it will be linked to the queue. And if we want to retrieve the second msg, we can use the msg except with the mtype one. It will share the message that doesn't have mtype one. So the second message will retrieve and print to terminal. So I'm gonna explain the tricks that we can use, using the msg object. So suppose using the double fee bug we have before, in our first submission CVE, we can convert this to have use-after-free on msg. And after we have use-after-free on msg, we overwrite them using the msgseg object. So the first trick is, we can use the msg object if have out of bounds read. We can override the mts, mts is message text size. If we override this large value, we can get the address leak from the out of bounds buffer if we have msg retrieve. For the second tricks, is we can do arbitrary read. We can do this by controlling the next pointer to the address that we want to leak. Because when we retrieve the message and we set the message text size to the 4K, it will seg to the next pointer kernel assumes that these messages has more than 4K size it will go, it will copy from the next pointer to the user when we do msg retrieve call. And so the third trick, is we can do by using this msg object is arbitrary free. We can do this if we have some specific kernel configuration. To get arbitrary free, we can see if we retrieve the message, it will call, do msg function. And in the end, we have called to a free msg, and then it will reach the security msg_msg free. As you see, it will call the kfree on msg security. And if we can control the security field, we have arbitrary free. But before we reach the free msg, we in control the list del, list del function, and need to take care of next and previous value. We actually can control them using the msgseg object, but the msgseg object cannot control the first 8 bytes, So... if we reach the list del using, the msgseg, the kernel will crash. Because we cannot control the next pointer in the mlist, but we can bypass them if the kernel have the config the back list enabled. If we have this config enabled it will call the list_del_entry_valid. We can easily make it return false and skip the list_del, by set the proof value with the list poison to. this is some constant defined in the kernel source. And fortunately kCTF kernel config has that field enabled. So we can use the security and the config_debug_list enabled, to have arbitrary free using the msg object. So to re wrap our three tricks, we can combine this to the kernel RIP control. So for arbitrary free, we can free the pipe buffer object in kmalloc 1k cache. This RIP buffer we can allocate using the pipes. So we use our first two tricks to do out of bounds read and arbitrary read to leak the address of pipe buffer in the heap. And we read the ops pointer in the pipe buffer to get the kernel text address. And by closing pipes and controlling the ops field, we can control the kernel RIP pointer. So we go back to our first submission, when we have use-after-free in struct file, and we develop our expert strategy. So with the first problem and the solution is we can overwrite the stack file with another type of object. We overwrite them using the msg object, using cross cache technique. And we free one of fc_context that is still pointed to the msg object. So we have use-after-free on the msg object, so we can reallocate them using the msg seg object. So we can find the msg object using the msg seg object. Then using this condition, we can perform, like our tricks before out-of-bound read and arbitrary read. So first we need to leak the address of pipe_buffer in kmalloc 1k. So we need another of msg object that's adjacent or near from the msg seg object. We allocate another msg object in kmalloc 1k and adjacent msg will have next pointer pointed to kmalloc 1k. And we can do out-of-bounds read and leak the address of kmalloc 1k. After we have the address of kmalloc 1k, we free the msg, msg that we talked before, the address, the msg that allocated to the kmalloc 1k. We allocate the spray the pipe buffer, the pipe, the old, the msg and the kmalloc 1k will be replaced by the pipe buffer. So from the address leak before from the out-of-bounds read, we know the location of the pipe buffer. Then we perform the arbitrary read on the pipe buffer, to know the value of the ops pointer And then we do arbitrary free using our arbitrary free tricks to... we control the security pointed to the pipe buffer. And we now we have use-after-free on the pipe buffer. So we just reallocate them with the msg seg object. We have fake pipe buffer and control over the pipe buffer content. And because the pipe buffer will reach the pipe buffers function with our fake pipe buffer, and we have kernel control- control kernel repointer. So after we have the kernel repointer we need to convert this to our object. We can easily find the stack pivot gadget to convert this to ROP chain execution. Using commit_creds and the prepare_kernel_creds, we can install the root credential to the current process. Before our ROP chain is executed, we have prepared the child process. will, this while loop that, if we already got root it it will break, if we already got root. And then after we installed the kernel, the root credentials, we copy the credentials to the child process. And we switched the namespace of pid 1 to the init process using the switch text namespace function. We call this using our ROP chain. And the child process that we prepared before will break the while loop, and it will set the namespace to the namespace in pid 1. And we cat the flag and spawned the root shell. So in this demo, we connect to the kCTF instance. We download our exploit to the internal and run the exploit and cat their flag will be printed and it spawned the root shell. (audience applauding) So now I will talk about our second submission. For the second submission. We use CVE-2021- 22600 to exploit the kCTF. This is double free bug in packet_set_ring function, in the af_packet kernel subsystem. So this is the snippet code to trigger double free. I will explain this bug code one by one. The root cause of this bug is, happens when we switch version from the version 3 to 2, but there's some field, because of the bug, There's some field that didn't clear if we change the version three to two. So there's some old set that didn't clear. So first we create the af_packet socket. We set the version to version 3 by calling setsockopt. The TP version variable will assign to the TP version. And we call the setsockopt with packet_rx_ring option. It will allocate a variable called pg_vec. As you can see, we can control the size of allocation by setting the tp_block_nr. So the allocated the pg_vec is stored in pkbdq, but it overlaps with the rx_owner_map because of the union data type. And then from the third setsockopt, we free the pg_vec, but it didn't clear the pkbdq. Now, we changed the version to the version 2 by performing setsockopt again. And when we call the packet_set_ring, rx_owner_map that's actually coming from the old set is read. So we read the pg_vec of pkbdq twice. So the rx_owner_map is upper limit, because it's upper limit with the pkbdq. And it didn't clear from the version 3, and in the version 2, it will be free. So we have double free. So we can control the- in this block we can control the target chunk size and we can control when to double free. So we choose the timerfd object as a sprayable object Using cross cache technique, we free our timerfd and the pg_vec to make slab page insert to page allocator. And then we spray with the msg object. And using the double free, we free the msg object and overwrite them them with the msgseg object. So we can control over the msg object. In this condition, We have the same case as the previous submission, our previous exploit that we have use-after-free on msg and reallocate them using the msgseg. We can just do the same technique. Plus we do at out-of-bounds read to leak the address of pipe_buffer on kmalloc 1k. And we do arbitrary read to leak the kernel text address. Then we do arbitrary free on the pipe_buffer. We overwrite the- and then we overwrite the pipe_buffer using the msgseg. Then we can close the pipes and control kernel RIP pointer, and we can escape the container. So this is the demo of our second submission. We download, we connect the kCTF and run our exploit. The kCTF will print it and spawn root shell. For the third and fourth submission, my friend, Billy, will explain the third and fourth submission. Thank you. - So let me talk to the next two submissions. One is CVE-2022-0185, it's the first time we used 0day but unfortunately we didn't properly report it to the Linux security team after the kCTF submission. Here is a description of the CVE. We highlight the keywords we care about- Heap based buffer overflow. But ability is placed as a filesystem context sub system. In case of unprivileged user namespaces, we can trigger the vulnerability. Briefly speaking, there is an integer underflow when boundary tracking. So if we let the size module and the page_size minus two, it will cause integer underflow. And in the following four places, will cause heap overflow on the the legacy data buffer. We carefully open data in the legacy data buffer, and cause heap overflow with one little byte. We treat this vulnerability as off-by-one. From the call legacy_data it is in a kmalloc-4k. So in summary, this vulnerability is off-by-one on the kmalloc-4k cache. We use cross cache heap overflow in order to overwrite the message structure, spray legacy_data buffer with message structure at the same time. Trigger off-by-one vulnerability on every legacy_data and it will have allowed change to overwrite the message list next last byte with zero. If we are lucky to overwrite message next last byte with 0, we can make the two messages point to the same next. Free it through the one message queue and another message queue will still point to the free chunk. And at this point we have made the same situation as the previous submission. Through OOB reading pipe buffer address and through arbitrary read to leak kernel past address. Next is to do arbitrary free and pipe buffer overwrite with message segment. Cross cache to control the kernel RIP for escape container. There is our demo. We've connected to the challenge and get exploit from internal and launch with the server get fail and take over node. There is a secondary way we use in the kCTF for, and this time we properly reported to the Linux kernel security, and get credit. Here is a description of our CVE. We also highlight the keyword we care about. Here is heap corruption in the io_uring. It is reachable in a container. This vulnerability is in the io_uring when request a same message of receive message operation. It will grab fs structure return before two operation, but if io_uring contents has io_uring same as before, he forgot to release the fs structure when finished the operation. So we will keep increase the users, it will cause integer overflow and none of the users reach the rule again. When fs structure reaches rule again, we use another io_uring with other io_urings that are io pool. It will free the fs structure after finishing the request, so we can have a free fs structure as current fs in use. The key point about cross-cache is allocate a lot and free up at the same time. So we allocate a lot of fs structure and free up, spray kmalloc-128 message segment to reallocate current fs as message segment. and then free the current fs again by io_uring and cross-cache again with kmalloc 64 message segment. Since it is still message segment, so we use message receive system call to leak the next message to get the kernel address in kmalloc-1024. Once we have leaked heap address, we can multiply contents by free allocate again. We can use it to forge the fs structure to do arbitrary read in order to leak kernel address. With our fake fs structure, we are able to arbitrary read by getcwd system call. After we get the kernel address, we can prepare our ROP payload and forge fs structure again. By system call fchange directory, from fchange directory to set_fs_pwd to path_put to dput and in the end to a retain_dentry. In the retain_dentry we can control kernel RIP when calling function point d_delete. Original demo video near about 15 minute to finish. So I speed up for presentation. Again, connecting to the challenge and get exploit from internal and launch. With a sever get to flag and take the GKE node. All vulnerability we use have already fixed in GKE. You can see the reference to the Google Security Bulletin GCP-2022-002 and GCP-2022-016. We believe that with cross-cache attack with message we can transform a limited primitive to arbitrary read, arbitrary free to escape to the container. But there is a limit here, we can only allocate size under 4096. In our last CVE, in our last submission we were able to convert UAF through the common situation and a previous submitted CVEs, but with some change. We still can escape container by cross-cache and message. As a result, we are awarded more than $100,000+ bounty from the Google kCTF VRP program. Thank Google for hosting such a great VIP program to secure the kCTF and GKE infrastructure. Here is our references. And thank you.
