We'll go ahead and get started. We'll have Jang Yu Leu and Jinga Yu present on the domino effect. Let's clap it out for them. Hello everyone. should get started. >> Okay. So, hello everyone. Uh, welcome to our talk the domino effect automated detection and exploitation of dump clobility at scale. So I'm Jung Yo and this my collaborator uh Jenu and this a joint work with Tai Kong and my Wester in in are both PhD student from Jak University uh focus on web security and software security and we spent our most of time studying vulnerabilities uh building security testing tools and thinking about how to make the modern world a better place better place and today we're excited to share our research on doming So I will first start a very quick overview of what dump clobbering is and then explain the challenges of detecting those vulnerabilities at scale and introduce our automated analysis framework hawk. And after that, Dindab will uh take over to prevent our large scale zero day hunting results and show how we turn those HTML injection into the the end to end exploits with DOM Corbin gadgets we find. So let's begin. So DOM chlory originates from a old but still standard browser feature defined in the HTML specification and allows you to perform named lookups on window or document objects to access elements on the page. While this feature provides a convenient way to reference like dump elements by their ID or name. It often opens the door to the DOM client attack in the scenario where those elements are attacker controlled and it wasn't the developer's intention to use them at all. all. By crafting a name collision between the DOM elements ID or name attribute and a property accessed via like the window or document, the attacker could plute the JavaScript value and change the application logic. application logic. So, and especially modern web applications often accept a wide range of user inputs including like scriptless raw HTML to support things like rich text formatting or like embed media. For a more concrete example to give a illustration for people not very familiar with this is that at first the attacker need to inject an alink uh or element to the page and then the page contains JavaScript that probably reads from like window remote to load additional scripts and if this undefined it fallbacks to use like a CD and URL and load script from there. However, with the attacker inject link uh whose ID equals to the remote, a lookup is no longer undefined and instead it refers to the inject a link and this is what we call a clober and later like when the DOM element is concatenate with a string type coercion occurs and for those link elements this convert them from a DOM elements to their ref value and producing a URL controlled by the attacker and finally Finally, the URL flows to the script. And resulting loading malicious code from attacker's domain. Since Domlobering was first introduced by Grace House in 2013, a lot of work has been working towards understanding various clobbering techniques and at the core. The key question is which HTML markups can clobber which JavaScript targets and probably under which browser implementations implementations because uh the clo well like a clobering behaviors is defined in HTML specification but the browser management varies slightly slightly and building on years of the findings and also the updates from the browser uh the recent paper from Oakuckland uh 203 called it's clobbering time is the first to systematically answer this question. It uncovered like over 30,000 cloing capable markups across five distinct techniques making a real scientific forward in this like in this field. However, identifying those clubbing behavior is just the initial payload landing step of the attack and for the full exploitation we still need the gadgets and the exploits. So the following question is can we automatically detect those gadgets and generate a working exploit so that we can really uh testing them at scale and better understand its landscape in our like modern web and this is get where our work comes in. So to do that there are two main challenges. The first one is that real world JavaScript especially minifi after minified or bundled is full of dynamic behaviors like aliensing or directive property lookups and it's very unfriendly to our static analysis tools and those features make it extremely hard to track how data flows through application and in turn makes a gadget a very hard problem. The second challenge is exploit generation. In DOM clobbering, the payload is longer a string which is usually the case but is a DOM element and that elements need to be compatible with the JavaScript operations in the gadgets and carry the attacker controlled values to the sensitive syncs. And if the element itself is malformed or is not incompatible with the gadgets, it may break the code and just or just fail to propagate a pallet. Okay, now let's take a look at a motivating example and show like how those challenges failed our existing like approaches. This is a zero day cross scripting caused by clobbering fund by hawk in my favorite red platform hackmd. The HTML injection was possible because developer allowed the name attribute on the iframe tag and no exposing its application to the DOM cloing cloing to escalate the HTML injection to cross scripting. uh hawk finds a vulnerable gadgets from the Google client API library used in the page and the Google client library is used by a website that tries to integrate the uh Google service like Google drive, Google maps or YouTube and once the library is loaded on the page it will precise the developer defined configurations in the page and callbacks embedded in the page and by searching the corresponding script tag that contains the configuration URL and then we'll use new function which you can dynamically generate a function like a generated JavaScript code uh by parsing those configuration objects and execute its code. code. But here's the problem that the source it uses document.cript is vulnerable to dump cloing. So the attacker can inject a clobert DOM elements that is not a script like F-frame with a craft the name equals to the script to hijack the configuration object and com combining the HTML injection and the D clubing gadgets in the page. The attacker could achieve C scripting by sharing a malicious uh hack MV notes and probably uh perform any unsured action on behalf of the user. And till now like both the HTML injection and DOM claring gadgets have been fixed by the hack MD and the Google respectively. respectively. And let's talk about the private work like the thing is a stack analys that try to detect this by looking at the source code but it fails to detect it because of the dynamic behavior and condition logic embedded in the gadget code. code. Secondly, exploiting the gadgets requires HMRs that satisfy both the data flow and control flow constraints along the chain. The data flow constraints ensure that the pellets can flow from one attacker control variables to another while the control flow constraints ensures that the program state like execute the JavaScript like execute the gadgets logic. So we don't like executed like executed exe. exe. uh for example the constraint two involves like a subscript operation uh of the clober value. So this means our collaborate objects must behave like a collection such as HTML collections. Uh if it's just a single element, the access on it will throw a runtime error and the gadget log just fail to execute and the thing fails to generate a working exploit because it only consider the cloing initial cloing pattern but ignores all the constraints in the gadget code and as a result it produce a payload like image name equals to script that would never reach the sync. Another popular client side testing to do invader uh from proigular uh failed to detect it is because it relies on a canary based testing. Well, this approach works very well for streambased exploits by checking whether partial attacker controlled value reach to sync. It does not apply to dump clobbering because dump clobbering requires a near correct HTML element and considering all the constraint from the gadgets and randomly generates HTML will just never reach the sync or even worse it will just break the website functionality and skip executing many real gadgets. Okay, Okay, now let's see how we solve the problem with Hawk. with Hawk. Hawk operates in three stages. So our key insight here is that instead of directly injecting HML markers from the start we first perform the T tracking on website defined values from those potential collaborable source to the syncs and this phase is non-intrusive and helps us identify a wild range of potential gadgets without needing to guess the payload. And next based on the tint trace we extract the data flow and control flow constraint and use them to generate the corresponding exploit markups. Finally we insert those craft markups to the page and replay execution and check whether the payloads reach the sync confirming whether the getus is really exploitable or not. Specifically in the first stage hog performs dynamic tent analysis on website defined values. We categorize the source into two tabs. First, the lookups on document can be directly shadowed by the attacker injected values like documented links, document tricks and in this case we can directly t the original values and observe how they propagate in the program. And second there are lookups on like a window object where the attacker club value is only used if the original value is undefined. And this is typical appear in like those fallback expressions like tenorary operation operators or like logic or checks. And in such case hawk will track those fkback values instead and identify opportunities where the attacker controlled value might take effect effect and and hawk will only inject its own value like random value when there is no website defined value is present as a fallback. fallback. The syncs you the sync used in our tent tracking are typical like client side syncs like inval set time out and others. others. The tented value in the page are managed in two ways. For objects with properties, we just insert another like specific field, a non- innumerable property so that it doesn't interfere with the normal program behavior. For those primitive values which very hard to to be tented, we wrap them with a special object that holds both a concrete value and its tent metad data. So this allows us to track even undefined values. Our 10 engine supports uh propagation across JavaScript operation uh built in JavaScript built-ins and browser native APIs and so on. Uh and the T propagation rules are implemented in a very extensible way like adding support of a new APIs is as simple as defining a single handler function function and we implement the N and using a code rewriting approach based on geology instead of instrumenting the browser itself. It gave us a a very flexible feature and and allows us to track all types of JavaScript values and supports cross browser analysis without relying on the browser internals. The output of the first stage would be the tent trace represent in a tent dependency graph and and the the backbone are the operations and there are concrete values and also the attacker control tented values. Next uh the challenge would be once we have this tent dependency graph like how do we generate the HTML markups that can leverage those operations observed in the graph and carry the attacker controlled strain all the way to the syncs and in the second stage in this stage like we will introduce a concept of a symbolic DOM which allows us to formally define the constraints that DOM elements must satisfy at each step along the path. And then we implement a simple DOM constraint solver that use those representations to automatically generate the compatible and exploit ready HML markups. So the first step to model so we need to understand what a successful exploit would looks like. So to model a valid exploit we treat the external payload as flowing through four key stages in the uh 10 dependence graph. The initial clobbering where the attacker's DOM uh elements first inject to the JavaScript program. The advanced clobbering where some parts of the first injected DOM elements get loaded and to the program. And the third stage is DOM to string where the string payload loads from the attacker control DOM elements to the page. And the final stage is string to string where the string is precised like or transformed and finally flows to the sync. sync. In the first stage, the DOM elements should contains a e name a ID or name attributes correspond to the named the lookup keys which is quite straightforward. straightforward. On the second stage, the attacker controlled elements must be further loaded. And this this stage is only necessary when there are additional JavaScript operations before the string uh payloads can be loaded. And since we cannot skip any JavaScript operations in the graph. So we need to use this advanced cloing techniques to fulfill them. And the problem would becomes if the best object is an attacker control dumped elements, how do we form it in a way to make the results of the operation could also be uh attack control dump elements and without raising any error. And there are few strategies can be taken. The first way is HML injection that we can inject multiple dump elements with the same name attributes and will looks like an array. The second way is to use the like well-known nested window proxy uh parent child or input form those relationship to clober lookups like whose pro property is a generous string and the third way targets a certain DOM specific properties like previous symboling and by arranging the DOM elements to match the expected structure and later on when the attacker there might several front uh several rounds of advanced clobbering and later on the attacker needs to load its string from the control elements to the program because the final payloads showed in the sync should be for in a type of string and there are three main way to achieve this. The first is the well-known technique through type coercion during binary operation like add and this and it applies to the A and area elements where accessing those elements and triggers the use of its ref value as a string. Uh the the catch here is the ref value must be a valid URL and browser will often autofix it with a protocol like HTPS or whatever is used in the current uh page and to bypass this attacker would need to inject a base tag before the clo element controlling like how those relative URL are reserved. Another two stage operations can be leveraged are function calls and get feud which is less documented. uh the for the function calls some JavaScript built-ins and the browser APIs would automatically cohers its arguments or property values to string. So in that case if the attacker control values passed to those and or excess in this way the browser would performs the coercion just like those during the banner operations. banner operations. The third way is to exploit the attribute name collision. Like if a property name matched to a legitimate reflected DOM attribute, it can be abused. For example, if the application risk the value property from the element, then the attacker could inject an input tag with a and with a payload saved in its value attribute because it's a valid like attribute name. And our paper includes a a comprehensive list of those attributes and APIs can be used in this stage. And finally there would be a rest of a string related transformation and finally flows to the sync. Okay. So we know what's uh like a legitimate DOM elements should looks like to describe those in a formal way so that we can solve them. We need uh we need this DOM symbolic DOM syntax. So it's basically a constraint syntax to describe a set of DOM elements with the same list of features and it comes four prims int boolean string node and a collection and each node represent a DOM elements which can has a tag name and several attributes and a node can have siblings and children and a valid symbolic DOM that can be concrete to the HTML must have one root node and then with our with each operation type and also the uh stage we just mentioned, we could generate a constraint on the input or like output elements used in the operations. For example, to achieve like document to DOM through the document.p P operation the DOM elements can be described on the page whereas the is road has tag name and has attribute uh constraint and when P equals to the script so we can concrete to a very simple uh HTML element and with those model constraints we can traverse all the operations nodes and attach those constraints to the attacker controlled nodes and each attacker controlled nodes in the graph should have two conjective sets of constraints as it serve as the return value of its precedent operation and arguments of the subsequent uh operations operations and finally we need to merge and solve those constraints and the key insight here is that we start from small and then connect those and finally concrete to the HTML. So what does it mean? This means that first we focus on the local constraints like a C1 and expanding it into a disjunctive norm form or DNF form. And in this form each local constraint can represent a valid like possible document DOM trace that satisfies those constraints. And next we will merge the formulas across different DNFS uh if they points to the same tented nodes. And during this uh process conflicts may rest and we just uh need to like get rid of them and only save the valid combinations. And you can think of the second stage as attaching different dom trees at their shared nodes. And this uh and discard any combination that doesn't fit together. And in the last step I would just we will just concrete the formulas to the HML marks as a input as exploit. And finally in the last step we would just inject our generate payload and replace the web page and see whether our canary value from the exploit could really flows to the sync. And next I'm going to hand over to my collaborator and he she will show the results. >> Yeah. Yeah. >> Okay. Okay. >> Hi. All right. So let's get to the real exciting part. Now I'm going to show some uh real world ex examples we found with our tool hawk uh as well as some um so those include some doming gadgets in the wild uh in web applications and libraries and also end to end exploitations. exploitations. So first we apply our tool hawk to tranquote top 5,000 websites and found uh 497 uh 497 zero day exploitable gadgets. So therea our cases includes uh modern web bundlers like webpack, rspack, vite, tissa, polyfuse and some core functional libraries like prism uh page findjax version two and version three and also some third party services including plausible analytics google client API library and also uh web application frameworks like astro. Our data set is public. You can access the whole data set through this QR code if you are interested. interested. So the first case I want to show you is the web pack. So according to an investigation, there are an average of 1.27 Webpack bundles per site among the Tranquites. So uh this vulnerability we found by Hawk can lead to cross-ite scripting on websites that uses Webpack SD bounder. So uh in case you're not familiar, so the pipeline of using Webpack is like this. First you have to feed the website source code to the Webpack to generate the production code which includes a compressed website code and also the Webpack runtime code where uh which is the Webpack library code and in that uh Webpack runtime code is where we found the DOM clobbering gadget. So with that do clobering gadget if that website contains an HTML injection we this gadget can cause cross-ite scripting. So let's jump into the code gadget. Um, so Webpack requests the developer to set the public health to be auto or you can you can either leave it to be empty in hope that the web will help you load some external scripts. But actually it is doing more harm than you think it is doing good. doing good. So if we pay attention to the code itself in the first box we uh we marked up. So the document.curren the current script can actually be clobbered and then in the box two the script URL is loaded as the document.scripts source. So in this case if we construct our payload like this with the source equals to https to https httpbs attacker.control.surfer server that script URL will be an attacker controlled uh control URL and then later finally this script URL is used as uh the webpack requirep which will be later load that script from that attacker control domain. So that will cause arbitrary script loading from the attacker server. So the second case I want to show is the astro. It is a widely adopted client server web framework to help you construct a web application from scratch. scratch. So in this code uh we can also find dump cloing gadget where the cloverable element is called uh is here the document.cripts but this case is a little bit special. So if we pay attention how the document script is used we can see it is accessed through array of from. So the code is treating the document scripts as a array not just one single uh HML element. So in this case we have to construct the payload as an HML collection instead of just single one uh HML element. So and then if we jump into the for loop to see how do we reach the final sync we can see two conditional checks before we reach the sync. So the first condition check checks whether the script data set that astroexec is equal to an empty string and the second condition checks um check the type which is an attribution uh which is the type attribution of the HTML element we got before. So to bypass those two condition checks with our payload the first script data set is not is undefined. So the first condition will return false which cause the code to continue execution and then when we reach the second condition the type again in our payload the type attribution does not exist. So the second contribution second condition will also return for us that will finally lead us to the final sync where the uh new script is loaded from a new script is created as a new element with a script type and then its inner HTML is copied from the payloads in inner HML. So finally we can achieve arbitrary code execution if we construct our payload as this HML HML collection. So in our case the inner HTML will be alert one alert one >> that caused the cross-ite scrifting and then the final uh finally case study I want to show you is the um editor is an editor previously maintained by by do so um in this code is a little bit straightforward. So if we see we can see the collaborable element is the window editor_home editor_home URL which in our case will be the httpb attacker.control.com control.com and then later that variable URL is used to uh be assigned to a global variable uh um editor config and then later that variable is concatenated with some constant strings to construct a final URL that will be uh that the code will be loading the script from it is also causing the scripts being loaded from from the attacker's domain. So uh finally I want to talk about the end to end exploitation. So to understand end to end exploitation we first need to achieve HTML injection. So as mentioned before the web application they accept and process all sorts of user inputs. But how does it become unsafe? So we have some basic findings. So first some sanitizers from these websites they do not strip the ID or name attribution by default and the web applications they are not aware of this DOM clobbering vulnerability and they rely on those ID or name attributes for intended logic. So due to these reasons the ID or name attribute they they sometimes slip through the white lists as they don't seem harmful at all. So based on how these web applications process the uh user inputs, we categorize the input factors of HTML injection into two types. The first is by user typing and the application rendering. The second is by the user copy and paste HTML elements into the page directly. page directly. So the first case is easy to understand. the the attacker might in input some raw HTML strings such as the image tag with source equals to X and on error equals to alert alert one into some user controllable fields and then those strings are later parsed and rendered by the web application without proper sanitization. sanitization. Um Um so the second input factor is to insert through uh copy and paste HTML elements into the page directly. This is based on several factors. The first is that the elements whose content editable attributes is enabled. They can accept HTML elements directly. And second is that the clipboard data can have text or HTML MIME type not just plain text. And also the browser clipperboard APIs they would sanitize cross-size scripts but they do not sanitize ID or name attributes as mentioned before they don't look harmful at all uh at most of the cases. the cases. So based on these factors, what if the website just saved these HTML elements input from the user or attacker without without proper sanitization that will cause the stored HTML injection. I will show you with one real world example we found. So this is the canvas LMS discussion at JHU in our school. So in the in the discussion board in the input box if you just directly input some raw HTML text into the input box they will be sanitized. But if you directly copy and paste HTML elements into the input box the website will actually accept those elements and render those elements in the website that will cause the HTML injection. This is uh usually caused by the web develop web application developers. They are mostly cautious about user inputs by typing but sometimes they are just careless about uh the contents from the clipper boards that's why it causing HML injection in this case. So we also manually um hunted for HML injection in the world in real world applica web applications. So we manually discovered 187 HTML injection vulnerabilities in various web applications including uh wiki, webmail, forum, editors, RSS, social media, chatbot and so on. So and we also summarized 12 editors and client side libraries that are vulnerable to HML injection. They can also be accessed in our public data set. And then in terms of end-to-end exploitation with all the DOM clobbering gadgets we found by hawk and also the HTML injection vulnerabilities we found manually we can achieve 12 end to end exploitation with our uh with those gadget and HML injection. So in those includes tw 11 of them leading to cross-ite scripting and one leads to uh CSRF and the affected applications include the Jupyter lab or notebook and also canvas LMS. So let's dive into the Jupyter lab or Jupyter notebook for details. Uh in summary we so we found HTML injection in HTML in Jupyter lab Jupyter notebook web page and also we found uh DOM clobbering gadget with hawk in massjets both merchants 2 and mer version three um the messj library is used by the jupitter lab to render mathematical formulas and though combining those two together we can achieve endtoend exploitation so in terms of the HML injection. We found that in the markdown preview page of Jupyter Lab, if you if we insert some uh if we just type in raw HTML uh tags, for example, image or whatever uh HTML elements, it will be accepted and rendered in a web page. And we also reported this uh end-to-end vulnerability to the developer of Jupyter lab and they also uh confirmed that it is not trivial to mitigate this vulnerability because the ID and name attributes they are assumed to work by a large number of widget libraries. So a very easy example is that for example there's a markdown file with two hashtags hello. This tag will be render rendered as an H h2 element with the id equals to hello and the id hello will later or somewhere be used as the fragment identifier in a URL to locate that element in the page. So if we take a look at the mathj library, it is a library used widely used to render mathematical formulas and it is used it is used by the jupiter lab. lab. So uh with our tool hawk we found uh dump cloing gadgets in both mass version two and version three and just days before they released the version four and they promised to fix this vulnerability but we haven't checked yet. just say um good luck. Okay. So if we take a look at the messj version two we'll find the uh window.msjs here is cloberable element and then later that variable is combine is concatenated with some constant strings to uh to construct a file url and here a type co coercion happens and finally that um file URL is used is used to uh load is used to to the script script source which is to load the the attacker control the scripts that will cause cross eye scripting and um but if we take a look at the version three just uh at the first glance there is no more global row windows jazz and there is no more loading of additional dependency scripts by default but by leveraging hawk we found that um massj version three they uh supports using the require macro which allows you to load extension scripts. scripts. So we further from that uh in version three of massj there is a global row element called document current script and then later that uh document.currencript current script uh is uh document current scripts src attribution is get and uh with some regular expression replacement operation uh the string from that payload is u is retrieved and then finally that t variable is assigned to the um to the r src which is which is the src attribute of one script element in a page that is also causing uh loading scripts from the attacker's domain. So okay So okay so till now we have both dump cloing gadgets from massj both version two and version three. Um and also with our research uh on dump cloing uh we reported those uh problems and Firefox is also considering patching the DOM clobering um by disabling the shadowing on document lookups um and they are they're referring to the zero day gadgets found by our tool hawk. That's good news. So to summary um we design and uh implement hawk a dynamic analysis tool for automatically detecting and exploiting dump cloing gadgets. So, and we also conduct a large scale study over the trunk top 5,000 websites and identify 497 identify 497 exploitable gadgets revealing the widespread presence of DOM clobering vulnerabilities in popular client side libraries and also we demonstrate 12 endto-end exploitations by combining the discover gadgets with real world HML injections validating their practical impact. impact. So our tool, the source code, the data set and the paper are all open source. Feel free to access them if you are interested. And also both of us uh Jung and Jenza, we are open to internship or full-time jobs. So if you have any opportunities, please feel free to reach out to us. Thank you.
