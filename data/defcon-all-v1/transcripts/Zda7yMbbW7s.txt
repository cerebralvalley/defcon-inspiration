Hi and welcome to our talk, Backdoor in&nbsp; the Core. My name is Alexander Krog and&nbsp;&nbsp; I'm a vulnerability researcher at the Vectorize.&nbsp;&nbsp; Besides that, I'm also a CTF player. Through that&nbsp; community, that's where I learned this wonderful&nbsp;&nbsp; guy. We've been playing together for a few years&nbsp; now. Yes, this is our talk about microcode. He's a&nbsp;&nbsp; grad student at the Danish Technical University&nbsp; and it's about microcode and hacking CPUs. &nbsp; But first, before we dig into the microcode&nbsp; itself, we need a little crash course on computer&nbsp;&nbsp; architecture. What we see up here is a CPU. It's&nbsp; a CPU core from the skylight processes. Inside of&nbsp;&nbsp; it, we have both the front end, that's the up top,&nbsp; and we have the backend down beneath it and some&nbsp;&nbsp; memory subsystem in the bottom. The front end is&nbsp; mostly in charge of doing stuff like fetching new&nbsp;&nbsp; instructions. That's where your high level... I'm&nbsp; saying high level for instructions here. That's&nbsp;&nbsp; the X86 instructions going in there into the fetch&nbsp; queue and they are getting decoded. And then,&nbsp;&nbsp; they will be translated into micro-operations. Micro-operations is what's actually running on the&nbsp;&nbsp; CPU. Micro-operations are then scheduled&nbsp; by the backend. The backend is the ALU,&nbsp;&nbsp; so the arithmetic logic unit and floating pond&nbsp; unit and stuff like that. And through that,&nbsp;&nbsp; the sorting stuff can go to memory through the&nbsp; memory subsystem. We're only going to take a very&nbsp;&nbsp; tiny section of the CPU and dive into today. The one we are diving into is this little&nbsp;&nbsp; tiny section. It's instruction queue and the&nbsp; decoding phase. Here when instruction comes in,&nbsp;&nbsp; it goes through the queue. If it's a very&nbsp; simple instruction, like just a simple move&nbsp;&nbsp; or an addition XOR - something simple like&nbsp; that - it will go straight through the simple&nbsp;&nbsp; decoders and they have a one-to-one mapping&nbsp; between micro-operations and the high -level&nbsp;&nbsp; macro-operations. We call it macro-operations&nbsp; and micro-operations from the ones going deep&nbsp;&nbsp; in the CPU. More complex instructions&nbsp; will go through the complex decoder. &nbsp; What we are looking at today is the really heavy&nbsp; instructions. That's something like return from&nbsp;&nbsp; a system call or when you enter a space, some of these instructions&nbsp;&nbsp; are multiple hundreds of lines long or multiple&nbsp; hundred micro-operations long. They can't just&nbsp;&nbsp; be directly decoded into a micro-operations,&nbsp; they go to the ROM and on the ROM, we fetch...&nbsp;&nbsp; Yes, we have like our normal CPU. We have&nbsp; microcodes. So you have an instruction pointer&nbsp;&nbsp; running through and executing code.&nbsp; Today, we are looking at that space. &nbsp; Why do we need this? Why do we need this complex&nbsp; set of microcode? Well, first of all, we have&nbsp;&nbsp; the complex instructions. Those cannot be directly&nbsp; implemented to transistors, but also bugs do occur&nbsp;&nbsp; in CPUs. So we had a Spectre meltdown a few years&nbsp; back, setting the scene of all of this. And just a&nbsp;&nbsp; few weeks ago, we saw the simply bug. So more and&nbsp; more of this kind of CPU bugs are popping up here&nbsp;&nbsp; and there. To fix these bugs post-production,&nbsp; we need microcode updates. Besides the ROM&nbsp;&nbsp; area where microcode is stored, we also have a&nbsp; small RAM address space. That is writeable and&nbsp;&nbsp; that's what we are going to look at today. Normally, these updates are assigned and&nbsp;&nbsp; encrypted by Intel, so you can't really have&nbsp; that deep dive on the... Go and look what's&nbsp;&nbsp; actually pushed by Intel to our CPUs. There&nbsp; hasn't really been a way so far to inspect&nbsp;&nbsp; that and we'll see what damage we can do&nbsp; with that if we actually get the access. &nbsp; This is a table showing how the address space is&nbsp; laid out inside of these CPUs. It's a very simple&nbsp;&nbsp; address space. It's totally linear. It goes from&nbsp; 0 down to 0X800. Or 8,000, sorry. Somewhere in the&nbsp;&nbsp; middle, we see a split going from read only at&nbsp; 7C00. That's where the writeable address space&nbsp;&nbsp; starts, so that's where we can inject microcode. Out on the columns, we see the instructions&nbsp;&nbsp; themselves. We have grouped them together in&nbsp; this table because that's how they're grouped in&nbsp;&nbsp; the CPU. They're called triads. One triad is free&nbsp; instructions and one implicit knob instruction. So&nbsp;&nbsp; every fourth instruction would just be an implicit&nbsp; knob. You can address them, you can jump to them,&nbsp;&nbsp; but they won't really execute anything. Besides that, we have the last thing and&nbsp;&nbsp; that's a sequence word. It's stored&nbsp; separately from the instructions.&nbsp;&nbsp; The sequence word is what groups these tracks&nbsp; together. They are the ones in charge of doing&nbsp;&nbsp; flow controls, so they can do branching based on&nbsp; testings, test instructions, and stuff like that. &nbsp; Let's take the first example of how an instruction&nbsp; could be implemented in microcode. This is the&nbsp;&nbsp; XADD instruction. It's exchange and addition&nbsp; in the same time. Normally, we see these X86&nbsp;&nbsp; instructions as a one atomic thing, but&nbsp; they're actually not. So this X addition&nbsp;&nbsp; is composed of these three micro-operations. Let's read through it. The first thing happening&nbsp;&nbsp; is that we take the source register. In this case,&nbsp; on the first line oring with a zero. The source&nbsp;&nbsp; register is the RPX in this case because it's on&nbsp; that side and RAX is the destination register.&nbsp;&nbsp; So oring with a zero, that just means that's&nbsp; basically just a move. We move it into temp zero&nbsp;&nbsp; and temp zero is a physical and temporary register&nbsp; inside the CPU that you normally can't access.&nbsp;&nbsp; Yes, so that's a simple move. Then, we do a zero extend, but in this case it's&nbsp;&nbsp; 64-bit. So it's basically... Again, just a move.&nbsp; We move it into the R-64 source register. That's&nbsp;&nbsp; basically taking RAX and putting into RBX. That's&nbsp; the exchange part of this instruction. After that,&nbsp;&nbsp; we do the final step and that's the addition. So&nbsp; we take the same temporary register and add it&nbsp;&nbsp; back into the destination register. We also store&nbsp; the resolving value in the destination register. &nbsp; Now at last, we see the final part. That's the&nbsp; sequence word coming into play. In this case,&nbsp;&nbsp; it's a symbol UEND and that means that the&nbsp; instruction, the macro instruction - the XADD,&nbsp;&nbsp; will end at this point. So we'll go and update&nbsp; the instruction pointer and fetch a new one. &nbsp; Now, simple example. What you saw before was&nbsp; the temp zero register. That's a new one to&nbsp;&nbsp; most of us. That's a temporary register only&nbsp; used inside microcode and it's not visible to&nbsp;&nbsp; the macrocode. We got 16 of these register,&nbsp; 0 through 15 they're called. Of course,&nbsp;&nbsp; microcode can also access all of the normal&nbsp; registers like RAX, RBX, and stuff like that.&nbsp;&nbsp; Besides that, we got eight floating&nbsp; point registers, just same thing as the&nbsp;&nbsp; temp registers. There's eight of them for XAND&nbsp; instructions also temporarily. Then, we can of&nbsp;&nbsp; course access system registers. That can be like&nbsp; segmentation of memory. Some of those registers,&nbsp;&nbsp; they're the ones that holds the state. Last, we&nbsp; have the U state register. That one will hold&nbsp;&nbsp; what modes the CPUs are running and all the&nbsp; critical stuff. So are we in 32-bit mode?&nbsp;&nbsp; Are we in 64-bit mode? Are we in a space and stuff like that. Often,&nbsp;&nbsp; the CPU will check that flag and do conditional&nbsp; jumps in microcode based on what state are we in,&nbsp;&nbsp; are we in privileged mode, are we&nbsp; in kernel space? Stuff like that. &nbsp; Of course, from microcode we can also access&nbsp; memory. There's multiple ways of doing this.&nbsp;&nbsp; One is for example, the virtual address space.&nbsp; The ones we usually do from macro-operations.&nbsp;&nbsp; We can store and read memory just as that.&nbsp; We can also access directly through just&nbsp;&nbsp; physical address space going just around all&nbsp; the page tables and stuff like that. Yes. &nbsp; Besides that, we have some very, very tiny memory&nbsp; dedicated for only microcode and inside the CPU,&nbsp;&nbsp; that's the ucode memory. It's a separate at&nbsp; address space from where we are storing the&nbsp;&nbsp; actual instructions. Data and instructions&nbsp; are separate completely. But here, we have&nbsp;&nbsp; around 0X100 keywords that we can also use a&nbsp; temporary storage. Each entry is 64-bit wide&nbsp;&nbsp; and it is used... The difference between those&nbsp; and the temporary instructions is that these can&nbsp;&nbsp; be used across multiple macro-instructions.&nbsp; So temp registers is for temporary storage&nbsp;&nbsp; inside one macro-instruction and this can store&nbsp; safe control registers when switching mode and&nbsp;&nbsp; stuff like that. And then, we can fetch it out&nbsp; later. So each entry has a dedicated purpose. &nbsp; Other than that, we also have two buses.&nbsp; Or a bus and a fabric. These are used for&nbsp;&nbsp; communication within the CPU. One of the very&nbsp; interesting one is the Control Register Bus.&nbsp;&nbsp; It can talk to all components inside one single&nbsp; CPU core. That could be the caches that are&nbsp;&nbsp; on the core. So for example, an L1 cache and&nbsp; maybe an L2, something like that. Besides that,&nbsp;&nbsp; we have the very important one, the microcode&nbsp; sequencer. That is the one in charge of scheduling&nbsp;&nbsp; instructions and going through the pipeline. So&nbsp; if we can go to the control register bus, we can&nbsp;&nbsp; access the microcode as well. Other than that,&nbsp; we have the Intel On System Fabric that is mostly&nbsp;&nbsp; used for external communication. So the ATT driver&nbsp; and USB stuff and other components that is shared&nbsp;&nbsp; between some of the cores. That is mostly for&nbsp; external communication outside of the main core. &nbsp; Yes. Okay, so now we talked a bit about that, but&nbsp; how do we place these updates in microcode? So&nbsp;&nbsp; we talked about that it's a ROM area, and that&nbsp; is read-only. When Intel pushes an update, how&nbsp;&nbsp; do they actually update a ROM area? That's where&nbsp; these match and patch registers come into place. &nbsp; What we see here is the bit fields and how a match&nbsp; and patch registers is laid out. We have 32 of&nbsp;&nbsp; these. Each time we fill a new instruction,&nbsp; we will check these fields. If we get a hit,&nbsp;&nbsp; we will jump to that address instead. So we&nbsp; have the present bit that will tell if this&nbsp;&nbsp; match and patch register is enabled. Then we have&nbsp; a source field that is, if we hit this address,&nbsp;&nbsp; go jump to somewhere else and that will be what's&nbsp; stored in the destination. As you see up here,&nbsp;&nbsp; both source and destination are two u address. So&nbsp; they're microcode addresses, but they're shifted&nbsp;&nbsp; by one. So one lose one-bit of position,&nbsp; and we'll talk a bit about how that works. &nbsp; Here, we have an example of a match and patch&nbsp; register. Let's say our CPU is running and the&nbsp;&nbsp; microcode instruction pointer hits 3C8. Then, we&nbsp; have a match and patch hit because we see a entry&nbsp;&nbsp; in the match and patch table with the same value.&nbsp; That means that we'll instead go and jump directly&nbsp;&nbsp; to 7C00 and start executing from there instead.&nbsp; This is what Intel programs into the CPU every&nbsp;&nbsp; time we apply a microcode update, it happens on&nbsp; every reboot. But we also find out through trial&nbsp;&nbsp; and error that this loss in position means that&nbsp; if we do hit 3C9, we would actually also get a&nbsp;&nbsp; hit and then we'll jump to the red area instead.&nbsp; So we will jump one micro-instruction further on. &nbsp; How can we use this, or should I say abuse this,&nbsp; when researching CPUs? So a lot of things we have&nbsp;&nbsp; been doing is to do dynamic inspection of state&nbsp; just also to figure out what's this code doing,&nbsp;&nbsp; but also to figure out what's instruction is&nbsp; doing. Here, we have a simple example taking&nbsp;&nbsp; from the ROM. It reads first from the RAM address&nbsp; space into temp three and temp two. But what if&nbsp;&nbsp; you want to inspect the state? Well, one thing&nbsp; we could do is that we can tell the match and&nbsp;&nbsp; patch register to go and jump somewhere else.&nbsp; So we see the overcrossed line, the red line,&nbsp;&nbsp; that is a test u state. And then normally, the&nbsp; sequence word will tell it to jump somewhere to&nbsp;&nbsp; the general protection fault and do a fault&nbsp; because this is a privileged instruction.&nbsp;&nbsp; But we can change that. We swap that out and say go to the&nbsp;&nbsp; RAM area. In the RAM, we have put our stuff and we&nbsp; will flip the test case. When we do that, we can&nbsp;&nbsp; tell it to either go and take the left path and&nbsp; say move these temporary registers into the RAX&nbsp;&nbsp; and RBX. These are registers that we can inspect&nbsp; from a debugger and from normal user space.&nbsp;&nbsp; Or if we are in a privileged mode, we don't&nbsp; want to screw up this instruction. So instead,&nbsp;&nbsp; we send it to its normal path and send it back&nbsp; the way we came from so we don't mess up the&nbsp;&nbsp; instruction and the CPU will still continue&nbsp; to function and run happily ever after. &nbsp; Okay, so how do we manage to make these changes&nbsp; now? How can we access the microcode sequencer&nbsp;&nbsp; when it's all locked down and locked up by Intel?&nbsp; Well, luckily the people at Positive Technology,&nbsp;&nbsp; they found a vulnerability in the Intel&nbsp; Management Engine that runs a software called&nbsp;&nbsp; Trusted Execution Unit. And they found a buffer&nbsp; overflow and that engine runs on a very highly&nbsp;&nbsp; privileged level. Through that, we can enable&nbsp; debug features. Debug features only Intel is&nbsp;&nbsp; supposed to have access to and that unlocks hidden&nbsp; instructions and undocumented instructions. Yes,&nbsp;&nbsp; we took the PC and expanded it to these square&nbsp; boards. That's actually the board lying up here&nbsp;&nbsp; that we are presenting from. And we have, of&nbsp; course, prepared flash image and stuff that&nbsp;&nbsp; you can flash on. So if you buy one of these, you&nbsp; can go and replicate some of our findings. Yes. &nbsp; The two instructions that we mostly use is the&nbsp; U debug read and the U debug write. These two&nbsp;&nbsp; instructions can write to microcode. It can&nbsp; read the data and read and write to the data&nbsp;&nbsp; areas we talked before. The write instruction&nbsp; also has a special feature where you can put&nbsp;&nbsp; in an address in the RAX register and just&nbsp; tell it to go jump straight to an address&nbsp;&nbsp; you specify in the microcode area. And it&nbsp; can also talk to the control register bus&nbsp;&nbsp; that we talked about before. So that's how&nbsp; we access all of these microcode features. &nbsp; Really good findings over at the Positive&nbsp; Technology side. But yes, so we tried all&nbsp;&nbsp; of this. We made our update and we got access&nbsp; to these instructions and we found this repo&nbsp;&nbsp; called Custom Processing Unit. It's a assembler&nbsp; that someone wrote for assembling microcode. It&nbsp;&nbsp; works from EFI BS. So you can take and compile&nbsp; microcode and put it on a flash drive. From there,&nbsp;&nbsp; you can apply the patches to a CPU. We did that&nbsp; and was hoping that it worked, but as soon as we&nbsp;&nbsp; booted up our Linux system that we're running on,&nbsp; all of our microcode patches, they were gone. Our&nbsp;&nbsp; initial thought was like, "Oh, we can't read the&nbsp; code. We don't understand it. It must be wrong",&nbsp;&nbsp; "Can't be anything else". Of course, we started&nbsp; writing our own assembler and we turned out to be&nbsp;&nbsp; very wrong and their code was running perfectly.&nbsp; But doing this, we took a different approach. &nbsp; What you have seen so far has been their&nbsp; syntax - the example we had before. But&nbsp;&nbsp; we made a more dynamic library. Actually, we&nbsp; made a Linux dynamically shared library that&nbsp;&nbsp; you can put into any C project that can change&nbsp; microcode on the fly. The assembly code looks a&nbsp;&nbsp; bit more like this. So we have more dynamic&nbsp; tooling, but also at the cost of syntax. &nbsp; What we see here is our function as YOLO. The&nbsp; first value or the first array here is ucode&nbsp;&nbsp; patch. That is the one we are going to apply. It&nbsp; moves 1337 into one of the temp registers. Then,&nbsp;&nbsp; it do dead beef into RBX and it takes temp zero&nbsp; and moves into RAX. So nothing tragic going on&nbsp;&nbsp; here. And then, it ends the instruction. Then,&nbsp; we use the patch ucode function to put these&nbsp;&nbsp; instructions in the RAM area. After that we will&nbsp; hook the sysexit instruction. we use that one&nbsp;&nbsp; because it's a nice playground instruction&nbsp; because it's never really used by Linux&nbsp;&nbsp; currently or anyone because it's a Intel-only&nbsp; instruction, so it doesn't work in the AMD.&nbsp;&nbsp; That is nice because when you fit a DBN or&nbsp; a whatever, they'll&nbsp;&nbsp; work on both hardwares. So this instruction...&nbsp; This is a privileged instruction, never really&nbsp;&nbsp; used. So we'll snag that one and use it for our&nbsp; and repurpose it as we like. &nbsp; Yes. We did that and we started to play around&nbsp; with the microcode. One thing we could do with&nbsp;&nbsp; this more dynamic approach and because we do&nbsp; it from the C language that we sought for,&nbsp;&nbsp; so we also have the power of all of&nbsp; C and it's dynamic powers. From C,&nbsp;&nbsp; we can program microcode changes versus putting&nbsp; it on a flash drive and being more static.&nbsp;&nbsp; We did that and we start just putting in... We&nbsp; made microcode that traced where is the current&nbsp;&nbsp; instruction pointer from macrocode and where is&nbsp; the current microcode instruction pointer? Then,&nbsp;&nbsp; we stored that to the normal RAM address space&nbsp; and we kept doing that. At some point when we&nbsp;&nbsp; lost our microcode changes, we know exactly at&nbsp; what instruction we were losing these changes&nbsp;&nbsp; so we could back trace, how can we fix that? And then, we discovered that in instruction...&nbsp;&nbsp; Let's say, instruction that's reading from the&nbsp; port IO space, so it's reading from hardware. We&nbsp;&nbsp; found that that has a lot of side effects.&nbsp; When we took a look at the opposite one,&nbsp;&nbsp; the write instruction, it was like three&nbsp; lines, three instructions long. This one&nbsp;&nbsp; was a couple of hundred long. It was way bigger&nbsp; than it should be. So it turns out that doing&nbsp;&nbsp; execution of the instruction, they have a&nbsp; hidden side effect from Intel side where they&nbsp;&nbsp; check the state of the micro-architectural state&nbsp; and reapply microcode patches if something seems&nbsp;&nbsp; odd. That's at least our interpretation of this. What we did was that at first, we just tried to&nbsp;&nbsp; nuke out the entire instruction and see if&nbsp; we just make it a big knob instruction and&nbsp;&nbsp; see if that would work. Well, it did work and&nbsp; we started keeping our microcode changes. But&nbsp;&nbsp; apparently, the kernel needs this&nbsp; instruction. Now, our kernel didn't work.&nbsp;&nbsp; But there's a solution for that. We just go&nbsp; 10 instructions down, try and disable it from&nbsp;&nbsp; there. Not working? Okay, what about five&nbsp; instructions longer down? Yes. Sure enough,&nbsp;&nbsp; when we chopped off that half of the instruction,&nbsp; we started to getting stable microcode changes.&nbsp;&nbsp; Now, we can update microcode from a Linux&nbsp; user space environment instead of just&nbsp;&nbsp; through an EFI shell. So yes, that's nice. I think we're going to do a demo of this. &nbsp; Yes. For the first demo, we're going to show&nbsp; this YOLO function which we had shown before.&nbsp;&nbsp; We are running this on the UP squared board which&nbsp; is already read, unlocked, and exploited. So it's&nbsp;&nbsp; just ready to run. If we look at this code&nbsp; again... Let me just zoom it. This was the&nbsp;&nbsp; YOLO function. It sets 0X lead and RAX and OX&nbsp; dead and RBX. And then, we have this wrapper&nbsp;&nbsp; around it which has some inline assembly that&nbsp; just calls the sysexitq instruction. If we then,&nbsp;&nbsp; just open it up in GB. &nbsp; Now, we are right before the YOLO function. And if&nbsp; we step over it, now our patch should be applied.&nbsp;&nbsp; Now, we are right at the sysexit instruction.&nbsp; If we step now, what should happen, normally&nbsp;&nbsp; at least, is we should get a general protection&nbsp; fault because this is a privileged instruction&nbsp;&nbsp; and we are in user space. But what actually&nbsp; happens is that if you look up at the top,&nbsp;&nbsp; RAX is set to 0X lead and RBX is set to 0X&nbsp; dead. So our patch was successfully added. &nbsp; Now for probably the demonstration you've&nbsp; all been waiting for, the backdoor.&nbsp;&nbsp; To show it, we are going to open up a browser.&nbsp; Here, we have just a site hosted here with a&nbsp;&nbsp; sweet little link here. "Click to get a calc",&nbsp; let's try and press it. And a calc pops.&nbsp;&nbsp; So that's very neat. But our exploration doesn't&nbsp;&nbsp; have anything to do with the JavaScript&nbsp; engine. Maybe if we open up Firefox, maybe&nbsp;&nbsp; we'll get another calc. But we wanted something&nbsp; even more general than that. So if we actually&nbsp;&nbsp; just copy the image link here and pop it into&nbsp; our shell and just wget it, another calc pops. &nbsp; So apparently, just getting this image pops&nbsp; a calc. How the hell does that even work? &nbsp; Okay, thanks. Yes, so you're probably&nbsp; all wondering how this works.&nbsp;&nbsp; For this, when we were engineering this, we are&nbsp; thinking about, "Okay, what kind of instructions&nbsp;&nbsp; can we target?" "Where is the place that we should&nbsp; put the Spectre?" So the RAMs were limited, we&nbsp;&nbsp; have few spaces, and we needed a good instruction.&nbsp; What makes up a good instruction? Well,&nbsp;&nbsp; it needs to be run very frequently. Every&nbsp; program should be using this instruction.&nbsp;&nbsp; Furthermore, this instruction that we are hooking&nbsp; into needs to touch some kind of user controlled&nbsp;&nbsp; data because we can't store the entire backdoor&nbsp; inside of this tiny microcode space. So we need&nbsp;&nbsp; something that touches user, that has user input. So our attention was drawn to the syscall&nbsp;&nbsp; instruction. The syscall instruction is the one&nbsp; in charge of handling kernel requests. So when&nbsp;&nbsp; you make a syscall, in the RAX register, you&nbsp; put a number specifying what you would like&nbsp;&nbsp; the kernel to do. In the RCX register, the CPU&nbsp; will put the return address. So where should&nbsp;&nbsp; the kernel return to after the syscall has been&nbsp; handled? If it's successful, then we should send&nbsp;&nbsp; it back to user space at the address specified&nbsp; at RCX. Also, a nice feature is that a lot of&nbsp;&nbsp; syscalls take parameters. Some of those can&nbsp; be that user data that we are looking for. &nbsp; Let's jump into some pseudocode. We can't show&nbsp; the entire microcode changes because it's very,&nbsp;&nbsp; very big. So we'll do a pseudo C thing&nbsp; that will tell how this Spectre works.&nbsp;&nbsp; So we hook the syscall instruction right in the&nbsp; beginning of the syscall instruction. And then,&nbsp;&nbsp; we first check, "Is RAX says to a syscall&nbsp; write?" Because if it's a write instruction,&nbsp;&nbsp; then it either writes to standard out or it's&nbsp; doing file manipulation or stuff like that.&nbsp;&nbsp; So for example, if Chrome or Firefox is going to&nbsp; cache a image you need for later, then we'll do a&nbsp;&nbsp; write to the file system. So that's pretty neat. Then, the next check we make here is that we just&nbsp;&nbsp; check for a matching value. We don't want to&nbsp; execute every image pulled from the internet.&nbsp;&nbsp; If one of these cases fail, go do the normal&nbsp; syscall. But if we hit both of these conditions,&nbsp;&nbsp; the first thing we'll do is that we will save the&nbsp; current state of the CPU. We'll take all of the&nbsp;&nbsp; registers that we touched during our actual shell&nbsp; code and we'll store it into memory. So that's&nbsp;&nbsp; just what we're doing here, we'll save the state&nbsp; of the CPU so we can exit cleanly. After that,&nbsp;&nbsp; we talked about... We set RCX and we put it at&nbsp; RSI. The reason we take RSI is because the RSI&nbsp;&nbsp; register contains a address for the image data. So&nbsp; that is the image data itself. And then, we add a&nbsp;&nbsp; small offset. And then we will now say, "Okay.&nbsp; Kernel, when you're done doing your request,&nbsp;&nbsp; now this is a new address that you&nbsp; should return back to after handling&nbsp;&nbsp; whatever you're going to do. So when you return&nbsp; to user space, execute actually our image data". &nbsp; But now you may say, "Oh wait, hold&nbsp; on a minute". CPUs nowadays has MMUs,&nbsp;&nbsp; memory management units. So you can't just&nbsp; execute avatar data everywhere you like.&nbsp;&nbsp; That is the neat trick about the syscall&nbsp; instruction because Linux has a syscall&nbsp;&nbsp; called mprotect. So instead of saving the image&nbsp; and doing the write syscall, well let's put the&nbsp;&nbsp; sysmprotect into RAX and change the request we&nbsp; make to the kernel and say to the kernel, "Hey,&nbsp;&nbsp; this address base in RSI, can you please go and&nbsp; make that read, write, and executable at the same&nbsp;&nbsp; time?" And then when the kernel jumps out of the&nbsp; kernel space, it will jump happily into executable&nbsp;&nbsp; code that we control. That is our image. "Oh,&nbsp; well. It's stored in the metadata of the image,&nbsp;&nbsp; so you don't see anything or the image itself". Let's briefly go over what shell code we actually&nbsp;&nbsp; put in. It's super simple. The first&nbsp; thing we do is do a fork. And then,&nbsp;&nbsp; we have two processes running in parallel. The&nbsp; parent process will now be the new main thread&nbsp;&nbsp; actually handling the browser like Chrome stuff.&nbsp; So what we do is we restore the saved context&nbsp;&nbsp; from the ones we saved in memory just on... Yes,&nbsp; the step we did where we stored all the registers&nbsp;&nbsp; into the image data itself. We'll now pull that&nbsp; back out and put it back in place in the CPU in&nbsp;&nbsp; that thread. And then, Chrome can continue its&nbsp; execution happily and won't notice that we have&nbsp;&nbsp; spawned a child thread. Now the child thread, they&nbsp; can just go do all the evil stuff it wants. In&nbsp;&nbsp; this case, it's probably a calculator. Before we finalize,&nbsp;&nbsp; let's look a bit at how did we do some of this&nbsp; reversing process. Of course, there has been&nbsp;&nbsp; some pioneers in the field that started out this.&nbsp; Positive Technology, and CustomProcessingUnit,&nbsp;&nbsp; et cetera. But still a lot of unknown instructions&nbsp; is there. So how do you reverse instructions in a&nbsp;&nbsp; new microcode assembly language where half of&nbsp; the opcodes is just doing unknown stuff? Well,&nbsp;&nbsp; one trick we did is that we used a lot of what&nbsp; we call side-by-side reversing. So on one half&nbsp;&nbsp; of the screen we take the pseudocode for this&nbsp; instruction, and on the other half we will have&nbsp;&nbsp; the actual implementation. And then, we can go&nbsp; back and forth and see, "Oh, on this side we have&nbsp;&nbsp; three compares there or three writes" or looking&nbsp; for bit patterns in the pseudocode and seeing,&nbsp;&nbsp; "Oh, we have the same three writes over in the&nbsp; actual implementation". And just after that we&nbsp;&nbsp; have that unknown thing, the unknown opcode,&nbsp; but what is it in the pseudocode and can we&nbsp;&nbsp; extract anything from that? That is one thing. Another is just like tracing like the dynamic&nbsp;&nbsp; tracing we saw before. Holding in the middle&nbsp; of instructions and pulling out the data that&nbsp;&nbsp; could be like from GDB that you saw before and&nbsp; also just maybe copy-pasting stuff. If we have a&nbsp;&nbsp; big instruction, then copy-paste the whole thing&nbsp; into the RAM address space and just redirect it.&nbsp;&nbsp; Now, you have a clone and now we can start&nbsp; flipping one bit at a time and seeing what&nbsp;&nbsp; slight changes did that affect. Most of the&nbsp; time it's a CPU cross bit, but some of the&nbsp;&nbsp; times with we'll cataloging and we see different&nbsp; values in our register after that instruction. &nbsp; Yes, we talked a bit about playground&nbsp; instructions like sysexit because it's&nbsp;&nbsp; never used, but there's actually more&nbsp; of these. So some of the virtualization&nbsp;&nbsp; instructions for virtual machines,&nbsp; hybridization, and stuff like that,&nbsp;&nbsp; that could be vmwrite, vmenter, and some of&nbsp; the hybridization. Those are instructions&nbsp;&nbsp; that is also never used unless you are doing&nbsp; virtual machines. But if you don't do that,&nbsp;&nbsp; these instructions will never be used and some&nbsp; of those take arguments in form of registers.&nbsp;&nbsp; So there you get playground instructions,&nbsp; which also happens to decode registers. &nbsp; Finally, before heading off, we want to talk about&nbsp; future work that we or even you might do because&nbsp;&nbsp; we are uploading everything right after this and&nbsp; making everything public. We really wanted to&nbsp;&nbsp; find CPU bugs, or should we say exploit CPU bugs,&nbsp; because we actually get a little bit of help from&nbsp;&nbsp; Intel here. If we do apply our microcode patches,&nbsp; the official ones from Intel, then we can also&nbsp;&nbsp; look inside the match and patch registers that we&nbsp; saw before. Now, we know where do Intel hook from&nbsp;&nbsp; and to in the ROM and RAM address space. Now, we&nbsp; can have a look inside what box has they already&nbsp;&nbsp; discovered and patch that we have never seen. Our goal or hope is that we could Red Unlock&nbsp;&nbsp; the CPU, so put it into this Intel debug&nbsp; mode without having the physical access&nbsp;&nbsp; beforehand. If we can abuse some of these bug,&nbsp; we think that it could be possible that one&nbsp;&nbsp; could take control of the micro-instruction&nbsp; pointer and point it to some of these debug&nbsp;&nbsp; instructions. That would be a very cool thing&nbsp; to do and hopefully we get the time to do it. &nbsp; Now last, I want to say thank you, especially to&nbsp; Kalmarunionen, that's our CTF team. They have been&nbsp;&nbsp; a great help both sponsoring but also having a&nbsp; hundred plus hackers around, always available to&nbsp;&nbsp; answer any stupid questions you might have about&nbsp; a CPU. Well, thank you guys. Also really, we&nbsp;&nbsp; acknowledge the work that Mark Ermolov, Positive&nbsp; Technology, and CustomProcessingUnit has done&nbsp;&nbsp; to lay the ground for this talk. Thank you for&nbsp; listening in. And do you all have some questions? &nbsp; Yes, there's one. &nbsp; First question is- First question is now that you know that you can&nbsp;&nbsp; do this, . So the question is what are the primitives&nbsp;&nbsp; could you imagine building from this? So in&nbsp; general, I think this is a very strong primitive,&nbsp;&nbsp; like putting a backdoor so hidden away. It's&nbsp; basically completely undetectable because the&nbsp;&nbsp; OS can't see it. But I could imagine, for&nbsp; example, game hacking and stuff like that,&nbsp;&nbsp; you can hide it away from any cheat engine&nbsp; check or stuff like that. I could imagine&nbsp;&nbsp; that could be useful there. Other than&nbsp; that, I don't know. I don't know. &nbsp; Okay. Yes, that's one. Where can we buy the dev bot? &nbsp; Where can you buy the dev bot? Okay, the&nbsp; dev box is a UP squared. On the slide,&nbsp;&nbsp; you'll find a link to our documentation&nbsp; and on there it will have the exact dev&nbsp;&nbsp; bot that we use. You can order it&nbsp; online and we have a flash image&nbsp;&nbsp; that you can just flash on it. And then,&nbsp; you're ready to go to play with this. &nbsp; Okay, I think there's no more.&nbsp; Thank you for listening.
