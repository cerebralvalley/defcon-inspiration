Hello everybody and thank you for being here today to in this session this presentation we are going to talk about security and the algorithms and the devices in charge of our security when we are navigator on internet or we are sharing files or information we're going to talk about keys hesit also we're going to we're going to talk about the future threats that can essentially break them for example example the quantum computing also in the in the beginning of the ending of the presentation presentation I want to propose you to use reconfigurable devices to uh be in charge of that security recon reconfigurable devices are devices that the the designer can program or can configure its internal hardware um architecture in order to make them resilient to new uh thread for example or to the to update the algorithms which are running inside of the of the device. But first of all let me introduce myself. myself. I have to say that I am the freak on that conference because I am not software developer. I am FPGA designer. FPJs are devices. I reconfigurable devices that I can program or I can design the or I can implement the design which is printed inside of the of the of the integrated circuit. I also write on internet. You can read me sorry better. Ah better. Ah thanks. Okay. I also write on internet. You can read me on hackster xhack a day and also on my own blog contourpath.com. You can uh if you want to tell me something you can write me on my email and you can find me also on Twitter with the name contropath. Well, first we're going to talk about the privacy protectors. The privacy protectors now we can we can find has algorithms and keys. Regarding has algorithm we have MD5 and the SHA fun SHA 1 2 and three. If you still using MD5 or SHA1 you are in the correct talk regarding the keys. We have SSA we have key and we and we have is we're going to see how they work very quick. very quick. Regarding hash algorithms, a hash algorithm is an algorithm in which I can introduce a variable length string and then it will return me a fixed string, a fixed length ext. The key is that a very small change um created very small change in the input string will produce a big change in the output string. So they can be used in order to um verify if a file has been changed in the transmission or not. As I said before we have a different has algorithm. We have ND5 and the SHA family 0 1 2 and three. Now only SHA 2 and SHA3 are still alive. The first one will that was broken was SHA zero in 1989 999 then MD5 and the other one broken was SHA1 h all of them die all of them was uh were broken because a set of um a group of investigators get to um create a custom hash by modifying the input string. This is known as a collision nowadays SHA 2 and SHA 3. But if we add a new player with like is the quantum computing only SHA3 will be alive. Regarding the keys, keys are essentially passwords are passwords that I can use to encrypt a file or encrypt some information. information. The the thing is that only the user who has the password, who has the key are able to read the data or decrypt the data. When I talk about data, I'm not talking about just a files. I'm talking about realtime data. For example, in TLS communications or SSJs is data that is sended I sent and recited in real time. We have two uh two type of keys. We have symmetric keys and asymmetric keys. Regarding symmetric keys are the easiest one. There's only one key for encrypt and one key for and the same key for decrypt. The problem here is the way in which the h creator of the key share the key with all the recipients. To fix that we have the asymmetric keys or asymmetric encryption in which h we have two different keys. We have a pair of keys. One key is used for encrypt the data and the other one is is used for decrypt the data. And the password used for decry data cannot encrypt encrypt the data. Usually the the key used for encrypt the data is known as public as public key because anyone can encrypt the data that wants to I be able to to read. read. Okay. All of these algorithms are just math and some logic. The names like Rendell, Lify, Helman, Chacha and and Salsa are the names of that algorithm that as I said they are based on mathematical and logic operations and key of that algorithms is the performance of the of them. The performance is the speed of how quick they can encrypt and decrypt data. And this is very important if we are performing a real time ciphering. For example, in the case of the TLS or SSA, SSH. SSH. So we don't talk just about algorithms. We also talk about the way in which we calculate the cipher. we we encrypt the data or we decrypt the data. This is known as the operation modes and it is critical for example for ice encryption. Okay. So we have the algorithm that we know now that are based on mathematical logic operations. So we need to make them reality. We need to implement them. We have two different uh ways of implement this kind of algorithms. We have the software implementation and the hardware implementation. The software is easy is the usual way in which we uh write the code in order to create all the ma or resolve all the mathematical operations. When we are talking about hardware implementations, we're talking about use an external hardware to let them to let the the external hardware compute the the mathematical equations and the the decryption or the the decryption. Okay, regarding the software implementation, h as I said before the implementation is made by code, C, Python, whatever Python, whatever this is not the performance that we can reach with this kind of implementations is very is very poor. Why? Because if I have a processor that it is running at three GHz or 5 GHz, the processor does not execute the instructions at that speed. So in addition we have the operating system. So uh all of these um elements between the hardware of the pro of the processor and the algorithm will make that the implementation of these algorithms just using code will won't be well the performance is not good. Manufacturers know that. So they uh what they do is implement the uh processor extension. Processor extensions are pieces of hardware that are near to the processor. We have processor extension to calculate SHA to calculate the I. The SHA is the SHA knee and the ice is the eyes knee. Another key of that kind of software implementation is that the keys has been stored in the hard disk. in the hard this if we want to make them nonvolatile and they will be implemented in the RAM memory if they can be bolated. bolated. One thing that make this so software implementations very useful is that they are very easy to to update. If I want to change uh some of the algorithm that I want that I have implemented, I just need to make an update of the software running another script and it will and it will work. However, uh this is not um this is not all good. As I said, the implementation in in algorith the of the algorithms made in by code has a very poor performance. Also, yes, we can use the processor extensions, but in this case, we are going to we're going to create a dependency of the processor and if we create an algorithm that uses a processor extension and then the other uh other user execute that algorithms in a processor which doesn't have that those extensions, our algorithm our algorithm won't work. This is especially critical when we are working with embedded system because not only manufacturers create the same processors even if they use the same ARM core h keys stores in hard this is absolutely non not secure and the same for the easy to update another player can update the software in order to make them vulnerable the other implementation we talk is hardware implementation when I talk about hardware implementation I talk about use an external device to execute the algorithms which is great. This h allow allows us to store the keys very securely because that keys can be read right only. So I can h send the the key and then no one will be able to read the the same key. Um this the hardware use in these cases are made by manufacturers that the algorithms algorithms that they implement algorithms they widely use algorithms. For example, usually all the hardware of this of this type implements SHA 2 256 normally and ice up to two 256. And uh finally it is true that we eliminate the dependency of the CPU. However, use an external device to execute the different cryptographic algorithms. It is more than execute these cryptographic algorithms in other processor. They allow a secure storage for keys which is great and we eliminate the dependency on the CPU. But we are create we are going to create a dependency of the hardware use and standardize the algorithms use. Okay. H as I said the manufacturer wants to sell the the hardware. So they will implement the widely used algorithms. One of that hardware is the well-known TPM strate platform models. They are cryptographic processors usually integrated in the same model board. They allow secure storage for keys and they include a set of cryptographic functions. functions. The nowadays we have two important version TPM 1.2 and TPM 2.0. TPM 2.0 is very very very famous because if you want to run Windows 11 on an old computer, you would need to to include a TPM 2.0 in the motherboard. Okay, let's take a look to the practical guide of the trusting computing group which is the group in charge of the developing of the TPM 2.0. We can read one problem that begs the developers for a long time was how to integrate multiple has algorithms if one of those has algorithms was later broken. We have seen that the has algorithms will be broken ever. And what can uh break that algorithms? Well, for now the most important thread is the quantum thread of the quantum computing. Quantum computing is an new computational mode that is based on quantum physics. quantum physics. It uses It uses unlike the traditional bits that we can find in a regular computer with they can take values of one or zero. Quantum computing use quantum bits or cubits that can take values values of one zero and all the values between them. They can they can do this because of the superposition principle of the quantum physics. physics. This h allow the processor to be able of execute of evaluate multiple choices at the same time. And obviously the quantum processors allow the execution of quantum algorithms. And the quantum algorithms that are related to the to cryptography are the group algorithms which use the quantum superposition principle principle and the use of this algorithm results in um help us to of to finding elements within an unordered list. Summarizing, if we have a set of solutions of length n, the set of solutions is reduced to a square root of n, which is great. The other algorithm that can represent a threat to the cryptography to the no to this cryptography is the source algorithm. This algorithm is used for finding the prime factors of an integ n. This result is in if we have a set of solutions with a length n it will be reduced in a logarithm of n cq which is also great. The use of these two algorithms is related in h translates in this table. We can see how algorithms of 20 256 bits uh 2 256 bit are reduced to 128 and the same for the keys and is an is another case regarding such and eyes all of them can be broken using the growers algorithm except SHA3 that you can see the reduction of the when we use quantum computing is known. So we'll still have keys or keys or hashes with a length of 256. This h the result of this is SHA 256 will be broken. Is 128 will be also broken. RSA will be broken. broken. Fortunately, TPM manufacturers knows that and they will implement algorithms that resist the quantum computing or not. This is a TPM from Infin and we can see in the in the bottom symmetric cryptographic the algorithm that in that it supports and there are no SHA2 512 or SHA3. So this kind of or this group of TPMS will be vulnerable in a in a time. So what I propose use reconfigurable devices. They are devices that are able to or reconfiguring its internal hardware. And by reconfiguration I am not talking about reprogrammability. I am talking about reconfigure reconfigure do its internal hardware. They also allow partial reconfiguration. We can h update only a part of the integrated circuit and h they will keep all the benefits of the hardware implementations. implementations. They can be based on FPGAs or other devices but but mostly on FPGAs. But what is an FPGA? FPGA is a matrix. It is an integrated circuit in which inside we can find uh we can find a matrix of different elements. We have input and output blocks and we have a big set of configurable logic blocks. Also, we can find multiplication and accumulation blocks and block RAM blocks. blocks. The key of these elements or what makes them great is that I can use a set of blocks to implement SHA2 256 for example. and another in another side of the in another part of the FPGA we can implement an RSA 248. These two algorithms are ex are executed are implemented in different parts of the FPGA. So they can be run in parallel. If any of them will become vulnerable because of the quantum computing or something else, it is changed it is easy to change them to a postquantum algorithm. For example, the SHA2 will be transl will be converted into S sha 3 which is for postquantum and RSA can be converted to key. H to test this I created this uh this project. I am not trying to selling you anything. This is an open an open source project and it is a hardware secure model based on the AMD Spartan 7 FPGA. As I said, it is open source and it changes the traditional I square C algorithms algorithm by the SPI via an SPI interface which can reach a speeds up to 50 megabits per second. The limitation of the 50 megabits per second is because of this device because of the device I designed and not uh we can reach higher speeds if we for example use a PCI communication and obviously it allows um remote update once it will be deployed. The advantages of using this kind of HSM okay reconfigurability reconfigurability allows to implement or to update the algorithms and also they avoid or com they avoid or allow us to confuse site talent attacks and timing attacks because I am um I am implementing the hardware within the device. So I can for example if someone will uh wants to attack me using a timing attack I just have to make that the the encryption using any key will take the same time and uh the same for site attacks. attacks. They are able to generate true random numbers and they allow also to implement or to execute multiple multiple algorithms at the same time and also paralle disable modes operation modes can be can be used possible disadvantages. Okay, FPJ are primarily based on RAM or RAM memory. This can be fixed. The design typically will require an external an external memory in order to store the the design and usually they don't incorporate don't incorporate inside a nonvolatile memory. Also we can fix that and regarding the the the power consumption they FPJ will consume more than a regular TPM. However, However, uh regarding the handicap of they need an external memory for uh to store the design. Okay. Uh AMD for example allow us to encrypt the design with IS256. The good side is that the key is stored within the FPGA using fuses. So they cannot be read externally. Also if if I uh disable the gtech interface to avoid anyone reprogram that FPGA it will increase the the security and the encryption key used for the flash memory that stores the design is gener is generated and and fused in the manufacturing process of the FPGA. And regarding the key storeware, it is easy. I just need to add to the board an apron memory for example. And then I can use an SPI driver that encrypts all data that I write in the apron memory. The key in this case can be also stored in fuses inside the FPGA. So they can be also write only the key cannot be read realtime cipher. I said before that I implemented an SPI interface instead of a a regular IQ square in interface. Why? Because the speed with H SPI interface we can reach around 50 megabits or even 100 megabits per second and it will be very uh will be crucial when we are talking about real time ciphering and true random number generation. I said before that I am writing I am um I am writing the hardware which is running inside of the FPGA which is printed in the in the FPGA. So I can control the length of the um of the the length between the connection of one logic block to another. But what I cannot h control is the purity of the wafer that I that the manufacturer use to h print the circuit and it will this result in the delay between the between two logic blocks will be will depend on the purity of the wafer and also the external temperature and the the external pressure. This will result in the the time expend by a bit or by the electricity from to go from one logic block to the other is a is completely random and the use of this kind of of HSM also allow us to create very custom very customized solutions. In this case, I created a very simple example when I have a camera camera connected through me to one crypto and then the communication with the host will be crypted. On the host side, I can add another crypto in order to decipher the the decrypt the data and uh talk with the host using SPI uh SPI interface at 50 megabits per second. second. And that's all. Thank you very much for being here and thank you to the crypto.
