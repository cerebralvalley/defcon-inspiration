good morning welcome to Def Con and today I will talk about a topic cannot make OS and iOS kernel debugging and hit from tree so here is the outline first I will introduce myself and what is the kernel then I will introduce how to do kernel debugging on Mac OS and as an iOS then I will use these debugging techniques to debug a real Colonel hip overflow last but not least the IOU I will introduce two methods to hit from tree ok let's start so Who am I my name is Min Jung you can call me spark I'm a security expert at Alibaba and I'm a PhD in Co HK and I'm a member of Blue Lotus and inside labs I worked in fire to an attention before our team have achieved a private jailbreaking iOS 9 and we will show more details in the future here is my Weber and Twitter you can follow me and ask me questions on that this work has a coarser code illusion he is my colleague and he's a security engineer at Alibaba I also want special thanks to my friends which helped me in this work so the topic is Cano debugging what is the kernel the kernel is actually the axon you exciting you is the computer system operating system operating system kernel developed by Apple it is open source software as part of the towel and operating system it has a provision called X is not Unix we know that X and you for Mac OS is open source it can be compiled and debuggable however action you for iOS is not open source you cannot compare it or the packet or visually but most implementations is the same as Mac OS however we can use some tricks to Cano debugging for iOS two and I will introduce it later let's talk about Mac OS debugging first a wise man once said to do a good job when a master shopping was tall so we need to buy some equipments first thing we needed to make books of a MacBook is virtual machine the system version can be different which means you can debug iOS 10.10 Alice 10.11 also you need to buy some commenters or keyboards like some Thunderbolt firewire or firewire cable after we have the equipment's we need to install CD key on the to make books for for the for the host macbook we need to execute a command called FW GDP for the department Bach we need to copy the Kannada develop development of kitty kid who the system library kind of order in a department book and execute the following command this command will set the environment and reboot the debug the debug MacBook after rebooting the hosts MacBook can start debugging with commander error DB GDP remote localhost note that we can get Connor Salida immediately it is very useful for us to debug the colonel also we can use some command like image list to get the kernel address of Hydra kernel extensions note we can only get partial kernel extensions if we want to get all the kernel extensions we need some other ways just like gdb we can use X slash and exit to read the data in the kernel also we have three ways to set the breakpoints in the kernel by using by we can use be asterisk address to set a breakpoint the RDB we needed to use the offset of the kernel cache class the canal is to calculate the address another way to post that debugging machine is to use a shortcut key you can use command + our class ctrl + shift + yeseo advanced and in a will pause the debug machine immediately we can also set breakpoints in the external source codes row in the dollars-three and current Konoe information through printers note that if you're this way too deep to debark the kernel you need to recompile the kernel and then put it in the debug machine also we can use commander script import this manner to load them the peasant script in the kernel in the ARD B it is very useful because the scripts that have helped me get a lot of information about the kernel for example you can use the printed who printed the zoom the zone information and you can use show the free list to show the element information in the free list there's another command color show all kernel extensions this commander can get all the kernel dresses of the kernel extension in addition you can implement your own peasant script you can find some examples in the KDK is passing folder okay so let's talk about iOS kernel debugging we know that there is no official tools for iOS Konoe debugging but we can use some tricks to do that before we debug the RS kernel we needed to get the colossal kernel cash unlike Mac OS we need to decrypt so cannot connect Hesh before iOS turned the kernel Quechua encrypted you can find a kid in though we iPhone wiki website and then decrypt the canal cache after Iowa strength there's no encryption but there are some in code so we need to unzip the kernel and because the kernel using image football after that you can extract Cano Cano information through talker and ADA note that there's no proponent in iOS so the most common way to get the register value is to is use the penny use the panic log something should the pay attention is if there are too many pain o'clock senior from the system will stop generated a penny penny o'clock so if you're one who debug your iOS iOS system you need to delete the penny logs if there are too many so you can use this to measure to do the other way is for geo brick is another is for under a brick u.s. although there's no key deacon iOS we can see us cannot have thought to do a paper economy married and right through to you dole and API when is called mach vm read another is Mark VM right but what if we don't have the task for PID page on no jailbreak what should we do well universe they need Canova nobility then you can use it when already two together the cannot hotspot then you can restore thus cannot has poured into the hosted special pot then you can use userland if I call the host get special pod together cannot has taught in the user land and then you can use mark VM read and mock be invited who to the cannot read and write in the user land after catching the cannot ask the next step is to figure out the kinetic space and slide yeah I'm sorry too is easy because there's only 256 potential in locations for the canosa light for the year I'm 64 and it's not easy we need to do something like first create an OS object in kernel then we need to find its visible pointer which pointed to the Knaus base region then we search backwards from the visible address until we find the kernel header the source code can be referred to the iOS cano utilities project application the colossal Ida we can get the root privilege for our applications through the kernel read and write and then we can use offset plus nozzle ID to find the kernel object address of related approach in memory it is very useful to debug the market in the X in your system so now I will show how to use the debug techniques to debug a ryokan or if overflow this vulnerability exists in the mark water extractor attributed recipe trap this is a new function added in iOS iOS and Mac OS 10 point 12 so that's why there's no job read for iOS 9 points 3.5 because this function does not exist in that version it's a new function in our Western this mock trip can be called inside the sandbox so we can we can attack the kernel in the inside the sandbox this function will first call your cohesion to get the recipe site from the other land and a sieve decides to the SD then it will use key lock to allocate as the size remember a block of memory with the size and then it will use copying to copy the data from the user land took to the kernel however the developer focus that the recipe size was a user land poor user mode pointer and then it used as a science value in the coffee we know that a user mode pointer may may be very large than the size value so it may cause the hip overflow if we want to debug this vulnerability we can set the breakpoints before and after copy copy IO the Z address are calculated through the offset in the kernel cash plus the colossal ID so you can as you can see before hippo flow we can find the flag of dead peace the dead people means this this block of memory are unused but the next block of memory is used with ffff after after we trigger the chief wolf overflow we can find that the first block is for our our data with a and and then the next block of memory is overflowed by our and by the first block of memory it overflowed study to vice with character P so now we have the ability to overflow arbitrary content of data but we need to find a way to do the kernel read and write so we need to do some country there are two ways to do hip-hip filtration I Western and Mac OS 10 point 12 I will introduce the first one we know that iOS and Mac OS turn on control we cannot use the classic we have map copy techniques to do the hippo from tree by changing the VM map size because Apple added a new mitigation that they were all check the three into the random attack which means if your changes the size of the VM copies the Kanawha panic so we need to find a way to avoid that luckily purium google project zero proposed a new way to do hip hundreds through the pre eric market the basic idea is to use mech pod airlock food to a luke i pc key message objects in the kernel memory the memory object contains a size field we can craft it and it doesn't have any pointers so we were not craft any pointer in the kernel memory so it will not be panic by using the exception pod we can send and receive the data in the kernel memory and this data will not be freed it's very important and the data we use the tool sender is the crush is the register value of the crash thread so if we want to send the information to the cano we need to create a thread and a setter the register value we want to send anything crashes the thread the data will be send to the to the address of IPC kim message objects plus I am sighs - 104 so why is the numbers were well off or we can use cannot be bugging to figure out it first the way us Cano debugging to figure out the address of Priya log pod buffer in the memory then we configure the exception and send the data to the kernel after that we can use cannot debugging machine like cannot read to get the data to inspect the data of the buffer as you can see we can find the location of the data in the buffer is t3c because we set the value of the I km size to e4 Oh therefore we can care we're over at elastic so that's why the number is 104 so now we when we get the object to do the hip functions the next step is to rearrange the kernel memory first the way allocates 2,000 pre a local post to ensure the following post continuous continuously and then the attacker can generate can work three ports when is the holder the second ax is first thought the third is second aport both of them in the zoom Russa 4096 then week then the attacker can freeze of holder and I use the overflow of the vulnerability to overflow the first report all of our flow data contains the ikm size and other fields of the ikm object the key point is to state the ikm size - 104 so why is 104 we can do a simple calculation that the first the address of the first port plus y o4 - 104 it will be the address of the second report which means we can control the second approach through the first pot so if we can control the second of the data of the second APOD the next step is to get the address of the second pot which means the way need a heavy information link so how to do that the first thing is the attacker give your the first pot to give the second pot a valid header if the second pot had a very header Uslan you send the data to the second important that the second importance i came next and I camp rivers will be said to point to itself which means if you'll receive the data from the cannot who get the day the second pole that you can figure out the colonel at the location of the second apology in the kernel memory after catching the shape address the next step is to get the canosa lied but before we do that we need to safely free the second aport the reason is if you don't do that the system will be a panic because it cannot detect us our memory corruption after freeing the second apart the attacker can I look at you declined to hold a spot of the second approach then the attacker can get the user client object us through the first of all note that the first eight part of the kernel userland a user client object is the vehicle address so which means you can use the visible address in the kernel cache compared with with this dynamic invisible address then we can calculate the canosa light in this case the canosa lighting is one pc zero zero zero zero zero after that the attacker can generate create a rope chain which can be used to do the kernel of african-american hey use the we can't use our centralized with with yoyo ID coffee in this way the attacker can copy the data from any address to the kernel buffer a big plus for it and as I use the first pod to get this data back to the user mode if we reverse the x1 x0 he can get a no memory right because one is to do the read another is to do the right so we get the rope chain the next step is to trigger this rope chain we can trigger this roof trans through the i/o connected again service this method will invoke get mental class return and a release method so we can create a fake of a table with evil and Sunita - the second approach then we can use the IO connect get service to trigger the rope chain and we mentioned before to do the kanuri than right when we get the ability of cannot read and write we can do the cannot fetch the ladies and the public cannot fetch technique could be referred to Yahoo note that this this traditional phone tree it's not deep stable because it need to do multi trans and used a lot of rope chain it only have a 50% said for it so if we want to care if one we want to have high success successful read we need to use poor country so what is pod we know that mag message is the most frequently used is a mechanism in acts a new and a we can use complicated message to still out of land host to the kernel which means we can send the pods objected to the kernel and in this case are we were standard 32 much thought to the kernel and each each pod use the advice and eight byte multipliers early to is 256 so the data will be send to the zone 1 256 note that though out of Lampard's sieve the in mark message other IPC object pointer and the pointer can be pointed to the Euler model dress therefore the attacker can overflow this load pointers and modify the pointer to point to a user mode and then we can control the spot in user mode also create a big task for this big pot as well so how to overflow the right spot the right product is the key so we need to do some fun treat to rearrange the kernel memory the first thing we should do is to send lots of oil pod to the kernel to ensure the new allocated blocks are continuous and then the attacker receives some message in the middle to take some slot then the attacker sends a messenger came to make the overflow point at the middle of the slot and then the attacker can trigger the if overflow vulnerability at the overflow point then we receive all the port from the kernel and check the value of the port as we mentioned we send the dead port to the kernel with FF FF if the value change to some other value which means we this port is overflowed by us and a weekend we can control the spot in the other land so if we can control a pod in the kernel what we can use is this ported to arbitrary cannot memory read how to do that we need to set the i/o bytes of the fig IP object to iqg task and craft fig task for the fig thought by setting the value at the fig has to pass the process of the attacker can do arbitrary memory read it is very it is amazing because the function doesn't check the validation of the J attached and adjust the return the value of that address as you can see in this function the the a old holy name to task to get the fixed task and if extract is controlled by us then it will use Gators PST task info to get the information and so the function will use a 1 + 3 8 o to get the together the data are that address and this address is controlled by us also in a way use the process ready to get the information on the even class where oh and this address is also controlled by us so you can get this so we can't get any address in the data in any address and we get the kernel kernel arbitrary readability then we can dump the kernel IPC object and cannot ask to our fake IPC object and and kovaydin to our freak IPC object and a big task then we can use a noodle an API called test against better pod to get the cannot has caught then we can use this cannot afford to do - to code - if I where is when a mach vm read analyze Marc VM right to arbitrary cannot read and write so here is the concurrence in we're talking about the kernel debugging it is very useful for us to do cannot exploit development and we introduced the two hip funky techniques where is traditional hip country because it needs rope chains to do Kanaan memory read and write and it need to do multi times so it's not stable for poor country it doesn't need any cages and only your seed structure it's stable with a high status of highest successful read but it's very easy for effort to fix it so that's all for my talk thank you for listening they'll have any questions so sorry well I think a long time [Applause]
