All right, welcome to the last track of track four of Defcon 33 and he's going to be talking to us a little bit of his research into SCCM security. security. >> Hi everyone, I'm super glad to be here and thank you for staying till the end. So today in this last session I will walk you through my research on SECM. So let me first introduce myself. Uh my name is Medi Aliasar and I work as a red teamer at Synactive which is an offensive security company based in Paris in France. So I've been working in IT security for eight years now and I started as a pentest and uh now I do full red teaming full-time and I mainly focus on breaking web applications and also on active directory stuff. Um so this talk will cover the following topics. Uh first I will present and describe some key internals of ECCM mainly to give you a grasp on how it works uh under the hood. Uh then I will cover the CVS I've discovered in ECM. Uh then I will introduce some post exploitation uh techniques that I've been using throughout my engagements but also these techniques that led to the creation of a tool that I will present here. And finally, based on the knowledge that we've got about the internals of SECM, I will share with you some techniques to backdoor an instance. So let's start with the SECM internals. Um for those who don't know what SECM actually is, uh it was renamed recently to Microsoft Configuration Manager. Uh so it's an IT asset management solution edited by Microsoft. It has been around for a long for a long time now and it is still widely used and deployed within organizations organizations and so to manage devices SECM installs an agent on the client and it provides uh code execution capabilities and for us on the offensive side um this makes it Microsoft's native C2 and it's a prime target for uh during our engagement and today there are many attacks attack techniques uh to take over an CCM instance. They primarily arise from misconfiguration or a lack of hardening. Um but they were widely documented to till now. So I I won't cover them cover them in this talk. So let's start with the first internal and the first concept to introduce uh this part is the client communication modes. So you have two of them. So SSM client client mainly uh talks over HTTP with the management points. Um so you have on the management point you can configure it either um with enhanced HTTP mode. So this is mainly using uh plain HTTP transport mode or you have HTTPS mode which is advised by Microsoft uh which requires an internal PKI uh to issue certificates such as ADCS um and this is done to make the clients uh do mutual TLS with the management point services. So this is this is what makes the client certificate certificate present uh u uh sorry this is what makes the clients present a certificate to to talk with the management point. So sometimes on in an engagement uh you need this that certificate and it can be uh a bit painful uh to to exploit SECM. Now let's take a look at how SECM works under the hood. Uh most services are web applications hosted on IIS. Um from what I've seen, it's a mix of old and new technologies that are glued together. Sometimes uh you have mainly ASI API modules alongside net applications. And you also have com that is heavily used between components often with deep chains of com calls. of when you do reverse engineering it can be hard hard to follow follow the code flow uh and as I have limited time I will focus only on the services that I tied to the management points and let's start with the SMS management point service so on the right side this is a snippet of a dump I did on the IIS um web configuration so this is only for the SMS management point service but we'll we'll get uh other snippets for other services and you can observe here that there there are three ASI API modules in use. One to serve policies to clients, another to serve scripts to clients and the last one to serve location services information to clients. clients. So this service has four different entry points. You have the standard one that is named SMS_MMP and other ones with the same modules but different authentication methods such as Windows OL and token O. Uh but also you can observe on the right side in the configuration on I uh anonymous authentication is enabled on three uh entry points out of four. So that's weird. Um and as I said previously when the management point is configured in HTTPS mode a mutual TLS is required from the client to access the services. Uh this is the theory but in practice you can bypass uh bypass this to do so you have to use the SMS MP token or at o entry points. Um and here there is no need for a client certificate. Uh as you can see in the snippet below um the first query we get a 403 error um from the server asking for a client certificate but if you hit it with the other entry points you get 200 and you can reach the services. What can you do with this? Uh for example on the this service you can hit the method m list on the get out module to get a full list of management points within a site. Um there are two interesting fields. You have the version with the build number of ECCM and also the SSL state which indicates if HTTPS mode is enabled on the management point or not. You also have another method which is MP list one. Uh you give it um a parameter uh of the site code uh so you can enumerate um management point from other sites within the same hierarchy. hierarchy. So you can do with this unauthenticated reconnaissance on HTTP but also on HTTPS if you use those TLS bypass mutual TLS bypass that I showed you earlier. Let me now cover another key service which is CCI messaging. Um this web app has a single ASPI module with entry point named in a similar way than the one we the ones we saw we seen before. Um and here too anonymous authentication is enabled on three entry points. So in practice CCM messaging is HTTP based communication protocol that use a custom method CCM post. Um and this protocol is mainly used by the clients to um discuss with the management points but also it's used by other site servers such as management points or between internal services. internal services. Um in this snippet below you have the structure of a message. Uh there is two main parts in a multiart body. The XML header and the request which is an XML document too. So in the header uh sometimes you get a message signature that that is included and it proves the device identity. So this is how authentication is done here in this protocol. And under the hood again the is API module serves serves as an single entry point that dispatches the messages to the right service handler. So it does so by uh checking the value of the target endpoint field in the header part in the system message and once again everything is done through com as the service handler are com classes. They are configured in within the WMI database as instances of the CCM service endpoint configuration class and these objects can be found in two distinct name space name spaces sorry. Uh so the first one is where the configuration is persisted between reboots. between reboots. Um and on this class you have two interesting fields. the co-class one which is the cellless ID of the com service handler to call and also the visibility field which is super important as it indicates if a device identity is is needed to interact with this service. So this is where the authorization level is defined and as you can see in the dump below the MP services are configured with either all or client signed uh visibility. Uh and I will circle back to this knowledge at the end of this talk for the persistence part. part. And once again when HTTPS mode is enabled there is also a trick to bypass mutual TLS for CCM messaging. Um so you need to use CCM system alt O uh entry point to do so. Unfortunately the other entry point with the token doesn't work at this time and as the alt was introduced in in recent version of SECM uh 243. This bypass won't work in older version of SECM. So how can you leverage this bypass uh to do some nasty thing on SECM? In fact, you can loot NA credentials without having Windows credentials on your on the network. Uh so there is a well-known technique to to harvest NA credential is to register a fake client. Uh when you got yourself a Windows credential for a machine account um that's needed so when you do that res registration the SSM client is approved and this is mandatory to pull policies. However, with this uh mutual TS bypass uh if you register a client without domain credentials, it is automatically marked as approved. Um thus this gives you the ability to loot NA credentials uh unauthenticated. So I plan on updating the tool SECM secrets which is a available on GitHub already. Um so I will do a pull request in the next day uh right after the def. So I've discovered this while preparing this talk. this talk. All right, another service that is interesting um is the one that is named SMS provider. It acts as an abstraction layer for read and write operation on the database. So this is a completely different role. Um it creates a specific WI name space named SMS the threeletter site code. Um and access control is managed through airbach uh with roles that are defined in the database. Um and there is also a local Windows group uh SMS admin that is created and it is it is granted access permission to do to the WMI name space. So this group automatically includes any user or group assigned an airbak role um as well as machine accounts of management point uh servers. So this gives you the ability to do some relaying and on top of that uh the SMS provider expose uh the admin service rest API. Um so this API offers two routes uh one for the O data protocol another another one for WI supporting get and post operation on over 700 classes and this API is a net application built with Owen and there is no way to configure or enable uh EPA. So this makes it vulner vulnerable uh by default by design to relay attacks. relay attacks. So you can't fix that. And let me finish with the tenant attach concept. So for a hybrid management approach, an SECM site can be configured with tenant attach. Um basically the feature allows the onremise SECM instance to connect to Intune and when enabled the SECM instance uploads service device information to Inune. But also from the in tune web console you can uh run some actions on those onrem device uh such as asking for a device restart but also installing application or running scripts and when this feature is enabled um there is a principle named config manager svc_gu ID that is created on your tenant uh so it's a it's an intra id application uh granted the following permission on the tenant so we have read and write collection data on the configuration manager micros service API which is uh an undocumented API but also we have directory read all uh granted on the graph API uh so this is an account that you can extract from the onremise to perform reconnaissance on on the tenant and uh I will get back to this later to this account and show you how you can recover its credentials from the database the database all right uh let's talk a bit about zero days so now I'm going to walk you through um how I I identified vulnerabilities in SSM. Um I will mostly detail detail the methodology I applied so you can see how exactly how uh to see exactly how the issues were discovered. So I got two of them today. So first one it's a CV 2024. It's an unauthenticated SQL injection that I disclosed ear earlier this year. Um so it affects uh CCM messaging uh the MP location uh Mocation manager handler um and as this handler is configured with visibility at all. So there is no need to prove the device identity which makes the exploitation unauthenticated. exploitation unauthenticated. Um and usually the management point and site server roles are installed on the same server. So as the site server has the CIS admin role on the database, this issue allows you to instantly uh take over the database and all the all the sites and to mitigate the impact Microsoft advises to install the roles on distinct servers. So the code is already available on our GitHub. All right. So to discover this issue, I kickstarted my research with by extracting uh strings from older libraries uh used by ECCM and some SQL format string patterns stand out right away. Also there were method signatures that show up in debing messages that are left in the code of the CCM uh which reveal the internal code structure and among those SQL patterns in the location manager library precisely there are some format strings used by the service to execute store procedures uh like the one calling mp get machine ID which is highlighted here in this snippet. Uh so looking at the code next with the the compiler like ID uh we can confirm that the input is formatted with into the SQL string without escaping. Uh so here comes the question how to reach this flowed method. method. Um and thanks to the code tree overview plug-in in IDA you can observe that there are multiple path uh to the flow method uh but one clearly stand out the one highlighted here as this it stands out as the simplest route to to reach the code. Um so this is the one I took for the exploit. Um there is less hassle and rest condition to to reach the code and following that shortest pass from the CTO I landed in the code location shown in this screenshot. And once again the debugging strings are helpful uh as they indicate that the get machine ID is called right before uh an update SF method. So this is helpful next to choose which request to pick up. So this is um an extract of the par request body in which there are at least 20 uh names of XML document that are processed by this service that I that you can reach and with this the information that you the hint that I got from the debug string. Uh I could quickly choose uh the update SF request as the one to use to reach the flow code. And here is the final CCM message to export the injection. So on the on the left side you have the header part where the injection point is located in the source ID field. So this is the injection point is in is in the header which isn't even signed by the client and the on the right side you have a minimal update SF request filled with the placeholder values just so the exploit can work. So that's the exploitation. exploitation. All right. So when the patch was released, I confirmed the issue issue was fixed because now they now use prepared statement. Um however, I've also noticed that the flow code is still there and it's um it's now gated by if condition uh which was raising questions about the purpose of that. Um and digging a bit further, I concluded that the if condition checks if a registry value is set on the system. Um so it's named disable additional validations. Um so this is clearly a fail safe uh to bypass that patch. Uh so when you exploit the CCM and you put that value, you will get the the vulnerability again. And I think the main main reason is that the first v version of the patch that was released by Microsoft uh was causing serious trouble to the customers. Um so they left left that uh if condition as a roll back for customer that are complaining about about the patch. All right, let's talk now about the other CVE 2025. It's an authenticated SQL injection in the SMS provider component. It's the admin service. Microsoft released a patch in July 2025 um to address this flow. Um so this vulnerability can be exploited using any SECM airback role even the read only one we'll see later why um and I am releasing the proof of concept for this uh issue right after this talk. So to discover this bug I reviewed the mo file for the SMS provider namespace. I looked mostly for patterns where methods had parameters that appear numeric but are defined as strings uh in the WI side. Um so this mismatch between the expected type and the one uh defined in into WI enables potential injection. So thus I identified the method um update classic deployment as a good candidate as it its offer ID parameter seems numeric but it's defined as a string in the WIMO. So you have its definition here in the snippet. All right. So looking into the code behind uh of update classic deployment I noticed that the SQL injection occurs uh in the permission check step. Uh so that's why a user with any airback role even the read only one can exploit this this injection. this injection. Um and from my point of view there are two ways to exploit this issue. Uh first is obtaining credentials for a for a user having any SECM uh administrative role. Uh but this this is not that easy uh on an engagement. uh otherwise you can perform authentication relay um against the admin service API uh on which AP EPA protection can't be uh required. So depending on the topology of the deployment uh you got you have multiple scenarios. Uh first if the SMS provider role is installed on a dedicated server or not on the man management point you can relay the management point to the admin service and exploit the G injection. If there are multiple management points deployed in a on a site, you can relay one management point to another management point hosting the SMS provider role. And if if there is a single management point in a standalone mode with also the SMS provider role on the same server, you can try some self relay attacks uh if it isn't patched against it. Uh so remember there is a CV for NLM reflection that was disclosed ear in June. So you can use that to to exploit All right. So I have other unresolved issues with the Microsoft but I cannot cover them in this session but stay tuned. I will share the details at the appropriate time when I got the the disclosure is okay. Uh let let me cover now the post exploitation phase uh when you got yourself an admin access. Let's see what what you can do from the database for example. Uh so in SECM there is the create and run script feature. It's super interesting as it allows you to run PowerShell on a single device. Um the execution is done as system and the process is spawned by the CCM exec process which is highly trusted binary and to exploit this feature. Actual public tools leverage the admin service API of SMS provider to do so. So there are some downsides downsides to do doing that especially during a red team. First all actions are logged. Um then there is also a double approval mechanism by default. So you need two accounts uh to exploit this. One to create create the script and another one to approve it. And finally you can't properly clean exploitation artifacts behind behind yourself. So I've I asked myself uh if it was possible to trigger the script execution feature directly from the database access and the answer is yes. Uh doing so bypasses the double approval process and it leaves minimal login press behind you and here is how to do so. We have some steps to do so. First you have to you'll have to create an entry in the scripts table. Um there are some columns that are interesting. Um for example the script type set to zero is for powershell you have the approval set you directly set it to three so to market the script as already approved you have a feature if you set it to one the script won't show up in the admin console finally the approver author are free text so you can spoof whatever identity suits you then we add an entry in the BGB task table uh which will reference the script that we've created so we have one part is the template ID we set it to 15 to say that it's a task to request script execution. Then you have the param uh column which holds a task param XML document. Uh so this document will include the the the ID of the script to execute. So this is the the document that will be sent to the clients and the client will use that information to fetch the script and execute it execute it. it. Then we add an entry in the BGB rest task table. So this is where you assign the task to a client. You can see there there is a field for the resource ID for the device manage device that you want uh the task to be executed on. Uh so this insertion trigger triggers a push notification to the client and it downloads the the XML document the script later. script later. uh to check the script output. Um in fact the tasks output is sent back by the client to the management point uh which stores it in the script execution status table. Uh so you have the exit status code along the route out output. So you have to after a slight delay you have to query that table. Um, regarding the login, uh, the only thing on the client side that is logged is the the task param XML document that is left in the CCM notification agent uh, log file, but the script content isn't logged. So that's okay. However, uh, be aware that the script which is downloaded on the client side is stored in a script store folder. U, but that file isn't removed after the execution. So you have to you need to delete it uh manually. And now I'm glad to introduce you SECMsql client.pi. It's a tool that implements this techniques to automate uh as much as possible. So it started as a fork of impact MSSQL client with queries to explore an SSM database. And this is a tool that I've created for a red team a year ago and that I've kept improving with the new ideas from the on the field experience but also the knowledge I've gained from my research throughout throughout the the time. So regarding the feature there are some queries to map the topology but also to list clients or list store credentials but also you have some features to run the powershell scripts on clients but also to decrypt the credentials automatically. credentials automatically. Um, and this tool also includes some anti-forensic measures as it automatically prepends a small command uh to your scripts to clean the script store folder so you can you don't leave anything behind you. And it also includes some helpers to clean the artifacts easily from the database. And let me describe you some of its key commands. Um, first to map the topology you can use SECM servers. Um, it will just list the servers within a hierarchy. So you can check for server from other sites. Then you have SECM devices. So you will get a full list of known devices. Um also devices from other sites. Um then an important one is SECM devices BGB status. So you get only the clients for for your current site um with with their BGB status. So the BGB status is the thing that will tell you if you can execute scripts on those clients. And to run scripts on a device with the tool, you will have to load its content first either in line with setp1 script or from local file with load PS1 script. Uh then you'll have to run SSM run script with the resource ID of your target device. Uh and at this step the tool prepares the commands to clean the script store folder as I said. Uh then to get the output you use last last task output. It's a helper to check the execution output. it use the generated go ids that are are kept in memory. Uh finally use the last task clean to which is a helper to clean the artifacts from the database. It will uh delete everything. everything. Um regarding secret extraction. So to look to look for secrets in the database database you can use SECM user accounts. It will query the table that stores NAA accounts and push accounts. Um and here the password field is encrypted with crypto API on the site system server and only that site system server that issued that blob can decrypt it. Uh and you also have SECM a apps. Um so this is uh the command to list the entry ID credentials that are stored when you um en when you have configured tenant attach feature. Um so here we have two columns. Secret key which is a blob that can be decrypted by a single site system but also secret key for SCP which uses a key shared between all systems. So this is mostly interesting uh it's it make it easier to decrypt and to decrypt these secrets uh we need to execute code on the site system servers that created the blobs and by default they are enrolled as clients too. So we can run a a specific script to decrypt those blobs. Uh so the tool has an SECM decrypt blob command to automate that process. Uh so you will need to give it the resource ID of a site system server and the blob to decrypt and depending on the blob format it will use the right powershell command snippet to decrypt it. So these commands use C# assemblies available on the CCM servers um and that expose methods to decrypt those secrets. So you have um more details in the slides for later. So it's time for a demo. So I will start as an authenticated attacker and I will exploit the mutual TS bypass to register clients. Then dump the NA credentials. Within those NA credentials, I will use one of those accounts to access the admin service API uh exploit the uh the SQL injection to create myself an admin access to the database. Finally with the with the database level access I will showcase you the MSS secm SQL client tool for some commands for so I'm starting with some recon with the bypass for mutual ts so with the token and with the alt out I show you that I can get the list of management points um over https And now I'm using a modified version of HCCM secrets um to dump the policies. So I'm not giving it any uh Windows credentials. credentials. And I will dump the policies like that over HTTPS 2. So this is the name of the client, the fake client that will be created. And here we go. And we have three accounts that are dumped by the tool. And the last one we'll use it later. So now I'm going to show you that the fake client that is created from in the SSM console is or is approved. So this bypass makes you create an an approved client. So now let's use the SECM user um user SECM read only to access the admin service API and exploit the CV 2025 and create ourself an admin account on this on the database. That's done. Now we can we can use ECCM SQL client to access the database and confirm that we are administrator. Now we can this the sites and it works. That's so now let me showcase um the SSMQL client first by executing commands on a domain controller. So now with the BGP status I confirm that the DC has online status at one. So we can execute commands on it. And right here I'm loading a command to execute on the controller. controller. Then I will launch it. Uh so you need to pass the resource ID uh to run the the command. And now we wait a bit and we going to get the the output. And that's all right. Okay. So with the last task clean you can clean behind yourself the insertion that were done in the database. So now let me show you how we can extract the credential and decrypt them. So this is for the user accounts. You we have the push account the NA account here. Uh so now I'm going to look for the site system server site system server which is on this deployment is the management point. So, CMC and I'm going to get the resource ID of this uh server then use that resource ID with SECM uh decrypt blob and pass the blob to. So, right here it's going to run the script on the target and wait a bit for the output. Then it will print out the the password for that account. And that's it. We have the password right there. right there. And now let me show you the same for those uh credentials for the application on enter ID. So this is pretty much the same. We get our blog our blog and we pass it in the command for decryption. And that's our that's all for this demo. All right. So, let me conclude this talk with two persistence methods. Uh first, it consists in backdoring the CCM messaging protocol with a rogue hand service handler. So you only have to do is uh create a DL that implements the ECCM endpoint interface and the method execute. It's a com method. Um so I will in this example the the method will receive PowerShell commands and returns the commands in the output. Uh then you have to you have to register the CSS ID on the management point. then create a new CCM service endpoint configuration WMI object that use that rogue CS ID and the P is available on our GitHub so you can grab it if you want to test it. Um and right here this is the signature of the execute method that you need to implement for this backd dooror uh to process incoming messages. And to do so the first argument is the CCM messaging service service. Um it has to be used to send the reply back with the send message uh method. Um and to process the incoming message uh it's as the second argument of the execute method. So you you use that argument and call the get body wide string method on it to get the input XML. input XML. Um let me show you a demo of this back door in action quickly. So this is a session on a management point. uh I open the WI explorer to explore the config related the SSM components and mostly the one for the service handler that are registered on the management point. So I'm looking for it here. So I'm going to look for the one that start with MP uh and the one with the the the visibility set to all. And this is the one that I'm going to copy and use as a template for for the the back door service handler. And now I'm going to compile the rogue service handler code which is written in C here. Then I will use reg to register the rogue class on the system. And finally, I'm going to use a PowerShell script uh to register the rogue service handler in the Wii database of a CCM as a CCM service endpoint configuration instance. Here it is named MP back door and the visibility is set is set to all. So we can exploit it on authenticated and we can see in the W MI explorer that we have our ro service installed and it will persist between reboots too. And now I'm going to use a Python uh client to interact with that service. Um so I'm I'm going to talk with the management point over HTTPS. And now I'm giving it the name of the service back door. Then the command to execute who am I for example. And we got the output here the enter authority system. authority system. And let's do uh some directory listing too. And we got the output too from the back door. So that's all for this for this demo. All right, last persistent technique without a demo but it's an idea. Um and the idea here is uh to back door the storage procedure uh that are used by endpoints accessible without authentication on the management point. Um so there are some legitimate procedures that are altered so that when uh so that when a specific pattern is detected in the input the procedure executes uh our supplied SQL statement and beyond functioning as a back door. This technique can be leveraged when a SQL injection exists but direct access to the database isn't possible for example and s and in such cases the management point acts as a SQL client allowing uh execution of arbit arbitrary commands through the back door procedure. Um and here below you have some example of unauthenticated endpoints uh exposed by by the management point and the stored procedure they call. Um so we have you have the two the first two one for the for downloading policies and um script uh script documents that call MP uh get policy body or MP get SDM document. Um but also the last one is more interesting because it is through the CCM messaging and through post request. So you can send uh bigger uh bigger uh payloads. So you you use the site information request for example on the MP location service handler uh which calls the MP get CFU. So this is a technique uh that I'm going to blog about a bit later with more details. So stay tuned about that. So thank you. Let me wrap this talk quickly. So all the code examples that I presented are accessible through the links provided here. uh and follow our blog for upcoming posts on this topic and also disclosure of pending issues. And thank you very much for your attention. I'm grabbing some questions for you.
