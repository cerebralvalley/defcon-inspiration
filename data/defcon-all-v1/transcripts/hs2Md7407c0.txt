hello i am mati vanu and i'm going to give a presentation about our paper titled titled protecting wi-fi beacons from outsider forgeries forgeries this research was published at the weissec conference just a few months ago and this was joint work with prasant adikari and christina pipper in this presentation i'm going to give a summary of our results so as the title of the presentation implies we're going to protect wi-fi beacons now before we get into that what exactly is a wi-fi beacon well wi-fi networks use wi-fi beacons to announce their presence to nearby clients and they are sent roughly every 100 milliseconds milliseconds by an access point on these beacons contain various contain various properties of the network for example they contain the name of the network they contain the supported bit range for example example whether the network supports the 11n or 11 ac standard ante also contains certain regulatory constraints constraints for example the maximum transmission power and so on but the problem in practice is that these beacons are not protected and they can be forged by an adversary on our work on this presentation we present present novel attacks that abuse beacons and motivated by these novel attacks that we discovered we implement a defense defense to prevent outsiders from forging beacons beacons and we also standardize this defense as part of the 802.11 specification that underpins wi-fi on on top of all of this we also have an implementation on implementation on linux and our defense might even become part of wpa 3. but before we get into these details let's take a step back and look at the history of wi-fi security on here we can see that previously previously the focus was on protecting transmitted data data of not beacons for example if we look at web on wpa1 and 2 they really focus on protecting data only now wpa3 does mandate that certain management frames are also authenticated however unfortunately this does not include beacons and even if we look at some very recent additions to wi-fi such as operating challenge operating challenge channel validation this only verifies the the information about the current channel that the access point is operating on in other words even if we use all the modern modern standards beacons are not protected and they can be forged by an adversary and and if we look at the contents inside the beacon we see that there's a lot of information that a beacon contains beacon contains and a small amount of information is indirectly verified when you connect to the network the network for example the wpa version that is supported by the network on the current operating channel is securely verified securely verified when you are connecting to an act network but network but all the other information that is contained in a beacon on here we are only showing a small part of the possible information in the beacon can be sport by an adversary and possibly possibly be abused so the first thing that we did in our research is that we searched for novel attacks that an adversary can carry out carry out by spoofing these beacon frames um one of the first attacks that we discovered discovered is what we call a power constraint attack attack namely as i already hinted at previously beacons contain the maximum allowed transmit transmit power that a client should be using for example if we look at the following beacon content beacon content and wireshark we can see that it contains the maximum transmit power that is allowed in a certain country certain country and on top of that the beacon can even further further lower the maximum transmit power below this this regulatory maximum in other words to simplify simplify an access point can command the client into using a certain maximum transmit power power on an adversary can abuse this by forging a beacon that instructs clients into using an extremely low transmission power in turn this causes the network connection of the victim to become almost almost unusable um that's because the frames that the victim is transmitting will no longer reach the access point access point essentially this means the client will no longer have a stable internet connection so if we look in practice we tested this attack against various operating systems and we found that an ipad on a macbook was vulnerable to an attack on the linux was also was also vulnerable to attacks other devices were not affected it is unclear why although we conjecture that after injecting forged beacons other devices may reset their parameters back to normal once they receive a legitimate beacon now we also found a minor variant of this attack this attack against linux namely when we were inspecting the source code of the linux kernel we noticed that it's also able to parse a vendor-specific a vendor-specific information element that belonged to cisco cisco and what's interesting about this implementation in linux is that normally if we follow this specification specification we cannot set a negative maximum transmission power transmission power but by abusing this vendor-specific element of cisco we can and it turns out that if we are able to set a negative transmission transmission limit then the drivers of linux do not handle this properly and as a result they forcibly are disconnected from the network network so let's show a demonstration of this in our demo we can see now that the victim has a has a stable internet connection however as an adversary we're now going to inject 10 beacons to make sure at least one of them arrives at the victim and if we now look at the debug messages of the victim we can see that it indeed lowered its transmission power transmission power and if we look in wireshark if we look at the spoofed beacon now indeed contains this power constraint constraint element that reduces the transmission power of the client if the victim now tries to refresh the website website or visit the new one you will see that the website is no longer loading meaning our attack was successful now a second attack we discovered is that we can lower a victim's bandwidth by spoofing beacons and this abuses the way that a normal wi-fi device transmits device transmits frames particular before a wi-fi device will transmit the frame it will first check whether the medium is idle is idle or not and in case another device is transmitting a frame it will first wait until this device finished transmitting and after that it will wait a very short amount of time called the s-i-f-s interval and this is a mandatory every device must wait this amount of time additionally it will wait an amount of time that depends on the priority of the frame that it wants to transmit this is called the aifsn period and for example if the device wants to sense sense a high priority voice packet this period is relatively short or if the device wants to send some background traffic this period is a bit longer then finally the device will also wait a random amount of time called the back off window or the contention window contention window and this is a random value and it is used to also give other clients an opportunity to opportunity to transmit as well in case during these three periods no other device started transmitting transmitting in that case the device can transmit its own frame now what is interesting is that the specific values for these waiting periods are determined by the network configuration in particular they are defined by defined by the beacon so if we look at a beacon and wireshark wireshark we can for example see that it advertises the specific value for the aifsn aifsn period additionally the minimum and maximum duration of the backup of the backup window backup window is also defined in the beacon and of course as an adversary we can spoof beacons where these values are very large and this means we can force a victim into delaying its transmission transmission for a longer amount of time and this effectively reduces the bandwidth of a victim now the impact can in fact be much higher than simply lowering the bandwidth of a victim and that's because if another device starts starts transmitting during any of these waiting periods then device the device will need to completely restart its waiting procedure procedure in other words words if there is another nearby client that is actively downloading or uploading a file our victim may simply never get the chance to transmit the frame and our attack will result in a denial of service so we tested this attack in practice and we found that linux is affected independent of the network card being used we also tested some apple devices on the year we found that the macbook pro the iphone iphone on the ipad are all vulnerable we also tested windows 10 on here we noticed that noticed that the impact depends on the specific network card that this operating system is using for example if we use an alpha or tp-link usb wi-fi dongle then we found that windows 10 was vulnerable vulnerable however if we used the built-in network card of card of certain laptops which is typically an intel uh intel uh network card at least on the devices we tested then windows 10 was not vulnerable vulnerable finally we also tested two android devices devices one more recent nexus 5x device and it was vulnerable to our attack but our our fairly old samsung device was not vulnerable to our attack so let's show a demo of this attack in practice we will target an ipad device and we can see that see that before starting the attack the ipad has a throughput of about 10 megabit per second then as an adversary we will spoof two beacons on the filukin wire shark this spoof beacon has the maximum value for the backup window and also the maximum value for the aifsn waiting period as well if we now measure the the throughput on the ipad we see that is substantially lower substantially lower meaning our attack has succeeded we also discovered several other attacks for instance we discovered a partial machine in the middle attack and this attack bypasses channel operating validation operating validation in linux and for the details i will refer you to the paper we also discovered battery depletion attacks where we can spoof beacons to make clients make clients stay awake longer than they normally are additionally we also found that it is possible to send beacons as unicost frames and this allows us to target target specific clients on this technique worked against all the devices we tested so how can we prevent these attacks attacks in practice well the idea is to authenticate beacons such that an adversary can no longer spoof them but before we get into those details let's first state the goal of our defense defense and the goal is to come up with a practical and practical and simple design to encourage adoption in practice practice so this means that our cryptographic operations must be efficient on that the bands with overhead must be low as well because beacons are sent at a low bit rate on even adding a few bytes will consume a significantly significantly higher air time finally we also want the defense to be straightforward to implement because then this increases the chance that vendors will actually adopt our defense in practice ideally we will accomplish this by reusing reusing existing cryptographic primitives that were already used in wi-fi so how can we achieve these design goals well we will rely on symmetric encryption encryption and we will reuse a crypto primitive that is already part of management frame protection additionally we only defend against outsider attacks in other words we assume that the adversary does not possess possess the credentials of the network and this assumption is similar to how broadcast on multicast traffic is protected in wpa213 as well so concretely how does our defense work well we add a new type length value element element to beacons on this element is shown here on the two more important two most important fields here are highlighted in green green now before i discuss the details of these two important fields i first want to mention that clients that do not recognize this new type length value element they will automatically ignore it this makes our defense backwards compatible with existing wi-fi implementations so first we have the non-field and the non-field is essentially an essentially an incremental number that is used to prevent replay attacks and then the mic field which stands for message integrity check is a cmaq or gmac authentication tag that is calculated over the beacon and here the cmac on gmac are existing cryptographic primitives of management frame protection protection on the interesting thing is that all wpa3 capable devices are already required to support these operations operations meaning vendors will already have implemented them implemented them by now so the remaining question now is which key will be used to generate this authentication tag authentication tag and here we decided to let the access point point generate a new beacon protection key when it starts and this beacon protection key protection key is always sent to the client when it is connecting connecting in particular even if the client may not support beacon protection the access point will still send this beacon key beacon key to the client and older clients will simply ignore this new key but new clients will treat it as an indication indication that the access point supports beacon protection protection now the interesting thing about this construction is that an adversary cannot manipulate the handshake that transports the beacon key this means that downgrade attacks are prevented because an adversary cannot somehow remove the beacon key that is being transported to a client the final important thing to discuss is how that clients handle beacons that are received before they have connected to a network because recall that a client only receives the beacon key once it has connected to the network meaning that beacons that are received before that cannot be authenticated because the client does not possess the key to verify their authenticity so how can we handle this in practice well our solution is that before and client connects to a network it will store store one beacon as a reference and it will extract extract extract all information of the network from this reference beacon and when the client now connects to this network and once it has received the beacon protection key it can take this reference beacon and then verify then verify its authenticity and in case this check fails fails it means there was an attack and then the client can disconnect from the network on the other hand if the the authenticity of the reference beacon can be be successfully validated then the client can can send data frames to the network on can operate as normal now that we have our defense an interesting question is how a client should act when it receives a forged beacon beacon and we decided that in that case the client should send a notification to the access point this allows the access point to detect rogue piece that are far away for example in our illustration here the rogue ap is out of range of the real ip ip however when a client receives a forged beacon beacon from the rogue ap it can notify the real access point of this and then the real access point can take appropriate appropriate actions so now i want to discuss some practical impact of our research first and for all we created our specification of our defense in collaboration with collaboration with intel broadcom qualcomm and huawei and our defense is now part of the draft ieee 802.11 ieee 802.11 standard meaning vendors will likely adopt this adopt this in practice as well moreover the wi-fi alliance has alliance has recently expressed interest in our defense as well meaning it might become part of an update to wpa3 or it might become part of a separate certification program our defense is independently also being implemented by linux linux where the kernel generates on verifies authentication tags authentication tags and host ap manages keys and enables beacon protection beacon protection if it is supported by the network we will now show a demo of this on a recent linux kernel linux kernel where if we start the access point on view the beacons and wireshark we can indeed see that the authentication tag is being added to beacons if we now connect to this network network the client will indeed receive the beacon protection key on it will enable beacon protection as a result result if we now inject the beacon as an adversary the client will detect this spoofed spoofed beacon in this demo we made the client print a small debug message and if we now look at the debug clock we can confirm that the forged beacon has been detected been detected on the attack has been prevented to conclude our talk we proposed a defense to prevent outsiders from forging beacons and our focus on practicality has paid off because our defense is now part of the 802.11 standard is independently being implemented by linux and that might become part of an update to update to wpa3 thank you for your attention
