Okay, so hello everyone. Thank you for joining us today. Welcome to our talk beat unlocker leveraging Windows recovery to extract Bit Locker secrets. Can you hear me? >> Let's go. Let's go. Today we'll take you inside our journey of attacking and securing Bit Locker. We show you how we found and exploited and fixed new vulnerabilities in the Windows recovery environment that allowed extracting all the Bit Locker protected secrets. But just before we dive in, a quick introduction. quick introduction. So my name is Alon and here with me on stage is Nanel. We're both security researchers working with the security testing and offensive re research team at Microsoft also known as storm. Our expertise is in vulnerability research focused on everything that happens before the operating system fully loads covering the various boot stages and its security features. So secure boot bit locker and others. But enough about us and let's dive into our agenda. So we'll start with the research background and give an overview of Bit Locker and Winter E. Then we'll dive into the vulnerabilities that we discovered, how we exploited them and finally we'll wrap up with the research results, the fixes and actionable beat locker counter measures that we can apply today. apply today. So starting with the research background today, we'll explore a security feature that is designed to protect your sensitive data at rest. It aims to defend against step scenarios where an attacker steals your laptop aiming to extract your sensitive information, compromise your machine, and even back door it. You're here at Defvcon, surrounded by some of the brightest minds in cyber security, and let's be honest, some of the juiciest targets for hackers. hackers. Now, picture this. You step away for a quick coffee break, and when you come back, your laptop is gone. Stolen by a thief that is after your research, internal tools, credentials, and personal data. personal data. Without data trust protection, the thief can directly access the contents of the hard disk, extract sensitive data, and even modify system files to install a back door, then even return the computer without detection. without detection. Data trust protection ensures that even if your laptop is stolen, your data remains encrypted and inaccessible, and your system remains untempered. Now, I know what you're thinking. The odds of your device getting stolen are so low that you're probably wondering if you should even bother worrying. So consider this. Studies show that the laptop is stolen every 53 seconds. The odds of a laptop being stolen are 1 in 10 and the average cost of a stolen laptop around $49,000. This figure reflects replacement costs, productivity loss, legal expenses, but most importantly the loss of sensitive data and intellectual property. These numbers emphasize the criticality of data trust protection features. And with that, let's dive into our journey of attacking and securing Bit Locker, which is Windows's data trust protection feature. feature. So, Bit Locker is a full volume encryption technology that is designed to protect individual disc volumes. Once enabled, it encrypts the target disck volume, protecting all data stored on it. Users have the flexibility to choose which volumes to encrypt based on their specific needs. And by default, Bit Locker targets the OS volume, ensuring that all data stored within the OS environment is protected and encrypted. So with Bit Locker encryption in place, even if your laptop is stolen, the protected data remains unreadable because it's encrypted. Now, the Bit Locker web model assumes an attacker similar to a common thief, which means someone with full physical access to the device, but without advanced credentials such as usernames or passwords. or passwords. Naturally, the BitLocker attack surfaces are shaped by the interfaces accessible to an attacker fitting within this thread model. And as we examined the different attack surfaces and interfaces, a surface, one surface stood out. A surface with significant vulnerability potential that surprisingly has received very little attention in perio research. This surface is the Windows recovery environment, also known as WinRe. Any bit locker attacker can directly boot into winner by holding the shift key while selecting restart from login UI rendering it a potential candidate for deeper inspection. Given winner's fit within the bit lockers web model the lack of behavior research and its high vulnerability potential. We decided to decided to conduct a security review that is dedicated towards finding new vulnerabilities, exploiting them, fixing them and then hardening winery and reduc while reducing his exposed attack surfaces. The end goal of our review was to make both Winery and Bit Locker more secure and resilient. So let's take a closer look at Winery, what it is, how it functions, and which areas present the most promising opportunities for vulnerability research. So Winery is Windows recovery platform and it's designed to address critical system issues such as SATA failures, persistent crashes, bit locker errors, and more. For instance, if your machine crashes, winery is the component responsible for analyzing the crash, identifying corruptions, and resolving it by using its recovery tools. Now, if you use Windows, the chances are that you've encountered Winery at least a few times in your lifetime. Architecturally, Winery operates with its own standalone operating system known as the recovery OS. This recovery OS is a lin version of Windows with recovery specific customizations. These customizations include a unique set of recovery tools that are all integrated to the known blue screen recovery UI. Among the exposed tools are familiar options such as startup repair, system reset, system restore, and others. others. for storage. This entire recovery OS, including all of the DLS, executables, drivers, they're all compressed into a single WHIM file named winery.wim. And this WIM file is then stored on disk. Then when Winery boots up, the entire WHIM is decompressed from disk into RAM, creating a temporary RAM disk that hosts the entire the entire recovery OS runtime. Any changes changes made within this environment within the RAM disk are not saved back to the original win file making the recovery OS inherently volatile. So modifications to the RAM disk are discarded upon reboot. Now when Bitlocker was first introduced, Winry had to evolve to support recovery from Biter related failures and this led to several architectural and design level changes to enable that recovery functionality. We wondered what impact these changes had. So let's take a closer look at them. The first design change focused on the location of winary whim. It was moved from residing in the OS volume which is now encrypted by Bit Locker to a dedicated recovery volume which is not encrypted. This change was necessary because winery must be able to recover from Bit Locker related failures. And if the OS volume is encrypted by Bit Locker and becomes inaccessible, for example, because of a decryption failure, storing Winery in that location would prevent future recovery. future recovery. Since winner is a critical component that must always be available, it was moved to a separate recovery volume and this ensures that it can function even if the OS volume is not accessible. The second design change focused on the integrity of the win file itself. So to support integrity validation of winery a feature called trusted wim boot was introduced. It verifies the integrity of winary whim winary whim by comparing its ash to a known trusted hash. So a simple hash comparison here if the hashes match the OS volume is automatically unlocked and if the hashes don't match the OS remains locked. Now these two states auto unlocked and locked define the level of access that winery has to the main OS. So in the auto unlock state win has full access to the OS. It can perform recovery operations without any user intervention. While in the locked state win has no access at all to the encrypted OS volume. Now this change was necessary because winery now resides on an unprotected recovery volume and it can no longer be blindly trusted. Right? because physical users can mount this recovery volume change win and boot and we want to prevent it of course. So with trusted whim boot any unauthorized modification to the whim file breaks his trust effectively blocking any tempering. tempering. The third design change and the last one focused on limiting recovery tools that are inherently risky to bit locker. So for example among the exposed recovery tools is the command prompt to prevent a scenario where an attacker abuses a command prompt to access the bit locker protected data. A volume relock functionality was added into win. This functionality is triggered every time that a risky recovery tool is selected and it basically relocks the OS volume. Then to regain access to the OS, the user must manually insert a bit locker recovery key. Otherwise, all contents remain completely inaccessible. So this is how it looks like in practice. If an attacker launches the command prompt, winner will whim is trusted auto unlock occurred. But if he launches the command prompt without inserting the recovery key, access to the bit locker encrypted volume will be denied resulting in the following lock error. So to summarize the impact of the design changes, as long as winary whim is trusted and no risky recovery tools are triggered, triggered, the OS volume is unlocked allowing winery to recover it automatically without user intervention. In contrast, if we win is modified without authorization by physical attacker for example or if a risky recovery tool is triggered, the OS volume is relocked preventing winary from accessing it. So now the critical question arises any attack surface is exposed because of these design adjustments. So as it turned out the auto unlock state in the auto during the auto unlock state winner parses files from unprotected volumes specifically the EFI volume and the recovery volume. This parsing presents a very interesting attack surface which wasn't valuable to explore before beatlocker's introduction because before beat locker there was no security boundary here. So even with full code execution within win and if a physical attacker didn't grant any new capabilities but this is no longer the case with beat locker any code execution within win in the auto unlock state can be utilized to bypass Bit locker and extract all the protected secrets. So we decided to take a look at this parsing of winery during auto unlock and to see if we can attack the parsers maybe find memory corruptions or logical vulnerabilities and potentially bypass bit locker. bit locker. So we decided to dig deeper into these files and their parsing and specifically the files that we will be follow focusing on today are the area agent XML and boot SDI files residing in the recovery volume and the boot configuration data store the BCD store residing in the EFI volume. So without further ado I'm going to pass it on to Netanel who will walk us through the research process of the parsed external files. Thank you. Thank you. So let's begin with the boot SDI file. The SDI stands for system deployment image format and it is optionally used for booting from virtual disk into RAM disk. This format consists of multiple binary blobs with certain blobs being essential for the RAM disk boot process. Below you can see the layout of RAM disk in memory which contains the OS disk image image and uh and more. When an SDI file is specified, it gets prepended to the virtual disc within the allocated RAM disk memory region. In our scenario, we are trying to boot Windows recovery from a whim file. The booti will contain a wim blob which contain an offset to the appended trusted winary whim. Additionally, the Buddhist DI must contain an empty NTFS volume. volume. This empty volume enables the in-memory whim to be presented as NTFS volume, ensuring compatibility with other Windows components. Windows components. Now, let's briefly examine a pseudo code that demonstrate this process. The code retrieves the sizes both the SDI and the WIM files, then allocates buffer large enough for both. Next, the SDI data is copied to the beginning of the RAM disk image buffer. Then the WHIM data is loaded and positioned immediately after the SDI data. During this process, the loading API computes the WHIM hash for validation. If the hash validation fails, the main noise volume gets relocked. relocked. Finally, the WHIM address is determined by adding the WHIM offset stored in the SDI to the RAM disk buffer address. Notably, there is no verification linking between the whim being used to the one that was harshed earlier, allowing the offset to be set arbitrarily. With that in mind, let's dive into our first vulnerability. We know that winner must pass hash validation when loaded immediately after the SDI. However, the boot SDI itself under goes no validation whatsoever. And since we can manipulate the whim offset to point to an arbitrary location, we could append an untrusted whim to the SDI and adjust the offset to point to it. The ash validation for the trusted whim will succeed, but the untrusted whim will be executed in its place. This allows us to load and execute any whim we choose while keeping the main noise volume still unlocked. Now let's see a demo for the first vulnerability. Here's the locked machine which we don't have the password for it. We press on uh shift restart to reboot into win. into win. Once we are in win, we'll execute the exploit we prepared beforehand. Now during boot, the main volume gets automatically unlocked by beat locker. We reach win UI actions. We select troubleshoot. Then we select advanced options. options. Then we select command prompt. Now the menu volume got relocked. We don't have the key. So we skip this drive. Now if we try to access C drive, we can't access it. It's locked by by bit locker. If we check the bit locker status, you can see it says locked. So we change path to the D drive which is the unreover the recovery volume, the unprotected volume. We execute the exploit we prepared beforehand. It it add the custom whim to the boot SDI and then adjust the offset to it. And once the exploit completed, we reboot the machine back to win. Now, instead of the winner EUI actions you've seen before, we expect our custom whim to be loaded and we set it to launch cmd. So, here's the cmd. We can change path to C drive. If we check now the blogger status, you can see it says unlocked. And if we list all the files under the C drive, here are the secrets. Thanks. Clicker is okay. After we completed our review for the bootst, we shifted our attention to the area agent XML. The area agent XML file contains the current recovery state and other configuration settings for winary. Upon win startup, it immediately consumes and parses this XML file. When winner state is configured to execute a scheduled operation, it will run the designated recovery operation such as startup repair or other tasks. All of these occurs prior to reaching the UI actions you've seen before. Today we will talk about two scheduled recovery operations that can get arbitrary executed by modifying the state in our agent XML. First offline scanning and then winary applications. Starting with offline scanning operation. These operations allow AV ran allow running AV scan from within winry against the main OS. It is mainly used against malware that does not run inside Winry. We found we can control which app performs the scanning though there are a couple limitations. First, the offline scanning is performed only by executing applications from within the main OOS volume. volume. Second, this application must be digitally signed by Microsoft or WHQL. Third, the signatures must be embedded within the binary itself. We search for all default application that satisfy these requirements and we discover approximately 30. No, CMD is not among them and we could not execute all the applications on that list mainly due to compatibility reasons. We reviewed them one by one until we got to application named TT tracer. This application is a time travel debugger utility that allows tracing and arbitrary executable. Okay. So if we can trace any binary we want. Let's trace AMD then. Right now let's go over the complete flow. Before starting the machine, we set the recovery state in our agent XML to schedule offline scanning operation. The XML is then parsed by WinRy. Then according to the step we just said, WinRy performs the scheduled operation which is offline scanning. It will execute TT tracer which will trace any binary you set. We specified CMD. So in our case, it will launch and trace CMD. And at this point while still in auto unlock state we have a full shell access with the main OS volume unencrypted. Now let's see a demo for the second vulnerability. Again here's a locked machine which we don't have the password. We shift restart again to reboot to win. We will execute an exploit we prepared beforehand. Now once we reach win we select troubleshoot then advanced options. Then command prompt. It prompts the It asks for the Blocker key. We don't have it. So we skip this drive. Again, if we check the C if we try to access C drive, you can see it says locked by B locker. If we check the B locker status again, it will says locked. Now we change path to the D drive which is the recovery volume the unprotected volume. We execute the exploit we prepared beforehand. It sets CT TT tracer to u the scheduled offline operation and once the exploit completed we just reboot the machine back to win. Remember this is the recovery volume and protected volume. We can do anything we want in it. Now instead of UI actions we expect an application to u spawn. So here is TT tracer and it ask us to accept the ULA. We accept it and CMD is launching as you can see. Now we can change path to C drive. drive. And if we check the B locker status you can see it says protection on and unlocked. If we list the files under the C drive we can see again the secrets there are. Okay, so we completed going over the offline scanning. Let's proceed with Winry applications. Winry applications. Another scheduled operation we can configure is winry apps which allows which allow us to run application within winry. If this application are trusted, they get executed while the system remains in auto unlock state. Otherwise, they run after the main OS volume has been relocked. been relocked. We went looking for the hash validation process and we found that trusted applications are registered within the win registry using their executable name and the file hash. The registry reside within the whim file and a physical attacker cannot modify it. Any alterations to the whim would cause it change would cause would change its hash causing the menu volume to be relocked during the boot hash validation. Now when win is scheduled to run trusted application it first calculate the hash of the target application then searches for a match in the registry. If a match is found application is marked as trusted. If no match exist the application is marked as untrusted. The menus volume gets relocked and then later the application still gets executed but with the main locked. So overall that sound as window trusted apps validation is solid and works properly right. Well, we wondered if an already registered application can expose an attack surface. So, we went hunting for registered applications. We found the legitimate usage of this feature by a program named setup platform. This program is applica. This application is registered as trusted app and used during Windows upgrade. Only once the upgrade completes the trusted app entry remains in the registry and it is not removed. So we can execute setup platform but what else can we do with it? We found that it register shift plus F10 hotkey to launch cmd. Convenient, right? Convenient, right? Well, we tried that but it fails to execute due to missing configuration file on the main OS volume. Since this configuration file resides on the Bit Locker protected main OS volume, it cannot be created or edited. This configuration uh requirement cause setup platform to terminate early due to this file being absent. Now the time window between the hotkey registration and the process exit is very short. It's quite impossible to trigger this hotkey manually. So we wanted to find another way to trigger cmd. We went back to analyze setup platform. setup platform. That is when we discovered something really interesting. Immediately after registering the cmd hotkey, the application searches for a file named setup platform INI on the recovery volume which is the unprotected volume and we can fully control it. Now by configuring this file properly, setup platform will trigger a message box which prevents applications from continuing the execution essentially blocking the application from early termination that will open an infinite time window to spawn cmd and type anything we want in it. Now let's move to let's see the complete flow. Uh we modify our XML and set the state for scheduling trusted app. More specifically, we set set a platform to get executed. get executed. Win then validates this app can be trusted. The validation succeed and the men volume remains unlocked. Next, set a platform gets executed. It register the hotkey for cmg cmd which is shift plus f10. It then looks for setup platform ini file and loads it which will trigger a message box. This way the process got blocked and won't continue until we press okay. Now we can simply press on shift plus F10 to trigger the cmd and we got a shell up and running while the menu volume still unlocked. Now let's see a demo for the third vulnerability. vulnerability. So again here's a lock machine. We don't have the password. We shift reboot. We shift restart to reboot into Win action. Select troubleshoot. Advanced options command prompt. Uh we don't have the key. So we skip this drive. this drive. Now we will try to access again the C drive. You can see it says locked. We'll check beat locker status again. Says locked. Protection is on. Now we change path to the D drive the recovery volume the unprotected volume. We execute the exploit we prepared beforehand. We set set a platform to get executed. We write down the setup platform INI file. And next we reboot the machine. Now instead of DI actions this time we expect our setup platform to get executed and we said setup platform INI. So we expect a message box and here is the message box. Now we can simply press on shift F10. We spawn the cmd. If we change path to C, we can change it. If we check the bill locker status, it says protection on. And the bloer status is still unlocked. Now we can just list the files under the G drive. Get the secret. Here it is. Now alone we'll continue to talk about the BCD parsing attack surface. >> Excellent. So now we have one more demo guys. So stay tuned. Um so let's move on to attacking the BCD parsing. parsing. So first things first for those of you who are unfamiliar with BCD and what it stands for. BCD stands for boot configuration data and it's basically the file that defines how Windows boots. It stores the boot entries, controls the boot parameters, recovery settings and many more boot related configurations. Now in the context of win the usage of BCD is considerably minimal. Win uses the BCD mainly to determine the location of the target OS volume on the disk so that winery knows which volume to end the recovery operations to right because we have multiple volumes on the disk. Winry must know which volume is the OS volume to recover. So when winer restarts it reads the BCD store queries the target OS volume location from that store and then aims the recover operations to this volume. So the first question that came into our minds when we investigated winery BCD usage is what can we potentially gain from targeting it. As it turned out wery places full trust in the target OS assuming it's out of attacker reach. Now, generally speaking, this assumption is solid from a design perspective because with Bit Docker enabled, the target OS is encrypted and the attacker has no access whatsoever. So, it's safe to store the configurations, query these configurations and not further validate them because again, a physical attacker can't change anything in the main OS. It's encrypted. Having that, having said that, since the target OS location is defined in the BCD, which we could modify, we began to question this trust assumption. Could BCD manipulations allow us to undermine it? So what if we could trick winery into thinking that an attacker control volume is actually the trusted bit locker encrypted one? Our focus has shifted towards identifying a primitive that allows impersonating the target OS location. The goal is to manipulate the target OS volume to point to an attacker volume such as the recovery volume instead of pointing to the trusted bit locker encrypted one. So anytime that we win then if we gain this primitive if we win queries configuration assuming it's out of attacker's reach the attacker can modify it because it will be in the recovery volume and not in the encrypted volume. So this primitive if gained would break the trust assumption. So we already know that the target OS location is controlled in the BCD as we just mentioned which we can directly modify. So what happens if we change the location directly in the BCD? Well theoretically it's possible but it isn't valuable. Let me explain. The target OS location isn't used only by winery but also by the boot manager which uses it to know which volume it should auto unlock for winery so that winery is able to access the OS. So any change to the BCD store would affect both the boot phase and OS phase. In this specific scenario, if we modify the target OS to point to the recovery volume, it would prevent the boot manager from auto unlocking the Bit Locker encrypted OS volume. And this encrypted volume contains the secrets that we are after. So in this state even it breaks the trust assumption but even with full code execution within win we're not able to access the secrets and that's why it's not valuable our primitive must not interfere with the boot procedure and the auto unlock functionality we must go deeper deeper so we decided to dig deeper into the way that winner searches for the BCD store any potential flaws at this stage would not interfere with the autolock functionality because it executes afterwards so to locate the BCD store we iterates over disk volumes and searches each one for a BC for the store the first store that is found is the one used to further calculate the target OS location. So a simple volume iteration logic. logic. Now this iteration logic is actually an interesting point to further inspect because if you think about it only the EFI volume contain the BCD store. So any attempt to locate the store on another on other volumes especially if they are not protected could potentially be abused. abused. Internally, the functions that are used to iterate over the volumes are find first volume and find next volume. These are two standard Windows APIs. And notably, the remark section highlights a very interesting point. It says that one should not assume any correlation between the order of the volumes that are returned by these functions and the order of the volumes that are on the computer. So, under the hood, these two functions perform further filtering and sorting which causes an inconsistency in the return volume order. So for example on the monitor you can see the typical volume order returned by the disk part list volume command. The order is first the OS then efi and only then the recovery volume. However the volume iteration functions would return a different order. It would return first the OS volume then the recovery and only then the EFI volume. Can you spot the inconsistency? With this iteration logic, the recovery volume is iterated over and checked for a BCD store before the EFI volume. By placing a custom attacker control store in the recovery volume, we can trick Winery into using it instead of using the EFIBC BCD store. So with this setup, Winery doesn't even reach the point of quering the EFI volume, which contains the real store they used to boot the machine. So any modification or corruption we make to that custom store will only impact Winery and will not disrupt the boot procedure at all. This flow allows us to achieve the desired primitive. During the boot phase, the boot manager lockets and uses the legitimate store from the EFI volume to unlock the encrypted OS and this OS contains the secrets. So during the boot, it's unlocked. However, during the OS phase, winner instead lockets and uses the attacker controlled BCD store due to the iteration logic that we just explored and it uses this attacker uh attacker store leading it to consider an attacker volume as the bit locker encrypted one. Right? Because once we control the store, instead of pointing the volume location to the main OS volume, the encrypted one, we just point it to a different volume that we control. control. So with this primitive, any configuration that we retrieve from the target OS previously assumed to be beyond an attacker's reach is now entirely under the attacker's control. So any sensitive configuration it queries which we weren't be able to modify, now we're able to modify. To further exploit this powerful primitive for bypassing bit locker, we need to find a winary flow with three characteristics. First, a flow that can be triggered either from the winner UI or from re agent XML scheduled operations so that we're able to trigger this flow freely. Second, a flow that doesn't trigger the wheel functionality so that we won't lose access to the secrets because we don't want a flow that when we trigger it relocks the volumes and we lose access to the secrets. Third characteristics is a flow that queries configuration from the target OS to perform sensitive operations. And this is actually the interesting part here. The idea is to chain this flow with the gained impersonation primitive to trigger sensitive operations in the auto unlock state. Such operations that we wouldn't be able to trigger beforehand. So after exploring different wary behaviors, we found a potential candidate. This is the push button reset feature, also known as PBR. PBR is Windows system reset tool and it provides various reset functionalities. It supports multiple trigger modes with one of its no modes known as only PBR meeting the characteristics that we defined for the exploit. So first only PBR can be triggered freely as a scheduled operation in our agent XML. Second this flow doesn't trigger the reload functionality thereby benefiting auto unlock. We won't lose access to the secrets. And third, it queries configuration from the target OS to perform sensitive operations, thereby can be controlled with the impersonation primitive. primitive. Specifically, the sensitive configuration that we identified was an XML file named reset session.xml, which defines all the operations executed by PBR. Notably, it included the decrypt volume directive, which instructs PBR to decrypt any arbitrary Bit Locker encrypted volume. So we now have all the pieces that required for the that are required for the full beater bypass exploit chain. The exploit required modifying few files on the recovery volume. So first the XML file with online BBR scheduled operation. Second the BCD store with the OS device element pointing to the recovery volume instead of the encrypted OS. And third the BBR configuration with the decrypt volume directive specifying the target OS volume to decrypt as the OS volume the encrypted one. With this setup, what will happen is that in the next winery reboot, winner will go through the online PBR flow. During the BCD store lookup, PBR locates the attacker BCD store and mistakenly identifies its target volume as a recovery volume. recovery volume. Then PBR loads its configuration from that recovery volume which instructs it to decrypt the Bit Locker encrypted OS volume. Once this entire process completes, Bit Locker secrets become freely accessible as Bit Locker was essentially disabled by PBR. So we went from a considerably scoped OS impersonation primitive to an arbitrary bit locker decryption primitive. With that let's see the demo the last demo of the day of the full exploitation chain. chain. So we have here a locked machine which we don't have the password to this machine. So we select shift and restart into win in order to execute the exploit. Inside win we select troubleshoot advanced options and then command prompt. Here the recovery key is requested. We don't have this key. We skip this drive. Now you'll see that we don't have access to the C drive that contains all the Balker secrets. Um so now what we need to do is to change drive to the D drive. That's the mounted recovery volume which we control. We placed our exploit in that volume. So we now execute this exploit. And once the exploit finishes, we'll reboot into winner. Now you will see during the reboot that PBR gets executed. This process takes a few minutes. This what you you'll see now on the screen. So we fast forward a bit. In the meantime, let me tell you what the exploit did. So it placed this XML file with online PBR and the recovery volume. It then created the BCD store on that recovery volume as well. So that the iteration logic considered this store and the volume the target volume on that BCD store pointed to the recovery volume instead of the encrypted volume. So now when PBR looks for its configuration it queries it from the recovery volume instead of from the encrypted volume. This configuration tells BBR to decrypt the OS volume that we are after it secrets. So what happens now under the hood what you see on the screen is actually PBR decrypting the OS volume essentially disabling beat locker on that volume. So once PBR completes, we'll go back into the main OS. You see now the login screen. And of course, we still don't have the password, right? So we'll restart into Win. And once we're in Win, we'll check what is the current status of BBR of of Bit Locker. Bit Locker. So select troubleshoot, advanced options, command prompt. At this point, you will see that the recovery key is not requested. We change drive to the C drive, list the the files, and you can see that we have full access to the C drive that contains all of the secrets. Now, if you wonder why this is possible, if we check the status of Bit Locker, you will see that the protection is off and the drive is fully decrypted. So, we bypass Bit Locker by disabling it, able to extract all of the secrets. So closing remarks starting with the vulnerability fixes. So all the discovered vulnerabilities and exploitation techniques were fixed in July speech Tuesday. The CVS for the vulnerabilities that we presented today are displayed on the screen. To further enhance the security of beat locker, we recommend enabling TPM plus PIN for pre-built authentication. This significantly reduces the attack surfaces and limiting only limiting the services only to the TPM. So if TPM plus spin is already enabled in your machine, you're protected from bit locker by bipasses that originate from win the web components log on screen and other attack surfaces because to get the bit locker key, you have to insert a pin. So to mitigate bit locker downgrade attacks, we advise enabling the revised mitigation. This mechanism enforces secure versioning across critical boot components, preventing downgrades that could reintroduce known vulnerabilities in Bit Locker and secure boot. For the full article on beat locker counter measures, please scan the QR code on the screen. screen. Lastly, we want to emphasize that our journey never ends. We continue to proactively research beat locker weary and the related components with a never- ending goal of increasing their security and resiliency. It was a pleasure for us to share with you our research project and the security work that we do on Wener and Bit Locker. If you have any questions, please catch us in the hallway and also feel free to reach out on social media. And with that, we would like to thank you all for joining us today. And we wish you a great rest of your day. Thank you.
