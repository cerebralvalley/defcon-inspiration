Uh I'm super excited to be here. First time at Defcon. So welcome to my talk. I am very tired. I'm severely hung over. So I really hope this goes well. Um now before we get into the talk itself, I wanted to start start with a short apology. So in case anyone didn't really read the talk title really well or the abstract and thought that they were going to hear about the latest developments in VBScript malware, I'm really sorry because we're not going to be covering VBScript but rather the other VBS virtualization based security. Uh so yeah, sorry for all the VBscript fans. Anyone is leaving? No. Okay. So yeah, I guess we can start. My name is Oid Vid. I'm a security researcher at Akami. I have a background in red teaming and threat hunting. And today I cover a wide variety of research topics in the Akami security research team. And uh for the last few months I really uh focused on uh the area of virtualization based security. Um I believe this is this is one of the most significant and interesting advancement in security that Microsoft have implemented in recent years. So we will start by briefly covering it. um understand what it does, how it does it and why it does it. And then we'll get to VBS enclaves um a feature which is enabled through VBS uh and have a lot of uh very uh cool security applications security applications and then we'll get to the fun part abusing VBS enclaves. We'll see how we can take this security feature and kind of flip it around uh and see what attackers can actually do with it. Okay, so let's start with VBS. And to uh talk about VBS we first need to talk about boundaries security boundaries. So uh traditionally in order to protect the operating system uh Microsoft and other uh operating systems as well relied on processor rings. So the idea was we'll put the uh OS the kernel in ring zero will restrict user applications to ring 3 and by doing that we can ensure that h attackers will not be able to tamper with the kernel. Well, this really proved to be insufficient over time. H attackers have done things like exploiting the kernel as it exposes a very significant attack surface or explo exploit one of the millions of third party drivers uh through bring your own vulnerable driver uh techniques and yeah this really again proved to be insufficient. So we needed new an additional security boundary into the OS and this is where VBS uh comes in. So uh VBS came to our life through HyperV. So let's start with that. HyperV, Microsoft Hypervisor, traditionally used to run VMs either locally or in the cloud. H now one of the most important things that HyperV has to manage and any good hypervisor for that is isolation. We would never want to allow a situation where one VM can access the memory of in general the resources but for our sake today the memory of another VM. So, HyperV already uh comes with a very strong memory isolation capability. I will not go uh too deep into how it does it. There are plenty of good resources online. Um but yeah, so we have this very strong ability to restrict one VM from accessing to the memory of another one. Now, um someone at Microsoft at one point probably had a good idea. Let's take this h same primitive isolation primitive and apply to the operating system itself. So now h instead of isolating two different VMs what we can do is we can take a region of memory within the operating system and isolate it from the operating system itself. So this is the root of VBS and it turns out that this isolation actually enables us to add additional security boundaries into the OS. So these are called virtual trust levels or VTLs. And here we have what we had before. So we had two execution modes. We had user mode ring 3 and K mode ring zero. Now when we add VTLs into the mix, uh things get interesting. So first we have VTL0. This is the least privileged V VTL which is also called normal world and you already know it. So VTL0 contains the normal operating system. We have their user mode and a normal kernel mode. Things get a bit more interesting when we introduce VTL1. Now VTL1 is more privileged than VTL0. And uh this privilege comes in the form of memory isolation. V code running within VTL0 cannot access code or the memory of VTL1. On the other hand, VTL1 can access the memory of VTL0. So this is how this privilege h comes into effect. So within VT1 we have two new execution modes. We have VT1 ring zero for the secure kernel. This is a more privileged kernel that can actually be used to apply and enforce additional policies uh to the normal kernel normal kernel and we have isolated user mode or IUM which is VTL1 ring 3. Now on one hand we are running in ring 3 so we can't perform sensitive kernel operations but we still enjoy the advantage of VTL of running within VTL1 isolation as the name suggests and the thing is that IM is going to be protected from threat actors that compromise the normal operating system. So even if an attacker were to compromise the kernel itself it will still not be able to access uh anything that is stored within IM. Okay. So these are uh VTLs and the new execution modes that they introduce and now we can move on to our next topic or our main topic VBS enclaves. So an enclave is an isolated region of memory within a process. The process cannot write into the enclave. It cannot read from the enclave. The only thing that it can do is it can invoke methods that are exported by the enclave. So h why uh why something like that is useful? Let's consider the following scenario. A process handles some sort of a secret. So either something like an API API key a password. Now let's say that the process was were to be compromised either directly or let's say the operating system itself was compromised. There's nothing to stop an attacker from then accessing the memory of the enclave and stealing this secret excuse me of the process and stealing the secret. Now what we can do if we h design our application with an enclave is we can store this secret within the protected and isolated enclave. Now if an attacker compromises the process they will never be able to exfiltrate the secret in order to actually utilize it. What the process can do is it can invoke the methods that are exported by the enclave. So we won't be able to just uh access the key and directly encrypt values using it or authenticate directly but we can request the enclave to do operations uh using it. So we pass a value and the enclave encrypts it. So uh this is the idea and the of enclaves. There are additional use for them as well but I think this is a a good start for that. And enlaves are a general concept. Um, and VBS enclaves are Microsoft's implementation of them using VBS. It is actually very elegant and a very elegant uh implementation. What we do is we simply kind of split the processes in half where the normal process resides in user mode VTL0 and the enclave will reside in isolated user mode VTL1. By doing that we ensure again that nothing in vtl0ero can access the data within the enclave. Okay. So let's h discuss a few more uh details about enclaves and how we use them in windows. So an any process can create an enclave and load what is called an enclave module into it. An enclave module is a DLL that is compiled specifically for the purpose of being run within an enclave exporting the functions and stuff like that. And once we load the DL again we can't access it directly. We can invoke its exported methods by uh calling the call enclave API. Um and uh one final note before we move on is that we can't just load any arbitrary DL into an enclave. Microsoft realizes that uh enclave running within an enclave should be uh something that is restricted and because of that in order to load a DLL into an enclave it must be signed using a Microsoft certificate that is designated specifically for signing enclaves. So this is going to be uh the main challenge for us uh for the rest of the talk. So uh now that you understand what VBS enclaves are, let's get to the fun part. part. how we can abuse them. So the first thing that came at least into my mind when considering this was the idea of running a malware within an enclave. So the same advantages that are provided to normal applications could apply to attackers as well. So first obviously we will have inaccessible memory eds and security analysts whatever they won't be able to scan our memory in order to identify malicious behavior. Another advantage has to do with behavior, not just scanning memory. Uh well, it turns out that it is much harder or pretty much impossible to trace API calls that are performed from an enclave. So, let's understand this. When a normal process triggers a Windows API, uh we go through a process that I believe most of you probably know. We go through a a series of system DLS eventually antl from there we invoke a sys call into the normal kernel. Now the thing about this flow is that edrs have a lot of visibility into it. They can hook our calls into DLS. They can monitor our activity in the kernel. There are a lot of detection opportunities here. On the other hand, when an enclave triggers an API, we no longer go through the normal VTL0 DLS, but rather through a separate set of VTL1 resident DLS. So instead of going through ENTDL, we go through ver DL VBS enclave runtime DL and from there when we invoke a sys call it is managed by the secure kernel. Now the thing about this process is that it occurs exclusively in VTL1 meaning that EDRs and any other security product will have zero visibility into anything that goes there. So yeah, another advantage that we gain. we gain. Okay. So yeah, we understand why it might be cool to load an enclave uh a malware into an enclave, but as I mentioned before, we can't really do that, right? Uh in order to load a DL, it needs to be signed. So in the next se section I'm going to cover a few uh approaches that attackers can use in order to get malicious code to run within an anklet. So we'll start with the straightforward ones I guess uh that I did want to mention uh uh before going to the next ones. So attackers could obtain a legitimate signature. Microsoft exposes enclave development to third parties and we could theoretically see attackers attackers legitimately signing their enclaves as we have seen them do in the past with things like drivers and executables. So they could either compromise an authorized signer or obtain signing privileges legitimate through a shell company. So this is one option. Another option would be to exploit a vulnerability in the loader in the Windows loader that is h in charge of validating the the enclave signature. So in theory someone could find a vulnerability in this process which would allow loading unsigned DLS into enclave. Yeah, which is exactly what Alex UNESCO demonstrated last year. So while this vulnerability was patched er advanced threat actors could certainly find additional avenues to get h past this uh verification and get unsigned code to uh run within an enclave. Now h these two approaches are certainly valid h and I believe that advanced threat actors nation state threat actors could use them but I don't know what about you I'm personally not a nation state threat actor h so I wanted to find something that might be more accessible to well less sophisticated threat actors h and I ended up finding two additional uh paths to get code running within an enclave the first one is the abuse of debugable enclave modules enclave modules So um it turns out that if you want to uh debug an enclave during development, it's actually not a straightforward task. Uh think about it. Enclaves run within VTL0, excuse me, VTL1. Our debugger runs in VTL0. If we simply try to debug an enclave, we won't be able to do that. As we can see here, you can't access the memory. You can't place break points. So yeah, Microsoft, I believe, pretty early realized this issue. And because of this they implemented debugable enclaves. So when you compile an enclave you can specify a flag that will mark it as debugable. And when you do that well like magic you can now debug the enclave. As we can see here we can now access the memory of the enclave. We can place breakpoint. H yeah we can have a good time. Now when I initially saw this I was kind of confused and surprised because uh we know that enclaves reside in VTL1 and we are accessing their memory. So it's kind of weird. H at first I thought that Microsoft might be pulling some sort of a trick here where they'll emulating the enclave when it is debugable. But upon digging into this I realized that this is not actually the case. Debugable enclaves still reside in isolated user mode. And to enable the debugging that we have just witnessed the secure kernel actually implements some exceptions. So uh for example if we try to access the memory of a debugable enclave we'll go through the secure kernel debug readwrite memory function that will determine if the enclave is debugable and if it is it will allow us to access it. Similarly we have the debug protect virtual memory which will allow us to h change memory permissions on debugable enclaves. enclaves. So h now that I understood uh more about uh these special enclaves I wanted to see uh how we can abuse them. So h first and pretty obviously it is not a good idea to use a debugable enclave for a production application. Microsoft clearly acknowledges this in their documentation because if you're using a debugable enclave you're essentially not really using an enclave. Anyone can access its memory. You might as well just allocate a normal memory buffer. Um but yeah so this is a risk. It is acknowledged. H but it turns out that there's another interesting way that attackers can actually abuse this. So let's say that an attacker obtains a signed debugable enclave. So a DLL which is signed and marked as debugable. It turns out that enclaves enclave modules are not paired to a specific process. Meaning that any process can load any module. So what an attacker can do is they can load this debugable enclave into their own process. What they can do next is they can take advantage of the H exceptions that we mentioned before to debug the enclave and achieve code execution within VTL1. So let's uh look at this process how it would look like. So here we have an attacker who obtained this type of uh debugable enclave and loaded it and we can see that the enclave contains an enclave routine in green a function which is exported by the enclave. What the attacker can do is they can obtain the address of this routine. They can then change their memory permission to allow a to page execute read write. This is possible because the enclave is debugable. debugable. They can then overwrite the routine and h invoke the the shell code by using call enclave. So we can't just h run arbitrary addresses within v within the enclave. But because this is an exported function that we over overwritten, we can trigger it normally by using call enclave. So yeah, this is a cool technique, it works. But there are uh some caveats to it. So let's cover them. The first one is that the memory access actually go both ways. So just like we can access the memory of the enclave, h edrs can do the same thing because as we just saw the exceptions apply to them as well. well. Despite that, there are still advantages to using this technique. So the first is that this is an unusual way to execute code which I pretty much guarantee that if you do today, no one will detect. Um so yeah, EDRs might not consider this scenario. Um even though they can theoretically detect it. And the second advantage is that enclaves even when they are debuggable will still use the uh call chain the API call chain in VTL1. So hooking their APIs will not be possible. The second unfortunate catch is that at the moment I haven't actually managed to find one. So I have scoured the internet. I have wasted way too much time looking through virus total in an attempt to identify such a file a signed debugable enclave. But at the moment I was yet to be successful. Despite that, I still believe that I haven't just wasted far five minutes of your time. H because I really strongly believe that it is only a matter of time before h such a file finds its way to the internet. So the thing about enclaves today is that it is kind of a not really adopted technology. Not a lot of people use it, but Microsoft have really began pushing towards this direction, implementing a lot of things using enclaims themselves. And I believe that we will we will begin to see more third-party drivers developers utilize them in the future and we all know developers developers up and one file will h find its way into virus total or into somehow into the internet and this technique now becomes available. So yeah I think it is worth considering. considering. Okay, so this was the first idea, abusing debugable enclaves and we'll now move on to the second idea, exploiting vulnerable enclave module. So, uh the idea uh is not mine, let's say, because I kind of applied a very well-known uh technique. Uh if you're not familiar, bring your own vulnerable driver is the concept where Microsoft enforces signing on drivers. So, you can't load a driver into the kernel unless it's signed. So attackers uh figured out that what they can do is they can obtain a legitimate signed driver with a known vulnerability, load it into the kernel and then exploit the vulnerability to get code execution. Well, what I thought was why not apply the same idea to enclaves. If we manage to find a vulnerable enclave, we can load it, exploit it and get our code running within. within. So uh luckily such a vulnerability exists. uh CVE 2023 36880. This is an information disclosure vulnerability in an enchive module used by Microsoft Edge. And despite it being listed as an info leak, if you check its um its patch notes, you will see that Microsoft mentions that it could lead to limited uh code execution. Um so I decided to dig into this a bit more. Uh this vulnerability was discovered by Alex go from the uh Chrome security team who also shared a PC for exploiting it. It is actually a relatively simple vulnerability h and it allows an arbitrary readr into the enclave. So even though this is a normal non-debugable enclave, we can after loading it write whatever we want into the enclave address space and read whatever we want from any address as well. well. So uh seeing this and understanding this I was able to obtain a file a vulnerable version signed version of this enclave. Um and I began attempting to exploit it. So the idea was well we have arbitrary read write h we could do something like leak an address of the enclave call stack. We can override it with a rob chain and then get arbitrary code execution. Well, it wasn't going to be that easy as I quickly discovered because enlaves are protected with ACG, arbitrary code guard by default. So, if you're not familiar, ACG is a mitigation policy that ensures no dynamic code can run within a process. Meaning code that wasn't present in the original files as they were loaded h cannot execute. In practice, it means we can't allocate new executable pages and we can't turn existing executable pages writable. So, uh we could theoretically create a full uh a fullchain exploit h which is not ex turned out to not be only a theory. I'll get into that later. But at this point of the research, I couldn't find a way to get a something useful with this enclave. enclave. H and here we see a demo of uh my own enclave that I have written in order to test this and we can see that when we try to allocate a readr execute page we fail with the error of we can't run dynamic code. So yeah at this point h I was kind of stuck and you might said it h got into my personal life as well. Uh I knew that I had something here. So the ability to write and read whatever I want into the enclave felt just too cool to be not useful. Um so yeah after some sleepless nights and many days trying to do something with this I came up with a a pretty interesting technique. So yeah this brings us into bring your own vulnerable enclave round two. So h I tried to remain positive and instead of think about what we can't do focus about what we can do. So the vulnerability vulnerability actually provides two very interesting capabilities. So first we can write whatever whatever data that we want into the enclave. So we can't execute it but we can load data there. So this is actually useful because we get this h secure vault that we h can store whatever information that we want either shell code malware configuration encryption keys and edrs analysts thing like that will not be able to t to touch them. them. The second interesting advantage which is a little less straightforward is that we the vulnerability allows us to actually work in reverse. we can trigger it in such a way that allows us to write data into VTL0 from VTL1. So we can trigger the exploit causing the enclave to write data back into the normal address space of the process. Now this is useful because as we mentioned before EDLs will have no visibility into this uh interaction. So if they try to uh monitor uh write right operations to certain memory regions they will not be able to do so if the right operation is performed by the enclave. So combining these two uh capabilities I came up with an memory evasion technique that I h mirage and mirage is a sleep obuscation technique. So uh the idea with the sleep ofuscation technique is that you create a payload which spends most of its lifetime in a sleeping dormant state. The payload then wakes wakes up occasionally based on a defined trigger execute its code and then quickly goes back to sleep again. And the idea is that during sleep we protect our code in a in a way that will make detection harder. So for example we can mark it its memory non-executable or we can encrypt it. So my idea was why not use this vulnerability to hide our shell code within isolated user mode during sleep. So let's understand how this technique will look like. So first Mirage will load the vulnerable enclave and exploit it in order to write shell code into it. Now at this point if an EDR takes a look it will not see anything. So our normal memory is completely clear. We are completely innocent. All the malicious stuff are in VL1 where they don't have access. Now occasionally we can trigger the vulnerability again writing our code back into VTL0 again from the enclave. So an EDL cannot have any trigger uh for any inspection. any inspection. We get our shell code. We run it and then we override it again with benign data. The cycle can repeat itself and the idea is that it makes it almost impossible for an EDR to detect us. So let's look at a demo of this. So here we h loaded Mirage. What it did was it loaded the vulnerable enclave, exploited it to write a shell code into it and then allocated a read, write execute buffer uh within VTL0. Now this is we will now inspect this buffer. But importantly, this is the last time that we're going to interact with this buffer from VDL0. Everything else is going to occur from the context of the enclave. So now what we did was we trigger the vulnerability again. And as we can see the shell code is now in our buffer written by the enclave and now we can jump to the shell code, execute it, and quickly overwrite it with benign data again. So that's it. We have our beautiful calculator here and we're out. Shell code is nowhere to be found again. found again. So yeah, this is Mirage. H we've released uh Thank you. Uh we've released a proof of concept code for this. You can find it on on Akami's GitHub. Um and before I move on, I just want to mention uh the amazing work by Cedric Van Bohaven and Mattel Malvika of Outflank. Uh it turns out that we worked on almost the same exact research er in very similar uh time and they published two months ago their own work on the exact same vulnerability. Um and and they were able to actually achieve what I previously described where they created a full rope exploit um that allowed them to implement a very simple a very similar technique to what I just showed you. So they weren't able to get arbitrary code execution. what they were able to do was to create a sleepuscation technique but their one relied on a rob chain. So I really encourage you to check out their work. I think it is more cooler than mine. Uh so yeah check it out. Okay. So h now h I hope that I was able to convince you in the last 30 or so minutes that an malware could find its way theoretically into an enclave. So the next question that I wanted to uh try to answer was what can we actually do while running within an enclave. It turns out that Microsoft really restricts h the attack surface within an enclave and they provide them a very very limited access to APIs. So we can't perform file operations, we can't do networking, we can't interact with processes, we can't access the registry, the list goes on. So most traditional malware techniques are not going to be accessible to us. So what can we do? So I was able to come up with a a few techniques that attackers can still perform even uh despite all these restrictions. So the first thing that we can do is we can access the VTL0 memory of our host process. So the enclave even though it runs in VTL0 can read and write the normal user mode process. So this could be interesting in the context where we for example we can inject an enclave. So we create a payload which injects an enclave into a remote process and from that point we can reside within VTL1 monitoring and uh tampering with the activity of the process in a way that can't be detected or at least easily detected. Let's take a look at another demo. So we have here our cute program that we uh receives a string and echoes it back to us. So we enter test, we receive test, we will now write that VBS is awesome and we will see that VBS is in fact awesome. So if we try we get confused and we write that VBS is lame, we will see that it now echoes that VBS is in fact very cool. So let's understand what is a happening here. So uh we have a very advanced piece of malware here as you can see uh that runs within an enclave. So we have an enclave running within the process and what it does is it monitors the buffer that the user supplies their input into and uh detects the string that we uh the bad string. If it detects it, it replaces it with a good one. So yeah, a nation state grade malware here. H but I wanted to show you this because of the mem copy line specifically and let's see what why it's interesting. So as we can see h our enclave will perform the mem copy operation. So if we will continue our demo we will see that what we do next is we place breakpoints on all instances of mem copy found within the memory address space of the process. And what we would anticipate now is we will see we a the breakpoint being hit but if we enter again a string we will see that even though the mem copy was being performed the breakpoint is not hit. So yeah this happens exactly because of the API uh evasion that I previously described and again the advantage here is that we can uh basically interact with the process in a way that cannot be detected. Another cool thing that we can do is we can execute code within VTL0. So we can't do that. So we can't directly execute VTL code in VTL1. This is forbidden. If you'll try to take an executable VTL zero address and directly execute it, it will not work. But what we can do is we can remotely invoke a VTL0 code from VTL1. So this can be performed by using the call enclave API. It turns out that this is a dual use um function. So normally we use it to invoke enclave methods from outside from the process. But another thing that we can do is invoke VTL0 methods from the enclave. So essentially we can pass any executable address through this and it will execute the code in VTL0. Um in in uh in terms of evasion again it is limited because the code will run in vtl0 but this is a kind of an again unusual way and we will see in the demo in a minute h how it does it. So uh here we have our uh malware here. What it do it will do is it will allocate a readr write execute buffer in vtl0ero and again like before we are allocating the buffer but this is the last time that we're going to touch it. So we have our H buffer here. We can see it is empty. H and next what we will do is we will pass execution to the enclave. That's it. it. When we do that we see that our calculator here popped. Our shell code executed. If we check the buffer again we will see that shell code is there. I left it there for the purpose of this demo but I could have just as well deleted it. And yeah, from the context of an EDL, you see a call enclave function being triggered and then a new thread launching calculator. So there there's no direct connection between the two which can uh help with evasion. Okay. Um so finally the last little trick that we can do with enkliffs that is my personal favorite has to do with anti-debugging. So uh enclaves are in a very advantageous situation where it comes to anti-debugging. Uh in pretty much every case when performing one malware analysis h the analyst will use a vtl0ero debugger either a kernel mode one or a user mode one. Now because of this the enclave has a lot of power over the debugger. if we can implement an anti-debugging check within an enclave, a a debugger will have no way to patch it out and overcome it. So yeah, this could be a very cool idea. The problem is that it is again not going to be that easy. Again, we don't have access to most APIs. So things the classic things like is debugger present, get dount, we can't access files, we can't access processes. So h despite that I was able to come up with a few techniques that we can perform from the enclave. So first we can manually inspect the p erh because we have access to the vtl0ero memory. So if we determine that the process is being debugged through the p we can terminate it from the enclave. Another thing we can do is while we don't have access to timing related APIs uh we can run inline assembly within the enclave. Uh so uh using the RDTSC assembly instruction we can measure uh clock cycles of the CPU and by doing that we can derive the time um and uh detect break points and other debugging related activities. So yeah we have some options and uh now how we can make this particularly devastating. So if you think about it, if we simply do like a call enclave with an anti-debug check, well the debugging can simply patch out the call enclave uh call and then will not even get to the enclave. So in order to make this particularly devastating, what we can do is we can move critical logic into the enclave. So preferably something that will be very hard to statically analyze decryption routine. We can implement I don't know a custom VM. You put it within the enclave and then from the same call enclave you also detect the debugger and now you create an impossible situation for a defender where on the one hand they have to pass execution to the enclave if they want to h see what is happening essentially essentially um and analyze the behavior of it and get the code to run properly. But as soon as they do that the debugger h the enclave can detect the debugger and terminate the process. So yeah, if implemented correctly, I believe that the only way to overcome this would be to h use a a hypervisor debugger or a secure kernel debugger. Both activities that I highly do not recommend. It's not fun. fun. So uh in conclusion, VBS is pretty cool. I think that again I think I said it at the start but it is one of the uh most significant security advancements that Microsoft has made in recent years. H as an ex redteamer I can say that it uh s certainly made my life harder. harder. VBS enclaves in potent in particular have a lot of potential. I think that we will continue to see uh more and more applications using them and that's a good thing. And finally, h I hope that I was able to convince you that you should also keep an eye on them from a from an offensive perspective h as we will see more of them uh in this uh use as well. That's it. Thank you very much. If you have any questions, I'll be here.
