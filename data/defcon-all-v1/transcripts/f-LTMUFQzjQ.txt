Okay. Hello. Uh welcome to this talk uh emulating embedded Linux devices at scale with light touch firmware rehosting. rehosting. Uh today I'm going to give this um a kind of higher level uh approach like overview of uh what we've been doing in the um advanced research team at Crowdstrike which is kind of giving us this um quite low investment high return ratio uh when we've been uh emulating embedded Linux systems. So by the end you should uh be able to kind of take the principles that I'm talking about here and apply them in other emulation stuff. So it's it's a little bit of a patchwork approach and a bit proof of concept but um hopefully a little bit helpful at least. Um at the moment most uh embedded Linux um emulation works in user space. Um, so a lot of the emulation approaches take a stock ARM or MIPS Kamu machine, boot into like a normal uh DRO, unpack the firmware into that guest, ch route into it, and then run binaries with like a LD preload uh to paper over incompatibilities in it. Um, this approach works pretty well if you're looking like a specific binary that you want to look at on a specific device. Um, there are some cases where it gets pretty tricky, but but in a lot of cases it's it's totally fine to do. But when you're doing that kind of approach, the with the LD preload approach, what you're basically trying to do is um compensate for missing peripherals and uh a different kind of system setup uh that the binaries are not expecting. So um NV RAM is the classic example. Um the user space way to to deal with NV RAM is to LD preload something like lib NV RAM um or some descendant of that with extra functionality. Um functionality. Um but there's certain cases where we can't really address really address um these kind of incompatibilities just by LD preloading stuff when the systems are designed to work more holistically. Um with a lot more moving parts um um so the traditional kind of way of doing things in user space doesn't really work uh for for those kind of approaches. Um so our question was was can we try and do it in a different way? Um we want to do vulnerability research on these systems. Uh we also want to do things like run honeypotss. Um so we want something like a full system emulation. We want we want the whole thing to be running as it usually would would on the device. Um but we can't really emulate just like a specific device in full without a lot of time and effort. And the payoff might be kind of kind of low for how much time we'd have to put in. So um we wanted to see if there's another approach that we could try um and try out a few different things and we came to to this uh this kind of rehosting concept. Um the main idea of this is that is we don't touch user space at all. We don't want to do anything in user space. We don't want to LD preload anything. We don't want to do any hacks during during the boot process. Um we just want to just offer up a firmware image to the uh to the rehosting environment. Um, ideally the the exact same firmware image that's bundled in a firmware update image and and for it just to run. Um, user space can only kind of interact with what the kernel gives it. Um, and we the rehosting approach means we we can make loads of changes in the kernel and do as much as we want in the kernel, but we just um kind of create create an environment create this rehosting environment for it. Um so we the idea is we would build a kernel for a particular KU machine with a compatible architecture and then modify the kernel to create an environment which looks as close to a real device as possible from the perspective of user space. Uh and then we can just tweak the kernel, tweak the CU machine like add peripherals, add hardware whenever we want um or whenever we need to. Um the question is then like where do we start with something like this? Um this? Um all uh embedded lensing systems are running the Linux kernel but they're but they're running a Linux kernel built for a particular system on chip uh or sock. Um Um a sock is just a bundle of CPU, memory, other peripherals, things like that, networking, storage bits and pieces. It depends on the sock. Um some peripherals might be integrated directly into the sock. Um, others might have like a physical interface that the hardware devs can wire another chip to like flash memory or something like that. Um, but each sock is designed by a chip manufacturer to cater to a particular market. So a specific sock will get used in lots of different embedded systems sold by lots of different vendors. So Netgear, ASUS, TPLink, they they don't buy they don't build uh the sock themselves. They're not chip companies. they buy a sock from a manufacturer and then use them in their devices. Um and so the same sock is used across multiple devices by by multiple different vendors. vendors. Um so the supply chain looks like this. Um the sock manufacturer makes the socks. So like Broadcom, Qualcomm, MediaTek, all these kinds of companies. Vendors buy the socks from the manufacturer and the manufacturers give the vendors some kind of uh SDK they can build their own firmware with to run on that sock. uh the vendor will use that SDK to build the kernel and file system binaries. Um binaries. Um so it kind of means that any code running close to the hardware is usually manufacturer provided like vendors won't really have much to do with that. Um and the vendors can do whatever they want in user space. They just use the like APIs and libraries or whatever um provided by the manufacturer as part of as part of the um SDK as well. Well, what does that mean for us? Well, we we're only trying to make changes to the kernel. Uh so instead of emulating a specific device by like a specific vendor, we think about it like we're trying to emulate the kernel or like uh create a new kernel that uh for that looks a lot like one for a specific sock. Um we're not trying to emulate the hardware of the sock itself and try and run a pre-built kernel on it. Um we did try that. It's it's a it's a bit tricky. Um but so we want to just build a kernel and uh sort of tweak a KU machine if we need to so that from the perspective of user space um user space thinks it's running on that particular sock kernel combo. Um the benefit of this in theory is that is if we can do this then we should be able to take firmware from any device based on that sock and and run it. So we could build one rehosting environment maybe get a few dozen possible emulated devices out of it. We've got a lot of things we can kind of lean on to try and do this. We don't obviously have data sheets from uh the um manufacturers for these socks. We don't have kind of proprietary information. We do have um came machines which we which are probably good enough for getting started. We have the Linux kernel. We have firmware images for devices that we want to emulate. We have reverse engineering tools. Uh we have GPL code in a lot of cases. Um we have compiled kernels. We have kernel modules that come with the firmware images. We have things like OpenWRT um DD-WRT kind of open source distros um which uh might have releases to run uh which run on the sock we're trying to target. They kind of do the opposite of what we're trying to do. They're trying to run on the actual hardware and we're trying to not run on the actual hardware, but they're kind of similar. We have kind of similar but opposite intentions. intentions. We we don't have data sheets. We don't have really that much source code for binary blobs. Um, we don't have documentation or we we just don't have unlimited time and energy um or unlimited money. We're just trying to get to a point where the thing runs well enough. enough. So, this is this is the vague plan. We we start with uh running a mainline kernel similar to version uh a similar version to the one that actually runs on a normal device. Uh and then we compile that to run on the most compatible KU machine we can find. Um, and then on that machine we we load the file system and just try and run stuff and we see what breaks and we try and figure it out. Um, and then we patch the kernel again and maybe make modifications to the CIA machine and we just keep doing this in this kind of twisted development life cycle. Um we can during all this we can infer behavior from the user space from user space to kind of figure out what it's actually expecting and what it wants what it wants to see and what it's doing and then we can make changes reflect reflexively in the kernel to uh to to um cover that. So this was the device we chose to do this. It's an older ASUS router. Um it's based it's uh the AC87U. Uh it was released in 2014. It went end of life probably 2021. It's based around the BCM 479 which is a which is a Broadcom sock. Broadcom sock. Um the 479's used in quite a few routters. Uh it's a router sock with a with a Cortex A9 core. It's in the broader sort of BCM 4708 477 4708 479 family. Uh it has an integrated Ethernet stack. Um it was it was built for the routter market. So it gets used in like routters and extenders and things like that. The list up here is kind of non-exhaustive. It's not every router that that runs with this um that has this sock, but it's you know a a list. It gives a kind of indication of what different vendors which different vendors were using it and what kind of device it's being used on. Um so the the AC870 was kind of chosen at random. Um, the idea was we were we were kind of looking at ASUS stuff and and we wanted to just kind of see how easy it would be to do emulation for them. But the idea is that whatever we build for the 87U would would maybe work for other ASUS devices running on the same sock and and there's a lot of them. Um, I should also say we we didn't actually have the physical device for this at all. We we worked only with the firmware image. So there's also a bit of an artificial restriction. Um, but I I kind of just wanted to see if it could be done. There's there's a lot of common wisdom when it comes to uh embedded systems emulation that you need the physical device to do it um to like dump the NV RAM and get all these things out of it. But we our intention longer term is to emulate devices at scale. So we don't necessarily want to buy every single device that we want to emulate. So it's a bit of an experiment in this as well. as well. Um the first step for this um we can just get the firmware and we get what whatever's publicly available. Uh we can get the most recent firmware from the ASUS website uh from 2021. Get the GPL code which has some like Broadcom commented code in there as well. Um the firmware images in this TRX format which is like an older Broadcom firmware file format. We can unpack these like really easily with Unblo. Um and using Unblos also means we we can carve out the specific sections of the firmware and we don't have to do any like post-processing stuff like we'd have to do with binwalk. So you can carve everything out very precisely with unblub. Um once it's unpacked, so we get this uh Lisma compressed kernel and we get the squash fs file system. We can with this unpack firmware, we can kind of get a sense of what the device is actually running um what what it's running on um and so what we need to emulate. So we get the kernel version out, a string out of the kernel which is uh 2.6.36.4b. for BR CM ARM. Uh it's it's this Linux 2.6 with some with some broadcast modifications, I guess. Uh it's compiled with GCC 4.3 uh 4.5.3 from Build Routt 2020.2. Um so we know what it's compiled with as well. We can use the same compiler and just kind of keep things as similar as possible when we're when we're building our own kernel. Uh we can also get the boot args out of the kernel blob. So this can give us an idea of how it's going to boot. Um they might not be the actual boot args that the real device is using but but it's a decent uh way to start. Um so the root the root boot arg points to uh mtd block 2. So it's booting from this particular part in flash. Um the init boot points to sbin pre-init. Um if we look at the the envelope file system, we can see that pre-init is is a link to um a binary called RC. There's no in it scripts or anything like this. It's um just this RC binary being run as pre-init. Um a lot of other B binaries linked to RC. It's a pretty big sort of system functionality binary. Um and it checks how it's invoked. So it will run in this like pre-init mode when it's when it's um run as pre-init and or in another mode if it's run with a different name. The layout of the unpack file systems tells us a lot how the system is expected to run. um kind of directories we'd usually expect to see useful stuff in populated or actually just broken sim links to temp. So temp is probably populated at boot time probably by the init process probably by pre-init in particular. Um and so in order to get as when we're doing our emulation we want to get the system into a state using the normal init process that expecting to expecting to perform. um so that we can run all the other binaries and things on it and the system will be up kind of in a in a normal state. So we don't want to go through and kind of paper over these things and do the stuff in user space. We just want to make sure that the that the init processes is capable of kind of completing. So this is we can apply this same kind of life cycle that we had before to this setup. Um we know the architecture, kernel version, compiler, the way the file system is being loaded from memory uh from flash, sorry. Um so the first kind of naive plan which is definitely going to fail uh in the first instance is like build a machine with a cortex core sorry find one build a a 2.636.4 2.636.4 kernel for it. Um attach the squash FS image uh which we carved out earlier. um on some kind of emulated flash device and then hopefully the kernel will load the file system and then we can try and run stuff and see what breaks basically and then try and fix it and make it better. Um for this one in particular, so Ky has this V Express A9 board which has a Cortex A9 core uh and flash memory. Um it's nor flash not NAND which is not a huge problem but in some cases it can be a little bit of a problem but just for just for the start just for starting this is not a huge blocker. Um there's a dev config for the V Express machine in Linux 2.6.36.4. So there's and there's um kind of machine code for the V Express series uh like Mac V Express code in the kernel as well. So we can we can build the 2.6.36.4 for kernel using the V Express dev config using GCC 4.5.3 uh from build routt 2012. Um getting that old version of build of GCC compiled using with old build route can be a bit of a hassle. So I'm not going to go into here but it just involves using like old Ubuntu machines and kind of commenting out bits of code that don't work every now and again. But it but it does get it does get built uh and it does work. Um once the kernel is compiled and running uh running uh it kind of works pretty well. It the the kernel loads it loads into um uh into the the V Express machine. Um you do need to edit the kernel config a little bit. You have to make sure that you enable things like squash fs and mtd and the kind of stuff that you want to have in your um rehosting kernel. Um, you also need to kind of think about things that you might want to compile into the kernel because we're we're not going to be able to load anything later. Like user space is not going to be able to load any kernel modules. So, we just want to in this kernel, we can kind of just build everything that we might potentially need into it as long as it's not incompatible with it with each other. And um it it kind of uh makes life a little bit easier later on. Um the first thing we have to do is is figure out how to present the file system to the kernel. Um, so we need to forge some kind of flash. Um, the flash needs to be laid out in like a super predictable predictable way. Um, but because we control the boot and the kernel, we can pass like u the mtd parts boot arg to uh the kernel which will tell which will tell it what our um what our flash layout is and where to look for certain things. So our we can still use mtd block 2 as the place we're going to keep our file system uh and just pass pass it in the mtd parts um boot arg to tell the kernel that that's where it should boot from. So the flash layout can look something like this. Um we have a couple of like imaginary empty parts then the squash fs file system. One of the quirks of the express um boarding came is it needs the file to be passed the the file that's passed as as flash to be exactly 64 megabytes. Um, but yeah, we can just we can just put what we want in the place we want it and just pad the rest of the file out. We compile the kernel, construct a K command uh that looks a lot like the one up there and um the file system doesn't unpack at all. Um, but we do get a relatively decent error. Uh, the one is it tells us it's failing because there something to do with the compression of the squash FS file system. So, it's figured out it's squash FS, but it can't do anything about it because it it know it's figured out it uses some kind of unknown compression. unknown compression. And the main problem is that uh the Squash FS image that ships for the AC87U is compressed with uh XZ. Um there's no native X compression support for the Squash FS for Squash FS in the mainline kernel until 2.6.38. Uh and we're on 2.6.36.4. So, Broadcom obviously does something a bit different. Um, and we can figure out what they're doing by looking at some of the GPL code. Um, the GPL code we got from the ASA site for this device has some squash FS related code that includes some XZ support. So, it's a slightly modified version um kind of backported XZ support. Um, and similarly, we can just backport XZ support from uh 2.6.38 into our own build or just use the code from this uh GPL release. Um, and it works. The system unpacks, uh, the file system unpacks. Um, but it's a it's a it's a bit of an indication of the kinds of, uh, tweaks we just make we have to make to the kernel as we're going along trying to do this kind of emulation effort. effort. Um, the first thing we do is just pass BNSH as the as the init um, command. Um, and we can just drop into a shell and poke around a bit. This is a bit like the situation we'd be in if we were if we were doing the ch route kind of user space approach but like of course nothing actually interesting runs because the system hasn't been initialized. The file system is mounted but but nothing's actually run. Um we we know that pre-init is the is responsible for the for the initialization. So um let's have a little quick look at it. So you can see here um a lot of what the pre-init code does is create directories, set up sim links, copy files around into expected places, mounts pseudo file systems, normal init process stuff. Um so yeah, in theory, if we can get pre-init like running and completing, it will be a big step into into being able to emulate the whole system. There's another notable thing about pre-init just from some static analysis which is that it contains routines which only work only run when it's running with um PID one. So we actually need it to be the init process in order for it to do stuff like it's a kind of orchestrating um process in the system in this kind of holistic architecture of the system. Um so we can't just invoke it from a shell and and expect it to work. um we can just start the thing again with the init boot ug um pointing to pre-init and see what see what fails basically I mean the big one is we get a whole load of MV RAM errors um this is definitely expected nvm is the most common blocker in in embedded Linux uh emulation um so we need something to work to take its place place Um we we kind of need to make a plan here. Um we're not touching user space. We just need to change things in the kernel um to create a device dev and vramm basically which would behave just like the dev nvm device would on the AC87U. Um we can get a decent idea of what uh how devs are actually interacted with by looking at the un user space binaries in the firmware image itself. So, BRCOM provides these like um these APIs to interact with MV RAM. Um in particular, this this uh lib RAM um so which is the kind of the official Broadcom Libra. Um on the 87U um user space binaries will uh import their lib the broadcom lib nvm and call functions like nvm get nvm set nvm commit. um we can have a look at this at this uh library and see what it's doing under the hood when it's doing these kinds of things and hopefully um yeah this can inform what we're going to do. H H so yeah looking at live MV RAM uh it opens uh it has a function called nvm in it which opens dev env stores the handle to it um at this global memory location and then nvm get will call um read on the open file handle with a pointer to a string of the key that wants to be read and another buffer where the value should be written. uh nvm set cause write uh on the open file handle with a pointer to the key equals value string. So they it's using really basic CIS calls that we can handle when we make our own kernel device that does something like this. Um so yeah we build a character device into kernel. Um in lib nvm the whole nvm buffer is expected to be a contiguous block of key equals value sprit string separated with by null bytes. So we can kind of just mirror what that is. Um we do need somewhere to persist it. So we can back it with with just another partition in the flash in the flash memory. Um we make sure that dev nvm the file is created so user space can call open on it and then we just handle all the read write uh IO operations as well. Um when we're doing that we we just look at what the Broadcom Lib MV RAM is expecting to receive and we just return what it wants. We're we're just kind of servants to the user space in that way. Um and this is a kind of a rough idea of the kind of code we have in the kernel. I'm sorry you probably can't see it here. Um, but Libby M is just doing super basic reads and writes. Um, we also handle calls to like NVAM commit which is an IIO tool uh with a specific command number and we just write the buffer back to the flash when we get that. Um, and then there's calls like nvm get all which is just like nvm get but passes null to the second read and if if we get that we return all the mv key value pairs. So we, this is relatively, I have to say, quick to write. Um, we can write it, compile it, try it, squash bugs, compile it. It's just kind of development life cycle stuff. And it just kind the the approach just kind of works. We can test it with the command line NVAM tool that's in the uh ASUS firmware and get all the data out of the sort of fake NVAM partition. Um but with this approach we just get a systemwide uh NVRAM device that has persistent backing and works in the way that um that the uh user space is expecting. And so it'll probably work on other similar era devices that use the same mechanism as well. Alongside building this we're also trying to emulate the things. So we have to solve the problem of what goes into the MV RAM. Um, as I said earlier, the common wisdom is that we often need to be able to dump NVAM from an actual device in order to do anything with it. This isn't actually the case on the AC87U uh or any of the other ASUS devices. There's a lot of logic which populates NVAM with default values. Um, and there's certain like uh NV RAM um keys that you can set that will trigger a kind of full NVAM reset. Um, so there's no need to have an actual physical device with these devices and and we can kind of freestyle based off the defaults that Priet creates. Um, and yeah, once we have NV RAM, PNET kind of works a bit better and gets a bit further, but we start hitting other issues that we we weren't necessarily expecting. So Pinet needs to be there to be more than one network interface. It's looking for F1, but it can't find it. um we can't just add another interface without tweaking the the V Express machine and the and the kernel um MAC V Express a little bit. Um but we can quite easily do it. Um we can just map a new Ethernet interface into memory in the KU code and in the Linux code we just add another like IRQ for the for the M in the Mac V Express broad files which lines up with the IRQ value in the KU code. Um it's a it's a relatively light light touch um uh strategy, but yeah. yeah. Um it's pretty it's a little bit fiddly. It's but it's not a huge amount to change, but it and it just kind of works. So um in our in our in our uh examples here, I just kind of copied the V Express machine, just created a new machine called BCM479 with modifications. modifications. Um Um and uh yeah, so it just means we can add like pass another net flag to ku and we just get another like a second network interface showing up in the guest. We can do this as many times as we want. Um but like two or three is is probably the limit for what you need. Um so pre is happy it's got two nicks. We get different errors. So we we start to see more errors relate to the JFS2 directory. This doesn't seem to exist. Uh there's no JFS2 partition on the firmware image. Um we can base it off. though of the of the firmware image downloaded from the ASUS site. So assumably there's something that's created or mounted at boot. Um to figure out what it is, we just have to go back to to pre-init and see what's going on. Um pinit tries to mount certain partitions from the flash to the JFS and ASUS JFS directories. Um it checks the procmtd uh file to find partitions with names BRCM, NAND and ASUS and then it just mounts those partitions as JFFS to those particular um directories mentioned earlier. And we can fix this just by modifying our flash um again and just changing the boot args again. Um we modify the flash layout. We make sure we tell the kernel which parts of the flash are called BRCM land and ASUS. And that means when we boot the MTD file will will contain those names the pre-init's expecting and pre-init will try to load those particular MTD parts as JFFS file system and and this just works. So the JFS directories are created the files are getting written there things like log files sis log certificates but it makes booting a lot smoother um and a lot less stuff fails. Um we're pretty far into the into the init process but we get a lot of errors out on the terminal. They're not like functionality blockers, but they kind of pollute the terminal a lot. Um, in particular, we get a lot of like dev GPIO related errors. Um, so to quieten down the machine, we we also create like a dummy GPIO interface, a bit like the MV RAM one. Um, a lot like the MV RAM approach, we look at user space binaries for GPIO interactions and see what's going on. the the binaries always just call um make an ioctal call on the open dev GPIO um file with just specific operation codes and like a bit map of the pin that they're trying to read or or write to being pulled or or poked basically. Um with GPIO we need to know what the active low or the active high state is for each. So luckily there's some device tree files we can look at to reference that and then we can use that to determine which bins are expecting what and we just return a kind of neutral state back on them. Um so it's a little bit trial and error but it's not a huge overhead and yeah once we once we once we get rid of the GPIO errors we get a few more kind of network interface is issues. Um we gave the system a couple of nicks but the driver for the nick that CU emulates doesn't actually behave how user space is emulating. Um so the CU machine is expecting the SMSC 911X and the firmware is expecting some Broadcom device um of some kind. So the specific point uh in Pinet that's actually failing is a function called get FI status which is checking the physical state of the interface by trying to read a register somewhere. Um, it does this again by making an ioctal call to a socket handle. Um, but it just fails because the the driver code for the SMSC 911X doesn't know what to do with it with that particular code. Um, this isn't really a huge blocker because we don't need the solution to be kind of perfect or complete. We're not trying to implement networking here, but we just do a little bit of fraud in the kernel. um we just we modify the SMSC 911X driver to intercept that particular operation code and then we just return whatever user space is expecting again um just to say yeah it's physically up um we just we just spam all the all the bits back like hi for this one in particular particular um and yeah at that point we're doing pretty well uh so we've got a system with a specific flash layout fake MVAM device fake GPIO slightly modified network driver and um it it mainly works. The main thing we need to the final thing we kind of need to do to get it working to be able to connect into it is to um have a kind of LAN interface attachment and this is a a solution that we found for this. So um on the on the A7U pre and it creates this like bridge device uh which get which takes the kind of uh the LAN IP address which is 192.1681.1 um F1 uh is one of the land LAN interfaces for that. So we can try to connect to whatever's on BR0 through F1. And the cleanest way that we found to do this was to kind of create a tap on the host with a static IP in the same /24 range as the router and just make sure KU knows how to use it. So once you have that, you can connect to the LAN interface of the device just like we would on the device itself. Um, Um, and yeah, demo time. Let me just try and get these demos these demo files up. Oh, hold on. Yeah. So, this is running the ASUS ASUS uh router here in the emulated one just on the right top right. Uh it takes a little minute to warm up, so the video is a little bit long, so apologies for that. that. But you should see there I'm dropped into the a shell on the kind of emulated um um on the emulated uh AC87U there and I've just sshed into it in the bottom bottom tunnel there. You can see all the processes are running in that um a lot of them. So assumably the the init process kind of worked. It got the system into a decent enough state to run stuff. We've got a few um network interfaces there as well. Um and we've got a whole bunch of services running and a whole bunch of sockets open. The interesting one there I guess is the um uh web management interface. A lot of the time this is the kind of high value one. So I hope this is the right video and shows us uh the web management interface loading up. It sometimes takes a little bit to to to get started, but yeah, here we go. So, we got the web management interface started. Um, we can log into it. This one in particular had already been set up. Like I the setup process is a bit boring, so I didn't kind of I didn't uh keep that as a video, but it's already been set up. And the web manager interface works almost almost entirely in this particular one. Um and yeah that's it. So yeah it all the interesting services kind of work including the web web management stuff. You can configure SSH to run on boot. You can change the NV RAM and you can do these things on the flash file itself or you can do it from inside the device itself. uh inside the emulator device itself. Um in this particular one there's in the AC87U there's a strange kind of parallel quantenna sock on there. It's expected to be on there which we didn't emulate but um but yeah no we get a pretty decent like 95% functioning like emulation for this device with with not a huge amount of um layout on it and we can do it for other devices as well. So other other Asus devices running the BCM 4709 work basically immediately. Um Netgear devices are quite quick to get running. They're expecting another um specific device to exist but as long as it exists in most cases it just kind of works. tender devices work as well. Um, but they use a different kind of NVRAM. Um, they handle all their own readrs. Um, and their their NVRAM is called like CFM and it's just a a partition in in flash as well. Uh, which just has like a CRC32 at the at the beginning, but other than that it's just it's essentially just um key store um key value store. Some devices are a bit more tricky. Um I guess partially because of the way that um the images are distributed. So we might not have everything tied up with a nice bow. Um others expect like a really precise flash layout. Um so it's more precision than just building a fake flash blob. Um and there's always like a little ramp up period in like going from like a blank NV RAM to a running system. So there's a little bit of like uh the process of what breaks and why going on. But in general, the baseline principles are pretty much the same when you're doing all of them. Um for a lot of these getting what getting devices from one particular vendor up and running might take a day. Um and once you've got like one vendor kind of covered, it can maybe take an hour or so to get another device in that family up and running. Um, and I'll show you a few more if they if they work. Um, yeah, this is a uh a DLink device, same configuration, same setup, also um uh also just the exact same emulation environment, just a different firmware, just a different image being passed um passed to it. Uh you can see web management stuff is running in theory. So we can have a look at that. And yeah, there we go. There's the DLink interface there. Um we can poke around poke around in that and have a lot of fun with that. Um another one. Now this is the Netgear. Again, with the Netgear one, we can just I I think I sped up this one so I didn't we didn't have to wait around for the whole thing to warm up. But um once it's configured and running, it's there's a few limitations like for the Netgear one, it's got this ping that it's trying to do before it will load the uh the web interface, but other than that, most of it um works here as well. Um which is quite gratifying. And then finally, there's there's an just one more example. There's a a Chinese vendor called FCOM that I have um have never really heard of before, but um we got one of those devices uh emulating as well uh just cuz it's based on the same sock. So we have all of these all of these uh all of this potential uh based on this one emulation environment. Um, so yeah, conclusions. It's still early days proof of concept, but it but it works is relatively light touch. Um, touch. Um, compiling support with these dummy peripherals is pretty smooth. It's it's not totally foolproof and not everything's going to be working the whole time. Um, but it but it just to get things running, it does seem to work. We can tweak Camu machines when we need to. We can make changes when we need to. Um we can take these same principles, we can move them to other um other socks as well. Uh we've started porting the same principles to other socks with some success. Um different socks have different profiles. The results may vary. Um vary. Um it can take a bit of effort in the first instance to create these environments, but like the return on investment can be quite high because you've got maybe tens or hundred or so like different devices you can emulate based off them. Um thanks for listening. I'll stick around for uh chat down here afterwards if anyone wants to have a chat. Uh feel free to get in touch on my massively neglected blue sky account if you got any questions. Thanks.
