hi everyone there is more people that I expect at 10: a.m. thank you very much for being for being here um you are the ones who survived until the last day in Defcon awesome welcome to taking off the blindfold detecting persistent threats on uh drch Edge devices we are part of the farad research team my partner here is octav kmo I am as Nares and uh we have some experience uh we working with firmware we have foundation on computer science and we like to play CDF as a resum we we like to break things to and do research so what we are going to see today first we are going to see a motivation for This research and this talk uh we are going to see some reverse engineer on trch fmw we are going to talk about vulnerabilities and attack vectors we are going to see mitigation strategies and at least we're going to see some conclusions and takeaways okay how this started we had a call of a client saying that was compromised H and when we came to the scene we see a lot of outdate outdated outdated Dr te equipment in the edge of of the network most of them are were so models that runs our RT and we start making some analysis we found uh previous vulnerabilities uh we with proven exploits on highend models of the Dr de routers that runs Linux and we start making some questions about it uh like are these vulnerabilities exploitable on the so rotors that there is other vulnerabilities that that are known and the principal question was where this router the entry point of the attackers okay we know that there is an increasing thread of advanced attack targeting Edge devices we we could name some examples like the S rat that was a sophical malare targeting s routers we also call name the hiat r that was a malware targeting right the rors but the the highend models and at least we could name Bening stin that was an NSA tool targeting Cisco routers that was leaked by the shadow Brokers group okay group okay so what what is the attack surface of these routers we found over to uh 5,00 exposed device devices mostly in the UK and we found a lot of vulnerabilities uh from an information disclosure to an an authenticated remote code execution so there is a lot of stuff okay so we had a bigor 2925 in our office running u ms 32 RT and we wanted to start making uh some research on that but we have a problem uh the code is close source so first we had to reverse engineer the firmware format we have to extract the firmwares understand previous vulnerabilities and and then look for new ones and finally determine if this was the attack vector or not so the first thing we did was trying to exract this firware using bwalk but it failed it produced no results so we tried to look for some specialized tools and we found this one rights so this didn't work with new fror the last commit was 12 years ago and that's why so luckily for us we found a talk by Philip lorett at hexagon 2022 where he described that by emulating DX withot loader we can extract we can make it extract the RTO esaral the disadvantage AG of this approach is that this is a manual process and you have to trick the addes for each FW version so and the other problem that it has is that these fare images have other executable components apart from the RTO kernel and file systems that you cannot extract this way so our approach was to reverse engineer the format and to try to write a tool that can extract all firware versions from different models extracting all file systems and excting all the executable components we name our tool wror Arsenal it's a collection of tools we wrote this in Python it's open source you can check it out at that repository and the idea is to support for all these new F versions and we will be showing along this presentations all the features that we implemented so let's talk about the fmer format it has two main sections one is called the binary section and the other one the web section both start with a header that contains the size and the pointer to the following section but in the first one we also have information about the firware version and some met metadata so the web section has a file system that is compressed and contains all the static files that the router serves for the web UI in the first section the bin section we have the boot loader that is row Ms 32 code this is the part that Philip emulated and then this is followed by the compressed rtos kernel and after that we have a very interesting part of firware that is the dlm code this is also a file system but is compressed and encrypted and we will talk a lot about about about it so to work with this we made a specification of the structure firmware using Kai TRS and the advantage of using this is that you can manipulate the firmware images with any language that you want from this list we use Python but you you can find this in the repo and use the language that you prefer and well by knowing all this we started to tackle the reverse engineering of the compression algorithm so during boot the rtos is decompressed to the same memory space as a boot loader and we did this following the steps described by Philip in his talk but when we started reverse engineering this we mapped all this in memory the boot loader and then the rtos after that and that allow us to discover that the decompression function is used during runtime to decompress other things for example the F systems and we also discovered some interesting strings like the one that you can see down to the right that says lc4 the compression failed but if it was lc4 the compression algorithm why we couldn't start it with in and the reason for this is that while they use a standard lc4 compression h block format they they read they have written a custom frame format so it's just a custom magic uh that is folded by sucession of block size and block data multiple times so we roted the compressor and a compressor for that format and then we moved on to the most interesting part that is the DM section so it turns out that the DM DM St stands for dynamic kernel modules so this real time operating system has the ability to load code modules during runtime dynamically and this is the this feature works independently from the firmware so dur in boot we can see that some um the modules that come with the F image are loading are loaded there are free modules and they are called br9 goip br9 app e we will talk a lot about about this one and B9 SSH so the first one contains information about the geolocation of ips the second one is used to block traffic from the users that are are in the Lan and the last one contains the implementation of s SSH server and this is really interesting because this implementation is separate from the firmware so these modules can be updated during runtime and if you install a a new version of app enforcement module or SSH these are loaded first and prevent the the modules that come with the firmware to be loaded you can see down to the right that for example in this case the br9 app e module was loaded first and then the version that that when the router try to tries to load the version that comes in the fmer image it will will fail so doing some more reverse engineering we found out that the modules are compressed using the same algorithm that uh the that is used for the the rtos but they are also encrypted so we recognized that the algorithm used was a modified version of XDA and this is a simetric encryption algorithm and what this means is that the keys for the crypting these dlms are present in the frameware so we look for them and we wrote a decryptor and encryptor for the modules so if you take a DM you decrypt it using this and you decompress it using the modified version of lc4 then what you get is a relocatable elf and this is very interesting because all the teral functions that this elf uses uh they have to have their symbols names so they are really easy to reverse engineer and this also means that the firmware must have an elf loader so we looked for that code and we found that there is also a symbol table this symbol table was described before by Philip lorett but only as an aid for reverse engineer because reverse engineering because imagine that you have a table where you have a function names followed by function pointers but now we know that this this table is actually used by the F loader to link these modules to the the kernel so and what this what the advantage that this give us to gives to us is that if we manage to write our own modules we can use all these functions without knowing their actual addresses so we implemented and all all these in our our tool we have two two functions one called pass frameware that will print a lot of information about the firmware image and there are one called stract that will stract all these pieces of of the image it will decrypt and decompress all the modules it will extract all the file systems for the web uh server and it will generate a binary image that contains the boot loader followed by the RTO scel but compressed so if you want to start reverse engineering this you must know the loading address uh and what we realize is that all right loaders start by calling the decompression function so this function is always located near the start of the image and since all this has to be loaded aligned to the page then we can search for the for the address of this compression function and if we remove the last three numbers what we get is the address the loading address of the whole image so with that we you can start your reverse engineering process engineering process we also implemented this in a functionality called f loading address and when we had all that we move on to H try to find some vulnerabilities and attack vectors okay so let's talk something about vulnerabilities we are going to show some of them uh starting with the B Basics we found that found that they had an insecure password storage that mean they store the passwords and the credentials and plain text in memory so if someone had access to the memory they could read the the credentials or maybe chain another exploit to to leak them we also found a non-constant time comparation uh on the credential that uh so they use a function that takes different amount amount of time to compare different passwords depending on how much uh correct characters are in the in the string and that leads to a side attack Channel a timing attack okay so uh drag de implements a second step authentication that's really nice but that was predictable because the code generation was done using a s Rand function but with with a the seed uh they use the time La lapset science boot so if you know for how long the router is running you could H predict the second Factor authentication codes we also found a n the reference in the hcp server uh that leads to a den of service attack but interesting of that is that it was exposed to one so if if the router was exposed to internet you could trigger that another reference and if we combine both vulnerabilities we could predict the Second Step authentication codes because we could make the router crash and then we know the the time lapses sence Boot and we predict the codes and that that's what what we are going to see in the next demo we could see on the right the serial communication with the router um on the no that that's on the left on the right top is a script that we run it triggers the the N the reference and the the router is reboting it will take like two or three reboots because it crash hard and on the right bottom we have the web UI when where we are going to try to log in so now it boots we are going to wait until the router initilize itself okay and now we are trying some different codes and we were able to predict the code and we are loged in okay so Octavio talk something about the AP enforcement signature feature is really great if you have a Enterprise and you want to to block to your employees some web or I don't know you will update update the signatures in order to BL block the traffic and how this process is made the there are two ways the first one is the manual we will see on the right bottom uh bottom uh that says import if we click there we could upload any file with some checks and the second way is the automatic automatic way uh in which we have to select the update server uh or say left the default one and we have to schedule the updates so for example every day at some point of the day it will check if there is some update for the signatures signatures and how they implement this uh the signatures you you could think on Json formats or jaml or something like that but DRC has the idea to imp this as a modu so to update the signatures they upload an L file with code and everything okay let's talk about some vulnerabilities related to to the modules the first one is they not also update certificates with code but they don't check for certificates or have SSL enforcement so if someone manipulates the the configuration of the r router or the DNS maybe a DNS hijacking they could upload a module to all the D routers that has this feature and we have found two two post authentication end points the first one is the one that I told in the last slide and the second one is that we found an endpoint to upload configurations f that doesn't have some checks so they allow us to upload an arbitary dlm now it's not only the API module but we could upload any modules that we want maybe we could I don't know upload another SSH module so we had three ways for an attacker to install malicious modules the first one is the post authentication endpoints that we talk just just in the last slide we have the model upload process that is hijacking the configuration or the DNS and the last one is the supply chain because the modules are very easy to craft so if they are easily abended to to the firmware and anyone with physical access or I don't know exploiting some vulnerability they could uh uh upload the module before selling it okay so now we know all of this we could start making some modules we implemented two commments the first one is the MS compile that is going to make a a a compilation of a source code into a relocatable relocatable elf of the architecture of Ms 32 and we also have another command that is up to DM as the name said it will convert an elf to a real LM compressing and encryp it with the provided keys so I we and we could appen some web headers so we could upload them to through the web web UI so we are going to see a demo we could see on the left H the version of the AP the AP module on the right in the middle we are going to uh run a server a custom server that is pointing the configuration of the auto updates so on the top right we could see the the serial interface with the router okay so once the server is up we are going to send we are going to trigger a new dlm so it's trigger we could see the the router has fetched the new module in our server we could see the far logo because it's our module and if we log in again we could see that the the version of the module has changed and we have some logs that said that it don't don't Lo this okay let's make something more funnier uh we are going to make a SSH back door why not because we have the the module so the original behavior of the SSH module is the top one when we have a new connection it's going to call the authentication function and the authentication function will set that it fails or it authenticates but what we are going to do is when a new connection enters uh when a new connection is open that is going to call our hook and it's going to check our credentials so if it they are right it authenticates if not it's going to call the original function because we don't want to change all the behavior of the module how is the road map for this first we have to do a reverse engineer of the SSH module to find that function that makes the authentication we are going to skip SK that part and we are going to say that the target function is the SSH account and El up out function after that we have to write and compile the hook we have to merh uh the hook with the original module and we have to modify the all the relocations in the entry table and at last we have to upload the dlm okay so on the right we could see the source code of our of our hook function that takes the same arguments as the target function uh an SS instance and a and a packet uh and it will check for our credentials if they are right it will return one that says it authenticates and if not it will call the target function U okay in order to compile this we use our command Ms compile and then we have to merge both binaries both relocatable elves as so said a relocatable elf uh have all the symbols in with no fixed address so they had a relocation table that will set every everything that use a symbol has to be modified has to yeah modify to to point to that symbol in the after after linking so we have all the symbols there and once we merge it we have the symbols of both modules uh we wrote a a common for for to do this that is with the name of Ms merch that you that takes two files and as an output has one file okay now we have to modify the relocations table so first we found we find all the relocation that uh talks about the target function we could see on the top right the two two examples we are going to use the first one if one if we uh print the code that is on on that address on the bottom left we will see that the jum is a jump instruction but the jump instruction only has his up code but doesn't have an address where to jump that is because the relocation entry will set which one is going to be that address on link in time and we will see there that is the target function so we modify that entry to uh give us a result the result the right that points to the to the hug function so time to hack okay so on the top right we have we are trying to log in with our credentials but we could see that it fails so on the bottom left we upload our back door Ed module and one once it's uploaded we could see that the file was was written successfully but we have to reboot the router and once it reboots we are going to try again with our credentials we could see that the modu was loaded okay so we try to log in and we will see that we we're are in and we could see some logs in the serial communication so this attack that that got Stone showed is is really dangerous imagine that an attacker can craft a modified SSH module upload it to the to your right dute router and you have no way of detecting this moreover if you up update the firmware the module will be still there it will persist and even will persist if you reset the configuration of the router because it's stor in a separate f file system in the flash so we started to think about some mitigation mitigation strategies the first one the obvious that we thought about was just to remove the feature because who wants to have Dynamic modules on the router but there might be some users that use this feature so well instead of knopping all the calls to module loading during Boot and during the up e update process we decided to think for a better solution and solution and we we devised a mechanism for checking in memory Integrity of the other modules that are loaded these mechanisms should fetch signatures from a server because the modules will get updates so you have to update the signatures but this time you'll have to check the certificates so once you got those signatures you have to find the loaded modules on memory uh calculate the actual signatures and compare them and if there is a mismatch then you have to notify the user somehow and all this functionality should be included with the firware so what we did as a proof of of concept uh was to use the up and sourcing module to introduce this code to the router because it's much easier to write a module than to patch the firmware because you have all these functions from the from the symbol table that you can use so uh one there are two things that we have to solve the first one is how to calculate a signature for a valid valid D dlm and the second one is to find the actual dlms in memory and do the comparison so we focus in that and we and we accepted some limitations we will not check the signatures every time a module is loaded as it should be we will only check signatures when our defensive module is loaded and we will not uh try to notify users for now so the first problem that we have to solve is how to calculate a hash or a signature for dlm we'll hash each signature um with MD because md5 was available on this table on this symbol table and we will hash only the static sections because the dynamic sections will change once loaded if the static section contains data we will hash it right away but if it contains code then as Caston explained during the module installation when it's linked all the jums and the function calls will be will get the actual addresses of the functions written to them so we will hash only the up codes for these sections and the up codes in Ms are the first six first six bits so once we got that we have to find all the modules that are loaded when the elf loader H installs a module it creates an object to represent the module and the space for this module is allocated using the linear allocator of the router so this allocator maintains a list of all the chunks that it has assigned and if we manage to find this object that represented dlm we'll get the dlm name a pointer to the text section of that module and a pointer to the main section that will get executed after the module is loaded loaded so the loader copies all the sections in a predefined order one after the other after the text section this order is usually defined by the compiler and we will assume that the this order this order doesn't order doesn't change so on the bottom you can see a little bit of the code of this defensive module the part where where we Traverse the the list of chunks from this allocator and we will check if this if a chunk if each chunk corresponds to the mod object if the CH corresponds to the module object we will print some information and if this module happens to be the SSH one then we will calculate the hashes and compare them so here's the demonstration this router is the one that has been back door and has a modified version of the SSH then we upload our defensive module using the oi on the left and you can see right there that once once it's uploaded uh it will try to find all the modules printing their information and when it reaches the SSH module it will start comparing the hashes for each section and it will find that the there there is a mismatch between the hashes on the second section so to wrap this up some conclusions and conclusions and takeaways all this relability will were found by us analyzing only the biger 2925 router but they are present in several models because all the Soho routers use this realtime operating system and even some of the of the higher end routers and run Linux emulate this the same realtime operating system so we found all these vulnerabilities and some more that we didn't show and they are all patched by now but we don't have CVA IDs uh yet and while some conclusions on on the offensive side the code for this realtime operating system is huge is really complex and we only analyzed a little bit of it imagine that the functions that are present in the symbol table are maybe not up to 5% of all the functions in this firmware so there are probably more vulnerabilities here to be found and that is why we are open sourcing our tool and we would love for you to try try it and we would love to know if it's useful and if you find something so there's a there's a repo story if you want to check it out and now some conclusions on the defensive defensive side we show that it's possible to create a defensive module to check the Integrity of other modules in memory but in an ideal situation this feature should be implemented by the vendor it should fetch this updated signatures from a trusted server verifying the certificates and for all this to work if this feature is included in the firmware also we must have a secure board chain to to trust that this is working right and to close some takeaways we think that close source firware as this example is just another case of security by by security all these vulnerabilities that we have found are really simple and they will they would have been reported sooner by the community if it were easier to engineer this these routers so some things as plain T text password storage the use of insecure functions to compare sensit data predictable random number generation and even the lack of SSL certificate validation so given all the the increase in attacks that we are seeing to Edge devices it would be really important for vendors to include uh features that facilitate observability vulnerability research by the community and to implement threed detection Solutions so this is it thank you for your time first for coming so early and if you have any questions please ask us there's a mic here if you want to ask F question get a sh well thank you guys yeah okay the question was if there is memory protection between the modules and the rtos and there's not all this runs on the same memory space so from a module you could modify the rtos code and vice versa yeah well for coming
