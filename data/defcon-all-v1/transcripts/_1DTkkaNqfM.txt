and with no further Ado please continue hello everyone and welcome to my talk Clash burn and Export manipulate filters to P Kel CF I'm hexit from Taiwan currently working as a security researcher at def cor I specialize in B export especially in lens kental exploitation I found some vulnerabilities in ly internal component like a urine kmbd NS tables and others today I will start from introducing Kel CTF and NF tables after that we will dive into the NS table internals and talk about three V abilities we discovered in NS T tables and in between these three viabilities I will also share some story behind the scene during my current CTF Journey so let's talk about first talk about the current CTF current CTF is part of Google vrp and the way it works is quite similar to a CTF challenge for about every two weeks there'll be new kol version released and attendee will be given an improved show and you will need to exploit it exploit the L kernel to get flag and submit it to Google to ear the Bounty there are three instance can be P LTS and cosos both use newer and quite similar chal conf while the mitigation one use an older Kel config with custom security patch to accept more one day exploit but the most important thing is Google offers pretty big bonti big bonti word with some simple Mass calculation we can quickly find for zero day LTS export the Bounty can easily surpass $50,000 us and with the same varability you can earn both 21,000 Max maximum in both mitigation and C instance which is quite appealing for the Bounty Hunter like me so why am I targeting NF tables first NF table config is enabled in Kern CTF and as you can see on the right there are multiple probabilities discovered just after Kos is announced so for Kos new being like me I think it's the right place to place to start okay so First We Take a brief brief intro to NF tables what exactly is NF tables NF table is a packet filtering framework inside Linux kernel it's used to replace the Legacy IP RP EB tables merge them into a single entry point it's use it use a simple VM design to implement its functionality and support new syntax for CI interface as you can see on the right instead of doing one by one new syntax can now allow config multiple things or at once and this capabilities come from its design which we will revisit later unless table is constructed by a Tre like structure it contains five core modules table chain rule expression and set table is the top level container for the below structures it's used for the management management purpose um each table will only belong to a specific n family and any object contained by the table will follow the same same family Chen in and tables will hook at predefined hooking Point provided by net filter infrastructure to do the packet filtering when any packet flows through the hooking point the chain will execute the expressions in the rule it managed to accept or to block or to modify the packets so what is the rule and what is the the expression as I mentioned earlier UNF table Implement its functionality by a simple virtual machine so you can think a rule as a small function that can contain multiple expressions and the expression is the instruction for NF tables there are different types of Expressions some can be used to manipulate VN registers and the others can be used to load or uh to read or WR data thing data thing packets and th set is used to store a group of some type like IP address or port or any binary data it can also be used as a map to map Some Type A to another type B and these data store in the set can be CED by the Expressions during packet filtering okay so after having some basic understanding of under tables I will now introduce the first varability I I discovered at the time I'm not so familiar with NF tables however I spotted something weird in the code base these three functions are all used to delete objects in UNF tables the NF table delete Ru deactivate will be C during rule during rule deletion nft flash will be C during full config config flush and nft set Elm catch all deactivate will be called during set element element deletion however the pattern here is a bit bit different the first to use is NFD is active next to check if the object is active but the last one NFD set element catch all deactivate use NF is active to check so so what's the difference actually this two function is used to used for object lifetime Management in N NF tables in order to understand how it works we will need to first talk about the best request mechanism for the for the query request for example the G rule operation above nink API will internally use nil link receip skp to get the ru information lessly however if it's some operation that will modify the control plan like new table or delete chain NF tables will use NF net link receip batch instead of the lockless one to serve the request the difference between them is that batch one can process a request which contains multiple operations and they will be processed sequentially and also because of this design the COI interface can conf multiple things or at once so um it sounds great but the thing become more complicated when any operation failed in the middle of a battery Quest and what NF tables do is to split battery Quest handle in into multiple multiple phases there are three phases during the handling of of the battery request prepare phase commit phase and the B phase during the prepare phase if every operation finished successfully in the battery Quest it will go to the commit phase and commit the changes however if something went wrong after every operation is processed it will go to a Bo phase and revert all the changes here's the lifetime management comes comes in any object in unet table has two State variable current generation and Next Generation only the OB object activated in current generation can affect the wrong time behavior and you can see the Next Generation as a temporary State for the prepare phase every operation modify the control plan like new or delete operations but only make changes on the Next Generation state in the prepare phase so without affecting the current generation State then during the commit phase it will commit the modification so the current generation will be finally be set as the Next Generation and the Next Generation state will be reset to a clean clean state but in the case if something went wrong during a both phase it only needs to clean the Next Generation State let's go go through some examples to know how it works after a b request is received it goes into the prepare phas the first operation is delete set C okay so the set c's next Generation state will be set to deactivate it next it continues to Pro to process a SSD SSD operation so the set D will be added into H table with Next Generation in activ ated stage if nothing went wrong we will go to the commit phase and at this phase current generation will be set as the Next Next Generation so the setd will be available in both run time and query operations and also the set will be free if something went wrong we will enter the Abol phase and at this phase all operation is done in the prepare phas will be reverted in a reverse order so the SD will be undo and also the D will be undo so okay back to the check so what's the problem here as we can see during the NF set element catch or activate it used NF is active to check if the element is still alive however NF is active checks current generation but the function actually runs in a prefer a which will only modify the Next Generation state of an object so the check here the check here will always pass and By Request multiple delete operations we can free the element multiple times in the commit phase so the B request to trigger double free will look like this since the lifetime check will always pass if the element exist during the prepare phase two delet set element of operation will both execute successfully and so when you get to the commit phase the same element will be free twice and we get a set casual element double free great so I found my first B for Curr Curr CF but the next day I found the I found the maintainer also find the same Bo and send the patch to col Mel list I mean why so okay this means my zero days officially turns into a one day and according to K CTF rules my bounty will decrease by 20,000 us ouch um also one day vulnerability means that everyone will know how to trigger the vulnerability and they may also come up with some exploit so I definitely need to exploit it before next kernel CTF slot opens so after we get the double free primitive we we use the explo method for CV 2023 44 to overlap the N table U data and N object to leak address and control IP due to the time limit of this talk I won't expand the detail of it but kudos to lonan K who discovered this technique and Sh it on Curr CF repository so now we develop the exploit and wait for the slot to reopen fast forward to 2 hours before the competition start I nervously check if there's any K updates related to UNF tables and guess what the patch previously posted on the milon list has been merged into the kernel and it added one line on it on it it says reported by lonon K which this means it's the patch may actually comes from zero day Curr CTF submission and since the reporter is Lan was exploit I just used this is indeed the case so just before I have a chance to exploit It On official instance I found it's a collision so yes I still found the instance and Report the flag but too bad for me the first Park do collide with an old current CTF submission that's how I look after the competition okay so I take some break and after three weeks later I found the second BL during the research I came across an interesting commit un table validate family when identifying table by handle the comit is added after the mitigation instance kernel version so it can be used to P the mitigation instance as a one day he added a check on nftd table Lookout by handle function which is used for searching a register table by the provided provided handle without the commit during the table look up we can provide a family different from the one which table belongs to but what can we what can we do with this inconsistency let's Trace where the family parameter comes from and where it goes goes n table Lookout by handle is only code from n table delete table and this function is used to delete users specified table from n tables the family the family variable is directly get from user provided input and it's saved into context family and passed to NFD flush table table function in nft FL table context argument is passed into every function ring object deletion For Me Trace Rich function use the context family is quite annoying so instead of just tracing into uh into each function I take the shortcut to trace where it is used so I just grabb the whole code base to find where the context family is used um grapping context family gives us a lot of result most of which comes from the unable expressions like um con limit net T proxy and Etc however most of them cannot be used for for the exploit but I finally found one that can be exploited it is in the nft log expression the trigger path I found starts from the nftd lock destroy this function takes context family as a argument and go to an loger putut now it's becoming the PF argument and use as a index to difference the logus logus array the value range of our input context family and also the PF here is 0 to to 255 but if we check the size of loger array we can find it only have 13 elements in the First Dimension so um we get the global array outut of bound exess here but what can we do with it after the log out it reference the me field and fed it into the module put the module put will again the reference and decrease the refcon value in the module object with this vulnerability logo can be any pointer still adjacent to a logus arate so we may cause some type confusion here and decrease some variable by one however we are dealing with a aor situation here in order to it ize this block we have to find the pointer store on global and can be Deen be Deen twice the first is to take our loger mean and the second is to take our module refcon to sum up what we what we have by now first the maximum value of P value we can control is 255 and we can trigger an outbound outbound reference count decrement by using a PF PF value larger than 12 which will execute like the code here to exploit this the first thing we need to do is to find the a global Pointer Place near the logos array so what do we have on the global by inspect the Maring GDB we quickly find there's a nfct expect hash placed near the near the logos and it's a hash table so in this case it seems to be a good Target since the the the pointer point to a array of pointers therefore it can be der referenced twice so and let's check how the code normally normally works first it get the me field at offset 1 18 HX and then get the ref count at offset 340 HX now let's check the auto bound access case first res set PF to 19 to get the hash table pointer nfct expect hash the first the reference we got is the index re entry of the hash table the entry point to the exal field of a un contract expect object so after one more reference it will access offset 340 hex down below the expect object but since the expect object only have a d size it will access all of bonds bonds fortunately all enough contract back object allocated in a dedicated cman cach so the memory space below will also contains enough contract expect objects so it will access to RCU field and decrease the next pointer in the structure but what does this next pointer use pointer use for as we know after RCU objects free Linux c will not instantly free the object and state it will hold the object for great period to wait for all other U readers to finish so the next point in the RCU head structure is used for temporary inory them and waiting for the RCU threat to process to free them so after the contract expect object are free they'll be like this train with uh trained by the next pointer and since we can decrease the next pointer in ASU head we can make it point point to another field inside the contract spec contract spec object and to exploit it we choose to land it on a user controlable field which contains a union of ipv4 and IPv6 address so when Ru callback triggered it will read the fake function pointer reset and we can control the rip however to actually exploit the vulnerability we came across some Rog blocks first we need we need the index tree of the patch table point to S effect object so we can use it to do the out of bound right but unfortunately the hash value is unpredictable next the outut bound written expect object need to be in the grace period so we can decrease its next pointer but the next period is a small time window and how to control when the kernel actually freeze the object but Al so long story is short we eventually found a way to exploit it by Pro Force and race to explore this first we use a type Loop to create a lot of expect objects and at some point the EXP count exceeded the value Max Max expected and start to evate the oldest expect object so the old object will be placed into the ru3 list and so on and so on and at some point Ru thread gets scheduled by the kernel and starting to process objects at this point we add another thread to trigger out of bound decre decrement in a battery class and keep and keep the system running for for a bit so we hope for the race scenario like this to emerge first a expect object allocated into index three of the hch table and then the expect object you want to aound right is free into the ASU free list then it continues to free another expect expect object after that we trigger the vulnerability 28 times in a b request to make the next pointer point to a user controllable value and finally the you start to process these three object and runs all F callback function great let's try it on local build and we successful successfully control RP to for for for for so what about remote let's try and we got an error message while trying to add the log expression since I have no idea what happened on the remote environment I decided to download the official busy image to debug it locally so I open the GDB and set some breakpoint on the functions but when I set the break on nft log destroy function GDB complained that it was not defined what's wrong here I later found that it's because the colel config I use is different from the one used in the official environment so the log expression I used to exploit the vulnerability not even compiling to the official official image image but I'm using the official build script to build the local test image why on Earth there can be any difference I mean why eventually I found it is caused by the official build script the mitigation instance is built by cosos kernel config Plus the Google Custom Security security patch and the official mitigation instance used cost config 6.1 55 the NF table expression are en enabled in CS config after the version 6.1.6 4 however the Bild script will always fetch the latest CS config so the mation kernel I build has the anable expression enabled but but in the official image they're actually disabled so well I think next time I should definitely check out the config first okay so after my second failure I revisit some old patch and I found a new bug the patch I was looking at is a is the patch which introduced the first SP the title says UNF table uh adep set backend to use GC transaction API so what does the GC means here it means the set garbage collection there are some set implementation support user to set a Timeout on on any set element and there are two types of GC to clean the expired elements the first one the syn the a synch the synchronous one will be called in the commit phase and the a synchronous one will be triggered periodically in the background but here we are we only focus on the a synchronous one so back to the patch in the commit message it says use GC transaction API to replace the old and buggy GC API and the B Mark approach so what is the difference between the new new and the old one let's take a look of the OG GC design in the old design the synchronous GC will run lessly and can be triggered at any time so in order to protect GC thre from Raising with the landing API both of them will check against a busy Mark set on the element before delete it to pre to prevent double free but sometimes the develop ER May might forget to check against it in this case there's a new function called nft map deactivate forgot to check a busy Mark and this leads to a double free varability varability so in order to avoid this kind of issues ANF table developer decided to replace it with a new design in the new GC design it introduced the GC sequence the GC sequence is a interet variable and it will be in increased before and after any modification to the control plan so it will increase at the beginning and at the end of both the commit and the abort phase during the GC procedure GC sh will record and check it to prevent race even though the GC thre still wrong lockly during the check it will now acquire the commit M text lock the new GC design look like this during battery request handling GC sequence will increase both before and after the comit and abot phase and in the GC thread it will first run lockless Le to store the GC sequence and collect all the expired elements after that it will acquire the lock and check if the current GC sequence match the safe one if they are the same then the elements will be will be released for this normal case since GC sh does not raise with the battery Quest the GC sequence will be the same and the element can be freed by the GC thread next we check if it still works in the race case if during the prepare phase GC sh triggered and sto the the global GC sequence in to transfer objects it will continues to run logless to find all the expire element and save them to and wait for the commute uh and wait for the mutex lock at the same time the MRE steep into the commit phase and some of the elements in the set can be released in this this phase the gcre will need to wait until the men sh release the mutex lock so it can find finally check the save GC sequence and delete the object and delete the element but in this case since GC sequence is recorded in the prepare phase and we check it after the battery request is done the GC sequence will be different so the element deletion will be skipped to avoid double free next let's check the case if GC sequence is recorded in the commit phase in this case since GC sequence increase after the commit phase the the GC sh can only and the GC sh can only check GC sequence after the Comm commit uh after the commit mutex unlocked the this race will also be detected so to sum up the new GC design require both the battery request and the GC sequence check to hold the lock this ures that even there's arrays while collecting the expired elements the check will always happen after the GC sequence has increased but does such do the battery request always lock means there's no race actually if you look closely we will find well it's not always locked to find where it release the lock let's first talk about the module autoload during the prepare phase if there's some expression type in the request is not found during the type resolution it will instantly abort and try to load the corresponded type module during the end of the bo phase after that it will retry the whole batch request again however maybe there's some efficiency consideration or to just only prevent that log before the function try to load the request the request module the m t will be released but could it cause any problem back to the diagram now there's a little time window in the B face that it will not hold the M Tex lock so there's a chance during the B phase the GC STK kicks in records the expired elements and do a GC sequence check in this case however the check will be bypassed and the GC sh can delete arbitr expired elements okay so now the question is how do we actually exploit the race our export plan is first we add a set element with a short time mod and then we trigger modu autoload to force it reverse the commit and free the element we just added and at the same time we trigger GC to collect lead lead element next modu Auto will release the mutex and we hope that the GC St will grab it and bypass the check so as a result the element we just added will be double free but in order to trigger double free there are two time window we need to enlarge the first time window is from the abort phas start to the set element released so the GT sh can recall the expire element before it get unlink by reverse reverse procedure the second time window is from the load module to its end so the GC thre can grab the lock and bypass the check for the first one since the phas will rever the operations in a reverse order we add a bunch of request after the adet element request to delay the unlink operation for the second one we just request multiple unkown type to make module autoload runs longer and here's the T okay so first reboot our machine and check the U is a nor more user and then we run the exploit so here we will first measure the execution time to set up timing parameter for the race and after that we set up the environment to enlarge the racetime windows then we trigger double free to leak address and control IP and we got the r show thank you guys great so now I write the export and submit it to Cel CTF and this is my Cel CTF submission I exported the LTS instance on Cel version 6.1.8 one but as you can see it is Mark with for dup in that line it is because for every OTS slot only the first zero day or one day exploit will be accepted and there's someone who also submit a Lotts exploit which Bit Me by 9 second that's too bad so we have to wait for the slot to reopen so yes just wait for about two weeks and and the slot will reopen reopen right wait until the slot to reopen yep but do you know the NF table will be disabled from April what actually there's an announcement on K CF Discord it says they are going to disable NF tables along with the switch to Kel version 6.6 it starts from March 15 but actually they delayed the switch so it's from April 1 okay so what's the date today it's actually March 22nd and the current CTF slot opens about every two weeks on average which means I won't be able to exploit it before they disabl the NF tables so on my third K cpf attempt I still could not get the Bounty which is is pretty is pretty sad to Su up my journey first I have hope and then I found bu one a lifetime issue and it collides with another submission then I found the bo two of how to use one day but I use the wrong config so it's unusable then I found the box three a locking locking issue but I lose the submission race and another table will be disabled so it's killed however there's a final plist a few days after I found there are some changes in the current CTF spreadsheet the person who previously get ahead of me is disqualified du due to a b collision and so this time I finally take over the slot and report it to go to the Google and last month I got my Curel CTF Bounty from Google which is my first Google VIP Bounty I'm so proud of myself thanks so um well I think this concludes my current CF journey and thank you guys is there any questions yeah if there's any questions you can come up here to this microphone
