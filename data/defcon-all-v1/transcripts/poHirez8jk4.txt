Cool. Okay, well, welcome everybody. We are going to be giving the talk, exploring Linux memory manipulation for stealth and evasion, strategies to bypass, read-only, no exec and distroless environments. Probably the title is self-explanatory but what we are going to be doing is to abuse Linux memory in order to bypass some file system based defenses and also to get [inaudible 00:00:24] in, well, yeah, to try to make it easier to pivot where you are talking about We'll explain what this is if you don't know about it. So for the people that read this title and doesn't understand a thing, don't worry because we are going to be starting from the beginning. We are going to be explaining you why we develop this technique and also how it works. So you should be able to follow everything. Also, we have a lot of demos. For the people that are already experts in things related to this, I will ask you to wait to the final fireworks because things are going to be getting harder and harder. So we are going to start easy and then we are going to start doing the complicated stuff. So just wait for the end. Also, as you know, this is our first DEF CON. So far, this has been a great experience. We are amazed with the community, with the conference, so I would like to start this talk asking you for a round of applause just for DEF CON because it has been a really great, great conference. Cool. So who am us? All of you know about this comment, I'm sure. So I'm Carlos Polop. I'm technical leader at Halborn. I have a lot of certifications. I was the captain of the Spanish team in the European Cybersecurity Conference a couple of years ago and also member of the winning European team in the International Cybersecurity Challenge last year. I'm also the author of HackTricks, HackTricks-Cloud and PEASS. Whoo. Thank you. And I'm just a lost nerd in the wilderness. I'm still studying telecommunications engineering. I love poem, I love C assembly and Linux internals, and I don't think I have much more to say about me. So let's continue. He's great. Okay. So what we're going to be talking about? We will start with an introduction about what the technique. We are going to present you the initial technique we developed called DDexec and then we are going to do a lot of demos. All these demos has been already recorded but we are going to be doing all but one live because we really like to do things live even if they can fail. So let's hope we will go okay. And at the end we have a surprise. We develop a new technique that we weren't expecting to present in this talk, but Diego yesterday finalized the initial step. So we are going to be doing a demo, and we will expect that it works. Yesterday, worked most of the time. So let's see. So, cool. Everything started maybe two, three years ago and the thing is that I was working in this company and they decide to move all the containers to distroless containers. And my boss asked me, "Hey, take a look to this new distroless containers because it is said that it's very complicated to compromise. Even if there are vulnerabilities in web servers in these containers, it's very complicated to compromise them. So take a look to them and let's find out what an attacker could do in this kind of containers." So the question was, is distroless is the new unhackable system. The reality is that in that time I didn't knew much about distroless, also about containers. So when I started taking a look, I discovered that I don't even have a shell in these containers. I don't have sh. I don't have Bash. So it was like, man, even if someone finds a remote code execution here, they're not going to be able to execute card, to execute the list, to execute whoami. They cannot execute a thing. So to me it looks like pretty legit. It is adding a lot of security. And I didn't know a way to bypass this at that time. Also, I started playing more with containers with Kubernetes. And in Kubernetes, you have this easy flag that you can set to true, which is read-only, which will make the file system read-only everything except one of two folders that have the no exec bit. Which means that if you manage to compromise a read exec container in Kubernetes, you're not going to be able to download binaries and execute them because in the only place where you can write, you have the node execute bit. And you don't know how annoying it is as a red teamer to compromise a Kubernetes container, see these Kubernetes token and don't being able to download kubectl and execute it. Of course, you can use Kool for insight but you need to use Kool to contact the Kube API manually and it's just annoying. So yeah, it make your life much complicated. At that time, I thought, "Hey, okay, so I cannot write in the file system but what happened if I could load something in memory and execute it. There is nothing preventing me from doing that." And I knew at that time that there were a lot of different techniques for do this in Windows. It's super easy to do this in Windows. But what about Linux? Well, there weren't some many and ware that were in there weren't that reliable. So at that point I just started my conversation with Diego and well, he came up with a very strange idea that ended up working perfectly. But before getting into this technique, let me tell you about the state of the art in that time about how to load things in Linux memory and execute this stuff. At some point, we found this blog from Sector 7 that told you that if you managed to write things in memory, you could use this shellcode using the syscall memfd create that will allow you to create a file in memory that is reachable from the file system so you could create a filing memory, load your binary and execute it. This will allow you to bypass these protections. And also while they we're developing the technique that now Diego is going to explain, we found that David Buchanan was doing something very, very, very similar but just smaller. And actually he was using a couple of extra techniques that we ended up implementing in our final technique called DDexec. But at that point, that was the state of the art. You couldn't do much and it was kind of complex and dark how to do this. So we came up with DDexec. So the idea that I had at the time was to trick DD into... overwriting its own memory through the mem file in the procfs file system. It turns out that somebody had that idea earlier, like three years before me. That was the post from Sector 7. Then I discovered that David Buchanan had a tweet where he used a way to write to the memory of the shell of Bash or the sh. This has the benefit that this doesn't need to disable ASLR because when we tricked DD into our writing its own memory, we needed to tell it at which address it needed to write, and that address must be passed in the arguments. So we need to know are which address right before the DD doesn't exist and that was the problem. With Bash, we have the benefit that Bash already exists. We use an inherited file descriptor, and that way we can write into Bash's memory, and we can check the mappings that Bash has beforehand. So, well, Buchanan, the tweet from Buchanan was, what did was just right to Bash memory to execute native code, a shellcode. It didn't allow to run a binary. So the tool that I made that I had to develop in shell scripting, what it did or what it does, DDexec is to parse the binary and prepare a shellcode that will perform the same process that the kernel does upon each call to execve. So we need to load the loader in memory. We need to load the binary because each ELF has headers that say the kernel or the loader, which portions of the binary need to be in memory and in which address and with what So we create those mappings. We read the data of these binaries into these mappings. We fix the permissions, then we prepare the stack. The kernel leaves some information for the loader. It is called the auxiliary vector. This has data which address is loaded in the binary and where are the headers of the binary and stuff like that. So we prepare the stack and then jump to the loader's entry point and the loaders will pass the... The binary will see which libraries are needed, then load them and link them and then jump into the binary's entry point. So we did the exec, we prepare all the stuff and let the loader do the rest. So he explain very, very easy. Just so you get an idea of how complex this actually is. All the thing that he said that he's preparing the memory and everything is actually a shellcode that is going to be created using shell script. So he dynamically creates a shellcode to load a binary inside memory using shell script. I mean, if you read this, it looks like the code is obfuscated but it is not. It just super ugly. So yeah, just so you get the idea. So let's just start with the first demo where we are going to be using DDexec to bypass So what we are going to be running, I have a coordinated spot running this with this read-only root file system which meant the file system are read-only and this allow us to download for example QCTL and execute it, and we are going to be executing QCTL using the DDexec technique. So I have the demos prepared here, so I can copy paste and I don't break anything. We are going to get a shell. We are not stealing the distroless part, so we still have a shell. If I run mount, do you see this? Do you read this? Yep. You can see that mount in read-only. And also something interesting is that dev shm here is read-write but it has no exec. So even if we have already here QCTL, if we try to execute it, we are not able. So what we are going to be doing is to load a binary with the DDexec in memory and execute Let's just start loading ls for example. So what we are doing here is just basically for encoding ls, to Ddexec. And here we have the arc zero and the arguments we want to pass in this case, ls -h. So it takes a couple of seconds to run because it needs to lower the memory. And sooner or later here, we just execute something from memory. So of course, ls was already in the system so it wasn't that complicated to execute it. The thing about big binaries for the DDexec is that it takes a couple of minutes to run. So here, where you can see is that we are going to be running DDexec in this video just because I don't want to waste five minutes of the talk waiting. So I have just cut the video and we'll show you that you can actually run QCTL. So if you check the last line, we are loading QCTL in DDexec. This is the magic of video. I just cut it and it just work and you will have the help of QCTL. So you could load even big Golang binaries with this technique. Yeah. The problem with QCTL is that it is a really large binary. It's like 40 megabytes. And because DDexec is programmed in shell scripting, all the processing of the binary is done with pipes between commands, and this makes everything really, really slow. So with QCTL, it lasted running like- They were 10 minutes, 10 minutes. ... 10 minutes, yeah, yeah. So that's why we had to cut the footage. So, after some months, I came up with the idea that DD can be used just to seek through the file descriptor that points to the mem file which is a presentation of Bash's or the shell's memory. So we seek through this file and then write with just an echo or a printer or something like that. So we can look for other seekers. Well, I didn't see the slide. So yes. So I'm going to explain why... Do we have a pointer? No. No, we didn't ask for it. So, POSIX file descriptors have a really cool quirk which is that when they are inherited, they are inside the kernel. They are exactly the same file descriptor. The structure that describes the state of this file descriptor is the same. I have here an example. We create a file, TXT, and we create a file descriptor. The file descriptor three with read permissions. And then we tell DD to write at offset 3. We tell it to write through this file descriptor, and we tell it to write zero bytes. When we read from this file descriptor which was displaced, DD performed on lseek syscall in this file descriptor to move the pointer inside this file to the third byte. So when we read, we will see that we are reading actually three bytes after the start of the file. And this is even... They are two different processes. This works just because it is the same file descriptor. So now we can just use DD to seek, and we can use other seekers, oh, like tail or cmp files, commands that allow you to specify an offset at which start to reading. We can use them. They weren't useful before because before in the demo, in David's demo, DD was used not only to seek but to write and it didn't use this quirk of the file descriptors. Using this quirk of the file descriptors, we can just seek and then just print off to write. So something fun about this technique is that now we don't depends of DD. So we found an EDR that was trying to search for this technique when we release it, and they were looking for DD using big numbers. So it was a very easy thing to bypass. You can even change the DD name to a different binary. But actually, now you can use 10 different common binaries in Linux just to move the file descriptor and write in any position you want in memory. So this actually allows you to bypass. The only idea that was catching this technique and potentially the new idea that will try to catch the technique. So let me show you very, very fast a demo about this. Because actually, in the same DDexec script that we have shown before, you can actually indicate in the seeker the binary you want to use and it will be used internally this So here we are using compare, and we could also be using XSD. There are more prepared inside DDexec. So you can just go to the README and take a look to the ones proposed in there. But actually, any binary that is executing on lseek with a few other requirements is going to be able to allow you to just load anything in memory. So actually we develop a very resilient technique in order to load things in memory and bypass these kind of protections. So let's move forward. Now let's start talking about distroless containers. So if you asked GPT-4 some months ago about what is a distroless container, he will tell you this. But basically what you need to know is that it has the minimum needed packages to execute whatever you want to execute. So if you want to execute a Python web, it's going to have Python. It's going to be having Python dependencies but it probably won't have bus, it won't have ls, it won't have cat. Let's say that it will just remove all the libraries and and packages that you don't really need to run what you want. So this is great because it makes the container much, much more smaller and also it makes it more secure. It makes very, very complicated to exploit vulnerabilities because we are used to execute the stuff that we expect to be there. That's the problem I had at the beginning before I start thinking about the memory stuff. So let me show you real quick how a distroless container looks like. So the previous demo, we have run them in my current macOS which is ARM. These techniques work for both ARM and x64. Now we are going to go to a coordinated environment in AWS. This also works in Kubernetes environments in the cloud that are in x64. So this way you can see that all the techniques kind of work in both architectures. So I'm going to update the credentials. Actually, the posts that we are going to be using for their next demos are this one. We have a node prototype pollution distroless container, and all of them have the read-only root file system. This is using the node distroless from a container from Google. This is one using Python, another one using Python. And then we have a PHP that is using the new Chainguard distroless images. Just so you can see that these techniques are working with both Google distroless and Chainlink, Chainguard distroless container. And also we have Ubuntu machine inside the Kubernetes environment because we need to capture some reverse shells to mimic a real environment. So what I want to show you here is that we are going to be getting a shell inside the flask distroless container. In this case, we have a shell. Some distroless containers, we have some shells. But if we execute ls, we don't have it. Whoami, cat, etc, pass. Oops. Well, do you imagine that? Cat not phone. So, yeah. Whatever. If you find a shell, just so you know, you can still use the buildings of the shell. So we could be reading the etc password using some the read too. And if we try for example to get a shell inside the node distroless container, we are going to see that we don't even have a shell here. So this is how a distroless container usually looks. Now you should be familiar with distroless. This is the demo. So far the demos are working. So this is pretty good. Let's do these Python distroless demo. This is the one I'm going to be putting in the video because actually we are not using our own technique, we're just using an old technique that already exists, but I want to show you anyway. The thing is that, well, if you find a command injection in a web shell, probably it has a shell just because usually to execute commands from a scripting languages, the shell is going to be used. So usually you will find this. But anyway, because you don't really need a shell. If you are compromising a distroless container and you want a rev shell, even if you don't have Bash, if the web server is running in Python, you can have a Python rev cell. So you won't be running Bash commands but you will be able to run Python code which is still pretty, pretty good. Also, scripting languages such as Python, Perl and Ruby by default, install some libraries that allows you to call syscalls. And that's actually everything you need in order to load something in memory and execute So this project right here, fileless elf exec is doing that. This is going to transfer some Python, Ruby or Perl... This going to transfer a binary, wrap it into Python, Perl or Ruby code and allow you to execute from these scripting engines. So for this in this specific cases, in this specific distroless cases, you won't need to use our technique. The scripting language is enough. So I just want to show you this real quick and actually we are going to be seeing a video. Here we have a vulnerable Python pods running our web server that I have port forward to my local host. Ls doesn't exist, cat doesn't exist. Using the built-in command, we can see that we have Python. Of course, it is running in flask. So we are going to be getting now. We are going to be abusing... Ooh, sorry, I wasn't expecting that. We are going to be getting a reverse shell now, a Python reverse shell. If you're going to be playing with distroless containers, forget about Bash reverse shells. You're going to be getting node PHP Python reverse shells. Here we have it. Now what we are going to be doing is just down here, we execute Ubuntu. We are going to be preparing kubectl to be execute with Python. So we have downloaded the project I just told you about, fileless elf exec. We are downloading here kubectl. Now we execute the fileless execute to prepare kubectl. And actually, this script looks like this. So basically, what this is doing is getting the binary in base64, decoding it in memory and just calling the syscall execle to execute it. Again, if you can call syscall from the scripting language, you don't need to do extra stuff to call syscall. You can just do it. So in our case, in our reverse shell, what we need to do is to create a children because it's not... Whenever we end executing this command, our rev shell is going to be dying. So here I'm just changing the code, well, to fork it to execute QCTL in a children. I am going fast because we have a lot of things to do yet. We are just starting. So, okay. Now we are going to be... We expose in this Ubuntu server the QCTL.py that we have prepared. In our Python reverse shell, we are going to be downloading this in memory again. And now, if we just exec it, we are going to be able to run QCTL in memory in Python. Again, we haven't used our technique but just so you know, if you find a distroless with Ruby, Perl or Python, you have a very easy way to just call syscalls and execute things But what will happen, for example if we have a node server, we have an express web server with an lc vulnerability. Well there is no in the server so we can still get a node reverse shell. Again, get used to getting this stuff in distroless. You can use node to enumerate the server so you cannot execute ls but you can create node functions that behave like ls. So you can enumerate files. And node cannot call syscall directly but we could use a shellcode like DDexec to modify a new node process to execute anything we want. So what we are doing here is to abuse the DDexec technique in order to load anything we want even in node who cannot call syscalls. So in this case I'm going to be using the shellcode that used the syscall create memfd that I told you about at the beginning. We are going to be creating a file memory, then we are going to be loading QCTL in it, and we are going to be able to execute QCTL in another distroless container in node. And actually, I'm going to try to do this demo live so this is going to be a good one. So let me get... We are going to be in our target machine, Ubuntu. I'm going to be port forwarding or 3000 to my local machine because there's where the vulnerable server is running. And here, I'm just abusing a prototype pollution to receive vulnerability in order there to get a reverse shell. This is outside of the scope but if you're interested about how to do this, you can check more information in HackTricks. So it was executed, and I forgot to capture the reverse shell, so we had a real problem No worries. I just need to restart real quick, which is going to take a minute. But now we can be prepared to capture the reverse shell. Never forget the most important part of reverse shells, the listener. Sorry about this. So Kubernetes take a minute in order to restart the bot. So that's what we are now waiting for. But in the meantime, we can just prepare the... I think he was looking at me angry. I suppose this should be now ready. Back on track. Actually, let me take a look if I have any other listener in there that could be bothering and actually have one so this wouldn't have worked again. Execute it and we have our node reversal. Great. And we go with the demo. So I told you that you can use node code, JavaScript code in order to enumerate the query machine. So you could use for example the OS library to get TMPD or information about the architecture or the network information. So you can still enumerate this distroless code, and you can also execute things in memory which is what we are going to be doing now. I have the demo here so you can follow it better. So here what we are going to be doing is importing some stuff and we are going to create a new JavaScript file with the code that we want to run and the shellcode that we want to roll. So we are going to be using the syscall trick in order to know where we need to override inside the memory we are going to be overriding the memory via proc self mem. And this is the shellcode that is going to be executing the syscall. Create memfd. So we prepare the file through our reverse shell. We write it in the file system and we check that it exists. So it exists right here. Now we call via fork this new child process that is going to be executed our shellcode. Now we want to check if this work, of course. So this is what the function is going to be doing and we can see that we have a memory file descriptor that we call dead just to for the sake of stillness, I guess. File descriptor 20, proc id 59. So this should be working. And now we are going to be creating this download function that basically is going to be able to download... To download something from the internet, load it in the memfd and run it. We download it. This should be working. And now if I execute the memory file descriptor, it should be executing QCTL. And here you have the help of QCTL. So we were able to bypass distroless protections by executing things in memory. So in order to make this more useful, I decided to make a DDexec, a demon of DDexec. So we can just load this program or in the end I actually made it a So we run this shellcode with any of these techniques that allow us to run native code. We run this shellcode and we can tell this shellcode to load a binary with the arguments and then just we will be able to run any program we want. So basically, this will allow us to use what I have sold you but to low binaries more frequently are more useful. Instead of doing everything once and again, it will allow just to send the binary and load it and execute it. So in this final demo before the bonus that I hope we have time for that, what we are going to have is a PHP distroless. We are going to using the Chainguard emails for this. In this case, we don't have a shell. We are going to be getting a PHP reverse shell. We are going to be loading the new demon. And actually, we are going to finalize this demo loading a Busybox because we are in distroless. It will be very ironic if we can have a shell in distroless. So that's what we are going to end up doing. So let me show you that in PHP we don't have any Bash, any sh. Sh file not found. So we are going to be getting a PHP shell and now we are going to be getting a PHP reverse So imagine that we have compromised our PHP web application with a . And we got a reverse shell with PHP. We have it here. And now maybe you want to follow explanation. So what we're going to do now is execute another PHP, another interactive session of PHP, which we will communicate through a pipe with. We're creating the pipes and now, starting the process. So this interactive PHP, we will send it commands to write a shellcode also through the mem file in the proc fs file system. And we will run this shellcode that I made, this demonized DDexec. We have the shell code in a ARM64 or x64. And now we have... Oh, shoot. He made a mistake. Things happen. I think we're good. This is for more complex . And now we are writing the shellcode because there is a shellcode that is a stager that reads the other shellcode because the second shellcode is really, really large. And now I have created this function that is called memexec. This function is the one that is used to communicate with this demon. We can throw it on URI to a binary. It may also be a binary inside the onfile system. And we pass it the arguments and we can run any command. In this case, we are downloading them from my server. And to finalize this demo, what we are going to be doing is load a Busybox shell inside this reverse shell. So what we have here is that now we can for example call set to enumerate in variables or run POWD. Basically, we put a Bash inside a distroless, which was the main goal of this. So we just defeat distroless security measures. So now the bonus technique. This is another technique that is kind of old and new at the same time. DDexec has a problem that we need to pass the file and unload it. And a loader is really, really complex. I have skipped a lot of peculiarities that ELF have and extensions from ENU and Sun Microsystems and things like that. They have added a... That's Mayhem. So you can have compressed sections and lots of things and Ddexec isn't doing that stuff. So another way to load filelessly a binary is to use the dlopen but tricking the loader into thinking that the binary is somewhere that it isn't. So this is a technique that was presented in a paper, I think, in 2004. So the idea that these people had was to... You have the binary that you want to execute raw in memory, the ELF. In some way, you get to hook to the syscalls the loader is going to execute and then call dlopen. Passing to this function some fake path. Since you are hooked to the syscalls, you can detect when the loader is trying to open this file path that you have given it and return a fake file descriptor. And in subsequent syscalls, you can see if the file descriptor that the loader is using is the fake one that you gave it. And in that case, you fake the syscall. So in case that you read that the loader is trying to read from this file that doesn't exist in the file system, you can just memcpy from the place that you have it draw in memory and write it where the loader wants to read that data. So in the paper, they didn't discuss how to hook to the syscalls. There was someone. His alias was mimics that in GitHub has an implementation that used code signatures to find the places that the calls to the wrapper functions, the wrapper functions to the syscalls and overwrite that calls with calls to its own code. The problem with this is that code signatures may vary between libraries. And so this isn't particularly useful because then if you have access to a system, you need to download this library and get with this code signatures and it is really complicated. So my idea to hook to the syscalls was to install a wrapper to install a signal handler for the signal SIGILL, which is a signal that the kernel sends to a process. If this process tries to execute an instruction, an invalid instruction. And then go through the memory of the loader looking for each syscall instruction and replace it with an invalid instruction. So now we have the hook that we need, and we can trick LD with just like the paper tool. Hmm? Demo. Well, they also discussed it to load libraries but if you make a binary look like a library, you can also get LD to load this binary. So later we are going to show you the links to all these tools that we have just showed you, Ddexec and then dlopen. But here just want to show you that this is actually working. So we have here a Debian container that I have already downloaded memdlopen.sh. And here, what we are going to be loading basically is just ls-le passing in the standard in the binary. So we do like this. You can see that it's actually much faster than the DDexec technique. And well, we still are working on this to improve some things. This is actually a project that I haven't dedicated a lot of time. This needs a lot of research. I have detected that it has problems with binaries that aren't position-independent, but, well, I think that I will manage to fix these issues. You want to show the code? So, this is the shellcode. It is long and it is complicated. Right now, it is only implemented for ARM64. There is a proof of concept written in C that is much, much more understandable. You can see the installation of the signal handler. We have also the replacement of the syscall instructions with invalid ones. And we can see here how this signal handler is taken from the stack. All the arguments that were intended for the syscall. Checking which syscall it was and faking each one. Anything, you have all this code in the repository with some comments to help you understand So actually the repository, you can find it in here. Here you have memdlopen, remote library injection. Well, we have it. We are going to be sharing the slides but if you want to take a quick photo, I will leave it for 10 seconds. Also, all the demos that I have been doing about this distroless, you have them in this report if you want to try them yourself. I hope to find some nice CTF in the future using this technique. What else? Well, also, Diego wants to share with us a very, our challenge you have. It is just a fun, really small challenge, but I think it is kind of weird. It is really easy to solve but to understand why it works is really interesting. I have shown it to... I post it in Twitter, and I think nobody liked it. So if you get to solve it and tell me why it works, I will give you a hug. Some final to end this talk. Me and my mates at HackTricks are preparing HackTricks AWS Red Team expert certification that we expect to release in Q4. So if you're interested in AWS red teaming, just follow us in Twitter, LinkedIn or go to this page. And thank you very much to you people and to DEF CON for organizing this. It has been a great pleasure to be with you.
