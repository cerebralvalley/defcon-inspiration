okay hello everyone my name is paranoid kid and i welcome you to reversing with dynamic data resolver if you are not already in the discord chat please go to blueteamvillage.org blueteamvillage.org click on the discord channel link and i would like you to give a warm welcome to mr holger mr holger and let's start with the talk hi and welcome to my presentation reversing with dynamic data resolver best practices best practices my name is hollywood i'm a security researcher at cisco talos and i'm mainly focused on malware research threat hunting and tool development if you want to follow me on twitter you can find me at hunterbr72 i'm the author of ddr and i will guide you through the different features of ddr today the installation process and i will show you some of the best practices how to use the tool and some of the special cases special cases and caveats which you can see later on in the demos where you might can might can run into so what is dynamic data resolver first of all dynamic data resolver is an instrumentation tool it is instrumenting the the binary so that you can trace all the instructions instructions the binary is executing and it is collecting all kinds of data from strings to register values memory locations etc etc it comes with an ida plugin which is controlling the back end so you can easily handle everything from this ida plugin and you don't need to to use the command line tools you can and i will talk about that later on ddr comes with the client server architecture architecture so you have the ddr server which is running on the malware pc side so the pc where you are executing malware and malware and you have the idler plugin which is running inside of idler on the analyst pc pc it is highly recommended to separate these two machines in theory you can run all of that on one machine machine but keep in mind that you are executing the malware at the end so the malware really runs on that machine and machine and you might not want to infect your machine where you have your either insulation on be infected with with malware so what can i do with ddr ddr was mainly built to resolve dynamic values values and this is why i've called it dynamic data resolver data resolver but it comes with all kinds of other features but for now let's keep with the data resolving option you have in ddr uh you have the option to resolve certain register values for example so if you want to know the absolute value of value of edi or eba ebx or eax you can just do that with one click you can also get the pointer to the memory memory this value is pointing to or even the pointer pointer pointer pointer and ddr also tries to detect if the address which is stored in a certain in a certain register if that one points to a certain api call like a virtual protect virtual protect and if that is the case it will also resolve the resolve the api call so you can see the the human readable name of the function call after you have run a trace and ddr has all the instruction traced uh you can highlight the instructions with a color you can just highlight them depending on how many how many times they were executed so you get a so you get a pretty quick overview about which basic blocks were executed by the program program and how often these basic blocks were executed by the program as you can see here on the slide if you if you see a color which is a more warm a more warmer color like a red or purple you know that this basic block or these instructions were instructions were executed many times so it is likely that it is maybe a decoding routine or something like that so that already gives you a good clue about about what the sample is doing and what parts of the sample are executed and what other parts are maybe not executed you can also get a table of all the api calls calls the sample has executed so you can search through this list you can search for certain api calls like a virtual unlock or something like that and then you can do a double click on the line and you're jumping to the disassembly code where this api call was called the same applies for strings as i mentioned earlier ddr tries to collect all the strings at runtime and it is filling this table with these strings strings and the table is again searchable so you can look for certain interesting strings like like pe for example if you're looking for a pe header pe header and then double click on this line and check out in the disassembly uh what's going on going on where this where this was used and we will see all of that later on in the live demo another neat feature of ddr is that there is a smart way to dump buffers at runtime so runtime so all you have to head over to all you have to have to hand over to ddr is the size of the buffer the address of the buffer and when or where you want to dump the buffer buffer and we will also see that later on for for example you just have to mark the operands the operands in the disassembly which are storing these informations here the size the address address and at the end where you want to dump the buffer unfortunately often malware comes with anti-analyzing features anti-analyzing features and it is not executed like it would be executed executed on a normal pc if you're running it in a vmware for example so a ddr comes with an option to patch the sample at runtime so for example you can knop can knop out certain instructions or you can toggle the e-flag and manipulate the the execution flow so if there's a conditional branch you can just toggle the e-flag and the conditional branch will do the exact opposite exact opposite but again we will see that later on also in the demo and last but not least you also have the option option to completely skip certain functions you can just mark a function skip it and return effect return value so if you have for example a certain function in your malware sample which is checking for virtual machines or for anti-debugging stuff you can just skip the function and return the value which is telling the sample the sample that it is not getting debugged or that it's not running on a virtual machine but again we will see that later on in the demo if either and ddr is not enough for you to analyze the sample you can also create an x64 debug script which is automatically executing the sample so it automatically loads the sample into into x64 debug x64 debug and it is automatically breaking at that address which you have highlighted in ida in ida when you have picked this menu point if you don't like x64 debug you also have the option to create an a new executable of the binary with an endless loop at the marked address so you can mark a certain certain location or a certain address in a site of either pick this menu option and then a new binary is generated which has an endless loop patched at that location so you can start the binary binary it starts to loop endlessly and you can attach your favorite debugger to it restore the bytes and proceed debugging it okay that should be enough for a quick overview about overview about what you can do with a ddr in the upcoming demo upcoming demo we will see all these features in details again details again so let's start with the installation of course first of all you have to download ddr you can get it from its repository on github and once you have downloaded it it comes with an installation script and pretty much all you have to do is to execute this installation script and it will guide you through the installation process again we will see that later on in the next demo demo if you want to stay up to date with the latest versions of ddr you can follow me on twitter and every time i'm changing fixing or fixing anything or releasing a new stuff for ddr i will announce that on my twitter account if this presentation presentation is not enough for the information you are looking for you can also find a very detailed blog post about post about ddr which i have released the end of may on our blog which is also describing the difference difference the different steps the different features features and the best practices and the caveats which you have to be aware of when you are working with ddr ddr has a client server architecture keep in mind that ddr is instrumenting the binary so the binary is getting is getting executed at the end so it is highly recommended to split the analyzing part and the execution part so that you have an analyst analyst pc where your ida is running on including the plugin and a dedicated malware pc where you are executing the malware sample and analyzing the malware sample the ddr server is controlling the back end of ddr which is implemented in or implemented using the dynamorrio framework and that one is at the end executing the sample instrumenting the sample and collecting all the data which is then transferred back to the idler plugin but we will also see that later on in a demo okay so enough theory let's have a look how that looks in the real world installing ddr is pretty straightforward all you have to do is to execute the ddr installer.pi script and it is solving all the dependencies for you for you after it has downloaded all the dependencies uh it will ask you a couple of questions uh about the ida installation directory the ipad address ipad address it is supposed to listen on etc etc and after you have done that you can start a local http server or in other words the script is starting an http server for you and then you can move over to the idler machine machine and access this http server and download the ddr ida plugin site of the installation then you unzip the file and copy over the content to to the traditional ida plugin directory that will change in the next release which is coming out very soon and we will move over to the either user plugin directory plugin directory so that has the advantage that you you will not need any administrative rights anymore anymore once you have done that you need to figure out which python version your idler installation is using and then you need to install two dependencies dependencies for this python version and the requests library and the pe file library and with this you are ready to go and you can move over to the server side again and start the ddr server okay so after we've seen how the installation works i can give you another quick walkthrough of the different ddr features which i've mentioned earlier okay for this and we have a dedicated analyst pc where ida is running on including the plugin and we have a dedicated dedicated machine where we are executing the malware on malware on and where ddr server is running on so let's move to this malware machine once on the melbourne machine side we are starting the ddr server via the ddr server python script and when we are signing it there's a little hind hind that you have to be aware of that there is a is a caveat with windows and or windows command prompt and python scripts scripts if you're marking any text in the output window window you're freezing the python application so so that means that if the plugin would try to talk to the server now you would run into a timeout if that happens will timeout if that happens happens you just need to hit escape a couple of times times or just do a control z and restart the server if you like okay so let's move over to ioda the first thing you usually want to do is is you want to run a trace a trace is filling all the internal data structures inside of ddr which are necessary for most of the other features and you have two options either to run a light rays or a full trace a full trace is a trace which is collecting as much information as possible per instruction so for example is it is saving all the register values for every single instruction a light phrase is only collecting collecting a subset for these informations but enough information information that you can have a look at the api calls for example or at the strings which were traced or to get a source operand and a light trace is usually something which i'm running when i start to analyze the uh the sample sample and if i just want to get an overview if i just want to highlight the traced instructions for example or if i just want to see the api calls then then the light trace is good enough later on when i'm analyzing an um crypto algorithm for example and i need need more informations for example all the registers registers then i'm running a full trace but i'm only running a full trace for a small address range or for a few basic blocks for example because this is really time consuming and and this is also consuming a lot of memory so so you usually want to limit the amount of instructions which you are instrumenting within full trace okay enough about traces let's run the light trace light trace and check how that looks like on the ddr server side so the sample is executed and the dialog box which you have just seen just belongs to the sample code it doesn't has anything to do with ddr don't be confused by that once the analysis is done you can see that the trace log file is written to the same directory where the original sample original sample is located and you can also see that the temporary trace files were deleted based on a server command and then transferred over transferred over to the idler plug-in so now we can switch back switch back to either and we also see this method can switch back to either and we also see this message inside of ida that the light trace was done successfully so this means that ddr now has all the information it needs and we can execute other features like highlighted traced instruction instruction which is giving us an overview about which basic blocks were executed or which instructions were executed okay so let's move to the next feature you can knop out instructions at runtime so if you have certain instructions which you want to get rid of like the dialog box which we have seen earlier you can just mark these instructions and you go to the patch menu in ddr and then the next time you execute the sample sample so for example you run a new trace then these instructions are knocked out they're not getting executed anymore and you can see here that it is responding much faster because we didn't had the popup of the dialog box dialog box and the sample immediately proceeded with with execution okay the next feature which i would like to show you is how you can use the dynamic values which were collected by the trace now first we are doing a trace again this time this time we are just doing a trace over a couple of instructions of instructions now you're picking full trace for marked address range address range and once this is done we have all the values from these instructions inside of the database so we can just go to get value for source operand source operand and we are getting the value for ra x in this case or we can just get the values from memory pointers like you can see here or all kinds of other informations ddr has next nice feature is that you can collect collect all the strings and api calls at runtime you can show both of them via tables where the where the ddr menu so if we are looking at the strings for example uh this is a list of all the strings strings which we have collected at runtime and of course you can also search for something in there like pe for example and if you're double clicking on the line then you're jumping to the line in the assembly where it is used you can also have a look at the api calls which were executed at runtime and again the same applies like for strings strings you can search for something and double click on it and then you see where it is used inside of the code you have seen earlier that you can manipulate the code flow by knopping out instructions instructions but there are also other options inside of ddr which are manipulating the code flow the next one which i would like to talk about is the option to toggle the eflex so for example if we have a code like this where we have a conditional jump and a comparison before then we can toggle the e flag at the jump jump in this case you have to know that eax can never be bigger than 5 so at least in theory this jump should always be taken in this code and code and the program should print out main a is not not greater than 5. if we want to manipulate that we can just mark the gla instruction instruction do a right click and go to the patch menu inside of ddr inside of the patch menu we pick a toggle e flag in this case we have to toggle the sf flag flag and once this is done we can now just run the sample if we want so for example again over the patch menu if we pick if we pick that one then the sample is just getting executed but we could also use any other feature of ddr running a trace or doing a dump which we will see later on all these patches which i have applied will will work for all the ddr features once we executed it then we can move over over to the ddr side and check the output of the file sorry of the executable so here you can see that the patch worked worked and the program printed out a is greater five and this should never happen happen the last option you have is you can skip functions at functions at runtime so for example if we have my function one here in this case maybe something which is doing some on the analyzing and debugging stuff you can just can just skip it and you can fake the return value now you're just marking the first instruction in the function and and you are going again to the patch menu skip function at marked address at runtime runtime and you hand over the faked return value and that's it now you can execute the sample again and if we are moving over to the ddr server side server side we see that the sample is getting executed executed and in the output of the sample we should see that the my function one output is gone because the my function one is not executed anymore another main function of ddr is that you can dump buffers at runtime and you can do that in a smart way later on on the string will be copied into this buffer which we are going to dump so remember that for later so for example if you have an uh function like virtual alloc all you have to do is you have to mark the operand which is storing the size of the buffer i get buffer size and the next thing is you mark the operand which is which is storing the address of the buffer which you're going to dump i'll get buffer address and last but not least the final step is you mark the instruction or the location where you want so use marked address to dump buffer to file file and that's it now you can go to the lab menu menu and execute the sample and the buffer gets automatically dumped at runtime once it is dumped it is transferred over to ida to ida and you can store it somewhere on disk and here you can see that it is the buffer which i've mentioned earlier where the program has copied the pe string into if you want to debug the sample inside of of x64 debug you can also uh generate x64 debug scripts debug scripts which are automatically setting a breakpoint at breakpoint at the location which you have so once you've generated the script you can load it into x64 debug all you have to do is to run the script it will automatically start the sample and break at the break point which you have highlighted in either so here we have reached the breakpoint the script the script has finished and now you can proceed debugging debugging in x64 debug last feature which i would like to show you is how to create endless loops in binaries so if you want to create an endless view endless view loop you can just mark the line where you want to create the loop and then you select create execute executable with loop at marked address now ddr is creating a patchable with loop at marked address now ddr is creating a patched binary and you can move over to the ddi server side and you will find a patched binary in side of the samples directory so now you can start this binary just double click on it and at a certain point we are reaching this endless loop endless loop so now we can attach our debugger to it and proceed debugging the sample so after pausing the sample of course first thing you have to do is you have to replace the patched bytes the patch bytes of them but this little help in the ddr server output to see what kind of bytes you have to patch now you just copying the address into this command and then finally you have the original commands back inside of the binary so when you when we are proceeding to debug you see that uh this changed to the original bytes and now you can just uh proceed debugging uh like usual and that's it okay so this was the installation and the feature walkthrough of ddr let's move on and have a look at some of the special cases and of course the best practices the better you know ddr the more efficient you can use ddr and as usual the easiest is to watch a quick live demo about it when i start analyzing samples i like to run a quick light trace light rays is only executing 20 000 instructions by default you can change this default value if you're running a light trace you can probably set that up to 200 000 but this is in this case for this sample not necessary so we can leave the default value once the light rays is executed we can highlight the traced instructions to see to see which basic blocks were actually executed executed this was pretty quick in this case so let's see what's going on highlight traced instructions traced instructions and we see that there are only four basic blocks which were actually executed so executed so seems to be that there is some anti-analyzing anti-analyzing checks going on so let's go into the last function last function which might be responsible for this check so when we are scrolling down a little bit we see that it seems to be that this function is dynamically dynamically resolving the get native system info api call call it's using get process address we see that pretty often in malware samples then it is storing the address in this variable and a bit later on it is subtracting 10 from this address and a little bit below we see that this value is pushed as an argument to the next function function so seems to be that the first argument of this function is this get native system info api call function address function address -10 so when we are looking at the first argument here uh we see that um it is executing executing a couple of mathematical instructions mathematical instructions and uh it seems to be that it is just uh adding 10 to it so it has restored the original address and uh it seems to be that it is uh just calling the calling the get native system info api call here at that point unfortunately in uh real malware this is often much more complicated more complicated so in real world in real malware we often see that these calculations are distributed distributed all over the place and they are heavily obfuscated obfuscated so it's often very difficult to figure out what is actually called at this this point with ddr that is much easier you can just do a right click and click and you can get the value of this function pointer pointer i just pick get value for pointer and source operand and then you see the value which was used at runtime so it is confirming what we assumed get native system info is called with this information we can now go one level up level up and we can change the name of this argument to function pointer get native system info minus 10 and we have also seen earlier in this function that there was one parameter pushed and this is the second argument of this function so if we are looking at the get native system info system info we see that this is actually a pointer to the system infrastructure which is storing all the system informations so again we can go to the function above and we can find the second argument here and we can also rename it to something like like system info or whatever you like as far as this is an uh stick variable which is pointing to a structure we can also also do some housekeeping and quickly assign the right structure to it so that makes it a little bit easier uh when we are proceeding the analyzers to see what's going on so here we can see that here is actually a comparison between the number of processors processors and three with a closer look we see that if the number of processors of the machine where the sample is running on is smaller than three then sorry then we are just printing out a likely virtual machine detected so if it has two or less processors the sample assumes that it is running on a virtual machine if it is more than two processors it is returning zero returning zero and again if we go one level up we can see here that it is comparing eax and only if it is zero then it is proceeding with the rest of the malware code if not it is just exiting let's rename the function quickly to something like something like vm check and let's proceed analyzing so of course we're also interested in the rest of the code and to make sure that we can execute it on the virtual machine we can just toggle the eflex like you have seen before in this case it's a jump zero so the zero flags are fine so let's run the trace again and wait for the results okay so um i've cheated now a little bit and fast forwarded the video we run into a timeout so um we are seeing this warning message here here and it is telling us that the response from the ddr server took too long too long by default the plugin waits for 30 seconds seconds to get the results from the ddr server another trace for example and if you like you can change this behavior behavior here under api timeout you can increase the timeout for example to 60 seconds or to 100 seconds or whatever you think works and then the plugin waits for a longer time time so you've seen earlier that you can also configure configure the number of instructions for example which which are supposed to be analyzed and you can play with these little with these two values a little bit of course if you're increasing the number of instructions much more data will be collected and the files which are going to be exchanged are also much bigger bigger so sometimes it helps to increase a timeout to timeout to do these kind of long traces but nevertheless make sure that you are not not that you are not increasing the number of instructions of instructions to a too high value keep always in mind that that it is also increasing um the file size of the data which is collected so coming back here to our issue that it timed out we can for example now look over to the server side and we can check if there was some internal error for example there could be a bug inside of ddr which caused a crash for example example or there could be some other reason and super endless loop or whatever yeah and a sleeper whatever and we see here that the analyzers run for 60 seconds so it seems to be that it was was successfully executed successfully executed when we are now going back to idler we can proceed with the analyzers of the malware malware to get an idea about what happened and usually you would know check these instructions instructions what's happening of course i'm not doing that in detail here in the demo and i know the result um and the reason for for the timeout in this case is a sleep timer timer so you have a sleep timer here which is actually waiting for 60 seconds and of course that is more than the 30 seconds seconds and we haven't reached the maximum number of instructions at that point yet so the ddr server side is still trying to analyze the the sample sample but there are no new instructions before this this sleep is not it's not over so this means that the analysis is still gone gone going on on the server side but the plugin runs into the 30 seconds timeout timeout so we could now increase the timeout for example yeah and just wait for a little bit longer or again we can just just uh here uh knob out these instructions and run it again i don't like to wait so i prefer using the knob out functionality and now we are running the trace again and now it should be a little bit quicker so light trace done done and now we can clear the old highlighted instructions nicely highlighted instructions highlighted instructions and we can highlight it again and now we can see the new path with all our patches which we have applied applied now you see that we have manipulated the vm check here even if it has detected the vmware nevertheless we toggle the e-flag here and and we are going this way and now it is checking here um if its name is evilmelva.exe is evilmelva.exe and if that is not the case then the sample is going this way and a quick summary of what's happening here is here is it is copying itself over to the temp folder folder and then finally here after the sleep it is creating the process and starting starting another instance of itself of itself and just from this other directory so this is a reason why we are seeing that we are reaching here this uh exit call and the sample seems to be or the execution of the sample sample seems to be stopped at that point in reality it has started the new instance instance and the new instance is running under this this name so it will now go this path and will proceed to execute the rest of the code this should be enough for this sample so let's move on to the next one one actually uh one which we found during a recent recent apt-time campaign it is an executable which looks like an xml file but as you know if if you run that on the command line it will execute it like any other normal executable so to get an overview about what it is doing as usual we are just running a quick light race with ddr and when we are moving over to ddr we suddenly see oops what that the window disappeared so it seems to be that maybe the process was killed or something like that interesting um so we are moving back to uh ida and we suddenly see that the light rays was nevertheless successfully executed so seems to be that the ddr process is still running still running in the background so what's going on here here to get an idea it's always a good idea to to check the api calls which were executed and when we are looking at those we see that that there's a couple of initialization going on on and stuff like rebuilding the irt for example example a lot of get process address calls and after that we suddenly see oh there is a show window api call api call that looks interesting because with show window you can manipulate the window and when we are looking into the disassembly we see that it is using get console window to get the the handle of its own window and then it is executing show window and show window is using esi for the command show parameter and esi is cleared right before so we can be pretty sure that esi is zero in this case but even if it would be a more complex function in a different sample for example of course we can always use ddr to get an idea about what this value is here in our case zero obviously so it is handing over zero to a show window and if we are having a closer look to the show window function we see that the command show parameter is offering a couple of different options and if it is zero it is just hiding the window from the desktop desktop so we have solved the the issue why the why the command window suddenly disappeared it was not killed it was just moved into the background so this is one of the examples how you can pretty quickly analyze what things things are how are what what's actually happen with your sample and now you could either leave it like it is in this case because it doesn't hurt or if you still want to see the window you just go ahead and you kill these instructions because you don't need them for anything else you can just knock them out about marked instructions and then we are restoring the uh then we are restoring the snapshot on the on the other side on the ddr server side server side and once it is restored we can just run the sample again just do it like trace again and now we can see that it is running normally like before but it is not hiding the window anymore window anymore because we have knocked out these instructions instructions and we are done and we can proceed with our analysis back to the presentation um another special case which i would like to talk about about is if you want to analyze the sample in an air-gapped environment so for example if the sample is doing excessive excessive anti-vm vmware checks or if you have to disable the networking for any reason there are probably many cases why you want to want to analyze the sample on an air gap system you can do that because the engine of ddr is implemented in a way that it is a command line tool and it is heavily leveraging the dynamo rio framework to do all the instrumentation instrumentation so at the end the dds server script is just just executing this command line tool to uh to inject the ddr dll into the sample the ddr dll is the engine which is doing all the analyzes analyzes which is saving all the traces and the registers and so on and if you're running this command line tool via dr run c client dll and the sample to instrument instrument you will get a json file back or actually you're getting two json files back back one for the instruction trace and one for the api trace these json files are looking like this so they have all the informations per instruction in them in it so all the registers or the values of the registers the registers at the point of the instruction the eflex the disassembly etc etc plus the memory location which might be interesting and so on and this is a json file which is getting transferred over transferred over to the uh ida site and which is the either plugin reading to fill in its data structures and with this file you can then use the plugin live view like you have seen before so you have seen earlier in the command that that you have to hand over a configuration file file and you will find in the docs directory of of the github repository a sample configuration file configuration file which is heavily commented and gives you an idea about what kind of commands you can write into this configuration file we will see in the demo you can also use the output of the ddr server script server script to get an idea about what's going on and what kind of values you can use so for example you can just generate a certain command via the plugin and plugin and then you can check the generated configuration configuration but again we will see that later on in so talking about the demo let's have a look at it and see how that looks on the command line so when you are running the command line tool the first time you can cheat a little bit by looking at the server output and the ddr server is printing out the command which it is actually executing executing and this is the command and the configuration which belongs to the command command which was automatically generated by the idler plug-in so you can start with using the gui and then you can have a look look at the configuration file and the command line command line and this gives you probably a good idea how these things are actually working and for the details again you have the the documentation in the docs directory of the github repository so let's just run this again once it is finished we see that it has written all these trace files to the samples directory so let's move over to the samples directory and we see that we have all these generated traces here but there are also further files like the dder ddr process trace trace and the protest phrase is a file which is is logging all the processes which were started by the sample and pretty much the same but for threats are these are these threats files they include the information about which threats the certain processes have started so if we are looking at the processes file for example we see that the original sample has executed a second a second process the evilnelve dot xc and ddr is following all the threats and processes which the original sample original sample is creating so we have seen earlier in ida the trace which was imported for the main the main threat and for the main process we haven't seen the second process in either either so far but the trace was generated so we can go back to heider and we can import import this one and then we have all the instructions instructions which were executed by uh the process even malware x the second instance of the buffer test.exe file and let me show you how that works okay so we are back in ida and you hopefully remember the sample which we have we have looked at earlier the buffer test.one that one which was checking if its name was even melba.exe and if that is not the case it is copying copying itself to the temp folder and executing itself itself under this name again and you remember that that we were only following the main process here here yeah and we were able to highlight the instructions instructions um down to the point where the initial process exited but of course we also interested in the second part second part and what happens with the second instance and what kind of code is the second instance executing executing one thing we could do of course is we could just uh manipulate the code again like we've done before done before or now we are importing these these files which we have just generated so we can clear the highlighted instructions instructions and we are using the load ddr trace button to go to the directory where we have imported these traces which we've seen earlier so now i'm interested in the one of uh evil melville.exe so i'm loading this one one for the traced commands and i'm loading this one for the api calls so api calls so now we have successfully imported these traces one for the api calls and one for the installer instructions installer instructions and now we can use ddr uh like we've done it before and you see that that the second instance which is running under even another axa is of course going this path and now we can proceed can proceed analyzing these these parts and look what it is doing like we've done before get source value or whatever helps us to understand uh what this sample is doing the last thing which i would like to mention is mention is that you should really keep in mind that it is good practice not to run traces for for too many instructions keep in mind that stopping at every instruction and collecting all the registers and all the memory informations of course it takes a lot of time and consumes a lot of memory so it's usually good practice to just mark the basic blocks you are interested in interested in so for example if we are just interested in the values of these two basic blocks it is usually a good idea good idea to just select them at basic blocks to list and this one select add basic block to list now and you can see that it was successful here and then run the trace just for the basic block list so and it is done that is much faster you don't have to wait for too long and you're not creating gigabytes of files just keep in mind that if you are doing that that you now only have these two basic blocks blocks in the database so all the others are not traced or not in the database in the moment the moment which means that if you try to get a value value from here uh of course you're you're running running into an error it's not in trace that can be a little bit confusing in the beginning if you don't think about it it but usually you are getting pretty quickly used to that and now you can analyze the functions which you are actually interested in interested in get source value or whatever you are looking for looking for okey dokey that's it so you have seen that ddr is not replacing your brain but nevertheless it speeds up a lot of reversing tasks and it is usually a pretty big help in analyzing malware samples with this i'm done for today and i'm giving back giving back to the folks from the blue team village
