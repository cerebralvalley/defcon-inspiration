- So, hello everyone. Thank you for tuning in. Today we're gonna talk about Apache Spark. Now I will just assume that while some of you may be familiar with the technology, a lot of you did not have actual chance to play with the cluster, so we'll take it right from the beginning. So we'll see how spark works, what makes it so popular, how to view some common and default settings and obviously some forms along the way to bypass authentication and execute your code, right? So let's get going. Right? So our story takes us back back to 2008, when the economy was not much better than today's. But Internet Explorer was still a thing and the big tech companies were doing what they did best, quietly collecting the world's data. And when you gather that much data and I'm talking about terabytes and terabytes of data, performing even the simplest calculations becomes quite challenging. Now you can either get a mainframe for half a million dollars plus whatever it takes to get an intern to work on it, or you can actually distribute your files over thousands and thousands of servers, that way your computations are small though multiplied, and that's one way to handle the load. And the standard way of doing so I would say is by using the Apache Hadoop Framework. Now if you've ever worked with Hadoop, you must know that it's actually quite a hostile environment. It's a fairly complex environment. So first of all just to give you a quick example, you need to fragment your files using the HTFS file system. So you end up with multiple fragments on multiple servers of these files, and then you need to do coherent computations on these small fragments, you need the MapReduce Framework. But then you end up with thousands of processes running on multiple machines. So you need a way to schedule them into a yarn, but then you wanna do some SQL querying, and so you need Apache Drill, et cetera, et cetera. So you end up with this plethora of tools each with their own latencies, each with their own learning curve et cetera, just to cover all your needs and data analytics. So just to give you an example, this is what it takes to do a simple word count on MapReduce. This is the first page of Java, this is the second page, which is downright outrageous for a data processing solution, right? In our story, we're around 2009 and there's this guy named Matei Zaharia, who was at Berkeley I believe at the time, who I imagine looked at this code and went, "Are you fucking crazy? That's not gonna work. "A word count is a simple operation, "a word count should be done in five lines of code." Apache Spark was born. Now this is not the official story, is that he was working on something called Mesos, which is a resource manager. And he invented spark as a proof of concept. But I like my version better. So anyway, needless to say we'll go through the code later on, but right off the bat you can see that you can do pretty much the same processing that you would do in Apache Hadoop, except that you can do it at 10 times less code in spark. But it's also much faster. So it's three times faster and 10 times less nodes, so it's 30 times faster. You see, Hadoop will flush everything to disk whenever it gets a chance, whereas spark try to keep everything in memory. It was developed in an era where memory was getting cheaper and cheaper, so it kind of makes sense. But if you're running in a cloud environment where you pay per the usage, having something that runs 30 times faster means that it's 30 times cheaper actually. And that partly explains the gigantic boom and popularity of spark amongst the big data companies. If you're serious about your big data processing and cluster font, you will be using spark for at least some part of it. And so yeah, as spark boomed, it was adopted by the Apache foundation and it grew to encompass many aspects of data analytics. If you can do SQL, streaming, machine learning, graph processing, it got you covered. So it's developed half in Java, half in Scala, but it also has some connectors in Python and R. So, yeah awesome. So basically when you have this powerful framework, suddenly everything looks like a nail. So if you're gonna do revenue prediction, you just hook spark to your, I dunno, Redshift cluster, download those financial statements and then you can do yourself. If you wanna do fraud analysis, user targeting, say you hook it up to your Cassandra database, your extra buckets, you upload or download those log files and then you do your computation. So basically what I'm saying is that Apache Spark is often at the junction of almost every important data store inside the company. And that's what made it interesting, at least for me. Because, well if you can access this tool, you're pretty much open to every database that's out there. So, you know, how is it protected? How does it work and what is it's security et cetera? And so what I did, I did what any sensible person would do. I went to the documentation, to their website, and I went to the security page. And lo and behold, from this magnificent sentence that says security in Spark is off by default, well fuck me. And what does it do again? Oh yeah, it's this gigantic framework, cluster of hundreds and hundreds of machines that have access to every database inside the company. And what does it do? Oh yeah, it does distributed data processing. Or as I'd like to call it, it's really distributed remote code execution, so I though this would be fun. And I hope I piqued your interest because really that's what sparked the whole endeavor, no pun intended. And I hope I got you interested enough to actually continue along this presentation so we can explore how it works and some worms that I found. Right? So yeah, in keeping with DEF CON tradition, I think it's time for drinking. I should not do this online, but Oh well, what the heck? So to DEF CON Safe Mode. Cheers. Yeah, that will kick in later. Anyway, so what is Spark and how does it work? I hope it's gonna be one take, because I can not do that five times. So how does it work? So as in everything in distributed processing basically, we start with a bunch of machines that we're gonna call workers. Now these workers are just basically brainless machines that will execute whatever is sent their way. Now on each worker, you find a JVM process or a spark process that's called an executor and that's gonna do the actual execution. Now you can have many executors on each worker and it's basically executer that defines your power in terms of parallelism. So if you have 10 workers and each worker, or each machine basically have three executors, so you can run 30 tasks in parallel. you have some default http ports that gives a status of what's going on on the machine, not interesting. But you have the random RPC port that is set for each executer. And it's this one that the rest of the cluster contacts in order to send and receive a status from the worker. Now the worker is fine, but the most important piece or one of the most important pieces is the cluster manager, and that's our second piece. And the cluster manager's sole job is to actually schedule the applications so the work that is coming and say, "Oh yeah, this application is gonna run "on these two workers. "That one is gonna run on the other three workers." So it has a status of the workers. What they're doing, are they online or not? Are they busy or not, et cetera. I know. Every component inside spark communicates using this spark RPC protocol that we will detail a little bit later on. And just to show you what it, you know-- To demystify this spark cluster, this is what it looks like. This is the basic UI of the spark master on port 80, 80. Oh, sorry about that. So yeah, you can see that we have two workers. One which is dead. We have an application that's running on two cores and since each worker has one core, so this application is running on two cores. It's being distributed over two machines. But the most important port really is port 7077, because that's the one that we're gonna contact in order to send an application, register it and schedule it to be executed in parallel on these workers. And it's gonna be these basic hypothesis that we're gonna make along this presentation that we have access to this specific ports. And the third component, and indeed the most important, is the driver. And the driver is really the brain behind the application orchestration. It's the one that's gonna slice up your application in small tasks, contact the call center manager, receives the worker and then send, distribute your workload to these worker and, you know, make sure that everything is going smoothly and aggregate the result. And the driver in a typical case, or typical scenario is gonna be running on your laptop. So you enter in that work, you have the cluster manager running somewhere, you have the workers running someone else, and you're gonna boot up the driver, right? To an obligation in Java, Scala Python, or whatever, and then send it to the cluster manager to be scheduled on the worker's, hopefully to execute some code. So that's one setup that's kind of common, right? So recon, so let's take this a specific scenario that I just listed. Let's say you end up in a network and you wanna hunt for some spark in order to exploit it, how would you go about it? Well, the first thing you wanna do I guess is basically if you are in an AWS environment, you can simply use the API, if you have proper access rights obviously, to look for every machine that has Spark master or other keywords in their labels. Similarly, if you're in a cube environment, Kubernetes, it's the same thing. But what if you end up in a more traditional network where you have to N-Map the shit out of things, how can you find that cluster? Cause you see N-Map does not really support spark. So we're in the kind of a pickle here. So the first thing we wanna do is to be able to fingerprint spark properly using N-Map. And to do that, I simply worked out a spark lesson on my lab, I put some wireshark in the middle and I analyze the traffic. And if you do that, this is the blog that you're gonna see. And basically the way to decompose this is fairly simple and it's quite repetitive actually, once you get to know spark RPC. So this is the spark RPC I was talking about. It's always starts with the same things, the same header is gonna be most of the time, 21 bytes of data followed by the payload, right? And this one bytes of data, a common header when you send spark RPC commands is composed of seven bytes of null bytes, followed by two magic bites. I call them magic bites, but basically they depend on the RPC and one that you're trying to reach and the type of message that you're sending. So in this case, I chose the example of verify endpoint, check existence message. And it's a magic bite is C305. I don't know if it's a worker sending a heartbeat message to the cluster manager is gonna be to 2B0F whatever, but in this case it's C305. But anyway it doesn't matter. Next we have eight bytes of data, like random data. You can put anything you want here, they'll just be echoed back by the cluster manager. And finally we have, four bytes of the size of the data that's gonna follow, so the payload that will follow. what is this payload? Well, you have a bunch of data, IP addresses that we don't care about, but the most important thing really, the heart of the payload if you will, is the serialized object that you can see here. And so it's a serialized JBM object called Check Existence. And if you look at the source code, check existence is actually a class, well it's a Scala case class. But just think of it as a regular class, which is immutable. And it has some interesting other properties in Scala that we simply don't give a fuck about. But yeah, it's a class. So Check Existence is a class and it has an attribute name. So basically the client or driver is sending this serialized class, to the cluster manager to see if there's an endpoint bearing the name that it sent. And the name that it sent is obviously master. So it wants to know if there is a master RPC endpoint listening on the other end of the communication. And if that is the case, the cluster manager will respond first with the 21 bytes of data that you can see it echoed back what we said before, right? In a different magic RPC, magic two bytes. But anyway, and if there is indeed an end point, it sends a Boolean that is set to true. So obviously, I took this special exchange because you can use it in N-Map to fingerprint the cluster manager. But you have to imagine that all Spark RPCs are these exchange of serialized data that are prefixed with some type of header. Either 21 bytes header, or 13 byte header that we will see a bit later on, but this is the main way that spark components communicate with each other, just serialized objects. So once we have this exchange all mapped out, then we can use it, to write a Lua script that will replicate the same thing. Once you do that obviously, once you deconstruct it like this, it becomes quite obvious to do it. So I wrote it N-Map script that's on my repo... Sorry, and I will push it to the real N-Map people once people have used it enough and debugged it enough. now we are able to identify a spark cluster. And you can see they've been true to their promise, authentication is indeed disabled by default. So this is pretty great. Now comes the interesting part. Now we know how to locate spark clusters inside a network. How can we execute code? Right. Cause that's what's real interesting. Now the standard way of submitting an application on to a spark cluster is basically you write your application in Java or Scala and what have you, you package it as a jar file, you boot up the driver and then you send that jar file to be executed or parsed if you will, by the cluster manager and the workers. Now, the problem is this. I find writing Java boring, sorry. And Scala makes me wanna shoot myself, So I decided to do it in Python. And luckily Spark supports Python. In fact, they have an official wrapper called pyspark, which actually takes care of all the nasty stuff that I just described. So it will put up the driver for you. It will serialize JVM object into Python object and vice versa and send it. And it will take care of all the nasty bits for you. So you just write a simple Python. And so yeah, pyspark, and if you pip install pyspark, it really is just a thin wrapper It downloads 200 megabytes of Jar file behind. So, hilarious. Anyway so from pyspark, we import these two classes per context per configuration. It's very straightforward, very intuitive I would say. So you defined the configuration. We're gonna name our application word count. We're gonna point to the cluster, port 7077 obviously. Now you have to define your IP, your public IP, that you're gonna bind on. We're gonna see why later, but if you forget this line, it will not work. No what what does it say so in the doc, but if you forget this line it will not work. you define the spark context. The Spark content is gonna be the client that's gonna initiate the communication, handle the communication with the cluster manager. when the Python interpreter comes to this line, it will boot up the JVM process we talked about the driver and it will initiate communication with the cluster manager. So quite naively and quite intuitively, I would say, what we wanna do next is obviously just send the code that we want to, be serialized and we want to be executed on the worker. So from sub-process P open the command ID. and this is really the first thing that I did. If you do that, it actually works. I got this result back and I was like, "Well, hang on. I know that user, that's me. "I have that user defined on my local machine, "not on the worker or the cluster machines, "what the fuck happened?" So it turns out I actually just executed a code on my own machine. So if you look at wireshark what happened? 'Cause I was curious what the fuck happened. I thought this stuff was supposed to serialize and send the command to be executed on the workers. Well what happened was, first, we actually sent this 21 bytes of data, check existence class, yada yada everything's all right. There is indeed a cluster manager listening. Then we send the register application class, which contains all the information of the application that we're gonna run. So you can see that we send word count plus other properties, right? The Cassandra responded with executed added class. So they gave us actually two workers, so this is great. What happened next? Well, the driver, so our instance of pyspark simply unregistered the application and called off the whole thing. So what the hell? Well to understand what happened actually, remember when I said that spark contacts pyspark will simply serialize whatever comes after the spark context line. Yeah, I lied. Well the thing is, in order to understand what happened, we basically need to talk about two concepts. First of all, spark APIs, and second, the notion of a DAG and lazy evaluation in spark. Counter-intuitively I would say spark API's are not methods they're data structures. So let's say you wanna open a file on Python or Java or Scala or whatever, you point to a file and then you call the method, the open method. This will only give you an object, a Python object that refers to a handle, a memory, whatever, but a single threaded object nonetheless. There's no parallels in there. And the proper way to do it in spark and still get that parallelism is actually to call text file method of this spark context class. So here SC is an instance of a spark context. So you point to a file you call it text file method, and text file method will grab that file, slice it in two fragments and then load those two fragments in memory. Similarly, if you want to load a list, you just don't define a list like that. This will give you only a single threaded object in memory. No, you need to call the paralyzed method, which will take that list, slice it in half or three or four, whatever spark deems necessary. And that way you will end up with two fragments in file on which you can perform work in a parallel fashion. Now these fragments are called partitions, and this is a very frequent term in spark that will come up a lot. Partitions are the main unit of work inside spark. And these collection of partitions are called resilient distributed datasets, which is really the most fundamental API in spark. There are others but they're ultimately all based on our other core. So once we have these partitions, then we can apply paralyzed work using something called transformations in action. Now what's a transformation? A transformation. Now let's say I wanna multiply every element on the list by 20 let's say. A concrete example will actually make it better. So I define a method called multiply, which takes an element, multiplies it by 20. And then I just called a map transformation, which will iterate over every element in every partition and yield, the same number of elements in the same number of partitions. So that's a transformation. There are other types of transformation which may or may not yield the same number of partitions and elements by the way. each spark API basically defines these transformations and actions to work on. And so what happens is, if I write this code, thinking that I just multiplied every element by 20, actually nothing gets sent to the workers just yet. Nothing happens. Nothing gets computed. I will have the same results as I had earlier. In fact I will have nothing. Why? Because when the driver starts parsing this application, all it does is it actually builds an execution graph. And this execution graph is called the DAG and spark terminology. But basically all it does is it follows all these calls that we made. So, Oh yeah. It knows that it's gonna call to parallelize, So it knows that it's gonna build an RTD, it doesn't care what's inside. It just knows that it's gonna build RTD with X number of partitions. Then Oh yeah, we're gonna call a map. It doesn't care what's inside the map. It doesn't care that we're gonna multiply every element by 20 that's not the driver's job, is only to follow to build this execution graph. So it knows that it's gonna apply a map. So given a set number of partition, it's gonna have and output the same number of partition. It's gonna continue to building up this tag. Maybe there's gonna be a filter map, which is another type of transformation, et cetera. And it's gonna continue building this graph until it hits an action, an action in spark is something that's gonna force it to do the actual computation. So things like save file or collect or take sample. All these methods that you can call in an RTD that will force it to actually have the right value. And once you do that, once you call a collect or an action in spark, then and only then will the graph be sent to the executors. The graph, mind you, not the code. Simply the graph. And it goes a little bit like this, or the driver will send to the workers, the graph of execution, the workers will go through this graph and they'll go, "Oh, I need to parallelize it, "but I don't have that list. "Oh, you know what? Give me that list." And the driver will send all these serialized objects one after the other. "I need to do a map. "Oh, but I don't have the function multiplied." You know, they ask the driver to serialize the function, the method and send it to the workers, et cetera. And then they're gonna apply those methods. And each worker will only apply these methods, will only follow the DAG on its own partition. So the worker number one will loop through the elements of the first partition. So one, two, three multiply by 20, 20, 40, 60, send back the result. The worker number two will do the same and the driver will aggregate the results and present it to the operator, us. So when you think about it this way, and you go back to our code earlier with our sub-process, that was just sitting there naked alone in the wilderness well, of course it didn't make it to the workers. The workers are only ever aware of the DAG. And so, if we want to execute code, we need to somehow embed it inside the DAG. So we need to put it inside a transformation, hence the following code. So here we can see we define a lambda function, anonymous function, and inside we put our Popen command execution stuff. And obviously we need to follow it by an action. Otherwise, nothing gets sent. And this is the skeleton that you need to follow, or the basic skeleton you need to follow in order to achieve code execution on spark, across multiple machines. Now, if you don't want to be bothered by creating RDD's handling parallelism and whatnot, I took this skeleton, put it in a fancier way or something like that. And I incorporate it in a tool called Sparky that will take care of all this stuff for you. So you just point to a closer point to the IP that you want to target, send the command that you want, and then how many times, on how many workers you want it to execute, and it will do that for you. It will build RDDs, build the APIs, call the transformations, the right ones in order to achieve the code, the execution that you want. And this is what it looks like really. So again, I'm gonna publish it on GitHub for everyone. So yeah, you will have chance to look at the code. So you can see here, we just point to the cluster, so we specify our public IP address, the command that we want to execute and the number of workers that we wanna target. And you can see here that we have execution on three machines. So great. I'd also had some, you know, I embedded some scripts to make it easy to run in an AWS environment. So like, dump some AWS secrets if you want, or search for files that are sometimes dumped by spark in some work folder. So you can search for AWS keys or secrets or whatever. So it automates a lot of the pentests in spark so... So that will get you started. Next. Now in this type of the execution that I just showed, there are some problems that are quite annoying. Let me go through them very quickly. the Python version on the driver needs to match the one on the worker, down to the minor version otherwise it will throw an error. Now there's a way... Yeah, so you can see even if you have a worker that's on 3.5, and your driver is on 3.7, you're kind of screwed. So there's a way around it. Since the check is simply done by checking the version info, you can just override it. So you can see here that I put it like 3.5. it will work so long as it is not too much of a gap between the two versions. So you can get away with 3.6 and 3.7 because the serialized objects are similar to some extent. But you cannot get away with a 3.4 and a 3.7, there's too much of a gap. And the way around that is simply well to override the file, but also to take advantage of the fact that a spark cluster or pyspark will use use pickle deserializer to deserialize the object. And in pickle, you can specify a method if you define the method, with use of an object, It will get executed first in the process of de-serialization. So before it fails, the de-serialization process later on, it will have executed the command reduce, which contains the execution command. And so, we simply need to define the object and send the method as part of that object and will get you through. So even though the job submission fails, you still achieve code execution over however many workers you decided. And I did not incorporate it inside Sparky because it's just a hacky way. Really the easiest way is simply to align your version of of Python with that of the workers. But oh well, this is just a fun trick to share. But really the most annoying problem is actually a network problem. See what happens when we just run the application, we send an applications, is we contact the driver... Sorry, the driver contacts the cluster manager on an RPC port 7077 as we just saw, and will send a register application class, along with the parameters, the applications name, et cetera. The cluster manager will receive that applications, "Oh yeah, I need three, four workers whatever. "I'm gonna fetch the workers, "send them to the driver along with some status data." But then just when you would think that it would be the driver's job to contact the workers, actually it's the workers who initiate the communication towards the driver. So they will be the one contacting the driver on a random RPC port calling, scheduling port to say, "Hey, we're ready." et cetera. And that's annoying because you don't necessarily have this flow of communication open. Cause the driver is... your computer is usually behind the NAT or whatever. So you've got a screwed, not only that, the workers who initiate another communication on another port altogether to call him the block manager ports in order to receive some data blocks, in order to request some serialized objects, broadcast some variables and whatnot. So that's annoying. So in sparky, basically I default the settings to some common ports in order to evade or it works in some settings, but not all. But really the solution is to use what we call a cluster mode. And in a cluster mode, you don't run the driver on your laptop. You simply contact the cluster management on port 7077. And then you tell it, "Oh, here's the application to execute. "And please also set up the driver on your own cluster." And that works pretty well. you only need access to port 7077. The downside is that you cannot use pyspark you cannot use Python. So I had to redo that skeleton I showed you earlier in Python, I had to do it in Scala. I had to build a jar file that will paralyze... That would execute whatever command you give it across however many servers that you want. So yeah, the code is again on GitHub, but you don't need to touch this one, it's this compiled jar file. So you can just use it as it is. It's yeah. So here's a quick example. So you can see we host this jar file that I just showed you on a server that is reachable by the workers, and you simply use Sparky again, to build that command to register this in question mode. So we just point to this jar file command we want to execute, and then we don't need any network condition, except for that 7077 to reach the cluster. And it works pretty well. And it will even auto delete itself, so you don't have to worry about all that. So this gets us through the network thing. So these are the main ways to execute code on spark that I found and on a spark cluster that has no authentication, which is enabled by default. let's go explore some other facets of spark. And there's this interesting interface called well it's the rest API. And it is interesting because when you go through the documentation, you can see that it's available on port 6066. And when you look at the documentation, it's really a read, okay, boring API. I was like, "Yeah, whatever." And I remember a few days later I was digging through the source code, like really lost inside the source code amongst weird scholarship Funkster's options and whatnot. And I remember I saw this keyword with the the rest, I was like, "Oh my God, I know that is." And I grabbed it and I followed it. And I came across this piece of code that says something like, "Submit requests, create." I was like, "Wow, I like to submit stuff. "I like to create stuff." So what does this thing do? And you follow this create submission request and there's talk about app resource, main class. I'm like, "Well hang on, app resources references a jar "usually in the spark code. "I thought this was a read only API, "what the hell is going on?" So I followed this create submission request and I came across this app resources indeed and application jar. So basically the rest API, even though in the documentation it mentions it's only a read only API actually in the code it accepts a jar file that you can send it. Which is kind of amazing. So all you have to do is simply build a JSON file, takes up a JSON input. So you build a JSON file with the right property. So the app resource, main class, spawn properties and so on, and then you can send your jar file and achieve code execution. And we can take that Jar file we used earlier, So simple add jar, send it a command arc. So it just accepts commands to execute in base 64. So this is really, I think a, who am I? Or some shit like... No, not who am I but touch some file and some folder, maybe 64 it's very simple. But there's different properties and you can just curl it this payload, then it will execute code. It's as simple as that, This is just to show you what it looks like. But yeah. But again, if you use this method, the driver, you don't need it to run on your machine, its gonna run in the cluster. So it's gonna run in cluster mode. And one thing I will mention, if you decide to use the rest API, or Cluster-Mode in general, you can send whatever jar file you want. You don't have to use my specific jar file. But you will get execution on one worker, but if you want to execute on 200, 300 or all the workers inside the cluster, then you need to use spark API. So you need to use some version of the spark, Like spark API I just showed in the Scala code earlier, or a Java code earlier. So anyway, so this was fun. And I thought, "Well I wonder if anybody blogged about it?" And I Googled create submission request, I called this and I came across the first results on Google was this script. And it was already published. And I was like, "Fuck, so close." Not only that, there was actually an exploit that was already published. It was actually a Metasploit model that was published by these fine gentlemen. I was like, "Damnit." sometimes you keep your head in the wheel and you forget to do basic research, but it was just, you know, months away. Anyway, let's get to the real stuff. Right? So authentication. Let's say you come across a cluster that has all the right switches on, right? So it's enabled authentication, and the way to do that in spark is fairly easy, you just enable authenticate to true. Then you define a secret as you can see here, and yeah all the components you do... You need to do that on every component inside the cluster. So every worker and on the cluster manager and the driver and that way, they all have a shared secret to communicate with. And if you have an authenticated cluster and you try to communicate with it, you will get this delightful error that says, you know, You send this check systems class, and then you get this illegal state exemption Sasl message. Now Sasl is an authentication protocol, challenge response protocol, based off a shared secret as you might've guessed. But yeah basically the server sends nonce to the client and the client hashes that with the nonce, hashes the secrets with the nonce and then sends the response plus other parameters that we will see, but fairly simple. But the funny thing is that even though the driver you configure the driver to use the secret and you tell it to authenticate, it will first attempt an unauthenticated session. So it will first try check existence, get slapped with this error and then it will try an authenticated message. It's kind of weird. So anyway, what does it look like? An authenticated session? Well, first the driver will send 21 bytes of data, the header, the usual Header. With the magic byte this time set to 2BO3 as opposed to C3O5 earlier. Followed by eight random bytes that we don't care about the size of the payload, which is definitely wrong in this case, because we're not talking. It's not 176 bytes. the most important thing in this authentication sequence is that it sends the spark Sasl user, which is gonna be the user used inside the challenge calculation. the cluster manager responds with 21 bytes of data as usual the header. And then it follows by the Sasl parameters to perform the challenge computation. So you have the nonce random value realm QOP which is are we doing authentication or encryption or both? These delightful algorithms to use in case we're doing Sasl encryption. Now just to reassure you spark does not use Sasl encryption by default. It uses AES, but it's definitely possible to use it as a fallback. And the algorithm... And this one is indeed used to calculate the hash. And the calculation of the hash, we're not gonna do it honestly. It's detailed and RFC 2831, but it take a bunch of data, you hash it in MD5, you take the output you combine it with the nonce and other stuff. And then you hashed a bunch... You hash other types of data and then you combine everything, which gives you the response to the challenge. Again, if you want to know more about it, Google it, RFC 2831. The driver sends the response to the cluster manager, if everything checks out the cluster manager acts. And then it can follow the classic flow of communication. So basically, register class, register application, or register with application executors added. Yada yada. Now here's what bothered me, or maybe not bothered me, what ticked me a little bit is that the dis-authentication step was only ever done when there was an RPC endpoint that was prefixed... A non RPC communication that was prefixed with those 21 bytes of data. But it turns out that is another header that is sometimes sent before payloads that prefixes payloads. And this header is only 13 bytes long. And is it's usually sent inside the middle inside the TCP stream. So it's inside the same TCP session. So I did not have much hope. I was like, "Well, maybe the class actually remembers "that it's an authenticated session. "So that's why, et cetera." But nevertheless, I thought, "Well, you know what, "let's see what it looks like. "Let's see if we served the communication from here, so we don't send this check existence header which prompts the authentication message. Let's just send, start communication from here, by sending these 13 bytes of data followed by the register application, because that's what we really care about. Right? We want to submit an application that's what we really want. And so if you begin communication at this stage, you first sent 13 bytes of data. It looks awfully similar to the 21 bytes header but basically... I'm sorry. You have four bytes in all bytes followed by the size of the payload plus the header so the size plus 13, magic bite nine which didn't change for some reason and four bytes of data, which are the actual size of the payload. And then you send the serialized register application with all its attributes, applications name, yada yada. And I sent this and I was expecting a massive header, you know, those JVM hundred line headers. But instead I got a Header, followed by register application class. And I was like, "Is it me? "Or is it just possible to bypass authentication?" I was like, "Hallelujah, it's amazing." Now bypassing authentication is fine, but what can we do with it? Cause I was not gonna recreate the 80 next messages that were following along. It's not possible. So what can we do with just one message? Well, if you look back at spark, how it works, the register application class is the one that prompts all that workload on the cluster manager that's gonna contact the workers and say, "Hey, I've got an application for you. "You better get ready, spawn those executor's "and get ready to take the work." So I went ahead to the workers and I replied my message hoping to see some process spawn or whatever, but I didn't see anything. And the reason for it I figured it out a couple of days later, was because when you send this payload serialized register application, when you send it, the cluster manager receives it. I immediately shut down the connection, the program terminated. So the cluster manager did not have enough time to actually contact the worker, spawn the process and let it do its thing. So the way around it in order to give time, you simply need to do a sleep. As simple as that, you send the register application, you sleep for five seconds. That way you give enough time for the cluster manager to contact the worker, spawn the executer and do its work. And once you do that, once you do that sleep, I saw like I put a watcher on the worker monitoring for processes and I saw this process get inserted. And the magnificent thing about it was that it took as parameters something that I was defining inside that serialized register application, the block manager port set to 8443. And I know it was me because I'm the one that set it in order to have communication come from the worker to the driver. So I thought, "Okay, authentication bypass is a sure thing." Remote code execution is a definite maybe. Because you can influence parameters that are being used to spawn a process. So maybe there's some injection there. So I turned to this register application class and saw like, "Okay, what's inside? "What can we define as parameters as options "that we can take advantage of?" So register application again I guess, case class, which extends the deploy message, which implements the trade de-serializable, don't care about that. Application description, what does it do? Okay. So if you're like me, you're gonna ignore all this shit and go straight to command, right? Because the command class, okay. That command case class, it has the main class, which is the class of the executer to be executed. We can not override that, have arguments environments, a bunch of parameters. Now, if you just want to inject semi-colon and, or a pipe, it will not work because these parameters are sanitized. So we need a more clever way of hijacking the execution and the way to do it... I found the easiest way to do it at least, is using these Java options. Now, if you're familiar with the JVM, you know that you can actually specify or control the behavior of that JVM. Like, I don't know which garbage collector to use, how should strings be compressed? The amount of heat memory, et cetera. Well it turns out, if you dig into the hundreds and hundreds of Java options, there is one actually that allow code execution. So yeah, this is an example of a JVM option. So there is one that allows code execution, and it's called an on out of memory error. And you just specify command that will execute it. The catch is that, it only executes it if there's an out of memory error like its name said. So if the executer or a process fails to allocate heat memory, then it will throw this error and then it will execute code. So how to make the executer trigger this error? Well a simple way is simply to add another JVM option Xmx, which will set the maximum amount memory to a particular size like one megabyte or two megabytes. And once you combine these two properties, then you have your code execution. And so ideally would like to do something like this. Obviously we cannot do it inside the code because while the driver will attempt to authenticate, which is no good, and anyway you cannot set the Xmx inside the drivers. So we really need to forge by hand basically that serialized object, embed these options and have a properly serialized object and send it to the cluster manager. And again, easier way to do it using Sparky. So I just point a... so this is just quick example to show you how to exploit this form. So this is the reverse shell that we want to execute, right? So I just point to a Sparky, I wanna execute this code, but as you can see, the cluster requires authentication. But if I add -b, then it will take advantage of this form and you can see it bypass authentication and just execute the code directly. And it's gonna execute it on as many workers as requested. So yeah, we just bypassed authentication, which has executed code on workers. So yeah. So now that we understood the hacker intuitive way of finding this form, let's go through the code. Because I think it's interesting. if you see, I made the distinction between the 21 bytes payload that was prefixed with 21 bytes of data or prefixed with 13 bytes of data, there's actually a difference in the dispatcher. So the register application is handled by the process one way message. And a process one way message called the receive method of the RPC handler, right? So it delegates to the receive. And you can see that at no point there is any authentication going on. Here the delegate receive will actually perform the work. If you compare that with the other receive method, which actually asks for authentication, you can see that it's quite beefy and different. So the correction basically the patch was to protect that same receive method, and another one used in streaming, which was affected also by this same time types of checks. And this was labeled CVE-2020-9480. I disclosed them on 24th of the same... which was very horrible, but anyway... And yeah, it was fixed by the Apache spark team. So thank you very much for the work that you guys have done, it's amazing. So in summary, what I wanna say is spark is awesome. I'm gonna say too bad security's not taken seriously. And I'm not talking about the folks who manage security there. I'm just referring to the fact that security is off by default. I don't think that should be something that we should have in 2020, especially for something that's a framework that does data computation. We all know how data is valuable. So I say it in reference to that. So that's... Anyway, I hope that will change. And finally, yeah, we only covered spark standalone where the cluster manager is actually the one doing the work. But it can have other setups where it's actual yarn that's doing the work. Or I think even the Kubernetes API server in spark 3. So yeah, there's a lot of ground to cover. So if you want to dig into it, go ahead. The code to sparky, if you wanna check out these spark API's the serialized object that's being forced not to bypass the... All that stuff is there. I hope you got enough of this talk to actually dig into it, because that's really the only way to figure out how stuff works. So yeah, your contribution are most welcome. And if you wanna hit me up on Twitter to talk about it more, please do not hesitate. And looking forward to talk to you guys and talk to you folks in the QA. Thank you very much. Bye bye.
