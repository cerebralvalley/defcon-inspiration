- Hi everyone, welcome to our talk, "Hacking the Supply Chain", about the Ripple20 vulnerabilities that haunt hundreds of millions of critical devices. A little bit about us, JSOF, so JSOF is a software security consultancy, we do a lot of security research, penetration testing. We help companies with their secure development processes as well as some training. My name as Shlomi Oberman, I'm a co-founder at JSOF. Today with me today we will be speaking Moshe Kol, a security researcher at JSOF and the finder of the Ripple20 vulnerabilities, and Ariel Schon, a security researcher at JSOF that was also heavily involved in the research. We'll be talking about Ripple20 in general today, explaining what it is and how it evolved. And we'll be going into detail about one of the vulnerabilities, the vulnerability that we find the most interesting out of the Ripple20 vulnerabilities. Short spoiler here, it's one CVE, but we'll actually talking about several vulnerabilities hiding behind this one CVE. And then we'll be going in depth into exploitation on a specific vulnerable device with a specific configuration. So what is Ripple20? Ripple20 is a series of 19 zero day vulnerabilities in a TCP/IP stack called Treck TCP/IP. We say 19 zero day vulnerabilities, but it sort of depends how you count, there are quite a few more discreet bugs in these 19 vulnerabilities, and two of the vulnerabilities were reported anonymously at the same time as we reported, so it depends how you count. These vulnerabilities were amplified by the supply chain to affect hundreds of millions of devices and all kinds of verticals. Medical, a lot of critical devices, industrial control, enterprise and a bunch of others, we'll explain how this came to happen. Out of the Ripple20 vulnerabilities, four of the vulnerabilities are critical remote code execution vulnerabilities. And in addition eight vulnerabilities, or eight CVs are medium to high severity vulnerabilities, some of which could also potentially lead to remote code execution pending further research if anyone wants to go into that. The vulnerabilities affect hundreds of millions of different devices, from brand names you all know. We currently have over 100 different vendors with suspected affected devices. Meaning the different vendors are looking into these devices to see whether they are affected or not. These include Fortune 500, Global 500 companies, such as those you see on the slides, as well as one-person specialty shops. And quite a large range and diverse range of vendors. You can see the Ripple20 vulnerabilities in medical devices when you go to the hospital, in your home, in your company, when you turn on the light, when you turn on the water. Transportation devices are affected by Ripple20. Pretty much everywhere you go you'll see IoT devices in general, as well as specifically devices affected by the Ripple20 vulnerabilities. Our current assumption based on what we know, suspected devices as well as the confirmed devices, meaning if and when vendor's confirm the devices are affected, we assume every medium to large organization in the US has at least one vulnerable device. Of course some networks have many more devices, such as data centers or utility companies. But even any other kind of organization might have an affected printer. A hospital might have an affected IV pump, et cetera. How did this come to happen? This happened because of the supply chain. And security in the supply chain. Not in the sense that somebody put a backdoor into a component built in another country. But in the sense that a piece of code, a library had a vulnerability, like any software has vulnerability, and this piece of code was sold to an operating system, it was then sold onwards to a system on module, and the system on module embedded the operating system with our vulnerable library. And from this point nobody even knows they are using this piece of code, this piece of Treck TCP/IP stack. This system on module is then used on different devices such as IV pumps, and the whole chain becomes vulnerable. Just imagine what happens if one of the companies along the way goes bankrupt or ceases operation just how difficult it is to track down the vulnerable devices, and how complex it is to fix and patch. So we have a network of devices, a network of different vendors each selling to each other to create final products. Built like Lego from different pieces in different parts. And then when one part at the very beginning of the supply chain strategically located at the beginning of the supply chain gets affected by a vulnerability, different devices along the supply chain affected. This is what happened with Ripple20. And this is why it affects so many devices. So why did we choose to do this research on Treck TCP/IP, and why we think it's important for the security industry as a whole? For starters this aspect of the supply chain of vulnerabilities existing in the supply chain, not as back doors but just as regular vulnerabilities, and traveling, rippling from device to device, and this ripple effect this is something that is mostly unexplored. It's been discussed, we've seen some examples, Ripple20 is really a prime example what happens when a vulnerability is located this deep in the supply chain, and exists for so many years. So this one vulnerability, multiple products, huge impact. And this is quite the beginning of the discussion of what should be done to fix these types of vulnerabilities. Another interesting thing is for us it was of course a good attack surface. And going forward there is the potential for zombie vulnerabilities. Zombie vulnerabilities, it's complicated vulnerabilities, we don't know if they are one days, we don't know if they are zero days, quite a few of the vendor's we believe won't be patching. Either they don't know they have Treck, or the devices can't be patched, companies went bankrupt. And so you have these walking dead vulnerabilities that are supposed to be patched. They have already been reported, but they still exist in the wild. So this is quite an interesting phenomena, for us as well as for the industry. Treck specifically was chosen because it's an extremely successful TCP/IP stack, used widely in the embedded device world in the IoT world, and has been available for over 20 years. So during those 20 years, 20 something years it spread throughout the supply chain from device to device, from vendor to vendor and reached its current's scope, current impact. One thing that's very interesting about Treck TCP/IP made our life quite challenging and made the life of network operators challenging, is that the Treck stack is extremely configurable. So every instance of Treck is slightly different, it depends when the vendor stopped support of Treck, what version of Treck they are using. It also depends how they compiled Treck. Compiled specific configuration options, specific features they bought or did not buy, how they are using what memory management they are using et cetera. So every version of the Treck stack looks different, and the vulnerabilities change, so some vulnerabilities affect different devices differently, some devices are affected by some of the vulnerabilities, by more extreme versions of the vulnerabilities, but others are affected by lesser versions. So very complex, very complex to understand. And of course at the very, very beginning of a long supply chain, so Treck doesn't incorporate any other components, which is not something that we can say about any of the other devices affected, which all incorporate different components including Treck. The research itself because Treck is so configurable we had to take a few data points. We used six different devices, we spent different amount of time on each device in order to understand what we're dealing with, in order to find variations to the vulnerabilities in order to understand whether certain vendors made changes of their own to the stack or to the compilation, and so we used six different devices with differing architectures in order to understand what's going on, where this stack is and how the stack is used. The research took approximately nine months with changing differing intensities throughout this time. Started September 19th, we reported to vulnerabilities June 20th, June 2020 of course. And then we discovered some things after-the-fact as well. For example, while trying to bypass our company firewall in order to see if the vulnerabilities route over the Internet, we discovered that because Treck TCP/IP stack allows for encapsulation, you can also encapsulate these vulnerabilities attempting to bypass firewalls. So we were able to bypass firewalls, at least some of these vulnerabilities will route over the Internet. As with any research involved in IoT, especially with so many different devices, we saw some pretty strange architectures, pretty strange firmware, and firmer structures. The device we'll be talking about today as one of these unique architectures. We can't talk about everything in the scope of 45 minutes, of course these are very complex detailed things, the exploitation of all the vulnerabilities is something that we won't be able to go into in-depth. We did release two white papers with full details as well as pseudo code and specific references to the devices and the memory configurations that we exploited, feel free to look them up on our site if you want to go into further technical detail. A little bit about the vulnerability or the CV that we'll be talking about today. So CVE-2020-11901 is a critical vulnerability in the Treck DNS resolver component. So client side DNS vulnerability. Once successfully exploited, it allows remote code execution as we've demonstrated and as you'll see today. The reason we think it so interesting and the reason we chose to talk about it, besides being remote code execution, is the fact that the DNS traverses NAT boundaries. So a device within your network will issue a DNS request which will potentially travel over the Internet allowing sophisticated high-end attackers to perform an attack from outside the network into the network itself, which is different from most vulnerabilities we see in TCP/IP stacks. Behind-the-scenes, four vulnerabilities, one what we call an artifact, so that's a bug that made it easier for us to exploit. And these vulnerabilities vary over time, so over the years Treck changed the stack and the vulnerabilities changed, so we see different versions of the vulnerabilities in different devices, depending on when they stopped using Treck. They also change between vendor configuration. One of the interesting thing that we realized during this research, as well as during the disclosure process, we realized that the supply chain complexities are a real problem for information security, they are a real problem for the vendors, they are a real problem for the network operators and they create a challenge for the whole industry. And we realize this is bound to happen again, we didn't research the whole TCP/IP stacks, there are other TCP/IP stacks, there are other pieces of code libraries that exist in the supply chains that have been lurking around for years. So this should also be the beginning of the conversation about what vendors and what the network operators can do to both reduce the impact of vulnerabilities like this when they happen using things like exploit mitigation. As well as how we performed such complex disclosures, and how we make sure as vendors how the vendor's make sure that their suppliers are performing secure development and testing the code, et cetera. So this is a wake-up call to some of the vendors and some of the network operators that this will happen probably again and again, and this can be prevented, or at least the impact can be reduced using the right techniques. Now I'm going to hand over the microphone to Moshe Kol, he'll be talking about the different vulnerabilities, where they are in the code and what they look like. - Thanks, Shlomi. Hi, I'm Moshe, I'm security researcher at JSOF. And I want to talk about the vulnerabilities that comprise CVE-2020-11901, also known as the DNS bugs. So first we need to refresh your memory about the DNS protocol. So DNS is a co-Internet protocol designed to mark between domain names and IP addresses. It's a client response protocol with a planned server architecture, so the client resolves the name by issuing a query to a DNS server. So for example if you browse to www.example.com your browser issues a DNS query of type a to one of the configured DNS servers, and the servers look up the name and returns a response, in this case of type a, the value of the response is and IPv forward address. So a little bit about the record types. So DNS servers can return multiple answers in the same DNS response. An answer is specified as a resource record. Each resource record is associated with the name, we talk about the type and class field shortly. The TTL specifies the number of seconds this record is valid. The value of the record is specified in the RDATA Field. Length is specified in the RDLENGTH field. So what questions and answers have a type in the DNS protocol? Some of the common types include type a, which specify and IPv address for the queried domain. Type CNAME, which defines an alias, a canonical name for the queried domain. This provides a level of interaction. And type MX, which defines a domain name of a mail server for the queried domain. So if for example you send an email for the Gmail.com, your mail client issues a DNS query for Gmail.com it gets back a domain name of a mail server for Gmail.com, and because this is a name not an IP address, this name needs to be resolved further into an IP address, the resolver issues a DNS query of type a in this case in order to resolve the domain name of the mail server. In practice most DNS servers simply hand in their IP address along with the name, but nonetheless, the functionality needs to be supported by the DNS resolvers. So little bit about the domain names encoding the binary format. So domain names are encoded as a sequence of labels, so www is a label, example is a label. Each label is preceded by length byte specifying the number of characters this label occupies, and then a domain name is terminated by a zero length byte, and according to the RFC, there is a maximum label length is 63, this will come up later. So what the designers of the DNS protocol noticed is that there is a lot of repetition inside the DNS packet itself. So in an effort to reduce the size of the DNS messages they employ a simple compression scheme. And this scheme compression is achieved by replacing a sequence of labels with a pointer to prior recurrence of the same sequence. So here you can see a sample DNS response packet, you can see Gmail.com is specified as an offset hex C from the start of the packet, and it so happens that Gmail.com needs to be specified multiple times inside the packet. So instead of specifying Gmail.com literally in the packet, again we simply use the compression features. We point to the previous occurrence at Gmail.com. So if we want to write SMTP.Gmail.com, we need to write only the first label literally, SMTP in this case, and the next two labels are specified using the compression offset. So in the compression schemes, compression pointers are encoded in two bytes, the first bytes begin with 11 as the most significant bits. And the other 14 bits specify an offset from the start of the header. So as Shlomi said, the vulnerabilities reside in the DNS resolver of TCP/IP and we find them in the DNS passing logic in the stack, specifically in the function called TFDNS callback. Here you can see a snippet of pseudo-code from this function, specifically the parsing logic handling MX resource records. So what we can see here is that the function computes the length of the MX hostname, based on the length a buffer on the heap is allocated, and then the MX hostname is copied as ASCII into the just allocated buffer. So what we can see from this snippet is that TfDnsLabelToAscii the function that is responsible for the copy it is not aware of the length of the buffer being allocated. What it does is simply copied bytes from the encoded names until another byte is reached. So this means that if for some reason expand label length returns a length value which is too small, we will have a heap-based buffer overflow vulnerability. So this motivates us to look further into expand label length and examine its operation. So you can see a pseudo-code of expandable length. What this function is basically doing it sums up over the length bytes while nine are in compression, so in more detail it reads the current label length, then checks to see if there is compression or not. If there is no compression which is the current case, it adds the current label length plus one to the total length variable, which is later returned from this function, an advanced name for that. If there is compression it rates the compression offset, it computes a new label pointer based on that offset. And then checks to see that a new label pointer points before the initial label pointer, so this means we can only jump backwards from where we were. And the process continues from this new label pointer. So as you can see there is no bound checks on the packet buffer here, so this led us to the first vulnerability, a read out of bounds vulnerability. This could result in a denial of service if for example while iterating over the length bytes we read from an unmapped page. But more interestingly we can cause an information leakage vulnerability. TfDnsLabelToAscii, the function that does the copy and has no bound checks either, this means the data from the heap could be interpreted as an MX hostname, this MX hostname is later resolved by the client in an attempt to get an IP address so this means that we can leak data from the heap inside the MX hostname itself in the query. This vulnerability affects Treck version at least 4.7, and it was fixed in later versions as we will see. We don't know the exact version of the fix, but nonetheless the vulnerability still affects devices in the wild. Due to the complex supply chain effect and nature of the embedded devices, so some don't receive updates. So this is nice and all, but we are looking for ASCII. So let's go back to the function that computes the length and examine its operator further. So there are more issues with expand label length, so there is a limitation on the maximum domain name length, the limitation is 255 characters, and this limitation is not enforced in expand label length. Further, it does not validate the characters of the domain name, they should be all alphanumeric and hyphens, but it doesn't validate it, so we can embed bytes within the name itself. And last but not least, the total length variable is told as an unsigned short, 16 bits width, and record that the total length is a variable that is returned from the expand label length. So what we tried to do is we tried to get our RCE by overflowing the total length variable. So what we need in order to pull this thing off is to construct a name whose length is larger at 64K. So we ask ourselves is it really possible, this is not trivial, this is possible over UDP. Can we overflow the total length variable within a single DNS response packet? The answer is yes and we used the DNS compression feature to achieve this. The idea was to nest compression pointers within themselves. So record that explain level length does not validate the bytes of the domain name itself, this means we can embed any bytes we want, as in this case we chose to embed compression pointers. You see in the example shortly. Keep in mind during the examples that we have two challenges to overcome, first there is a limitation on the DNS response packet size, the maximum size allowed over UDP by the network stack is 1460 bytes. And keep in mind that we can only jump backwards from our current level pointer, so we need to overcome this challenge also. So here we can see on a slide the basic construction we used. This is the name arranging the matrix-like form, each row in this matrix has length 16. The blue squares represent compression pointers, and the pink squares represent potential bytes. We talk about those shortly. So let's assume we're starting expanding the name from this offset, offset hex F in the first row. We can actually achieve this in practice by using another compression pointers that will land us exactly on that spot, but for now take this as a given. So if we start expanding from this offset, this byte is interpreted as a length byte. In this case there is no compression, so the function skips 0F plus one byte and moves to the next row. And as well as it adding 16 to the total length variable, the process continues. Notice that we stay in the same column because of the special matrix shape, until we reach a branch byte. And the purpose of the branch byte simply to lead us to the next compression pointer, in this case the branch is 0e, so we land in this compression pointer. We know that this is a compression pointer because high label is C. So what the function does is read the compression offset, in this case is 0e, it then checks to see that way point backward from our initial position, so our initial position was hex F, so hex E is less than hex F, so continue expanding the name from here. And the process continues with each branch byte, we reach another compression pointer and you can see with this compression trick the total length variable nearly doubled itself. So if we continue until we reach a null byte, in this third example we'll reach a total length value a 1500 bytes. Which is pretty neat if you consider that the name itself only occupies 128 bytes. So of course this doesn't overflow the total length variable yet, but in order to overflow it we use the maximum level length allowed, 63. x3f, instead of xf as shown in the example. So using exactly this construction, we reach and name of length 64K. Greater than 64K that's overflowing the total length variable itself. And remember that if expand label length, returned a value which is too small, we have a heap-based buffer overflow, which is a good RCE candidate. Also note that this vulnerability can be triggered in response to every query type supported by the network stack. By using CNAME resource records, which must be parsed regardless of the original query type. So this vulnerability affects the latest Treck version at the time of disclosure. And thus are considered dangerous from our point of view. So at this point we decided to purchase another device that runs Treck. In this case another UPS device, and we want to know if the vulnerability affects them or not. And what we found is that Treck fixes the read out of bound vulnerability, but they fix it badly. So you can see that the RDLENGTH variable from the resource record itself checks against the remaining size of the packet, and now after the fix expandable length accepts a third argument, level end pointer, which is computed based on the RDLENGTH value. And what explain level length does when it reaches this end pointer, it simply stops processing without any error and returns the current total length. So this is perfect from an attackers standpoint, because RDLENGTH is attacker-controlled. So if for example we specify an RDLENGTH value which is too small than the actual value, we'll enable this buffer overflow. So here you can see a resource record, and instead of specifying RDLENGTH value of 20, we would specify seven. So the label end pointer would point here, expandable length returns five in this case, but TfDnsLabelToAscii will copy the entire MX hostname, that's overflowing our buffer. So this is the second heap-based buffer overflow vulnerability we found. And we also found during the MX parsing logic a memory leak, we found that we can leak another infrastructure. So you can see another infrastructure is allocated, and in these two overflows the other info is not freed. So this means we can link another infrastructure by specifying an RDLENGTH value which is strictly less than two, like one. Or by causing expandable length to return a zero length. For example by using a bad compression pointer. So the size of the leak is hex 3c. And this artifacts come in handy when exporting any vulnerabilities, and in fact we used the exact same memory leak in our exploit, as you will see later. So to summarize we saw three vulnerabilities that comprise CVE-2020-11901. And an artifact. So the first vulnerability, the read out of bounds affects other versions of the network stack and was fixed in later versions. The integer overflow vulnerabilities as far as we have seen affects both old and versions of the TCP/IP stack. And the bad RDLENGTH vulnerability as a result of a bad fix further read out of bounds vulnerability, and thus affects only newer versions of the stack. The artifact is present in both old and newer versions of the stack. And the main takeaway from this part is that the device can be affected by one or more vulnerabilities depending on the exact version of Treck they are using. And this fragmentation makes the life of the IT security personnel more challenging to know whether their devices are affected or not. So now I'll hand over the mark to Ariel Schon, and he will talk about exploitation. - Thanks, Moshe. Hi, I'm Ariel, I'm also a security researcher at JSOF, and today I'm going to talk about exploiting the vulnerabilities, the CVE on a Schneider Electric UPS device. So a UPS device is basically a big battery, UPS stands for uninterruptible power supply. And you connect devices to it instead of directly to the wall or to the outlet to protect them from power outages or power fluctuations of any sort. So exploits on UPS made by Schneider Electric specifically on the network card. This network card houses at Turbo 186 processor, it's an x86 based processor, all code runs in 16 bit real mode. And meaning also there is no modern mitigation at all, so no DEP, no ASLR. This processor is x86 based, it's not strictly x86 as seen in this processor is a weird segmentation scheme, instead of shifting the segment register by four bits, like on an x86, it does it by eight bits, and we'll see this feature come into play later. During this research we had essentially no debugging capabilities. So no JTAG, no GDPE, nothing of the sort, so we relied mainly on static analysis and reverse engineering, using only limited crash dumps as assistants, these crash dumps as a visible feature, a basic stack trace and some registers, but nothing much. So just to recap the vulnerability, Our primitive is a heap overflow through DNS response parsing and this is a rather new trick implementation, so we can only overflown with alphanumeric characters and hyphens and periods, and we're going to use the bad RDLENGTH vulnerability variant that Moshe talked about earlier, so when exploding heap overflows, generally there are two methods, either through metadata corruption, so overflowing free list pointers, block sizes, stuff of this sort. Or by overflowing application specific data structures allocated on the heap. Generally metadata is considered a more generic exploitation method as it doesn't rely on extensive shaping, and this is the exploitation method we used in the earlier CVE on the DG Connect device that we have a white paper about this, and you can find that on our website. And we wondered if we could use the same technique on this device as well. So the Treck heap as implemented on the Schneider Electric UPS is slightly different, we have a free list that looks like this, it has a size field, a next pointer to the next free list block, some free data, usually containing garbage if it's a free block, and another post size field. So this heap features a tight fit preference it will always allocate the smallest free block available for the allocation size requested. It also features free block coalescing, so there are never two adjacent free list blocks. It has all sorts of verifications and asserts that we didn't have in the previous implementation on the DG device so for example on every heap operation, the entire field list is checked, one of the checks is that the first size field and the last size field really do match. Allocated blocks however are only checked when free, so this is a bit easier. Corrupting the heap in a way that will not cause a premature crash due to these checks on heap operations. Using only alphanumeric characters is rather hard, so we chose to go in a different way by overflowing data structures this time. So we know we can overflow through all DNS response types, however we chose to overflow through MX requests specifically. Because when the device boots up it will send out three MX requests, so that was a good exploitation benefit. Also three requests is very good for us for the heap exploits, since interactivity is always advantageous, it allows us a bit more flexibility in shaping our heap exactly the way we want. And we don't really mind that this happens only on device boot as we would probably have to crash the device anyway. As said, we added limited debugging capabilities during this research, so not much insight into the heap and how it is shaped. So we would like to get the heap into a state that is as deterministic as possible. So crashing is good in this manner. And then crashing the UPS network card has a very low penalty, as it doesn't affect the actual UPS operation in any way, it doesn't affect the power supply, and the network card will automatically boot right up after a few seconds, so the penalty is very low. So overflowing data structures, we chose to overflow a structure called tsDnsCacheEntry, this structure holds information about the DNS request response pair, it has all sorts of interesting fields. For example the DNSC other info PTR field that holds a list of other info instructs. These structs hold information about a certain DNS response. So for example if you resolve the name the structure has a field that will hold a pointer to the name that was resolved. Other than that it has other pointers and interesting fields, such as it's a doubly linked list as you can see, so it has a next and previous entry pointers, and these are always interesting from an exploiter point of view. And this structure is referenced often in DNS response parsing, which is a logic we can easily trigger, so this was a natural candidate for us. So assuming we can overflow the structure what can we do with it? This is a pseudo code snippet from the parsing logic. It specifically shows how CNAME records are parsed. So we can see that the first thing a pointer is taken from the DNS cache entry into a stack variable, then the CNAME is allocated on the heap, and the data is copied, this is of course the data we control as we provide the DNS response. And subsequently the pointer to the new CNAME allocated on the heap is placed into what the stack variable points to. Again, this pointer was taken from the DNSC cache entry, so if we overflowed the cache entry we can control to where this heap pointer to where our CNAME was written to memory. This is a controlled pointer, essentially we can write a four byte pointer to data we control on the heap and x86, this is to bytes of set in two bytes segment. We can write this to any alphanumeric address, as our overflow is alphanumeric. This is a relatively strong exploitation primitive writing data into places you're not supposed to write to is always good when you're trying to exploit. So this is the primitive we chose to continue with. Our overflow is a simple heap overflow, meaning it is from the end of our buffer with no offset into the next what lies in the heap next. So we would like naturally the cache entry to be placed after the MX name buffer. Because of the heap structure in this specific Treck implementation we have all sorts of limitations. For example, the cache entry is allocated on request of creation, however the MX name buffer is allocated on DNS response parsing, so chronologically it is allocated later when we want it on the heap to be allocated before so we can overflow into the cache entry. Also, because free blocks are checked very often in this heap implementation for corruption, we cannot write free data, we must overwrite only allocated data, it will be best if we can overflow directly into the structure without corrupting any heap data in the way. So we need to shape the heap in some way to get this to happen. A specific hole pattern is preferable as we would like to overcome the chronological problem, and we can do this because of tight fit preference. So if we create for example hole two as seen in this diagram, this is the size of the DNS cache entry structure, and before it we have another hole the size of the MX name buffer we're going to allocate, we can overflow from the MX name buffer We do need to separate them with some allocated separator to prevent the two free blocks from being coalesced together and ruining our shape. So we need some allocation primitives to create this. We need an allocation to create holes, and an allocation to create the separators. So to create the holes we can use a temporary allocation primitive. And this is relatively easy to achieve as every DNS answer has all sorts of names in it, or we can cause it to have names in it. So either MX PTR or CNAME, they all have name fields, and these names cause allocation of the heap. So the allocation is the size of the name we provide, and its data is of course of alphanumerical and controlled. This allocation will also cause a small other infrastructure to be allocated as well, and we need to take this into account when shaping. This allocation is freed when DNS parsing fails, or when the record TTL expires. So this allocation is perfect for creating free regions of arbitrary size, which is what we need. However, we need to separate our free regions, and we can do this using other info memory leak, that memory leak artifact Moshe talked about earlier. And just to recap shortly, we can see that another infrastructure is allocated, and only then the record validity is checked, and if it's not valid we will exit through an error flow. That will not free the allocation so we basically have a memory leak of a known size, and we can use this as a separator between our two free buffers. So we managed to shape the heap into the specific shape we want, and this allows us to reliably overflow the DNS cache entry every time. So assuming we can do this and we just showed that we can, what can we do with a CNAME pointer override primitive? What can we overwrite? So this pointer right has all sorts of limitations, it's written to an address we overflow into the cache entry, so naturally only alphanumeric addresses are allowed. However this is a string overflow vulnerability after all, we are overflowing with name, so we do have a trailing null byte. In x86 a little NDN architecture, this trailing null byte can be the most significant byte of the segment allowing us a bit more flexibility in the addresses we can reach. However, nothing in this specific firmware is placed in strictly alphanumeric memory address, so no code, no heap, no stack, no globals. Nothing interesting we can overwrite. Luckily, due to the weird segmentation of this Turbo 186 processor we can use a little trick and easily combine two alphanumeric bytes to reach a non-alphanumeric byte, or a non-alphanumeric segment. And this will look something like this, for example we choose a segment to be null byte 4b, which is allowed, it will then be shifted by eight bytes and an alphanumeric offset will be added to it, resulting in linear address containing which corresponds directly to a non-alphanumeric segment. So using this technique due to the weird segmentation feature we can reach the heap utility code section which has all sorts of functions such as free, which we can overflow. Again, this is a 16-bit real mode, so we don't have DEP or ASLR allowing us to overwrite code with our primitive. So when overwriting code in x86 one of the interesting destinations to overwrite was a pointer is a far call opcode, a far call has an absolute address encoded directly in it, so if we could overwrite this destination address with our pointer we can cause the execution flow to move into our ceiling buffer. So we can patch the far call using our primitive and execute our controlled payload. So we do this exactly, we patch far call in the free error flow, which is called when metadata corruption is detected. Naturally we do corrupt the heap when overflow, so this error flow will take place when our allocated blocks are freed. So let's shortly recap what we did here. First we shipped the heap in order to get the MX name buffer to be directly for the DNS cache entry with a separator in between that's not shown here. We then overflow to write the dnscAddrInfoPtr to point into a far call in the free function. We then parse and process a CNAME record containing some evil alphanumeric payload, which will be allocated on the heap and the pointer to this payload will be placed into the address we put in the dnscAddrInfoPtr, overwriting the far call destination directing it to our evil payload, which can be for example some alphanumeric shell code. Triggering this payload is rather easy, the free error flow will be triggered when the MX name record we overflowed from is free. And the CNAME buffer specifically what we overflowed with contains two stage alphanumeric shell code which will decode itself and allow us to run essentially arbitrary payloads, achieving arbitrary payload execution, specifically our payload just turns off power to all UPS outlets, turning off any critical device that was supposed to stay on, and we will now see a short demo of this payload execution. Thanks for listening and tuning into our presentation, We'll be glad if you would join us in the Q&A.
