Hello everyone. Welcome to our talk N-days are&nbsp; also zero-days. Can hackers launch zero-day RCE&nbsp;&nbsp; attack on on popular software only with Chromium&nbsp; N-days? In this talk I will introduce how we use&nbsp;&nbsp; Chromium in this pop-up calculators in many&nbsp; popular software hoping to draw attention to&nbsp;&nbsp; browser security application software. This is&nbsp; our first DEFCON presentation. Let's introduce&nbsp;&nbsp; ourselves. First, we are all security&nbsp; researchers at Tencent Security Xuanwu&nbsp;&nbsp; lab. I'm Bohan Liu, mainly engaged in browser&nbsp; security. I'm also a Google Chrome bug hunter. And&nbsp;&nbsp; Zheng Wang is our co speaker. He mainly engaged&nbsp; in browser security and kernel security. He found&nbsp;&nbsp; several bugs in Apple Safari, Linux kernel and&nbsp; VirtualBox. Another co speaker is Guancheng&nbsp;&nbsp; Li. He is a co-founder and the former captain of&nbsp; r3kapig team. He previously&nbsp;&nbsp; focused on system and software security.&nbsp; Currently, focused on LLM with security. &nbsp; There are six sections in this talk. Let's&nbsp; begin. First is introduction. As a browser&nbsp;&nbsp; security researcher, I researched the Chromium&nbsp; bugs for a while and wrote many N-day exploits&nbsp;&nbsp; and I can do a little reversing but I know little&nbsp; about web security. Given those skills, can I find&nbsp;&nbsp; zero-day RCE bugs in popular software? And if&nbsp; so, what can I do to make this work easier?&nbsp;&nbsp; Now let's take a look at how Chromium has&nbsp; been integrated into public software. &nbsp; Firstly, there is Chromium-based development which&nbsp; included default mobile browsers. Second, there&nbsp;&nbsp; are a browser component for various programming&nbsp; language. For example, the Electron framework&nbsp;&nbsp; is primarily used for software development of&nbsp; the... Not just for C++, Java and the Dolphin&nbsp;&nbsp; language. CEF is also commonly used in . Additionally, the QtWebEengine plugins&nbsp;&nbsp; is commonly used in Qt and the Chromium may&nbsp; also be present in other popular supply chains,&nbsp;&nbsp; so Chromium is everywhere in software. Based on data from CV and the Google Project Zero,&nbsp;&nbsp; Chrome may be found around 3,300 vulnerabilities&nbsp; with CVID each year. Of those, around a third&nbsp;&nbsp; vulnerabilities each year are from in-the-wild&nbsp; attacks and also many of those vulnerabilities&nbsp;&nbsp; can be transferred into render RCEs through&nbsp; detailed analysis. Those vulnerabilities can&nbsp;&nbsp; also be introducing into popular software&nbsp; through the sublime chain I mentioned about.&nbsp;&nbsp; Google has an old attitude towards vulnerability&nbsp; disclosure and the bug tail will be made public&nbsp;&nbsp; after being fixed 40 weeks. With the bug tail and&nbsp; uni test patch, attackers can quickly make its&nbsp;&nbsp; applied code. Sometimes it's even a zero-day&nbsp; bug for the stable version of Chrome itself.&nbsp;&nbsp; We strongly oppose the disclosure of vulnerability&nbsp; details before they're fixed. But it has to see,&nbsp;&nbsp; hackers has a lot of priori knowledge&nbsp; for chromium-based software attacks.&nbsp;&nbsp; Therefore, we conducted the experiment&nbsp; on some published software to see&nbsp;&nbsp; how long the introduction of those&nbsp; vulnerabilities can have an impact. &nbsp; Let's start with the definition of RCE window&nbsp; period. RCE windows period equals the time the&nbsp;&nbsp; vulnerabilities was fixed in the software, minus&nbsp; the time the vulnerability was exploitable.&nbsp;&nbsp; This is hard to process to determine the T&nbsp; exploitable. We use a new calculation method&nbsp;&nbsp; to roughly describe software's RCE windows period.&nbsp; The master is called UpdateGap, which is a time&nbsp;&nbsp; difference between the software's update time and&nbsp; its Chromium component update time. The larger&nbsp;&nbsp; the difference, the higher the likelihood of the&nbsp; software being exploitable during the period. &nbsp; In our experiment we select some&nbsp; popular software on the market&nbsp;&nbsp; including like Slack, Skype, Discord and others.&nbsp; The experiment was divided into three steps. The&nbsp;&nbsp; first step, we download the software's old&nbsp; versions through mirror state. The second&nbsp;&nbsp; step was identify the browser component in the&nbsp; software, the version and the release dates.&nbsp;&nbsp; In the Slacks that we calculated the&nbsp; update gap to describe the software's&nbsp;&nbsp; RCE windows period. We use our database which&nbsp; concludes over 200 POCs to assess in testing.&nbsp;&nbsp; Those POCs were used to search for high risk&nbsp; exploitable vulnerabilities during the period. &nbsp; The final results varied greatly with different&nbsp; software having update gap, ranging from nine to&nbsp;&nbsp; 800 days. Rambox Chrome components was relatively&nbsp; independent and updated more frequently. On the&nbsp;&nbsp; other hand, Dropbox hardly updated its Chromium&nbsp; component. The rest of the software mainly used&nbsp;&nbsp; the Electron or lib CEF component which&nbsp; most updated periods over 90 days. &nbsp; Some softwares has a longer update period. This&nbsp; code has average update time of 350 days. The&nbsp;&nbsp; shortest update period is over 70 days. As&nbsp; a result, 90 versions has a long RCE period&nbsp;&nbsp; which was demonstrated at Black Hat USA&nbsp; in 2022. Other software such as Slack has&nbsp;&nbsp; more frequent updates with a peak period of 30&nbsp; days. However, they hid a zero-day disclosure.&nbsp;&nbsp; They didn't deal with it in time. For example,&nbsp; while Chrome zero-day bug was public disclosed&nbsp;&nbsp; on GitHub on April 2021 and Slack just released&nbsp; its new version the day before that day. However,&nbsp;&nbsp; it wasn't until June that the bug was patched. It&nbsp; has an RCE window period of almost two months. &nbsp; After analyzing update we have come&nbsp; to several conclusions. Firstly,&nbsp;&nbsp; RCE window period uses the most software leaving&nbsp; attackers to find in the bugs for exploit.&nbsp;&nbsp; Second, a software built on the Electron&nbsp; framework tend to have a shorter RCE&nbsp;&nbsp; window period due to the lower code comply&nbsp; which make updates easier to implement.&nbsp;&nbsp; Finally, we have aware that many companies have&nbsp; increased their update frequency at specific&nbsp;&nbsp; times. However, even with those , RCE windows period still exists. &nbsp; Let's come to attack surface and vulnerability&nbsp; discovery. Desktop software develops user browser&nbsp;&nbsp; components to display specific pages and trigger&nbsp; certain client-side behavior in specific scenarios&nbsp;&nbsp; making client-side development more&nbsp; convenient and universal. However,&nbsp;&nbsp; for hackers during the RCE window period it's&nbsp; easy to launch through the RCE attacks when&nbsp;&nbsp; they can control the load in the page. This kind&nbsp; of attack has many advantages for hackers such as&nbsp;&nbsp; having many exploits with N-day vulnerabilities&nbsp; and no domain-specific requirements. Therefore,&nbsp;&nbsp; all hackers need to find is a situation&nbsp; where they can control the loading page. &nbsp; So what's the scenarios that&nbsp; developers tend to overlook?&nbsp;&nbsp; The questions arise. What is the&nbsp; function of browser components?&nbsp;&nbsp; What code runs in the browser component?&nbsp; Let's start with how they are developed.&nbsp;&nbsp; For Electron, all codes are developed by Nodejs.&nbsp; It's divided into main process and render process,&nbsp;&nbsp; communicate through IPC. All code is stored in the&nbsp; resource folder and there are less modification&nbsp;&nbsp; to Electron framework code. So it is easy to&nbsp; locate the source code. If we can compressed,&nbsp;&nbsp; we can also recover them from the ASAR format. As&nbsp; for debugging, since there are less modification&nbsp;&nbsp; to Electron framework code, most of the&nbsp; time we can just use command flag to open as&nbsp;&nbsp; internal development tools like extract or remote&nbsp; debugging ports just like this flag. Debugtron is&nbsp;&nbsp; a universal Electron framework debugging tools. We&nbsp; can see here. Is also compiled in this measure. &nbsp; CEF is another popular component. Here&nbsp; we use libcef C++ lib as an example.&nbsp;&nbsp; From the perspective of development, the code&nbsp; impacted by developed is divided into two parts.&nbsp;&nbsp; The privileged API function implanted by&nbsp; C or C++ is used to impacted&nbsp;&nbsp; client behaviors that can't be impacted by browser&nbsp; components. The other part is the local resource&nbsp;&nbsp; code implanted by the HTML which is the code&nbsp; running in the browser component and used it to&nbsp;&nbsp; display the interface. In the development logic it&nbsp; initialized first and then registered the passing&nbsp;&nbsp; and the processing logic of the provided domain&nbsp; and then registered the provided API function to&nbsp;&nbsp; form a JS object for the local resource code to&nbsp; code. And finally, load the local resource from&nbsp;&nbsp; the perspective of the CEF library is actually&nbsp; a series of codes to exploit the function. &nbsp; For CEF is feature, it exists in&nbsp; the form of a dynamic link library&nbsp;&nbsp; and the use of CEF is more flexible and&nbsp; more checked before use. A privileged API&nbsp;&nbsp; is implemented by C or C++ and the local resource&nbsp; code is implemented by HTML. And the location of&nbsp;&nbsp; the local resource file is uncertain and the&nbsp; file may be encrypted. So are there any common&nbsp;&nbsp; debugging method? Yes, you can hook the API in&nbsp; libcef and then get and modify what we want. &nbsp; For example, the developer will use CEF&nbsp; stream reader create for file CEF zip zip&nbsp;&nbsp; reader create or other extension to load&nbsp; the resource. We can get a file location&nbsp;&nbsp; and the correct password via hooking the info&nbsp; transfer between APF functions as shown here.&nbsp;&nbsp; As for debugging all the developers cost CEF&nbsp; initialized with static configuration parameters.&nbsp;&nbsp; We can still dynamically modify the remote&nbsp; debugging portal, a perimeter to jam developed&nbsp;&nbsp; capabilities by hook and we can also build more&nbsp; powerful debug tools based on development mode. &nbsp; Let's take a look at some cases that developers&nbsp; tend to overlook. First is white list of allowed&nbsp;&nbsp; loads. Developers are only allowed to use the&nbsp; browser components to open specific pages such&nbsp;&nbsp; as the official homepage. Other page will&nbsp; be opened by default system browser may&nbsp;&nbsp; be completed by coding shell execute open.&nbsp; For example, SCAP has the same logic here.&nbsp;&nbsp; The first way to bypass check is page redirection.&nbsp; Here are three ways, hyperlink navigation, 30X&nbsp;&nbsp; redirection, JavaScript navigation. Its principle&nbsp; is that this kind of check is handling click and&nbsp;&nbsp; then the page was loaded in Chromium component&nbsp; and the subsequent jump in Chromium will no longer&nbsp;&nbsp; trigger the check logic again. So SSO is a good&nbsp; pass tools as shown in the figure on the right.&nbsp;&nbsp; The following is a demo that use page redirection&nbsp; to imply RCE attack and software. For security&nbsp;&nbsp; reason, all the software mentioned in this topic,&nbsp; we will not disclose the product information.&nbsp;&nbsp; Okay, after click, calculator is here. &nbsp; Another point is checking itself like an incorrect&nbsp; regular expression or an incorrect triple check.&nbsp;&nbsp; This is another real case. The check use a&nbsp; long regular expression. They asked ChatGPT&nbsp;&nbsp; and wrong here. It told me&nbsp; that there are many domain&nbsp;&nbsp; easily redressed by hackers can pass&nbsp; the check. For example, abcdjd.hk.&nbsp;&nbsp; This is a mentioned&nbsp; before. It's also an AM software. After&nbsp;&nbsp; bypassing the URL check we can easily load our&nbsp; HTML code. We can easily pop up the calculator.&nbsp;&nbsp; In addition, another point&nbsp; is the special URL handler.&nbsp;&nbsp; There are two main aspects. One is the&nbsp; scheme registered in the system registry&nbsp;&nbsp; and the other it's the custom domain&nbsp; registered in the browser component.&nbsp;&nbsp; This they are provided domain is easy to&nbsp; miss some check we're operating in them. &nbsp; The follow is the demo use this method.&nbsp;&nbsp; We can see that AM software is a vulnerable&nbsp; area in this development case. Okay. &nbsp; The next is page code injection. Is a classic&nbsp; input, but let's focus on the unique aspects of&nbsp;&nbsp; XSS in Chromium-based software. Here are three key&nbsp; differences. Firstly, XSS type. In web security&nbsp;&nbsp; XSS typically include stored&nbsp; and dom based accesses. In Chromium-based software&nbsp;&nbsp; resources are often stored locally so XSS&nbsp; in this contest is typically dominant based.&nbsp;&nbsp; Secondly, this vulnerability in this current&nbsp; measure in web security, the code that handle&nbsp;&nbsp; user include is typically located on the server&nbsp; side. So the bug discovered mainly through black&nbsp;&nbsp; box testing. However, with the code location tools&nbsp; we virtually developed with, we can black box code&nbsp;&nbsp; a review via chromium-based software. Thirdly,&nbsp; input source. Again, web security input,&nbsp;&nbsp; you already come from URL parameters of the&nbsp; database. In Chromium-based software there&nbsp;&nbsp; may be fewer URL parameters but more unique input&nbsp; sources which we will discuss in next section. &nbsp; Based on those features as a researcher,&nbsp; know a little web security, I mainly use&nbsp;&nbsp; the master to discover&nbsp; access local resources. Firstly, I use the&nbsp;&nbsp; tools we developed to decrypt local resources.&nbsp; Then identify the unprotected resources that&nbsp;&nbsp; result CSP and filter out input point that&nbsp; can be controlled. Finally, a trace rider,&nbsp;&nbsp; this input can trigger code injection. This image&nbsp; is a real world case I found. I help extracted&nbsp;&nbsp; the key code and you can see that the get URL&nbsp; param function precise, the parameters in the&nbsp;&nbsp; URL and they put them into the page using&nbsp; in HTML in two, finally leading to XSS. &nbsp; Another thing to note is that XSS vulnerability&nbsp; can also be deducted through the supply chain.&nbsp;&nbsp; For example, the issue 2959 of Typora. Typoras&nbsp; can execute the JS code in Typora by exploring&nbsp;&nbsp; access vulnerability in the mermaid library and&nbsp; then use provided API to achieve RCE. We can also&nbsp;&nbsp; use the Chrome and the vulnerabilities from two&nbsp; years ago such as CVE-2017-5070 to issue RCE too.&nbsp;&nbsp; Here is a demo of the exploit for this case which&nbsp; shows that a Chromium's N-day bug can also do the&nbsp;&nbsp; same things as a provided JDPI bug. The iframe&nbsp;&nbsp; feature is often used in note-taking&nbsp; or document applications to make a&nbsp;&nbsp; page display more convenient. However, when&nbsp; the scope of the iframe excludes the limit,&nbsp;&nbsp; it can also cause security questions. This&nbsp; is more dangerous than in web security&nbsp;&nbsp; because it can be exploited with result&nbsp; provided API or specific domain need.&nbsp;&nbsp; We also found a RCE bug in a popular note-taking&nbsp; software using this trick. We would have liked&nbsp;&nbsp; to release it here as an example but it is&nbsp; being fixed so we can't disclose it here. &nbsp; For desktop software it may face much more complex&nbsp; situations than on websites. Such as no network or&nbsp;&nbsp; speed requirements. This requires some local cache&nbsp; to solve this problem. When reading or writing&nbsp;&nbsp; data the cache is modified first and then updated&nbsp; with the servers through communications. In this&nbsp;&nbsp; logic, the developers usually trust the cache&nbsp; and don't check it again when updating or loading&nbsp;&nbsp; it. Cache data can exist locally in various forms&nbsp; such as database, Json or custom format. So how do&nbsp;&nbsp; we find them? The easy way is to monitor the&nbsp; programs, fail read under right operations.&nbsp;&nbsp; The image on the right shows the&nbsp; applications that uses [inaudible&nbsp;&nbsp; 00:22:16] to store cache data. You can see&nbsp; that when the database content is modified&nbsp;&nbsp; there are no validated checks on the data&nbsp; when it's loading any frame to be inserted. &nbsp; Here is another interesting example. In&nbsp; certain applications I found that this&nbsp;&nbsp; three party components were added, the ape.&nbsp; Insert a frame here into the page. But the SRC&nbsp;&nbsp; attribute is strictly displayed in. But in the&nbsp; local cache I found a custom property code HF.&nbsp;&nbsp; I modified to access code and reopened the&nbsp; program. The page was modified to any HTML&nbsp;&nbsp; code here, which means that we bypass&nbsp; the check by modifying the local cache.&nbsp;&nbsp; And another, when I assessed&nbsp; the web version of this ape,&nbsp;&nbsp; I found that the webpage also changed to any&nbsp; HTML code here. This means that the server&nbsp;&nbsp; didn't check the update of the local cache&nbsp; and the vulnerabilities become stored XSS.&nbsp;&nbsp; Therefore, by using the code injection, we&nbsp; bypass the validation and achieve the RCE.&nbsp;&nbsp; And further, we polluted the server&nbsp; which can lead to more remote attacks. &nbsp; Okay. Some applications also allow developers&nbsp; to develop plugins and update them to the&nbsp;&nbsp; store. Use providing more convenient extension&nbsp; functions. Software give plugin developers more&nbsp;&nbsp; functionality but plugins you really need to&nbsp; be officially reviewed before put on the store.&nbsp;&nbsp; With the browser components hackers may use&nbsp; it to bypass plugin reviews for example. Where&nbsp;&nbsp; plugin is reviewed, the page in the plugin&nbsp; is a normal page but is put on the market,&nbsp;&nbsp; the website is redirected to a page with expired&nbsp; code thereby utilizing a waterhole attack. &nbsp; The following is an example of IntelliJ&nbsp; IDEA software. The &nbsp;&nbsp; plugin is loaded and attacker can use&nbsp; the browser component to achieve RCE.&nbsp;&nbsp; So when a plugin is reviewed it's&nbsp; necessary pay attention to the URL in it.&nbsp;&nbsp; Now in the video we load the plugin. &nbsp; Okay. Some functions may have an&nbsp; arbitrary page loading process,&nbsp;&nbsp; such as a pre-rendering of crawlers&nbsp; or notes. For example, the Burp&nbsp;&nbsp; Suite was found the RCE bug in while scanning function in 2021.&nbsp;&nbsp; Now, our co speaker Guancheng will give&nbsp; the other part of this talk. Thank you. &nbsp; Hello everyone. I'm Wang Zheng the co speaker of&nbsp; this topic. I will finish the last part. Given the&nbsp;&nbsp; short duration of RCE window period, it is crucial&nbsp; to develop exploits quickly when it comes to typer&nbsp;&nbsp; vulnerabilities, a common technique is to use&nbsp; optimized functions to create inconsistencies&nbsp;&nbsp; that ultimately lead to type confusion. However,&nbsp; for Use-After-Free bugs, there doesn't seem to&nbsp;&nbsp; be a publicly universal exploit method. Here&nbsp; we propose a universal exploit method for V8&nbsp;&nbsp; so if it will beat, use level three bugs, which&nbsp; will be demonstrated in Chromium version 91. &nbsp; Let's start by discussing some relevant background&nbsp; knowledge. We V8 Minor Garbage Collection recycles&nbsp;&nbsp; objects from the young generation which divided&nbsp; memory into two spaces. Newly located objects are&nbsp;&nbsp; put into the from-space and when the first&nbsp; garbage collection is caught. It identifies&nbsp;&nbsp; surviving objects and copies them from the&nbsp; from-space to the to-space, updating object&nbsp;&nbsp; pointers. Then the from-space and the to-space are&nbsp; swapped and the next round of garbage collection&nbsp;&nbsp; is performed. During scavenging there is also an&nbsp; additional set of root objects which are pointers&nbsp;&nbsp; from old-space to new-space objects where it&nbsp; uses red barriers to track this references.&nbsp;&nbsp; When the garbage collection identifies relevant&nbsp; references in the old space, it does not reclaim&nbsp;&nbsp; the objects in the new space. Here is a Chrome 1307610.&nbsp;&nbsp; This was caused by a missing red barrier when&nbsp; generating the last in-task object for red&nbsp;&nbsp; expression object. When lasting desk act as&nbsp; the SMI range and the right expression is in&nbsp;&nbsp; the old space where the lasting desk object&nbsp; is in the new space, the garbage collection&nbsp;&nbsp; mistakenly identifies object as reclaimable&nbsp; due to the lack of reference information&nbsp;&nbsp; resulting in a used of a free vulnerability. In V8 there is a type of string called one&nbsp;&nbsp; byte internalized string. Its memory layout is as&nbsp; follows. From front to back they are the objects,&nbsp;&nbsp; map, touch, string length and the strings content.&nbsp; The core idea we used was to forge a string with&nbsp;&nbsp; an extremely large length and place the target&nbsp; object after the string in memory. Then we can&nbsp;&nbsp; use the search method of the string to find the&nbsp; address of the target object after the mark.&nbsp;&nbsp; Thus, we can construct the address of primitive.&nbsp; Using the same technique, we can place a JsArray&nbsp;&nbsp; after the fake string by using the string's auto&nbsp; bounce right, we can modify the elements member&nbsp;&nbsp; of the JS array to point to arbitrary address&nbsp; and achieve arbitrary memory read and write. &nbsp; To finish such a string we need to create a&nbsp; fixed string map. V8 use this map to identify&nbsp;&nbsp; object types, so our core technique is heap&nbsp; spray. We can heap spray a large number&nbsp;&nbsp; of ArrayBuffer objects to obtain a stable&nbsp; address and then for the next three mappers&nbsp;&nbsp; of the string map at this address. Finally,&nbsp; we placed the fixed string in a JsArray and&nbsp;&nbsp; allocate a large number of JsArray objects to&nbsp; occupy the last index placeholder. The fake&nbsp;&nbsp; string is placed on the left of the JsArray&nbsp; and forge the string object is on the right. &nbsp; Here is a demonstration video of our&nbsp; successful exploration in Chrome version 91.&nbsp;&nbsp; So as for Chromium-based software, we&nbsp; found firstly a little bug or feature&nbsp;&nbsp; can lead to RCE attack. Secondly, there&nbsp; is lack of attention to the attacked path&nbsp;&nbsp; of browser vulnerabilities now, so the&nbsp; attack surface is relatively rudimentary&nbsp;&nbsp; and hackers also can bypass mitigation&nbsp; directly using powerful Chromium bugs.&nbsp;&nbsp; This section is about how to achieve&nbsp; RCE attack without sandbox escape bug.&nbsp;&nbsp; The sandbox is a security protection mechanism of&nbsp; Chromium. Even if a hacker gets the render RCE,&nbsp;&nbsp; it still cannot get the full execution&nbsp; permission. This includes mandatory access&nbsp;&nbsp; controlled environment, isolated process when&nbsp; HTML rendering and the JavaScript execution. &nbsp; And finally, the limited resource access and&nbsp; limited IPC or kernel interaction access. The&nbsp;&nbsp; N-day of sandbox escape is much less than&nbsp; the N-day of rendered RCE. So how can we&nbsp;&nbsp; achieve RCE in sandbox enabled software&nbsp; without a Chromium sandbox escape bug?&nbsp;&nbsp; First, let's say what's the difference between&nbsp; chromium-based software and Chrome browser? The&nbsp;&nbsp; one is privileged API. It is used to implement&nbsp; some client-side behavior in specific cases,&nbsp;&nbsp; such as as uploading files and&nbsp; downloading files. For renderer,&nbsp;&nbsp; it is a series of JavaScript objects. For&nbsp; browser, it is a series of handler code.&nbsp;&nbsp; Different frameworks have different&nbsp; implementations. The principle is to&nbsp;&nbsp; register some JavaScript objects in the context of&nbsp; the render process and when the objects are called&nbsp;&nbsp; we can execute the implemented handler&nbsp; in the browser process through IPC code. &nbsp; Based on this idea, there are usually two&nbsp; implementation logic for the privileged&nbsp;&nbsp; API. The first one checks the URL loaded by the&nbsp; browser component when it is initialized. If it&nbsp;&nbsp; is a specific domain register the privileged&nbsp; API in the loaded context, at one time,&nbsp;&nbsp; only the privileged domain can obtain the&nbsp; JavaScript object. So when we call in some&nbsp;&nbsp; function from this API, the browser processes&nbsp; cause it without checking the source domain.&nbsp;&nbsp; The second logic is to register the privileged&nbsp; API object no matter what domain it is at&nbsp;&nbsp; initialization and then the developer can check&nbsp; the domain when the privileged API is called. &nbsp; There are some problems with both methods. For&nbsp; the first one, whatever land privilege, the&nbsp;&nbsp; WebView gets a privileged context. For the second&nbsp; one, how to properly check the IPC access is from&nbsp;&nbsp; privileged domain. The research on Electron has&nbsp; become relatively metro. In August 2018, Maestro&nbsp;&nbsp; shared research on context isolation, which is&nbsp; used to isolate the context between the renderer&nbsp;&nbsp; process and preload. If this configuration is&nbsp; disabled, even with sandbox protection enabled,&nbsp;&nbsp; the renderer process can also copy privilege&nbsp; the functions it supported from preload such as&nbsp;&nbsp; shell doc shell dot open external function. At Black Hat Asia in 2019, Luca shared his&nbsp;&nbsp; research on preload. He discovered that even with&nbsp; sandbox protection enabled preload.js may still&nbsp;&nbsp; access certain native node classes and some&nbsp; Electron modules. Attackers can use prototype&nbsp;&nbsp; pollution to execute arbitrary obstacles. At&nbsp; Black Hat USA year 2022, a sandbox bypass method&nbsp;&nbsp; was demonstrated when the renderer process could&nbsp; load the preload.js file in a child file frame by&nbsp;&nbsp; overwriting the node integration in sub frame&nbsp; members of the web preference of the context. &nbsp; We can also overwrite the context&nbsp; prefers context isolation and disable&nbsp;&nbsp; CISO in new renderer process. Then we can send&nbsp; unauthorized IPC call with prototype pollution.&nbsp;&nbsp; We also found some interesting and&nbsp; wide-ranging issues with a second model,&nbsp;&nbsp; but since the vendors are working on a fix,&nbsp; we will share the details when it's fixed.&nbsp;&nbsp; We will make some security recommendations&nbsp; for Chromium-based software in two aspects. &nbsp; Firstly, it is necessary to update the latest&nbsp; version of the Chromium. Secondly, for software&nbsp;&nbsp; with high coupling, it's better to use a stable&nbsp; development version and introduce security patches&nbsp;&nbsp; with software updates. Thirdly, we should&nbsp; enable the Chromium sandbox and the last,&nbsp;&nbsp; we need to disable unused software&nbsp; functionalities or enable internal mitigation.&nbsp;&nbsp; For developers, we can add backend validation&nbsp; and front end rendering validation. Follow the&nbsp;&nbsp; principle of latest privilege, both privileged&nbsp; domain range and privileged API function.&nbsp;&nbsp; Enable universal mitigation such as CSP and be&nbsp; vigilant about input from unexpected sources. &nbsp; Here is the conclusion and takeaways of&nbsp; this topic. In this topic we verify that&nbsp;&nbsp; a large number of Chromium-based software have RCE&nbsp; window period. And then we summarize some software&nbsp;&nbsp; attack surfaces that cooperate with the RCE window&nbsp; period, which makes the hackers can latch zero-day&nbsp;&nbsp; attack with Chromium N-days and give real-world&nbsp; examples. And we share a general V8 use level&nbsp;&nbsp; three exploit method in 32 bit which may help&nbsp; make it exploit faster. At last, we provide some&nbsp;&nbsp; security recommendations to help strengthen&nbsp; the security of Chromium-based software.&nbsp;&nbsp; At last, thanks to all the partners for their&nbsp; contribution to this topic. Here is the list. &nbsp; Okay. Thanks for your listening.&nbsp; Here is the references of this&nbsp;&nbsp; topic. If you have any problem, you can&nbsp; contact us with email or on Twitter.
