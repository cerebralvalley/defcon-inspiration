Good afternoon, Dev Con. All right, so my name's&nbsp; Ceri. I was a software developer for 18 years&nbsp;&nbsp; within the DRM and security solutions space.&nbsp; Around four years ago, actually to this week,&nbsp;&nbsp; I joined the infosec game and joined Pen Test&nbsp; Partners to get involved in all the cool stuff&nbsp;&nbsp; that we all do. So I've been dedicated to Red&nbsp; Teaming and offensive security tooling for the&nbsp;&nbsp; last two years, and an author of several&nbsp; tools that are released to the community,&nbsp;&nbsp; including BOF.NET, SweetPotato&nbsp; and SharpBlock. I more recently&nbsp;&nbsp; become a member of the Rubeus maintainer team. So what we're going to cover on this talk today,&nbsp;&nbsp; so I'm going to give a very quick introduction&nbsp; to Kerberos. I'm going to go into some active&nbsp;&nbsp; directory specifics on authentication and&nbsp; authorization. I'm going to quickly cover&nbsp;&nbsp; some of spoofing attacks from previous&nbsp; vulnerabilities, namely the Sam the Admin&nbsp;&nbsp; group of CVEs. And then I'll cover the premise&nbsp; of this talk, which is a new spoofing vector. &nbsp; So a quick introduction then to some changes that&nbsp; have been made to Rubeus to exploit, I should say,&nbsp;&nbsp; on a new set of enumeration tools for this&nbsp; spoofing vector. And then finally, what can&nbsp;&nbsp; be done to sort of mitigate this new vector? So what is Kerberos? So it's an authentication&nbsp;&nbsp; protocol used by Windows and Unix, Linux-based&nbsp; operating systems. The machines are joined to&nbsp;&nbsp; a common realm to facilitate mutual&nbsp; authentication of principles in the&nbsp;&nbsp; realm. So a principle being a user or a computer&nbsp; that's within that realm. And they're identified&nbsp;&nbsp; by both a name and a principle type. Now Kerberos doesn't actually deal with&nbsp;&nbsp; authorization, it only deals with authentication.&nbsp; So the authorization is left to the implementer of&nbsp;&nbsp; that particular Kerberos stack. Windows and Unix,&nbsp; Linux, those can be joined to the same realm. &nbsp; So what does a principle name look like? So it's&nbsp; made of your typical name string. That would be&nbsp;&nbsp; the SAM account name, so John dot doe, or it could&nbsp; be a user principle name which is typically in&nbsp;&nbsp; the form of an email address. Or even an LDAP&nbsp; distinguished name is a valid principle name.&nbsp;&nbsp; And of course the service principle names&nbsp; for computer accounts or service accounts. &nbsp; And there's also the name type. Now this name&nbsp; type is a hint to the Kerberos authentication&nbsp;&nbsp; scheme so that the KDC has an idea of what sort&nbsp; of name principle it is. And as you can see from&nbsp;&nbsp; the table below at the bottom there's a number&nbsp; of different principle types there, but the most&nbsp;&nbsp; common for users is the NT principle type. So active directory actually uses a fairly&nbsp;&nbsp; complex search algorithm for when you're&nbsp; authenticating with the principle. It uses&nbsp;&nbsp; multiple attributes within LDAP to actually find&nbsp; the user that you're looking at. And then as&nbsp;&nbsp; far as authorization decisions are made, it's&nbsp; always made from what's called the Privileged&nbsp;&nbsp; Attribute Certificate that's embedded inside the&nbsp; Kerberos ticket, collectively known as the PAC,&nbsp;&nbsp; I guess. Principal name collisions can occur&nbsp; and this has led to previous vulnerabilities&nbsp;&nbsp; and privilege escalations in the past. I've tried to sort of simplify active directory's&nbsp;&nbsp; method of searching for a principle. So the&nbsp; default when you actually log into a Windows&nbsp;&nbsp; machine, it uses the name type of empty principle.&nbsp; And let's say for this example we got a username&nbsp;&nbsp; of John Doe. Now by default, it will go to the&nbsp; right-hand side of that search. So because we're&nbsp;&nbsp; using the NT principle, active directory will&nbsp; actually search the SAM account name field first. &nbsp; Then if a user's not found, it'll then actually&nbsp; append a $ to the end of the name just in case&nbsp;&nbsp; that you're actually authenticating with a&nbsp; computer account because computer accounts&nbsp;&nbsp; generally have a $ at the end of their name. And then finally, if it still hasn't been able&nbsp;&nbsp; to find a user, it'll switch to the left-hand&nbsp; side of that flow chart where it'll search the&nbsp;&nbsp; user principle name field instead. And then&nbsp; if there's no username found at that stage,&nbsp;&nbsp; you get a user not found error. And of course, any&nbsp; time in between those stages, if a user is found,&nbsp;&nbsp; it then will try and authenticate the password. So the actual ticket makeup within an active&nbsp;&nbsp; directory network, so the green area is what&nbsp; you'd say is part of the actual Kerberos RFC.&nbsp;&nbsp; That contains sort of the client name or the C&nbsp; name and the service that the ticket is destined&nbsp;&nbsp; for and a start and end date. And then the&nbsp; Microsoft specifics sort of come into play,&nbsp;&nbsp; which is the red area of the ticket. Now you can actually have a ticket with&nbsp;&nbsp; the PAC missing altogether. Now when it comes&nbsp; to Windows-based authorization, even though the&nbsp;&nbsp; outer green ticket is actually valid and it has&nbsp; a client name that's within the active directory,&nbsp;&nbsp; you'll actually log on as an anonymous log on&nbsp; if the PAC is missing. So as far as Windows is&nbsp;&nbsp; concerned, you always need the PAC really to&nbsp; validate the user within active directory. &nbsp; So with that in mind, so what&nbsp; actually happened with the Sam&nbsp;&nbsp; the Admin group of CVEs? So back before&nbsp; the patch for the Sam the Admin CVEs,&nbsp;&nbsp; you could actually request a TGT without the PAC&nbsp; present. So what the actual vulnerability was,&nbsp;&nbsp; you would do that, you'd actually request the&nbsp; TGT without the PAC being embedded inside it. &nbsp; Now the ticket is valid for eight hours. So in&nbsp; between that TGT that you've requested... Sorry,&nbsp;&nbsp; let me rewind a little. So first you actually&nbsp; create a machine account using the Mac. Now that&nbsp;&nbsp; machine account would be named similar to let's&nbsp; say a domain controller. The only difference is&nbsp;&nbsp; the Sam account name field won't have the $&nbsp; at the end. So it's exactly the same name,&nbsp;&nbsp; but it doesn't contain the $ at the end. And then you request your PAC list TGT,&nbsp;&nbsp; which means that the only thing in the ticket&nbsp; is the C name itself. So there's no PAC in&nbsp;&nbsp; there at all. Then you rename the account to&nbsp; something else, nothing that exists within AD,&nbsp;&nbsp; but you do have at least a Kerberos ticket now&nbsp; that looks like the domain controller ticket. &nbsp; Now because you've renamed the account, you then&nbsp; request a service ticket from the KDC. The KDC&nbsp;&nbsp; will realize that there's no PAC embedded inside&nbsp; that ticket. It'll use the same search criteria&nbsp;&nbsp; as I just covered on some of the previous slides.&nbsp; But it'll use the official Kerberos C name that's&nbsp;&nbsp; embedded in the green part of that ticket. And&nbsp; because you've renamed the account in between the&nbsp;&nbsp; point where you've got the TGT, it would've then&nbsp; actually found the real domain controller account&nbsp;&nbsp; and embed the PAC for the real domain controller&nbsp; and that resulted in a privilege escalation. &nbsp; So this is going back to the search algorithm&nbsp; again. This is the area within the Sam the&nbsp;&nbsp; Admin group of CVEs that were affected.&nbsp; So there's the right-hand side there,&nbsp;&nbsp; where the Sam account name is searched and then&nbsp; it would append the $ and so on and so forth. &nbsp; So what did they do to fix the Sam the Admin?&nbsp; Basically, you can't request a PACless TGT&nbsp;&nbsp; anymore. Microsoft deemed it that it wasn't&nbsp; a reliable way to identify users within AD&nbsp;&nbsp; unless you had a PAC embedded within it. There&nbsp; were some other hardening attempts. So the Sam&nbsp;&nbsp; account name fields also now must end with a $&nbsp; whenever an unprivileged user creates it. And if&nbsp;&nbsp; few other the hardening artifacts were made to&nbsp; the user account control settings. But you can&nbsp;&nbsp; actually still request a PACless service ticket. So what's the new spoofing vector? So obviously,&nbsp;&nbsp; the Sam account name had been looked at previously&nbsp; and it has been hardened quite considerably since&nbsp;&nbsp; that group of CVEs. So I decided to have a&nbsp; look at the user principle name attribute&nbsp;&nbsp; and see what we could do with that. So the user&nbsp; principle name, or the UPN, is made up of the&nbsp;&nbsp; username element, the at and a domain suffix.&nbsp; So basically, it looks like an email address. &nbsp; Now to modify that particular attribute within&nbsp; active directory, you're looking to have right&nbsp;&nbsp; permission on the public information attribute&nbsp; set or generic right over a computer or user&nbsp;&nbsp; account within active directory. So these&nbsp; are the sort of the prerequisites needed&nbsp;&nbsp; to abuse it. But duplicated UPNs are not&nbsp; allowed in the same way that duplicate Sam&nbsp;&nbsp; account names are not allowed. So how exactly&nbsp; can we spoof if no duplicates are allowed? &nbsp; Well, as it turns out, you can actually set&nbsp; an invalid user principle name within active&nbsp;&nbsp; directory. It doesn't actually have to have the&nbsp; domain suffix. So you can see from the screenshot&nbsp;&nbsp; there, we've got the John Doe account where&nbsp; the Sam account name is set to John Doe, but&nbsp;&nbsp; you can actually still update the user principle&nbsp; name to mimic that of another account; so let's&nbsp;&nbsp; say the domain administrator in this case. But there's another difference to this search&nbsp;&nbsp; algorithm then, is going back to the name&nbsp; type hint that I talked about earlier. If&nbsp;&nbsp; you specify the name type as anti-enterprise&nbsp; as opposed to principle, which is typically&nbsp;&nbsp; the default one used within Windows, the search&nbsp; algorithm changes. It actually searches the user&nbsp;&nbsp; principle name first before Sam account name. So&nbsp; as I'm sure you can glean from that, basically,&nbsp;&nbsp; you can get your spoofed account name to&nbsp; be found first prior to the real account&nbsp;&nbsp; that's sitting within inactive directory. So as part of this I made some updates&nbsp;&nbsp; to Rubeus and they should be live on the&nbsp; current master branch on GitHub. Basically,&nbsp;&nbsp; a new argument has been added to specify the&nbsp; principle type. So from the group that we've&nbsp;&nbsp; seen at the start of the slides, you can actually&nbsp; now specify the principle type that you're using.&nbsp;&nbsp; So as an example there on the left, that's how&nbsp; you would typically authenticate using Rubeus at&nbsp;&nbsp; the moment, and by default, that would use the NT&nbsp; principle type as opposed to the enterprise type. &nbsp; So in a typical active directory network, the&nbsp; screen on the left there would actually, you'd be&nbsp;&nbsp; tempting to log on as the domain admin. And since&nbsp; the domain admin password is not super secret,&nbsp;&nbsp; you get a pre-authentication failure because the&nbsp; password is wrong. But going back to the modified&nbsp;&nbsp; John Doe account that I just showed you earlier,&nbsp; once you add the principle type of enterprise,&nbsp;&nbsp; the search algorithm changes and then it&nbsp; actually ends up finding your John Doe&nbsp;&nbsp; account. But the ticket that you actually get&nbsp; back contains administrator as the username. &nbsp; So from the outset, the ticket actually does&nbsp; look like a domain admin, but it's not. The PAC&nbsp;&nbsp; inside that ticket still identifies John&nbsp; Doe, but it's still only the outer green&nbsp;&nbsp; part of the ticket that I showed you earlier&nbsp; that make it look like it's a domain admin. &nbsp; So what abuse vectors do we have for this?&nbsp; So like I said, Windows is completely out.&nbsp;&nbsp; It purely relies on the PAC for authorization.&nbsp; But when it comes to Kerberos authentication&nbsp;&nbsp; on your Linux and Unix machines, which&nbsp; leverages the GSSSAPI, they tend to do&nbsp;&nbsp; things differently. Like I said, the PAC is very&nbsp; specific to sort of Windows-based environments. &nbsp; So what tends to happen is GSSAPI stack will&nbsp; actually rely on the green parts of the ticket&nbsp;&nbsp; for validating the username. Then the permissions&nbsp; for the user is done as a second stage. So it&nbsp;&nbsp; may do an LDAP search on that name that is&nbsp; extracted from the ticket, and more often&nbsp;&nbsp; than not that ends up being the Sam account&nbsp; name that it's searching on. But, of course,&nbsp;&nbsp; we've authenticated using the user principle name.&nbsp; Other solutions also include mapping Kerberos&nbsp;&nbsp; principle names to local account names. So for&nbsp; example, you can create a machine using root$ and&nbsp;&nbsp; that will actually log you in as root on Linux. Andrew Bartlett actually discovered that,&nbsp;&nbsp; who is the person that discovered the original Sam&nbsp; the Amin group of CVEs. He basically realized that&nbsp;&nbsp; using the Mac permissions and having the ability&nbsp; to create a machine with a low privilege user,&nbsp;&nbsp; you could call the machine root$, pass&nbsp; that ticket to SSH and then you'd be&nbsp;&nbsp; logged in as root. That issue actually&nbsp; still exists today and is a no fix. &nbsp; Now the issue with that though, you can't actually&nbsp; impersonate existing AD users because you wouldn't&nbsp;&nbsp; be able to create the machine in the first&nbsp; place because you'd get a collision with a&nbsp;&nbsp; Sam account name if you were trying to impersonate&nbsp; another user. So that's where this new technique&nbsp;&nbsp; comes into play, where you can now actually&nbsp; impersonate valid active directory users. &nbsp; So these are the services that I've seen so far&nbsp; that are implement GR or Kerberos authentication&nbsp;&nbsp; via GSSAPI. So SSH I've confirmed is vulnerable to&nbsp; this as well as Postgres database. I just haven't&nbsp;&nbsp; had the time to check all the others&nbsp; yet. But essentially if any of these&nbsp;&nbsp; are claiming Kerberos authentication and GSSAPI&nbsp; support and they don't look at the Windows PAC,&nbsp;&nbsp; they're all going to be vulnerable to&nbsp; this sort of attack, would be my guess. &nbsp; Okay, so onto the demo. So both of these are&nbsp; live already. I made them live just before the&nbsp;&nbsp; talks. Rubeus has the new argument so that you&nbsp; can specify the principle type. And I've also&nbsp;&nbsp; created a new set of Python tools to allow you&nbsp; to enumerate potential vulnerable Linux or Unix&nbsp;&nbsp; hosts within an active directory network. So it's&nbsp; a little bit small, but we'll work through it. &nbsp; So what I've set up here is, again, we're going&nbsp; back to the John Doe account and to be able to,&nbsp;&nbsp; or the prerequisites to be able to actually abuse&nbsp; this issue is having sort of a generic right over&nbsp;&nbsp; an account. Now you'd need to authenticate&nbsp; as that user, so shadow credentials has&nbsp;&nbsp; worked for example. So you don't necessarily&nbsp; need to do anything brutal like changing the&nbsp;&nbsp; user's password. You can still use shadow&nbsp; credentials with this sort of technique. &nbsp; So what we're going to do now is just change the&nbsp; properties of the user principle name from what&nbsp;&nbsp; it was to administrator. Okay, and you'll see,&nbsp; obviously, in active directory that the user&nbsp;&nbsp; principle name is now set and ready to spoof.&nbsp; But the Sam account name is left as it is so we&nbsp;&nbsp; are using John Doe. So I'm just going to show&nbsp; that without specifying the principle type,&nbsp;&nbsp; you would essentially be trying to log on&nbsp; as the domain admin, which is incorrect&nbsp;&nbsp; in this case. So by that authenticate&nbsp; you get a pre-authentication failure. &nbsp; But then if we use the new principle type&nbsp; argument and specify enterprise, the search&nbsp;&nbsp; algorithm changes and now be authenticated as&nbsp; John Doe, but with the username of administrator.&nbsp;&nbsp; And if you look at K list, you'll see that&nbsp; that has been imported. Just looking at it,&nbsp;&nbsp; it does look like an administrator or domain&nbsp; admin, but the PAC actually says differently. &nbsp; Now the GSSAPI tool, the way that works is it&nbsp; does a search with inside active directory,&nbsp;&nbsp; even looking at the operating system field of&nbsp; every computer object. And any that don't have&nbsp;&nbsp; the word Windows in there, it assumes it to be&nbsp; a Linux or a basically non-Windows based host.&nbsp;&nbsp; And then once it compiles that list of machines,&nbsp; it'll then try and connect to each one of them&nbsp;&nbsp; over SSH to determine what authentication&nbsp; schemes it supports. And then if it does&nbsp;&nbsp; realize that particular host supports GSSAPI,&nbsp; it flags that host as potentially vulnerable. &nbsp; So you can see in the output here, there are&nbsp; two machines found. One is Ubuntu and one is&nbsp;&nbsp; Santos. Now Ubuntu and I believe all other&nbsp; Debian based Linux operating systems don't&nbsp;&nbsp; have GSSAPI enabled by default. But Santos Red&nbsp; Hat Enterprise Server, they do. So the minute&nbsp;&nbsp; you join a Santos or Red Hat based Linux server&nbsp; to an active directory network, you're going to&nbsp;&nbsp; be sort of vulnerable to this sort of attack. Then the final stage is now that we've got our&nbsp;&nbsp; ticket imported and ready to use, we can use&nbsp; SSH with the minus K argument to indicate that&nbsp;&nbsp; we want to use Kerberos authentication.&nbsp; You have to fully qualify the username,&nbsp;&nbsp; so you'll see administrator@adjing.com and&nbsp; here we connect it. But the big question is,&nbsp;&nbsp; of course, what groups are we a member of? So by doing a quick ID, we can actually see&nbsp;&nbsp; whether we are logged into the Ubuntu or at&nbsp; the Santos host as the domain admin. So I&nbsp;&nbsp; did contact MSRC about this and they basically&nbsp; said the issue I reported is as designed. I was&nbsp;&nbsp; sort of questioning whose design they were talking&nbsp; about. So I had a quick look at what the RFC says,&nbsp;&nbsp; and the RFC says it quite clear, as far as I'm&nbsp; concerned; principle names that differ only in&nbsp;&nbsp; the name type should identify the same principle.&nbsp; So they are not listening, as far as I can see,&nbsp;&nbsp; to what the Kerberos RFC spec says. So what sort of mitigations? Well,&nbsp;&nbsp; Microsoft themselves could harden the user&nbsp; principle name by ensuring they all have a&nbsp;&nbsp; domain suffix. Or additionally, they could&nbsp; crosscheck the user principle name against&nbsp;&nbsp; the Sam account name to make sure that there&nbsp; are no collisions there. But in the meantime,&nbsp;&nbsp; defenders out there, what kind of approaches can&nbsp; you take? I guess disable anything that's GSSSAPI&nbsp;&nbsp; based authentication that's joined to an active&nbsp; directory realm. Or the less drastic approach,&nbsp;&nbsp; maybe monitoring for user principle name updates&nbsp; that don't conform to a valid UPN, and then&nbsp;&nbsp; obviously investigate those sort of anomalies. So I think I'm running out of time for actual&nbsp;&nbsp; questions. What I will do, if anybody does&nbsp; want to ask a question, I'll certainly hang&nbsp;&nbsp; around outside if needed. I want to give&nbsp; a big shout out actually to Charlie Clark,&nbsp;&nbsp; AKA X by PH. He's been a great sounding&nbsp; board for some of the Kerberos shenanigans,&nbsp;&nbsp; I'll say, that we've discussed on&nbsp; this talk. Thanks for listening.
