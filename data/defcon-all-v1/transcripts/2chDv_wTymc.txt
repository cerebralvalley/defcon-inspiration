hey guys so today I will try to demystify Windows kernel exploitation by abusing GDI object my name is safe I'm Egyptian and I work for sense post that's the Egyptian traditional dress over there now I want to start by saying why am I doing this so a year and a half ago I started getting into binary auditing and wanted to do static analysis and finding vulnerabilities and I found City is really enjoyable but at the same time they don't really represent three world scenarios so I decided to go for patch the thing and finding one date and the result was epic and I hope you guys like it through the talk so what we will learn through this talk is first abusing two types of GDI objects to gain ring z to exploit primitives the next thing is analyzing two one days I'm a 16 or 98 and I'm a 17 or 17 by using this techniques now the complex is pretty complex I will try to simplify it as much as possible however there is a full white paper and the slides and exploit code and everything that is up on github at the moment you can access it on that link the first thing that I have to talk about before we get that into the juicy stuff is the terminal pool you can think of the kernel pool as the heap memory for that the Windows kernel uses the first thing about the kernel pool that is that any object allocated to the kernel pool will have out size a pool Heather attached to it for 64-bit systems this header has 8 bytes in size and for 86 bit systems this header is 8 bytes in size the next thing is that small allocations or allocations smaller than thousand bytes will be allocated to a pool page that has the size of 1,000 bytes so the kernel pool is effectively defragmented in 2000 by pages and that allocation dynamics work as following so the first allocation will be allocated from the beginning of the page subsequent allocations will be allocated by the end of the page the next thing I want to talk about is kind of poo spray why do we do chemical spraying or Feng Shui in the exploit development first of all it is used to get the permanent pool memory in the term allistic State the way it does it is by doing a series of allocations and deallocations the season the allocations is used to create memory holds that hopefully our vulnerable object will be allocated to thus having a vulnerable object allocated in a space between objects under our control that we can abuse to gain an extra exploit primitive or an extra access to the memory next to it the next thing I want to follow through is kernel port corruption and we will focus about x86 integral over flows or integrals in general so the first equation want to talk about is 80 plus 81 will result in one well that's not really the truth behind it the truth is that it actually results in a much larger number zero one zero zero zero zero zero one but x86 registers can only hold up to four bytes in memory so this number is larger than 4 bytes max int and the most significant byte or the world or anything like that is larger than four hwhiteland will be truncated thus equals to 1 the first part I want to address is linear overviews and this can be a result of man copies with a larger sized and allocated object or a memory copying group that is larger than the located odd so if an overflowed size was supplied to a location object it will result in a very small object being allocated and then a memory copying loop or a memory copying function will try to copy a large number of data to that small size allocation effectively overflowing the next object in memory with the amount of bytes electrons to copy the next one is out of bounds rights so if you think of an overflowed size that will allocate a smaller object and then the function expects that object to be of a certain size and tries to write to a certain offset of it it will effectively write this object or this data in and out of bounds location from the allocation of allocated object size now both of these we'll see how we can exploit them when we get to the exploitation part of the one days and now I want to talk about how are we able to gain exploitation primitives out of these type of computed options and one of the things that I'm going to focus about is actually have use of GDI objects now there are certain objects that are gated to current memory that has some interesting characteristics that can give us some sort of memory reading the writing primitives for example if we want to get a relative memory read and write which is basically reading and writing in the memory location adjacent or relative to the object location and memory the object that can be abused has to have a size number that can be extended to be able to access out of bounds memory adjacent to this location if you want to get arbitrary memory read and write the object has to have some sort of data pointer memory that if you can corrupt or control its value you'll be able to eat and write from any location in memory of communal space and if you look at the process of how this is done if we consider that we have two objects that are allocated adjacent to each other and these objects both have a size member and a data pointer member we can use the first object or any pool corruption primitive to actually corrupt the size of the first object thus extending the amount of memory that they can read and basically going to the adjacent objects memory and then use the first object as a manager to actually overwrite the address or the data pointer of the second object and use the second object as a worker to read and write from any point in memory now this expectation technique can be used in kernel and is heavily used in browsers as well the first of these objects that can be abused is the bitmaps now GI bit nerfs techniques have been there from 2015 so the first people who talked about it was scheme team and was later every detail by niko acronym I hope I said this right I am Diego fathers the colonel the bed map is represented in memory by a serf object and has a pool tag of GH question mark 5 which represents a number that from one system to another so this can be zero or one or two depending on the version of Windows or GRA five when it's allocated to the local side list which is outside of the stock and this is how the serf object structure looks in kernel memory so you'll notice that they're highlighted a couple of numbers which are interesting to us so first of all is the seasoned bitmap which represents the width and height of the bitmap so if you can corrupt any of these members you'll be able to effectively extend the size and gain relative memory read and write the other member is the pv scan zero which is a pointer to the bitmap datum the way you can allocate bitmaps to memory is by calling create bitmap user mode function this takes a width a height the planes a bits per pixel and the bits of the bitmap and allocates a bitmap in memory the way you can free bitmaps is by calling the read object the next point is the way you can read the bitmap memory is by using it bitmap bits these are all user mode functions you can call them from any application and the way you can write to bitmap memory is by using set bitmap bits so how do we exploit or abuse bitmaps basically if we consider that we have two bitmaps that are allocated adjacent to each other as a result of our kernel and pool spray you if you have a pool corruption that allows you to overwrite the width of the height of the sizzle bitmap that effectively extends this bitmap too right into the adjacent bitmap memory and then we can use that as a manager and overwrite the previous can zero of the second bitmap and then we're able to read and write from any point in memory the next thing I want to discuss is an undisclosed technique that has not been discussed any more anywhere I just had a conversation with JP about it and he mentioned that they have seen some in the oil but it has not been documented anyway so this is effectively sort of an OD technique and the way we address it although the object that we use is actually color palettes so color palettes are interesting objects the I represented internal memory by using the palette or the XML object name and they have the pull tag of gh number 8 or GE 8 this is the x86 structure of the object and this is the 64-bit structure of the object now the members highlighted are in red are the intercept number interesting members to us so you find that the SI entries number which represents the entries count of the pallet can be overwritten or extended there which allow which gives us a lot of memory read and write primitive and the second number that we can abuse is the P first color pointer so this is a pointer to the array of colors of that color palette which is highlighted in green and usually allocated by the end of the object so you can allocate GDI palettes by using the create palette function that takes a logical palette structure that contains the palette entries as well as the parlament non entry numbers and the PAL version and the palette entries contained RGB color value for each palette you can flip edit objects from kernel memory by using the lead object and you can read palette memory by using cap pallet memory caplet entries function from user mode and you have two functions to actually write to palette memory in the kernel which are set palette entries and animate palette and we will look at the difference the differences between them in a bit so how do you exploit palettes if we have a pull spray that results in two adjacent palettes we are allocated in memory and we have a pull corruption primitives that allows us to actually increase or extend the size or the number of entries of the specific palette that will give us relative either right into the adjacent object which is another palette we can abuse that by overwriting the P first color pointer and use the second palette to eat and write from anywhere in kernel memory so there are some technique restrictions in regards to using color palettes an exploitation first of all the minimum permit allocation size for x86 system is 98 bytes and d8 points for x64 ones so when you corrupt see and his number it has to be larger than these values the next restriction is related to set palette and this function when you try to write to palette memory using separate entries if you clobbered any of the the current member any of these members it will block writing to memory which effectively will result in either a blue screen of death or will result in harrowing out without actually writing to kernel memory so the first restriction in regards to set palette entries when you call separate entries from user mode it will reach gr e separate entries and then it will call ax keep an object you and set entries you see in the highlighted parts the palette is actually checking for the P transcurrent and the P transpose which are the transformations that happen on the specific color palette if any of these numbers are set it will try to access this memory location and then will try to end the value in it by 0 so if these memory locations were set and not allocated it will error out without writing anything to memory the next restriction is that when you call separate entries and after the previous function was called it will actually try to access the HDC had member which presents the owner device context of the palette if this value was set it will try to get the owner of the palette to set its value in the device context now this will effectively result in a blue screen of death which we killed the whole system and not error out and not write anything the second restriction I want to talk about is about animate palette so animate palette has to have the pointer pointed to by P first color has to be even hot one the first byte of it has to give an odd value and we know the reason why thank you as Diane said specifically the limit per pallet would only work on entries that has a PC reserves flag set and that flag is effectively 1 so that's why you can see highlighted there that the function tries to test for 1 before writing now this didn't really represent any problems and ate x86 systems since you can easily control where the palette is and control the first byte of the poor memory to be an odd value however in 64-bit systems it proved to be a bit of a problem to set the first part of the address since it's always FF now we need to know what on the next step of exploitation what we need to do is we need to try to steal the system new process token and what this is is that each process that is running on your system is represented by an e process structure and permanent memory this structure contains very interesting members it contains a member such as an image name which is the exe name and it contains a token the token is the most interesting part of it since this token represents the security permission that this process has also it has a member that is the active process links which is a doubly linked list that is actually pointing to the next he process entry in criminal memory and the unique process ID which is the PID of the current process the AUSA's to these members defers from one system to another and specifically for this talk will be covering Windows 8.1 64-bit and Windows 7 sp1 at for x86 bits and these are the offsets to these members and the game plan we have to actually exploit this or get the system talking about system processes first we need to get the address of the e process or the system Z equals structure in memory and the way we can do this is actually there is an exported symbol called PSMs through system process so if you load the entire stack on duty x key into a programs memory and get its offset and subtracts it from the base of the anti-us common memory you would get an offset that this specific entry is located in the kernel and then you can easily leak the entire space kernel address from the drivers and then just add that this offset to it and successfully but the system a process stops application from the kernel mode then we used arbitrarily primitive that we got to actually read the token of the system process and move on to iterating over the active process links until the process current ID or the unique process ID represents the current process ID and used arbitrary right primitive to just replace our current process token with the system one effectively gaining to type privileges the first exploit I want to talk about is actually a mess 17 or 17 well I really doc did that I don't know if that's true Microsoft has actually released it back in March and that's the patch that I reversed but it can be a my 17 or 13 as well but I'm not very sure about it it affects all versions of Windows but I found it very difficult to exploit it on 64-bit ones so I was only able to exploit it on Windows sp1 x86 and the function that is vulnerable is Angela's brush and the type of the vulnerability is an integral overflow that leads to an out of bowl right first of all the first step that I took to analyze this kind of exploit is to actually diff the patch that was released it was released back in March 2017 this is the function that is vulnerable which is and realized brush and this is the patch so it was very difficult to find out where the problem actually is as you can see there were many changes all of the changes were related to some integral checking mechanisms of interview checking functions so I decided to look at the function itself at the first step to do that is to actually find a way to trigger that function from user note so if you do the first function you need to call is create pattern brush and this will create a brush using a specific bitmap that you supply and then use a function pad PLT which actually writes the brush pattern to a specific path in memory and then this will lead effectively to that function so the way you do it in code in C code is this this is actually an expert of the exploit code so in the future any of the code that is between this rectangle is actually expert from the expert code itself and above code will reach this following either snippet with a control value in EDI the first step that this code will do is actually get the value of EDI which is the bitmap with and then it will get ECX and multiply it with this value which is at the time is 20 and this can be controlled by the bitmap format and then it will divide this value by 8 and then it will multiply both values the width and height after the following calculations and then it will add 444 to it which obviously if you don't have checking leading to that function you can easily overflow but what happens on the overflowed function so when I overflow that integral what will happen later is the most important part so if you notice if you follow the instructions of what happened with this oil for the integrator you notice that it will leech the following code snippet or the code branch which is actually an allocation function so this overflow tentacle can will be added to 40 again and then post as the size value for a location function so if you remember we said that if you're able to influence the size of any allocation and overflow the integral to a smaller size then the allocation will result in a small application that is unexpected by the function itself or by the system so after the allocation happens we can try to trigger this a specific number of bytes that object will actually allocated so we need a tan byte object to be allocated and we will know why 10 bytes specifically in the layout next slide so we know that the overflowed value is 1 0 0 0 0 0 0 10 and then we have to subtract 40 and 44 from it and we will reach the following number which is ffffff hc' and then if we take the result and look at the factors of that result the factors of that integral watched which represents which two numbers 1 multiplied together will actually result in that value will come out to HC and 1.d4 1.d4 1 if we take HC as the width and effectively reverse the calculations that happened in the previous slide by multiplying it by 8 and then dividing it by 20 will result in 23 so if I take this and create the bitmap that will be used by the creepatin brush by having the width of 23 and a height of one day for one day for one i will effectively reach the location function with a concluded value of 10 bytes and then after the function allocates the object it assigns that verse of the allocation to ESI and the next step it takes is actually try to read to write to certain offsets of ESI so the function is expecting ESI to be of a certain size hopefully minimum of 84 since if you supplied zero to be added to 40 and then 44 and to become an 84 byte structure so what happens when we overflow it with a 10 when we actually allocate a 10 bytes structure is that the lost highlighted in blue value will actually write to an out out of bonds offset of the object so yes I plus 3c is actually out of bounds since the allocation size is 10 bytes and it gets added to a pool header of 8 bytes which makes it 18 bytes so yes I plus 3c will actually write to the next object in memory the problem is that this object actually is allocated at the end of the page this object gets freed at the end of the function which if you could if you have it in the middle of a pool page it will effectively corrupt the next chunks header and that would be a problem when it gets freed at the end of the execution since it was the whole system will do scream with a bat will had a value so why specifically 10 bytes object if you look at the calculation that located object size is 10 and then the bitmap pull Heather if it's an adjacent bitmap is 8 bytes and then the bitmap or the surf object starts with a base object of 10 bytes and I'll set to the bitmap height a sport team will result in 3ec which is the same value that application price to write to so now we have three options we can use this extended size bitmap to actually override the address of an adjacent bitmap which is the old technique and in orbit read and write it will work fine and then reconvene system we can use the extended bitmap to demonstrate arbitrary read and write capability of a pallet object or we can do a bit of work and this is what we were chose to go with and use the extended bitmap to actually extend an adjacent pallet to gain relative read and write and then how the relative read under templates work and then use the extended pallet to actually override the P first color member of thus next toilet and game arbitrator Nevada right from there the first things is first so we need as I mentioned the object is allocated in the burner and if the object was allocated in the middle of a pool page then when the object gets freed at the end of the function the system will blue screen of death so how do we get over that is by allocating the object at the end of the page so when the object gets freed there is not a next chunk header and the pool will not we checking functions will not be able to check the next pool header and thus the fee will work normally and the application or the system will not crash and the first thing we need to do is we know that the pool pages are separated into thousand bytes each and we need to know that our object will be in total 18 bytes 10 bytes for the object that we supply and 8 bytes for the table header so we allocate a bitmap of size fe8 which we live we allocate our own 2000 of these tilde the pool memory becomes in a deterministic state which we leave an 18 byte unallocated memory at the end of the pool then we use the window Clause memory menu name which is available for the stage session pool to locate 18 byte strings that will fill in the unallocated 18 points memory then we freed the bitmap that we just allocated and continued to allocate an adjacent bitmap or an X that map of size 7 F 8 and then allocate pallets of sizes 7 e 8 effectively filling the thousand page or thousand by pool page with our control objects then we free some of the 8 employed many names that we allocated and hopefully if everything were we should find our object allocated at the end of the pool page as we see in quantity so now the object is allocated in the end of the pool page when the function frees the object it will be free it fine and also we have an addition bitmap and if you remember the calculations that if it's rise to 3c when it has an adjacent bitmap it would actually extend the size of this bitmap so the addition when we look at the a bitmap position bitmap in memory we notice that in the first part over the top part the size is 1 8 1 a 8 by 1 which is the width and height and after the overflow or the out of bone rights happened we notice that the size would be one a eight and the height would be six effectively extending its size and it would effectively be past its bounds so the next step to do is to actually extend that bitmap object to read and write to the next adjacent pallet and change the C entries number from 1 a 3 which is the original size of the adjacent pallet to FF FF effectively gaining a little bit and write to four gigabytes of kernel memory so how do we find this manager palette in the allocations so we have 2000 2000 palettes that are being allocated in memory how do I find the specific one that was extended this by roofing looping over that get palette entries function until one of the palette and the schools will result in the return value of a larger number than expected which is the size of one III so until we find one that returns a number like larger than three eight eight that's our manager palette that will be used to actually overwrite the next palette and memory so if we look at the next palette and memory the top part is before we actually try to overwrite true and we will use sub palette entries on the manager palette to actually set the P first color and updated to a value under our control so some of the data that we were reading using a palette entries we contain actually some pool addresses that will allow us to leak some pool addresses and calculate from it a header address from one of them which is the beginning of the pool page so if we change the palette and resolve one of the pool pages to the beginning of a pool page effectively pointing to the bitmap header since we need to get the set up to actually fix the bitmap object because of as is at the time we finished execution that system will clash because there were will habits that were corrupted so the way we can find it is again by looping over our located pallets using gap palette entries finding the first by the palette entry was the first void of 35 which represents the number 5 and if you remember the bitmap object has a tag of GH 5 which is part of the pool header so the first byte of that object will be actually 5 so the first thing we can use the extended palette which we extended its size as a manager palette and use it to set the P first color of the adjacent palette object of the worker palette object and then use the worker palette object to actually in Colonel Albert recurrent and read and write so the next step would be to use these primitives to actually read and/or change the current process token privileges so we start by getting the initial system process and as I explained earlier calculating the address of this kernel system process and then using arbitrary to eat a security token and then use a kernel right to replace our own and we will see now directly exactly how this happened so we start by using no privilege user and I've put in the code some debug breaks so when we execute the code it will actually break on kernel memory we just reload our symbols again and we find our place so on the right is our code we trigger the overflow and break on the location functions to see where exactly if everything is went right and you can see the pool layout went correct so we have our object allocated at the end and we have an extended pal bitmap and our manager palette and the worker palette in there I'm sorry forcibly fast all the videos would be available on the github page and then I'm going to fight the GDI object when debug extension to actually dump the structure from a pointer we can see the original bitmap size which was 1 & 1 in 3 and after the override we can see that the height of the bitmap to 6 next we view the pallet object that is adjacent to this bitmap to see if what changes happens in memory and we noticed that its original size is one e3 and then after we use set palette entries to actually overwrite its members we notice that the size will change from 183 to ffffff and now if we take a look at the next palette entry in memory we noticed that a pea first color member value is the default value or the original value and after we override the body using set palette entries the value has changed pointing to a beginning of a page or bitmap object and if we dump the memory that is located at this location now this is the updated P first column and if you look at the memory that is in that location we noticed an original but this is the global bitmap header and we already read the original bitmap adarand after we write to it we successfully modified the header to a fixed one and this is going over that a process token stealing so if you look at the poster structure that is saved in our code and dump the image filename and token we see that this is actually assistant process and that this token is the system token if we move on to get our current process itself we notice that the process name is exactly our exploit name and the token is normal token or not no privileges user token after we used a better read than arbitrary try to actually place our our current process token with a system one this means that we successfully actually got system privileges and when we continue execution and go back to our virtual machine when we will notice that we are actually system now so that's the first exploit I want to talk about so from a low-profit user abusing GI objects and abusing GDI palettes to effectively gain system privileges on the system basically doing an innovation of privilege on which usually the next exploit is ms 16 or 98 which is a region of affecting the region object and it's an integral over flows leading to a pool overflow if you look at the store biopatch the thing we notice that the function ulong multi is added to the fixed function which effectively checks if the two integral supplied will result in an overflow value and then it will pass this value back to a location function that's the patched version the unpatched version though will multiply to some calculations or a register and then pass the value directly into an aggregation function that will allocate that value so again how we reach this function if we look at the function site X that is defined in either which is VL function will notice that it works on a path objects so the way I did it was I just googled for MSDN fill and path and I reached a function fill path so when I call fill path from user memory and I think the example code out of either I notice that I'm not actually hitting the function that I'm supposed to hit but instead I'm hitting the first function of the series which is a mesh fast fit by analyzing this function I found out that it's actually checking the type of the device context that I'm doing this Phil path in and turns out that it required a memory bitmap path device context and this is the code that is used to actually reach the vulnerable function so the next step would be to control the location size so we know that the value is these in assembly instruction load effective address array X plus re x times 2 + 2 AC X's but effectively multiplying this value by 3 and then shifting that this value by 4 or a nibble exact factor here multiplying this value again by 16 so if we got the max end value and divided by 3 and then add 1 to it so we can actually overflow it resolved in 5 5 5 5 5 5 5 6 and then to check if we multiply this value by 3 again we'll notice that the value will be 1 0 0 zero - now that's before the shifting so after we shifted by four it will result in 1 0 0 0 0 0 0 20 and if you look at that value and you know that this expert is actually affecting Windows 8 64-bit then you'll notice that this value will not overflow at 64-bit register however I noticed during my analysis of two experts so far that if any some of the location functions wind up getting cold the size is actually casted into a 32-bit register before the allocation effectively truncating its value so this value will be 20 which will be supplied to the application now how can we actually control this allocation size well I found a book a blog post by Nicolas Economo which was great it was 4 ms sixteen or thirty nine and the exploit code itself or the funk the vulnerable function looks to be exactly copy pasted from the current function I was analyzing and he pointed out that the number of points in a given path is what actually controls this allocation value so how can I control the number of points in a path by using the user mode function polyline to and supplying a point array calling this function 156 times with three f1 points will result in a control value being allocated in kernel memory so if you multiply the two numbers you will result in 5 5 5 5 5 6 now someone will tell me this is much smaller than the value that we discussed during the calculations and you are right and due to the shifting of for this value will actually result in 1 0 0 0 0 0 0 5 and after it gets shifted by a nibble this value will be 50 and the one will be truncated we do our pool Feng Shui and the exact same thing as the lost exploit so we create we allocate large size bitmap and you notice that this time I left an allocated space of 80 the reason being at the time I didn't know a function that I can abused like menu name to actually allocate and deallocate small-sized object so I went with a weird way with the next step okay time to oscillator objects which have a fixed size of 40 bytes and then unallocated the bitmap and allocate a region object of size bc0 now this is just padding we don't actually need this object but if you allocate the bitmap adjacent to a vulnerable object directly it will actually the overflow will not result in over writing any of the interesting numbers that we find and then when we keep the space that is left with vc0 with our bitmap and three our accelerator tables and hopefully if everything went right we should find the following cool layout which shows that our object is allocated at the end of a pool page so when the object is freed to a low pressure system and the bitmap and the agent object is adjacent to it so we needed to control the overflow since if we overwrite this large number of points to a small allocation size it will actually blue screen of death the system faster than well then we can do anything the function responsible for copying points this B construct cut could add edge to get this function will actually cast the previous point that Y to attend and the current point of Y to our eleven then it will check the current point at white against a certain value which on my system was one-half zero and proved to be working on all systems now this value is actually a shifted value by four so the current point at Y will be shifted by four and then compared to this value if the value was more than that the comparison results in a value more than the one-half zero then it will not copy the point if it's less that it will copy the point and this gives us a primitive to actually control how many points are copied to memory thus controlling the overflow number so the way we can control it is by actually setting in our point array the third point to 20 decimal which is 14 hex shifted by 4 equals 1 for 0 which is less than 1/2 0 so until we hit a specific point of the polyline to Lu which is one half iterations that will copy exactly the number of points needed to overflow the bitmap height and width so before the overflow on the top we can see that this isn't bitmap is actually 1 and 52 and after the overflow happens the sizzle bitmap is one and half FFF effectively extending its size and where this value comes from is actually the same function that copies the point will subtract the previous point at Y from the current point at Y and if it was signed if it was unsigned it will write one to that offset and if it was signed it will write F F F F to a second offset we again loop over get permits this time to find out worker and luckily enough the next handle was the worker or the next bitmap in memory and we get some of the addresses that are leaked using it but get bits and from them calculate the offices or the addresses of the overflown objects so we can fix their headers and then we can use set bitmap it's on the manager to actually set the previous can zero of the worker and use the worker bitmap to write to anywhere in memory are with read and write and then we use this to fix the bitmap or and the region object or float headers and ultimately steal the system token and this is the video of it happening so we again we're using low privilege user and we have debugger breakpoints at on our exploit code we run our exploit at this point they're not so much ASCII art so we stay black like this now we break out our debugger we see that actually we are just before the the vulnerable function or triggering the integral overflow so we break at location and after the location after the right and we continue execution we notice that at that location we control the value and we'll be able to allocate a 50 byte object and try to copy around five gigs of points to it which will result in direct overflow but luckily we found a way to actually control the overflow as explained earlier and if you look at the layout of the pool after the location we'll notice that it's the same layer that was shown previously and we have allocated object dementia bit map and worker bitmap and we used a great GDL object Dom extension after modification to a Cheatham these bitmaps from memory so we can see the structure is clearly we see that the manager bitmap Sissel bitmap was sizes 1 and 52 and after the overflow or the point copying we find that the size is now one and ffff effectively extending the bitmap reading or writing functionality to four gigs of kernel memory and then we continue execution and we can see that we can use this manager bitmap to actually overwrite an adjacent bitmap objects Pistons 0 this is the original previous conceived of the worker bitmap and after we said that in peds we were able to overwrite it with a value that we control pointing to one of the global headers of the bitmap or region objects and a phenomenal memory we'll see this value of the header is actually clobbered and not a valid header and then we use the link addresses and they read a bitmap it's to actually read a proper header and fix the objects idle and then F a continue excuse in which the part where we will steal in the system talking and now we verify that it's actually a system token and not any poses and this is the token value then we go into finding our current process it was a structure in memory and we used a very kernel memory read and write to actually override this value with a system process talking and we verify that our current process was actually overwritten by assistant process token and we just continue execution and go back to our virtual machine to find that real system so the conclusions of this talk that we have not only one but I knew this undisclosed technique to GDI objects that we can abuse to gain our if economy eat Android and abuse political colour-pop corruptions we can identify hopefully and exploit the same type of vulnerabilities in kernel and any other objects and the tools used in this talk was Ida bendeth when debug and VMware and guys have got hold of me that my email or my Twitter handle and if you have any questions modifications suggestions or if you ever find they go forest because I really would like to meet him there's not enough time to take questions on stage so be heading to the chill-out lounge just next to this room if you guys have any questions or have any suggestions please cut me passion there thank you
