Hey, thanks everybody for showing up so early.&nbsp; I talk about Xtensa based Qualcomm WiFi chips,&nbsp;&nbsp; and so why even looking at WiFi chips? So in my&nbsp; opinion, these WiFi chips are pretty powerful&nbsp;&nbsp; chips. So even though they're just intended to&nbsp; run to do your WiFi stuff, they are still general&nbsp;&nbsp; purpose CPUs. So you, in theory, can run any&nbsp; software on it. But the problem is the proprietary&nbsp;&nbsp; binaries which come with those chips, they make&nbsp; it difficult to run your own code. And why would&nbsp;&nbsp; we even want to run our own code? So what we&nbsp; could do is enable additional functionality.&nbsp;&nbsp; I have a few examples in a few seconds, and we&nbsp; could also make security research easier. So&nbsp;&nbsp; in case we manage to enable dynamic analysis,&nbsp; security researchers have it easier to identify&nbsp;&nbsp; bugs and report them to the vendors. So some&nbsp; examples for additional features which we want&nbsp;&nbsp; to enable is, for example, we can enable Monitor&nbsp; Mode on chips, which is not available by default. &nbsp; For example, this is Monitor Mode on a&nbsp; Broadcom-based chip running on an Nexus 5,&nbsp;&nbsp; which is not available by default. And then you&nbsp;&nbsp; can use your standard WiFi tools like&nbsp; dump and so on.&nbsp;&nbsp; We could also build a complete state machine for&nbsp; Berkeley Packet Filtering inside the firmware,&nbsp;&nbsp; and then compile our rules for packet filtering&nbsp; on the user space, upload them via the kernel to&nbsp;&nbsp; the firmware and run them directly into firmware,&nbsp; so that way we could save some power when we do&nbsp;&nbsp; Monitor Mode basically. And if we have&nbsp; access to even a deeper layer firmware code,&nbsp;&nbsp; we can also implement stuff which is closer to the&nbsp; physical layer of WiFi. So here's an example of a&nbsp;&nbsp; pilot-tone jammer, which only jams the pilot tone&nbsp; of OFDM to be very power efficient. So now that&nbsp;&nbsp; we have all these great goals in mind, let's take&nbsp; a few steps back and look at the bigger picture. &nbsp; So in general, there are two kinds of WiFi chips,&nbsp; so FullMAC and SoftMAC chips, we will look at&nbsp;&nbsp; FullMAC chips in this talk. The biggest difference&nbsp; is that FullMAC chips run the MAC layer inside the&nbsp;&nbsp; firmware. So this also means if you want to change&nbsp; something in the firmware in the MAC layer, you&nbsp;&nbsp; have to change the firmware directly. So it's not&nbsp; possible to do these changes on the driver itself.&nbsp;&nbsp; So the examples I have shown in the previous&nbsp; slides of stuff which we could want to enable&nbsp;&nbsp; this was based on the Nexmon framework. I&nbsp; developed this together with a colleague of&nbsp;&nbsp; mine back in my time at university. So&nbsp; this enables us to, so this framework,&nbsp;&nbsp; will be used to modify Broadcom chips, and&nbsp; we even managed to get deep modifications&nbsp;&nbsp; working as this I showed&nbsp; earlier, and you can get it at nexmon.org.&nbsp;&nbsp; And apart from Broadcom, there was also work&nbsp; done on Blackhat 2020 about intel-based chips.&nbsp;&nbsp; And there was also a talk about Hexagon-based&nbsp; Qualcomm chips on DEF CON 27 and Blackhat 2019. &nbsp; But this talk is the first talk to explain&nbsp; Xtensa-based Qualcomm WiFi firmware.&nbsp;&nbsp; So some background, how are WiFi-SoCs structured?&nbsp; So normally you have an application core and then&nbsp;&nbsp; separately you have your WiFi core, and optionally&nbsp; in some cases, you also have a real-time core&nbsp;&nbsp; which handles time critical stuff like the&nbsp; distributed coordination function in WiFi.&nbsp;&nbsp; And now for the chip which we will look at, there&nbsp; are several drivers available. So there's ath10k&nbsp;&nbsp; by Qualcomm directly and also a corresponding&nbsp; firmware with that. And then there's ath10k-ct&nbsp;&nbsp; by Candelatech. So this is interesting, so&nbsp; Candelatech bought the rights from Qualcomm a time&nbsp;&nbsp; ago, some time ago, and they have their own driver&nbsp; and their own firmware. So they got the source&nbsp;&nbsp; code somehow from Qualcomm and added additional&nbsp; features. And so basically you have a complete&nbsp;&nbsp; separate kind of driver and firmware in addition&nbsp; to the original Qualcomm driver and firmware.&nbsp;&nbsp; And lastly, there's also the qcacld drive by&nbsp; Qualcomm. This is used for factory processes.&nbsp;&nbsp; And also what you can do is you can&nbsp; use the Candelatech driver and run&nbsp;&nbsp; the Qualcomm firmware. This is also possible. &nbsp; So this is the chip and the board I have looked&nbsp; at. It's the IPQ4019. It's based on development&nbsp;&nbsp; board by eight devices. This board is called&nbsp; Habanero. And in the middle of it, you can see the&nbsp;&nbsp; chip we are interested in. And this is basically a&nbsp; development board for a WiFi-enabled home routers.&nbsp;&nbsp; So what does this chip look like from the inside?&nbsp; So as I said, this is used for WiFi-enabled home&nbsp;&nbsp; routers, and one of the big vendors in Germany&nbsp; is also using it. So this is AVM, the FritzBox.&nbsp;&nbsp; And apart from this, the chip&nbsp; consists of, as I explained before,&nbsp;&nbsp; also of an application core and multiple&nbsp; WiFi cores. And the application core runs&nbsp;&nbsp; on OpenWRT in this case with a pretty old&nbsp; kernel. And for WiFi, they have one core&nbsp;&nbsp; for 2.4 gigahertz and one for 5 gigahertz. And&nbsp; they use a PCIe to communicate with each other. &nbsp; So now to the firmware itself. As I said, it's&nbsp; an Xtensa-based firmware. Xtensa was initially&nbsp;&nbsp; developed by Tensilica but now it was bought by&nbsp; Cadence and it's on a little endian firmware. It&nbsp;&nbsp; consists of a ROM part and RAM part. The RAM part&nbsp; of this firmware is stored in the file system of&nbsp;&nbsp; the OpenWRT, and it contains multiple segments,&nbsp; more on that later, and it's lz77 compressed. The&nbsp;&nbsp; ROM can be patched and there is also a code&nbsp; swap mechanism which allows the WiFi code to&nbsp;&nbsp; evict code from its own memory space to the host&nbsp; memory space. And of course, there's no security&nbsp;&nbsp; enabled by default, so no secure boot, no&nbsp; stack canaries, no address randomization.&nbsp;&nbsp; What's also nice is that there's already,&nbsp; by default, there comes this debugfs which&nbsp;&nbsp; you can compile into your driver. And this&nbsp; allows you to get direct memory access via&nbsp;&nbsp; this mem_value file. So you can directly&nbsp; read and write memory directly into the&nbsp;&nbsp; chip. There is also a debug mask, which is&nbsp; very useful, which you can set for example,&nbsp;&nbsp; doing visualization of the kernel module to&nbsp; increase the verbosity of the driver itself. &nbsp; So the interfaces which are used between basically&nbsp; the application core and the WiFi core, so there&nbsp;&nbsp; are two interfaces. The BMI interface, the&nbsp; Bootloader Messaging Interface is used to boot up&nbsp;&nbsp; and it's implemented in ROM. So basically during&nbsp; boot up, this is used to start up the chip. Then&nbsp;&nbsp; after boot up the WMI is used to communicate with&nbsp; the chip. And this is mostly done to send commands&nbsp;&nbsp; like, please now start WiFi scanning, do channel&nbsp; configuration, stuff like that. So this is how&nbsp;&nbsp; the loading looks like. So the driver offers two&nbsp; methods for a loading firmware either via the BMI&nbsp;&nbsp; method or via a copy engine. And because in our&nbsp; case we have a compressed firmware, we need to use&nbsp;&nbsp; the BMI method. And what's basically happening is&nbsp; that the firmware from the file system is passed&nbsp;&nbsp; through the driver via BMI to the WiFi core and&nbsp; it's then uncompressed on the WiFi core itself. &nbsp; So this is the file structure of the firmware&nbsp; file. So basically there are two big parts&nbsp;&nbsp; like this upper one and this lower one.&nbsp; They are identified with an IE header.&nbsp;&nbsp; And then after this there is the segment header&nbsp; and this is identified with these magic values&nbsp;&nbsp; here. It'll tell you if it's compressed&nbsp; or not, and on which address it should be&nbsp;&nbsp; loaded into the chip. And it'll also tell you&nbsp; what's the decompressed size of this segment.&nbsp;&nbsp; And if you notice already&nbsp; there are, in this case, the&nbsp;&nbsp; first part and the second part have the same&nbsp; address. So this means that they get overwritten.&nbsp;&nbsp; And then apart from the segment header, there's&nbsp; also some metadata as I just explained it. And&nbsp;&nbsp; after this, the real data starts and then it's&nbsp; compressed. Then after this, the next metadata&nbsp;&nbsp; starts in the next part, which is then also, of&nbsp; the firmware data, which is also compressed. &nbsp; So basically why these two separate parts is?&nbsp; My assumption is that the first part is just&nbsp;&nbsp; needed for boot up and you can also see it's much&nbsp; smaller in size than the second part. And after&nbsp;&nbsp; this we can basically override it with the real&nbsp; firmware code. And if you look at the driver logs,&nbsp;&nbsp; you will see this whole process being&nbsp; done twice. And I assume this is done to&nbsp;&nbsp; load up the first WiFi core and the second&nbsp; WiFi core. If you look at the memory layout,&nbsp;&nbsp; we can see that it's repeating. So if you just&nbsp; do a long enough read of your address space,&nbsp;&nbsp; you will see these patterns. And my assumption&nbsp; is that this is used to realize different memory&nbsp;&nbsp; access rights. So you can have the same memory&nbsp; on different offsets, and depending on offset,&nbsp;&nbsp; you can realize different access rights. Then to the Xtensa architecture. So there are&nbsp;&nbsp; two major things I want to point out about this&nbsp; architecture, which were new to me, and which also&nbsp;&nbsp; came with some problems. So the first one is the&nbsp; use of literal pools. So each load instruction,&nbsp;&nbsp; so this is just a load instruction, instead of&nbsp; being PC-dependent, it's basically independent&nbsp;&nbsp; of that and you just calculate an offset to&nbsp; a fixed LITBASE to get an immediate value.&nbsp;&nbsp; And in addition to that, there is also window&nbsp; registers. And so this means that, so that way&nbsp;&nbsp; I explain in a second, but what it's used for is&nbsp; basically to not needing to store, to save and&nbsp;&nbsp; restore, your registers when you call a function.&nbsp; So here in this example it's using Call8,&nbsp;&nbsp; so there can be different values here or four&nbsp; or 12 or whatever, but my firmware used Call8,&nbsp;&nbsp; and if you load an value in a10 and you call a&nbsp; function, it'll be available in a2 of the callee. &nbsp; Okay. So this is how these window registers look&nbsp; like. So basically you have these overlapping&nbsp;&nbsp; sections in your memory space and you have way&nbsp; more registers than you need for one function. And&nbsp;&nbsp; that way they can just shift the window for each&nbsp; function call if you have nested functions and&nbsp;&nbsp; they can basically just rename it and&nbsp; make it accessible to the next function.&nbsp;&nbsp; This is how literal pools work. So&nbsp; now the memory space is horizontal. So&nbsp;&nbsp; assuming we have a function, my_patch, which&nbsp; wants to call a function wlan_main. So the&nbsp;&nbsp; example for this will look like, there's first a&nbsp; load instruction which loads intermediate value&nbsp;&nbsp; into a register. And then the call is just&nbsp; using this register to call the function.&nbsp;&nbsp; And if we want to get this, the immediate value,&nbsp; we need to know the LITBASE and then we can look&nbsp;&nbsp; up the offset for this function we want to call,&nbsp; wlan_main in this case. And then we get the actual&nbsp;&nbsp; address of the function, so for example here.&nbsp; And this is done for each load instruction. &nbsp; So this means what's also the case is that this&nbsp; LITBASE needs to be set up somewhere. And this&nbsp;&nbsp; is usually done at the very beginning of the&nbsp; firmware. And this is a fixed value. So for&nbsp;&nbsp; me it was x408001 and this is the code how it&nbsp; looks like. So you basically have the start of&nbsp;&nbsp; this literal pool and its size and then you store&nbsp; it into a2 and write this to the special register,&nbsp;&nbsp; LITBASE. And so actually the offsets are negative,&nbsp; this is why you add the whole size to it and then&nbsp;&nbsp; a load instruction later on&nbsp; will have a negative offset.&nbsp;&nbsp; So basically the existing firmware does expect&nbsp; that this literal pool is used if we want. And&nbsp;&nbsp; this comes with some problems if&nbsp; we want to patch existing code. &nbsp; Another problem was that this LITBASE is not&nbsp; supported by disassemblers. So for example,&nbsp;&nbsp; IDA 7.7 added support for Xtensa but&nbsp; does ignore the LITBASE call. For Ghidra,&nbsp;&nbsp; there is an Xtensa plugin but it didn't support&nbsp; LITBASE either. Radare2 ignored LITBASE,&nbsp;&nbsp; has support for Xtensa. And there's also a Binary&nbsp; Ninja plugin, but it ignored LITBASE as well.&nbsp;&nbsp; So what I need to do is create some patches&nbsp; for Binary Ninja in this case. So in this patch&nbsp;&nbsp; I just ignore the dependency of the program&nbsp; counter and directly use this fixed LITBASE.&nbsp;&nbsp; And so this is the patch for Binary Ninja, and&nbsp; there's also the patch for Ghidra which works the&nbsp;&nbsp; same way. And I have a link to GitHub repository&nbsp; later on where you can find these patches. &nbsp; So now if you actually want to change some code&nbsp; in the firmware, I decided to use the Nexmon&nbsp;&nbsp; framework I have shown earlier, and I wanted to&nbsp; basically modify it to support Qualcomm-based&nbsp;&nbsp; WiFi chips. So this is how the framework&nbsp; looks like. It's pretty complicated,&nbsp;&nbsp; but we only need a subset of this&nbsp; to make it work. So what we could&nbsp;&nbsp; do with the original Nexmon was we&nbsp; could extract the ROM flashpatches,&nbsp;&nbsp; the RAM UCODE for Broadcom-based WiFi chips,&nbsp; and we could use C to write our patches, and of&nbsp;&nbsp; course we could call existing firmware functions.&nbsp; And then in the end with this whole framework,&nbsp;&nbsp; we were able to create a firmware file which we&nbsp; could then run on Broadcom-based WiFi chips. &nbsp; So how does this Nexmon framework work on a high&nbsp; level? So we have our own patches which are in&nbsp;&nbsp; patch.c, we have a wrapper.c which is used to&nbsp; have some steps for the functions which are&nbsp;&nbsp; already existing in the firmware. And we use&nbsp; GCC and a GCC plugin to compile .o files. And&nbsp;&nbsp; also the plugin will help to create this Nexmon&nbsp; pre file, and this Nexmon pre file basically&nbsp;&nbsp; contains some metadata which we use later on.&nbsp; And for example, we can use this file with an&nbsp;&nbsp; awk script to create some linker scripts and then&nbsp; we can use the linker scripts and the O file to&nbsp;&nbsp; create an overall patch file. So this patch file&nbsp; basically includes everything which we need.&nbsp;&nbsp; And now after this we can use another make file&nbsp; and the elf file to create basically a blueprint&nbsp;&nbsp; to copy out the relevant section of this elf&nbsp; file. So we might not need everything, just the&nbsp;&nbsp; patch we wanted to introduce, and some stuff we&nbsp; might want to override in the original function&nbsp;&nbsp; and the original firmware. So now we have this elf file and&nbsp;&nbsp; this make file, which is basically the blueprint&nbsp; for object copy. We can copy out the relevant&nbsp;&nbsp; sections, then we can dd them into the firmware,&nbsp; the firmware binary. This is how the original&nbsp;&nbsp; Nexmon worked. I needed to do some changes to make&nbsp; it work for Qualcomm. So first of all, there was&nbsp;&nbsp; no support for decompression. The Broadcom-based&nbsp; firmware had no compression. It was just the whole&nbsp;&nbsp; memory, the whole RAM of the chip basically,&nbsp; uncompressed. I also needed to add support for&nbsp;&nbsp; multiple binaries. So as we have seen earlier,&nbsp; the firmware consists of multiple segments and I&nbsp;&nbsp; needed to add support for that. And we also needed&nbsp; support for the LITBASE I mentioned earlier.&nbsp;&nbsp; And then if we make this happen, what we could&nbsp; do is compile and link our own patches. We could&nbsp;&nbsp; patch the second segment I showed earlier, and&nbsp; then we can compress it back into the second&nbsp;&nbsp; segment, add some padding bytes, and then write&nbsp; the firmware file basically. So this is the plan. &nbsp; So I will not explain the decompression. This is&nbsp; pretty straightforward to implement. I will start&nbsp;&nbsp; with the support for multiple binaries.&nbsp; So I needed to extend the GCC plugin,&nbsp;&nbsp; which creates this metadata file, the Nexmon pre&nbsp; file. And so basically what this GCC plugin does,&nbsp;&nbsp; it looks for this attribute in the&nbsp; source code. And this attribute tells&nbsp;&nbsp; basically where the following, the code after&nbsp; this, goes into the firmware, in which chip this&nbsp;&nbsp; is used and in which firmware version this is used&nbsp; in. And I extended this basically to also include&nbsp;&nbsp; the target firmware file. So after compiling&nbsp; where should this go into, of which segment&nbsp;&nbsp; of the firmware file should this go into. So basically then after we compile everything,&nbsp;&nbsp; we have this new Nexmon pre file and it basically&nbsp; contains the address, which is here where our code&nbsp;&nbsp; should go. It has basically the type, so here it's&nbsp; a patch. Then if it's a dummy, it's basically an&nbsp;&nbsp; existing function in the firmware. The next column&nbsp; is the .o file that this will be compiled into,&nbsp;&nbsp; this is the name of the text section&nbsp; in the .l file. And now we also added&nbsp;&nbsp; the name of this bin file where the compiled&nbsp; code should later on be copied into via dd.&nbsp;&nbsp; So with this, I already wanted to compile some&nbsp; patches. So my goal was to use this pretty simple&nbsp;&nbsp; patch. So just jump into some code into this&nbsp; function and then write 1, 2, 3, 4 basically&nbsp;&nbsp; into this memory location and then jump to the&nbsp; original code, which is wlan_main, in this case.&nbsp;&nbsp; I could use the esp32 GCC compiler to&nbsp; compile my stuff and load it into the chip. &nbsp; And I used the debugFS which I have&nbsp; shown earlier to check basically after&nbsp;&nbsp; the chip was up if the memory address has&nbsp; changed accordingly. But this didn't work.&nbsp;&nbsp; And the reason is this LITBASE I have explained&nbsp; earlier. So basically all the load instructions&nbsp;&nbsp; expect this LITBASE to be set at the very&nbsp; beginning of the firmware code. So this&nbsp;&nbsp; means the chip which runs the existing firmware&nbsp; code expects a load to honor this LITBASE. And I&nbsp;&nbsp; need basically now some way to tell my code either&nbsp; there is this existing LITBASE, please use this,&nbsp;&nbsp; and already this filled up. But unfortunately,&nbsp; there is no such parameter existing in GCC,&nbsp;&nbsp; so we need to do something else. And I just&nbsp; highlighted here all the load instructions&nbsp;&nbsp; which are problematic. As I said, the offsets&nbsp; are not right, they need to use this LITBASE.&nbsp;&nbsp; So what we could do as an alternative is&nbsp; just avoid load instructions and handcraft&nbsp;&nbsp; our assembly ourselves. So we just avoid&nbsp; all the references and have basically no&nbsp;&nbsp; immediate value. So this is obviously not&nbsp; something we want to do in the long run. &nbsp; So I came up with two possible solutions. Either,&nbsp; as I said earlier, we can tell the linker somehow&nbsp;&nbsp; where the existing LITBASE is and how full it is,&nbsp; or we can use our own LITBASE value. So I decided&nbsp;&nbsp; to go with the second option. I felt like that&nbsp; this is more flexible in the long run. And what&nbsp;&nbsp; this implies is basically that I set the LITBASE&nbsp; to zero at the entry of every function. This can&nbsp;&nbsp; be done using the GCC plugin and at the function&nbsp; exit I need to reset it to its original value.&nbsp;&nbsp; And I needed to do this into the assembler&nbsp; itself because there is this target-dependent&nbsp;&nbsp; feature which is called relaxation. So if you&nbsp; have a call to a function, this assembler will&nbsp;&nbsp; basically relax this to a load and a call. And&nbsp; then we have again a call which is problematic. &nbsp; So how does this looks like in the assembly?&nbsp; So as I said, this relaxation is problematic,&nbsp;&nbsp; and after relaxation we basically have this&nbsp; a call is extended to two instructions,&nbsp;&nbsp; this load and this call. The thing is that in&nbsp; between those two, basically we need to reset the&nbsp;&nbsp; LITBASE to its original value so that then if&nbsp; I call the existing function that the remaining&nbsp;&nbsp; code can run as intended. And yeah, it took me a&nbsp; while to find out that I cannot manipulate this&nbsp;&nbsp; behavior in GCC. It needs to be patched in the&nbsp; assembler directly because it's target-dependent.&nbsp;&nbsp; So how does this relaxation look like in the&nbsp; assembler? So basically you have this sort&nbsp;&nbsp; of a lookup table, on the left-hand side, it's&nbsp; looking for a pattern like this. So here it's&nbsp;&nbsp; looking for a call8, and if it finds this kind of&nbsp; pattern, it'll be relaxed to a load and a callx8. &nbsp; And what I basically need to do is find the place&nbsp; in the assembler where these build instructions.&nbsp;&nbsp; So basically the right-hand side here, these&nbsp; are called build instructions where those are&nbsp;&nbsp; iterated through and then they are applied and&nbsp; pushed on istack. And all I did is basically&nbsp;&nbsp; to look like is the current OPCODE code a callx&nbsp; and was the previous OPCODE a load instruction?&nbsp;&nbsp; And if this is the case, then I can basically&nbsp; add an additional instruction which will reset&nbsp;&nbsp; the LITBASE with an WSR instruction. So now&nbsp; that we know all of this and we have patched&nbsp;&nbsp; our assembler, we can go back to our original&nbsp; patching code and we can use this GCC plugin&nbsp;&nbsp; and the patched assembler to compile this much&nbsp; easier to look at code, which is pretty nice.&nbsp;&nbsp; So this is how the assembly looks like basically,&nbsp; after we have saved our parameters, we will save&nbsp;&nbsp; the current LITBASE into a15. We will write O&nbsp; into a14 and write a14 into the LITBASE value. &nbsp; And then we can do all of our load instructions.&nbsp; And at the end, we reset, or we copy back the&nbsp;&nbsp; value which we have stored in a15 into the&nbsp; LITBASE value before we do the final call out.&nbsp;&nbsp; So there are still some open problems with&nbsp; this. So this implementation is not very good,&nbsp;&nbsp; obviously. I take away two registers for&nbsp; the assembler, it would be better to have&nbsp;&nbsp; this based on the stack based implementation.&nbsp; Also, there's this support of disassemblers,&nbsp;&nbsp; which I mentioned earlier. So I have these&nbsp; patches, but still, for example, you cannot use&nbsp;&nbsp; IDA if you want to. And also there is no support&nbsp; in this GCC I'm using for naked functions. So this&nbsp;&nbsp; is a problem. So what I do is, you need some&nbsp; way to sometimes to override an existing call&nbsp;&nbsp; and firmware to even get to your patch. And&nbsp; either you're lucky and it's like they are using&nbsp;&nbsp; the literal pool, then you can just override&nbsp; the value in the literal pool. But if not,&nbsp;&nbsp; if it's like a direct call, then you need some&nbsp; way to just compile a code to your own code. &nbsp; And you could easily do this with naked functions.&nbsp; But in this GCC version, or GCC for Xtensa&nbsp;&nbsp; at least, does not have this option for naked&nbsp; functions. And what's also missing right now is&nbsp;&nbsp; that there is no text console. So you cannot just&nbsp; do a print F and somehow dump this in your host&nbsp;&nbsp; system to do some basic debugging. But we could&nbsp; implement this ourselves now. So basically, I have&nbsp;&nbsp; this very creative name now for my framework,&nbsp; which is called QCAMON instead of Nexmon.&nbsp;&nbsp; So this is how the folder structure looks like.&nbsp; At the very top you have some build tools,&nbsp;&nbsp; then the disassembler patches which I mentioned&nbsp; earlier for Ghidra and Binary Ninja, and then&nbsp;&nbsp; there's a folder for the original firmwares and&nbsp; a folder for our patches which we want to apply. &nbsp; The build tools contain the patched assembler and&nbsp; GCC and also the GCC plugin. The firmware folder&nbsp;&nbsp; basically contains the original firmware&nbsp; and it already comes with a structure&nbsp;&nbsp; which would allow different chips in&nbsp; different firmware versions. And there&nbsp;&nbsp; is also a make file which already extracts&nbsp; the relevant parts and decompresses them.&nbsp;&nbsp; And then at the end we have our patch folder,&nbsp; which we can use to store our source code,&nbsp;&nbsp; which we want to introduce into the firmware. So now to a brief demo. So this will basically&nbsp;&nbsp; show the code I had in my slides earlier. So&nbsp; we just want to set 1234 or store 1234 into&nbsp;&nbsp; this memory address and then jump to the original&nbsp; code. And in addition to what I've shown earlier,&nbsp;&nbsp; we also need these two lines to basically have&nbsp; an entry point into our firmware code. So all&nbsp;&nbsp; I'm doing here is creating basically overriding&nbsp; stuff in the literal pool to point to my code.&nbsp;&nbsp; So everything is prepared in the&nbsp; repository, you just need to run&nbsp;&nbsp; make and this will already compile the C files&nbsp; and .o files. It'll also compile the wrapper,&nbsp;&nbsp; it'll create the linker scripts and create&nbsp; a .l file and then use this blueprint,&nbsp;&nbsp; make file I mentioned earlier, to copy out&nbsp; the relevant parts and bin files. Then we can&nbsp;&nbsp; start reassembling the whole firmware file&nbsp; via compressing it, adding bytes, and then&nbsp;&nbsp; basically creating the firmware&nbsp; file, how it's expected by the WiFi&nbsp;&nbsp; chip. So here it's called firmware-5.bin. I will now ssh into this board I've shown&nbsp;&nbsp; earlier. And in order to access the debugFS, we&nbsp; first need to set up the wlan0 interface. Now we&nbsp;&nbsp; can go into the debugFS and look at the memory&nbsp; location basically without any modifications.&nbsp;&nbsp; So for this I can just use dd to copy out the&nbsp; relevant section and use hexdump to look at it.&nbsp;&nbsp; So here we see that the first few bytes are not&nbsp; 1234, and now we install our modified firmware,&nbsp;&nbsp; just copy it over via scp, and we will need&nbsp; to remove and add the PCIe driver to apply&nbsp;&nbsp; the changes. Now we can ssh back into the router.&nbsp; Just checking the kernel log real quick. So here&nbsp;&nbsp; it's already setting up the regulatory domain, so&nbsp; this looks quite good. So it did not crash on us.&nbsp;&nbsp; And we can go back into the debugFS, set&nbsp; up the wlan0 first, go back to the debugFS,&nbsp;&nbsp; then basically we can copy out the&nbsp;&nbsp; same memory location, which we just did a second&nbsp; ago and have a look at it via the hexdump. &nbsp; Yep, if you can see here. We modified&nbsp; two whole bytes bytes. Thanks.&nbsp;&nbsp; So with this quick summary and future work, so I&nbsp; was able to modify the Nexmon framework to make it&nbsp;&nbsp; work with the Qualcomm-based firmwares. The demo&nbsp; page patch I have shown, you can get it via this&nbsp;&nbsp; location. There's also the patches for Binary&nbsp; Ninja and Ghidra in there, the GCC plugin, and&nbsp;&nbsp; already a pre-compiled version of GCC and also the&nbsp; patched Binutils file for the assembler. So this&nbsp;&nbsp; shows that it's possible to make modifications&nbsp; on Qualcomm-based firmwares. There are still&nbsp;&nbsp; some improvements which I need to do to make&nbsp; modifications even more easier. I also want to&nbsp;&nbsp; look at this production software driver thing I&nbsp; mentioned at the very beginning. So this qcacld,&nbsp;&nbsp; so this is basically using this QDART software&nbsp; on a PC to communicate, and you can get it from&nbsp;&nbsp; some sketchy Chinese servers. And then I want to&nbsp; see if I can maybe enable some features which are&nbsp;&nbsp; used in this production software also by&nbsp; myself. And I also want to look into this&nbsp;&nbsp; codeswap feature a little bit more. So I want to thank Martin Korth,&nbsp;&nbsp; aka ProblemKaputt. So he did some awesome Game Boy&nbsp; Advance reverse engineering and he helped me out&nbsp;&nbsp; in some cases. So it turns out that he did not&nbsp; even reverse engineer the main processor of the&nbsp;&nbsp; Game Boy Advance, but also the WiFi chip used&nbsp; there. And this turns out to be like an older&nbsp;&nbsp; Atheros-based WiFi chip, which he also reverse&nbsp; engineered. And Atheros later on was also bought&nbsp;&nbsp; by Qualcomm. And I think lots of knowledge from&nbsp; there still went into their products. And I also&nbsp;&nbsp; want to thank Rqou, aka ArcaneNibble. I used her&nbsp; script to do the unzipping of the firmware file.&nbsp;&nbsp; That's it from my side. Thanks everybody. If&nbsp; you want to reach out, here's my email address.&nbsp;&nbsp; If you have any questions, shoot. &nbsp; I have also one more note, I found a firmware&nbsp; file which contains all debug symbols for this&nbsp;&nbsp; IPQ4019 chip. I have no easy way to distribute&nbsp; this without the original repository being taken&nbsp;&nbsp; down. But if you want to poke at it yourself,&nbsp; reach out to me, I will send you the link. Any&nbsp;&nbsp; questions? No. Okay then, thanks everybody.
