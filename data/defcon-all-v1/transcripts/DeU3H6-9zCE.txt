hi welcome thanks for attending our talk this is room for escape scribbling outside the lines of template security security so my name is alvaro munoz also known as pawn tester in the social media i'm security researcher with the github security lab security lab and with me my colleague alexander miros also known as olek mirosh in twitter and he is security researcher with microfocus fortify team so today we will be reviewing the security of content management systems or cms or cms and basically we will be focusing on the security of the template systems that are used by cmss so a content management management system is basically an application that is used to manage different types of web content content so that users can actually create upload and publish different type of content so normally this content is based on some some structure some some kind of documents called templates called templates which are used to generate and create dynamic content dynamic content so these these templates normally allow a subset of different programming language capabilities so they are powerful in the sense that they can be used to run arbitrary code code because of that they are normally sandboxed in order to prevent arbitrary code execution and remote code execution execution so our research will focus on both net and java and java based content management systems because these are the languages that are more prevalent across enterprises enterprises and our assumption is that the attacker can control can control the templates right so we don't really care about how the attacker get control of these templates in the first place if it's through it's through a server-side template injection or if it's maybe through our cross-site scripting scripting that allows him to submit arbitrary templates in the in behalf or of someone else or maybe they have permissions like uh if you are a user for example in sharepoint if you have an account in surplus you are allowed to create your own sites and content so no matter how the attacker can edit the content of a template template our research will focus on escaping and breaking all the mitigations put in place to prevent arbitrarily execution so we will first start with dotnet and basically we will focus on on sharepoint we will present five different ways we were able to break the sandbox used by serpent to prevent arbitrary code execution and then we will move into the java part where we will present four different template engines including the most popular ones such as velocity and free marker and then we will present 10 different content management systems and we will try our bypasses on them so we will wrap up with some takeaways and conclusions and then open the floor for q q a so let's start with the dot net part thank garvaro in this part of our presentation we will present different types of security problem in content management system and give example them in sharepoint but before that before that we need to explain some basics of sharepoint security sharepoint has two types of types of aspx pages the first type is application pages pages each of these pages are the part of sharepoint server sharepoint server and implements some application logic they are stored in specific folders on the file system of course user are not able to change them they are regular aspx pages and are processed by server without any restrictions in opposite to them there are side pages they are stored in database and they can be customized even by users sharepoint parts of them in special mode also known as safe mode actually they are more like some sort of templates for presenting dynamic content dynamic content on this slide we can see a diagram how server works with this page the tool provider can fetch content from file system or database based on the virtual path of the current page sp page sp page parts filter will decide either it is site page and safe mode should be applied applied or it is an application page and it will be processed without any restrictions so page filter is critical element from security design security design design let's have a look on simple of aspx page for our attacks we are only interested in server side components for example directives there are special instructions on how servers should process the current page often they have some number of attributes we can include server site code either in special blocks or as embedded server site code to be processed by server control should have have the runoff server attribute the next hour element is server side command block it is ignored by server and server side include directives allow us to include raw content of arbitrary file now we can better understand what safe mode means mode means site page will not be compiled so we cannot use server side code we are not able to include files from file system file system we can only use allowed controls this list is defined in safe control section of web config file page filter also applies an allow list for directives and even for attributes of most of them there are many other restrictions for example event binding is blocked as well as we can see it is a set of quite strong restrictions how it can be bypassed as we know safe mode is enforced by page filter filter so our first question was is there any place where sharepoint does not use it and answer yes there are such places such places page filter will not be applied for the content argument content argument of the parse control method if it's called only with one argument or if the second one that actually has name ignore parts filter is true in addition page filter is used at rendering time rendering time but is ignored at design time but even if page filter is not applied sharepoint uses another way to verify the input the input it is a verify control on save list method method from editing page parse type that aim to do the same task at the original page filter filter block markup with unsafe elements but let's imagine for a second that we are able to bypass either this method or page filter page filter by the way each such bypass is separate vulnerability and we will present them in a few minutes so how could we get remote code execution after escaping in the safe mode remember since past control method never cause compilation cause compilation we are not able to use server side code or perform any other attack that require compilation but if we escape safe mode we can use unsafe controls or directives let's review several of them one of the most interesting interesting and safe controls is object data source it allows us to invoke public methods from arbitrary public type we can see an example of payload that uses start method from process type to launch calculator launch calculator so actually it is arbitrary record execution checkpoint execution checkpoint but let's review a couple other and save controls controls xml data source and xml controls allow us to get content of arbitrary xml files the similar results we can get by server site include directives that return us content of any text files let's see how we can escalate from arbitrary file rate to arbitrary code execution if we can access web config file we will be able to get validation key from machine section key section by the way this value may be present in other places such as internal sharepoint properties properties we will see this example a bit later with validation queue we can generate a valid view state that will execute arbitrary code commands on the target server we can use tools such as user serial.net folders now when we know what safe mode is and how we can compromise sharepoint server if it is bypass we can move to our next part of this talk talk here we will present five different types of security problem in content management system we will show examples of each type in sharepoint sharepoint we would like to stress here that our target is sharepoint server with default configuration configuration all vulnerabilities allowed us to execute arbitrary code on the target and all attacks were performed by regular user regular user no admin rights were needed let's start the first type of security problems in it is access to resources with sensitive information it can be configuration or business information business information in various files logs database or even process memory process memory of course proper sandbox should prevent this problem but it is not always the case we already mentioned mentioned that page filter block server side includes directives includes directives and verify control unsafe list method should do should do this as well and actually does but if it's called with faults in block server side includes argument it will not block them and we found such usage of this method it was used for design mode the next building block for our tag how we can provide our markup for this mode mode we can send it in webpart.xml parameter of the render web part for edit method of the web web page services so we can try to include web config files there here we can see the full payload for the mentioned parameter and address to relevant web services server returned us the entire web config with validation key we use it for view state based visualization attack which led us to executing arbitrary os command on the server time for our next type of security problems in content management system a loud dangerous element that can be used for attacks we have found an example in sharepoint safe control list or should we call it not so safe control list in this case it is contact link suggestion micro view control control we can see snippet with interesting code from its implementation where request parameter is parameter is passed to dataset read xml method if we can control input for this method we can get our data decentralized by xml serializer with control type we already showed in one of our previous work that in this case we can get arbitrary code execution unfortunately this payload is too big for slides but you can find it in our whitepaper whitepaper the last piece of the puzzle is our side page with this dangerous control you can see it on this slide for the attack we just need to place to place the payload into the text box and click on submit button and desire command will be executed on the sharepoint server for us the next type of security problems looks quite interesting so we will explain it with more details and present two separate examples from sharepoint server dynamic content usually means that we will have at least read access to some properties or attributes of some objects for some system like sharepoint we can even write even write values there of course sandbox should filter out filter out the access to dangerous property or attributes attributes let's review basic type of such filtering the first case is when only one level of properties or attributes is allowed allowed we can see examples on the slide name property of user object of select value from menu from menu we can imagine the object and its properties properties like a branch with only leaves if an allow list is applied it's relatively easy to review all elements and after such review we can make sure that there is no dangerous elements what if block list is used instead we still need to verify available objects and it can be a bit difficult so in general we do not consider block list a safe approach because of potential gaps there we will see in java part a couple of examples of search by passes the most interest case for us is where access to nested properties or attributes attributes is allowed for example request dot out user dot name or menu.selecteditem.txt menu.selecteditem.txt because of branch leaves analogy many developers may imagine this case as branch with branches and leaves and they may apply a filter only for the first level objects but it is not tree it is network and we may jump from one branch to another branch another branch or even to the trunk for example on the slide we can see how we can access application instance from a new web control let's illustrate this problem by examples from sharepoint there is wiki content web part control it is allowed and can pass our markup into the parse control method please note it is called with false value in ignore parts filter argument so page filter will not be ignored the following snippet shows that in this case the virtual path will be created from from up relative virtual path property and if you remember patch filter will apply restriction based on this virtual path so if we are able to change it we can fool the page filter to not apply any restriction to our markup let's try to graph such payload first snippet fails because when ispx parser tries to assign a new value to a productive virtual path the page property of our control is not defined yet defined yet so we need to delay this assignment a little bit we can do this by data binding so by the time the property is bound the page property will be already defined here is our final payload we can notice that our new path points to application p page setting ispx it is assigned to tooltip property and letter will be transferred to up relative virtual path so the page filter will think that this is system page and will allow any unsafe markup for example we can use object data source control to start calculator calculator let's see how the real attack may look we can see a site page with our payload path to system page it will be assigned to a relative virtual path in safe markup contain subject data source control source control we will invoke start method of the process type process type and we'll start calculator let's save it here here for our attack we need to upload it to the target server in our case it's sharepoint with default configuration that means that means a regular user have access to their own personal sites personal sites and we can use them to host our site page we are notificated as regular user with name attacker sneaky isn't and here is his personal site let's upload our page here now to trigger our tag we just need to click on it but before that let's take a look on the right slide of our screen our screen we can see task manager on our sharepoint server and we will notice if calculator start now let's start let's start our talk and here is our calculator let's move to our next vulnerability in sharepoint now we will try to export read access to nested properties we will use control parameter it allows to bind to bind value of property from another control to select to select parameter also it support nested properties properties we need to deliver value of target property back to us to do this we will use xml url data source control source control that sends a value of select parameters to external server now we need to find accessible property with sensitive information here we should explain should explain a little bit configuration process of sharepoint online servers obviously they are installed and configured automatically but what about configuration parameters that are unique for each installation they will be provided in special file that will be used for unintended configuration what is important for our attack all such parameters including validation key will be stored in initialization setting property so we just need to find a path of an asset property to it from any allowed control and here it is here is the whole page that will send value of validation key to attackers to attackers server.com with this value we successfully got arbiter record execution by execution by and save the civilization in view state now let's move to the next type of security problems it is connected with our previous work on a safety civilization civilization such kind of problem relevant when text or binary data is converted to an object and the type or class of this this object is under attacker control plus it is not is not properly limited actually it doesn't really matter really matter what mechanism is used classical decelerators decelerators json and marshallers type converters or even even some custom approach all of them are potentially dangerous potentially dangerous for successful exploitation attack address may need to find proper gadget on the system we have found examples of such problems in sharepoint and were able to perform arbitrary code execution attack using it but patch is not available yet so we are going to publish all details of this problem when fix is released the last type of security problems in this part is the classical time of track to time of use problems they occur when the server invalidates some input some input but this input is later changed before the server use it we have found such problem in web part editing surface page user input is validated by the already mentioned refine control unsafe list method method but later the server may modify the input and remove substring that much specific regex pattern pattern let's look on the next input for validation validation control on safe list it is just one server side command block so it will pass validation but sharepoint removes the highlighted yellow substring yellow substring and parse controller will see two comments and some markup note parse control has only one argument so page filter will be ignored time for our payload we can notice our unsafe object data source control and calculator command in this part of our talk we saw multiple ways to achieve a repeat record execution on the sharepoint server by regular user this vulnerabilities reveal reveal five different types of security problems in content management system now let's switch back to alvaro and he will present even more vulnerabilities with templates for dynamic content in different java frameworks and products products thank you alexander so switching to the java part we will be reviewing four of the most popular java template engines including including free marker velocity pebble and gene java and also we will be reviewing 10 different content management systems that are using these engines and then we will be trying our bypasses on these content management systems systems so a brief introduction into what template engines are basically these are systems systems that take two different inputs one is the template where we are mixing static and dynamic expressions expressions and also the template context where we store objects store objects that are required by the engine in order to resolve to resolve the dynamic expressions in the template so for example here we have the user.name expression user.name expression in order to resolve it we need to get access to the user object in the context and then invoke the get name method so because it's invoking java methods it's basically dangerous in the sense that attackers can execute arbitrary java code and that's the reason why most of them are sandbox in order to prevent arbitrary code execution so apart from the objects that are normally and directly used by the template and there are a number of other objects that are exposed as part of a template api that is normally normally provided by the content management systems systems we which basically um provides objects such as for example the request and the response the satellite context or even the http session so those objects are available in most of the template apis template apis and also many more that are part of this api api so we will be discussing first some generic generic bypasses that means that are independent of of which engine is used no matter if it's a free marker velocity pebble or gin java right so those bypasses are bypasses are based on finding different objects that we can use to get arbitrary code execution code execution so we need to inspect the template context and look for this kind of objects objects so in order to do that if we have access to the jvm we can basically debug it or we can instrument it dump the template context and inspect and and analyze all the objects in there however without if we don't have access to the jbm jbm we will have to read the template api documentation documentation maybe do some brute forcing in order to guess names like for example request or wreck or response resp and things like that and also in some of the engines like for example free market it is possible to list all the variables or or objects that are exposed in the context so apart from those objects that are directly exposed to the to the context we have some objects that are indirectly exposed like for example if we are exposing the http session because session because it contains some attributes all of those attributes will be available for the users and attackers to to access and use same thing with the servlet request and same thing with the servlet context servlet context so those are stores of attributes stores of objects that the attacker can get access to access to and use to look for interesting objects so so let's see how many objects we can find in one simple content management systems in this case it will be live ray and in order to do that we will be generating this template that i'm showing you which basically is listing all of the content context variables then all of the the request attributes then we will show all the requests request sorry the session attributes then the servlet context attributes and because the servlet context will expose the expose the um spring framework context we will be also listing all of the spring bins all the objects the objects are registered in the spring context so if we if we render that you will see that we have a bunch of objects directly exposed to the context those are part of the template api template api and some of them are very interesting others are exposed as part of their request attributes request attributes session attributes and servlet context attributes so as we can see here we have interesting interesting objects like an instance manager or for example the example the web application context for sprint that we use to list and access all the spring based based so as you can see there are hundreds and thousands of objects here there is a total of almost 900 objects and because of some of them will have public fields that we can get access to then this number will increase to thousands of objects so back to the slides this is why we call it object dumpster typing because this is how we felt when i'm trying to find very few interesting objects interesting objects that can lead to remote code execution among among you know thousands and thousands of objects objects so now we will be reviewing the top three objects three objects that we found that lead to remote code execution so for example the first one the class loaders was available in 10 out of the 10 content management systems that we analyzed analyzed so normally you can get access to this class loader class loader instance through the get class loader method in the class type but that is normally blocked by any block list by any sandbox so you can still get access if you get access to a protection domain or a satellite context as we saw before so once that you get access to a class loader you can obviously uh load arbitrary classes and then also then also load arbitrary resources from the class path path but apart from that you get access to a url url object and then from there you can access any file from the file system that the application server has permissions to access so here you have an example you get the url using the get resource method and then you point the url to the uh password file and then you open a connection and read the all the bytes from from that file so um apart from the standard web class loaders because content management systems are normally deployed on top of separate containers or application servers the instance of the class loader that you will be able to access is normally an instance of a web application class loader that are slightly different because of the way that class loading delegation work but also because they expose a number of additional methods additional methods that are not part of the abstract class loader class loader class so here are just some examples um just mention because we don't have time to cover them in this talk although it's explained in the white paper that we are publishing as part of this uh this talk and there are different ways of using these methods in this web application class loader to get remote code execution code execution just to mention some of the of the of the vectors the vectors we will be able to upload web cells instantiate arbitrary objects perform jndi injections or even initialize attacker control classes so we were able to found um to find sorry sorry um web application class loaders in nine out of the ten content management systems that we analyzed and then the second object the most uh second most uh easy to find object in this template context was the instance manager or or the object factory object factory so this was found in nine out of the ten content management systems and is normally found as part of the servlet context attributes but also it can be accessed through one of these web application class loaders so if you get access to an instance manager what you can do is basically instantiate instantiate arbitrary types so with that it's very easy to escalate to remote code execution execution we here we have an example using the script script engine manager but there are a bunch of classes that can lead to remote execution once that you can instantiate them and then invoke arbitrary methods on them so the number three object was a spring application context application context it was um only available in four out of the ten the ten cmss that we analyzed actually the only four that were used in the spring framework underneath and is normally accessible through one of the satellite context attributes context attributes but also if the content management system is using a spring mvc framework for the model controller and view layers then a spring will inject some additional objects in the template context context like this spring macro request context that will that will provide access to the spring application context context so once that we have access to the spring context we can get an instance of a class loader and initiate a class rather based attacks like we saw before before we can access the spring environment that will give us access to the system environment and also the system properties and also sprint properties including things like encryption keys and so on but probably more interesting it will give us access to the spring bins registered in this spring application context context so being able to access and manipulate those pins we can basically control the application logic application logic and do things like register new users and delete users perform transactions anything that the application application is normally doing so that's normally behind an authorization layer so no matter what roles we have we will be able to perform and this kind of application logic control control so apart from that we will also be able to disable the template engine sandboxes if we if we are able to get access to the template engine configuration bins and also instantiate arbitrary objects like we will see later by accessing json or xml and marshallers so those were like generic bypasses independent of independent of what template engine was used underneath and now we will be reviewing some specific sandboxes by passes for each of these four different template engines so we will start with free marker because it's probably the most popular one one and also because well it was analyzed and and researched by different researchers in in the past like for example james kettle back in 2015 when he presented server-side template injection template injection vulnerability class uh by the way if you haven't watched this talk it's highly recommended he presented a bypass that is based on a kind of add-on or module that is deployed on the default configuration of free marker um so tonito ralpha last year presented a different bypass but this one is not universal in the sense that it depends on a non-default configuration of free market and also being able to find gadgets in third-party libraries that may or may not be available in a specific target and then ryan hansen this year probably at the same time that we were doing this research research presented a vector that leads to remote code execution by uploading a web shell but this is not universal in the sense that it only works with tomcat servers servers so free marker sandbox is a method based block list which basically means like that for example methods like the get class loader class loader method in the java lan class is blocked and you cannot get an instance of a class loader using this method but as we mentioned before you can still use the satellite context or the protection domain to get access to these class loaders once that you get access to a class loader the class loader methods are not blocked by the block list so you can still interact with them and load classes get resources or initiate remote execution attacks that we saw before and also reflection is is forbidden to set fields and values but is is allowed to get and read field values so we use that uh to perform an attack but this attack is not universal in the sense that it depends on being able to find an instance of a url class loader so this is normally the case in tomcat class fees and jetty but it's not universal for other application servers application servers so in this case we are basically getting the url class loader from the protection domain or from the satellite context and then we are basically uh instantiating uh by invoking this new instance method a new url class loader that is pointing to the attacker control jar file in this case the pawn.jr file once that we get access to this class loader we can get a load arbitrary classes from our own jar file and we will be loading the pawn class class and now the class is loaded but it's not initialized if we want to run arbitrary code we need to initialize the class class we cannot instantiate the class we cannot invoke cannot invoke static methods so those vectors those ways of ways of initializing the class are not a possibility for us but we can get access to public static fields fields so just by adding a public static field and getting access to that the class will be initialized and the payload will be executed be executed so that was not a universal bypass in the sense the sense that we are required to find an instance of a url class loader and we want to find something that works no matter no matter if we are able to find that kind of class loader or not so we are interested in finding some classes that contains public static fields fields of a given type which contains a method that we can use to instantiate arbitrary types types to find them we use codeql which is a language that you can use to query your source code in the same way that you use for example sql to query um a database and what we wrote was this query that basically says okay find me all the public static fields fields which are of a given type that can instantiate arbitrary types we run this query on free marker source code and we found four different results those are four different fields different fields of different types but all uh all those types extend types extend from the bins wrapper type which contains a new instance method which we can use to instantiate arbitrary types arbitrary types now this is the universal um remote code execution bypass execution bypass for free marker basically we need to get us access to any type of class loader from from either the protection domain servlet context context and then load the object wrapper interface interface and get the default wrapper field from this interface this interface and then we can use that to instantiate arbitrary classes arbitrary classes since we want to have this payload basically contained in in free market classes we are using the execute class in order to run arbitrary system commands but you can use other payloads other classes to get arbitrary code execution so this was fixed in version 230 of free marker marker and also if you get access to the spring bins bins and you can get access to the free market configuration as we saw in some of the content management systems that we analyzed we analyzed you can basically disable the sandbox for example you can get the class resolver for the default configuration that is non-sandboxed and then set that class resolver as the class resolver for the current and sandbox configuration so effectively disabling the disabling the the sandbox and being able to execute the the the old payload from james kettle once again again so now we will be reviewing eight different ways to escape the sandbox and get remote code execution in liferay our live ray instance here and what we will be doing is basically using this template and here what we are going to do basically is first of all we are going to use the class loader class loader payload which will allow us to read the password file password file using the url trick that we explained before before then because the instance of the class loader is a web application class loader we will be using that class loader to upload a web cell by using the right method method this will be available in the cell in the cell the cell jsp file then we will be also using the web application class loader to get access to an instance manager and then instantiate arbitrary classes in this case we are instantiating an expression language processor and then running the id process then we will be also showing a different payload payload that depends on using an instance manager this time is taken from the self-led context and again we are using the el processor to run the id command and then we will be using a json deserializer as an instance manager in the sense that we will be um using that to instantiate arbitrary types arbitrary types we will be getting access to the json digitalizer from the spring application context context and because this json dc align uses a white list an allow list here we need to register or we can actually register register any arbitrary types for us to use in the in the json payload so now by deserializing this json payload we are effectively instantiating an el processor and then again running the id command id command same thing uh but instead of getting the json digitalizer from the spring beam context uh live ray um directly exposed this json factory util which we can use to create a json digitalizer and then once again use that to instantiate arbitrary types and then we will see the two free market specific bypasses that we explained a few minutes ago the first one is using the url class loader that is the case here because life ray is deployed on tomcat and then we can basically access our own jar file jar file load it and then by getting a public static field static field the class will be insta initialized and the payload will be executed and we will be able to check the output of this payload um by checking the the standard output and the last payload that worked in live array actually there's there's a couple more but um well it was more than enough than enough is this universal rc so we are loading the object wrapper interface loading the default grabber field and then using that field to instantiate arbitrary types in this case the execute a type that we will be using to run the id so now if we save this template and we will be showing the results of these payloads payloads the first one was reading the password file the second one was deploying a web cell that we can now access and run arbitrary commands and the third one was using different methods to run the id command as you can see here all of them worked so also the url class loader was basically basically running the static initializer block of this class which is basically a printing point in the standard output console we can check that here it is so it worked and back to the payloads um uh the universal rc payload also worked as we can see here so switching to the velocity sandbox this one is based on a class and package based blog list so things like for example the whole java lang reflect package is forbidden and you can get access to it all the methods in java land class and class loader are also blocked and you cannot cannot use them but when we review the implementation implementation we found that and there is a flow in the way way that this sandbox is checking the the block list block list so for example when you run the request separate separate and then you get access to a class loader and run the load class method basically the execution of this method will be will be checked against the block list and the class name class name of the current class loader will be checked against checked against the block list instead of checking the whole hierarchy of classes only the current object class will be checked against the block list which means that because java lang class loader is an abstract class it won't never be the case that the current current object class will be a java lang class loader in this case it's a parallel web app class loader which is not present in the block list and therefore class loader methods are effectively allowed and there is a feature feature in velocity which allows you to invoke static methods from a javalan class object object so once that we are able to load a class we can we can just invoke any static method and use that to get remote execution easily so now moving to gene java sandbox um this one is based on a method block list and this is actually very short only nine methods and um where it contains a piece of code that is pretty effective in protecting the sandbox so every time that a method invocation happens as part of the evaluation of the template the result type will be checked and if it's an instance of a javelin class then it will throw an exception and it will refuse will refuse to return a java line class instance by not being able to get in access to arbitrary classes arbitrary classes we are normally not able to get or to be able to instantiate arbitrary types however we found a flow um as you can see here the invocation of a class cannot return a javalan class instance but it can return can return an array of java lang classes or a map including java land classes so what we can do is find a method returning an array of classes and then accessing any arbitrary item of this array array so the second part of this bypass was basically being able to access a secret or hidden or hidden variable in the context that is called underscore underscore underscore interpreter in lead speech so by using this this hidden variable we were able to access a bunch of different objects including all the context objects and we were able to use those two parts to get an instance of a class loader so as you can see here we use the interpreter to get the context and from the context we got all the functions and from the functions we got the underlying methods which are in instances of java lang reflect method and this class contains this get parameter types method which returns a java an array of javelin classes so as i explained before explained before we are able to get an instance of any uh um um an individual item of this array and by doing that we are finally getting an instance of a java line class from there we can get the protection domain get the class loader and then initiate any class loader based attack so last pebble sandbox we were able to find a couple of bypasses that we reported to the maintainers unfortunately they didn't have time to fix them on time so we decided not to leak these bypasses yet and we are keeping them classified for a few months few months uh so with that we will switch back to alexander who will wrap up the talk with some conclusions thank garbaro thank garbaro first of all let's summarize our results after this research more than 30 new vulnerabilities were found and reported to the vendor more than 20 different products are affected we can see their names on this slide based on these results we can make the next conclusions these are not the problems of of specific product or framework and content management system as a class should be on red team radar templates for dynamic content that can be managed by user is the main target for such system there are specific areas with higher risk from security point of view it's a good idea to perform security review and testing for such places always try a reduce attack surface surface as much as you can and stay safe
