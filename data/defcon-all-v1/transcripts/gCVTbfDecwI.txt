Hello everyone. Thank you for my introduction. So. I was so nervous before my first DEFCON&nbsp;&nbsp; talk that I prepared 62 slides for&nbsp; this 20 minute talk. We will try&nbsp;&nbsp; to manage it. So we'll talk about prototype&nbsp; pollution vulnerabilities in JavaScript. &nbsp; This talk is based on the joint work&nbsp; with Mozart and&nbsp;&nbsp; Chris Tyson. In this paper, we study prototype&nbsp; pollution vulnerabilities and their gadget and&nbsp;&nbsp; implemented the tools to detect them.&nbsp; I already presented part of this talk,&nbsp;&nbsp; the exploits that we detected in the real&nbsp; application and details of the gadget on&nbsp;&nbsp; application. Today I want to present more&nbsp; details about our methodology, about the tools&nbsp;&nbsp; and let's start with some introduction in&nbsp; JavaScript and how inheritance work in JavaScript&nbsp;&nbsp; throw an example. So we run not dis, which&nbsp; executes the quote in index file. The first line&nbsp;&nbsp; of the quote creates an empty JavaScript object. There are time allocates a new object with the&nbsp;&nbsp; building property proto that points to the&nbsp; object prototype. The object prototype has&nbsp;&nbsp; a bunch of functions that we can reuse,&nbsp; for example, to shrink. To implemented&nbsp;&nbsp; inheritance, JavaScript allows to extend the&nbsp; prototype with a new property. In this example,&nbsp;&nbsp; we define the property X with value 42. Things&nbsp; get more interesting when we create some other&nbsp;&nbsp; object in principle unrelated from the first one.&nbsp; Both object show the same prototype. When on time&nbsp;&nbsp; executes the last line of the court to print&nbsp; X property for the second object, he tries to&nbsp;&nbsp; find prototype in the object itself. Since X is&nbsp; undefined for the second object, there are time&nbsp;&nbsp; we look up the property X in the prototype. In this case it brings 42 to the terminal.&nbsp;&nbsp; Well, let's consider the thread model of web&nbsp; applications to see how this feature can affect&nbsp;&nbsp; security properties of the application. Index&nbsp; dis file creates a simple web app that handles&nbsp;&nbsp; two requests, update and backup data. Threat model&nbsp; can send any request to the server, for example,&nbsp;&nbsp; update with any parameters. Let's see what&nbsp; happens when the parameters are sent this figure. &nbsp; The code creates an empty object. It reads&nbsp; proto property and then the attacker adds the&nbsp;&nbsp; property shell with the value cal to object&nbsp; prototype on this. So this called pattern is&nbsp;&nbsp; called prototype pollution vulnerability. But how can it affect our application?&nbsp;&nbsp; Let's assume that this code handles backup&nbsp; request. It just execute backup script by&nbsp;&nbsp; the helper function. Not that that tiger cannot&nbsp; control any arguments of this helper function&nbsp;&nbsp; in this function. We can give some options in&nbsp; terms of which shell you want to use. If we don't&nbsp;&nbsp; specify this option, it'll use the default shell&nbsp; and then it'll run a new process. If the attacker&nbsp;&nbsp; sends backup request after prototype pollution,&nbsp; we execute the function and options door.&nbsp;&nbsp; Shell reads the attacker controlled property&nbsp; from the prototype and runs the calculator.&nbsp;&nbsp; So we get remote code execution and this code&nbsp; fragments call it pollution gadget. If you find&nbsp;&nbsp; gadget in the quote of no GS itself, the impact&nbsp; is much higher because it potentially affect of&nbsp;&nbsp; all applications. In summary, to achieve&nbsp; remote code execution we need two steps&nbsp;&nbsp; Identifying prototype pollution and identifying&nbsp; gadgets. How to identify prototype pollution at&nbsp;&nbsp; scale. We implemented static analysis for&nbsp; not JS applications and N P M packages. &nbsp; We use the analysis where we mark the&nbsp; attacker control data by the input label.&nbsp;&nbsp; However, we cannot define these things&nbsp; syntactically because not every property&nbsp;&nbsp; assigned not not every property assignment leads&nbsp; to prototype pollution. Instead, we use what we&nbsp;&nbsp; call multi label paint analysis To find the&nbsp; sync, let's see how it looks at this example.&nbsp;&nbsp; We assume that all arguments of the function&nbsp; air attacker controlled and mark them by input&nbsp;&nbsp; label. We propagate the input label and if we have&nbsp; a property read with the tainted property name, we&nbsp;&nbsp; change the prototype to proto, it means attacker&nbsp; can potentially read object prototype. Here. &nbsp; When the analysis detect the property assignment&nbsp; with receiver that has a proto label like this&nbsp;&nbsp; one, it reports the code fragment as potential&nbsp; prototype pollution vulnerability. This is the&nbsp;&nbsp; main idea of our analysis. We implemented it&nbsp; based on the code query analysis framework&nbsp;&nbsp; and evaluated our analysis on 100 vulnerable&nbsp; packages that we collected. The best result&nbsp;&nbsp; achieves 97% recall. It means we&nbsp; detected 97 vulnerabilities out of 100&nbsp;&nbsp; and which is necessary to find&nbsp; vulnerabilities in the real applications.&nbsp;&nbsp; The second question is how to identify the&nbsp; gadgets. We analyze not JS codes and use&nbsp;&nbsp; dynamic analysis to detect property leads from&nbsp; object prototype. Then we use static analysis to&nbsp;&nbsp; find flows from these properties to internal not&nbsp; JS function calls. You can find all details of&nbsp;&nbsp; this analysis in the paper in presentation.&nbsp; I want to show some interesting results. &nbsp; So we detected 11 different&nbsp; gadgets in not JS APIs.&nbsp;&nbsp; The first gadget is phone function which execute&nbsp; new process. Let's look at this. Code details is&nbsp;&nbsp; not important here but we see that the property&nbsp; shell and the property and can be undefined&nbsp;&nbsp; and there is a flow from these properties to&nbsp; the internal function call which is actually&nbsp;&nbsp; vulnerable. It's simplified version of spawn&nbsp; function from no JS at IP implementation.&nbsp;&nbsp; Let's see how remote code&nbsp; execution can be achieved.&nbsp;&nbsp; Suppose that the backup handler calls spoon&nbsp; with no attacker controlled arguments.&nbsp;&nbsp; That attacker first followed the prototype&nbsp; by update request. As we saw early,&nbsp;&nbsp; they add property shell with value node to object&nbsp; prototype the property ends by another request&nbsp;&nbsp; and sent backup request to execute PON function. Let's see what happens. When spoons executes,&nbsp;&nbsp; it treats the value of the shell and&nbsp; AMP from the prototype. It allows&nbsp;&nbsp; the attacker to run node dis in debugging&nbsp; mode by controlling environment variables&nbsp;&nbsp; and connect remotely. To execute arbitrary&nbsp; code for this, we implemented the shell base&nbsp;&nbsp; on node dis remote debugging protocol. You can see&nbsp; the short demo on the slide. The second T is the&nbsp;&nbsp; required function. The required function is used&nbsp; to include external packages to an application.&nbsp;&nbsp; So each application has a lot required calls. Is this a simplified snippet of value function? As&nbsp;&nbsp; we can see, it reads a package configuration file&nbsp; package and integrates entry&nbsp;&nbsp; point if this is defined in the property main if&nbsp; main is undefined it not JS used default value.&nbsp;&nbsp; Let's see how we can exploit it. If&nbsp; the attacker pollutes the main property&nbsp;&nbsp; to exploit the gadget, we need to require a&nbsp; function call for a package without the main&nbsp;&nbsp; property defined. An example of such a gadget is&nbsp; bytes one of the popular gadget npm ecosystem.&nbsp;&nbsp; Let's see how it works. When the attacker&nbsp; triggers this code to execute the require a call. &nbsp; The attacker triggers the backup handler&nbsp; which par second config file of byte package.&nbsp;&nbsp; Since the main properties undefined,&nbsp; it look ups the property from the&nbsp;&nbsp; prototype. To achieve remote code execution,&nbsp; attacker should control this malicious file.&nbsp;&nbsp; As you can guess, this is a strong&nbsp; requirement. The attacker should be able&nbsp;&nbsp; to upload some malicious file to the system,&nbsp; for example, using another vulnerability.&nbsp;&nbsp; Let's see how we can bypass this limitation. By&nbsp; combining these two gadgets, the key idea is to&nbsp;&nbsp; use required gadget to trigger the spawn gadget.&nbsp; To achieve this, we need to find the existing file&nbsp;&nbsp; that execute spawn function. If we identify a&nbsp; file in node dis default distribution, then we&nbsp;&nbsp; increase the impact of the exploit because every&nbsp; application use node dis already have this file. &nbsp; An example of such a file is npm&nbsp; dis which runs node instance.&nbsp;&nbsp; Let's look at the end-to-end exploit the attacker.&nbsp; Pilot the main property with the pass to M J S&nbsp;&nbsp; and the end property as required by spawn gadget.&nbsp;&nbsp; Finally, when backup is triggered not JSS&nbsp; executes the required function. Loads dis&nbsp;&nbsp; that calls PON function. The spawn function&nbsp; reads the attacker control at environment&nbsp;&nbsp; variable from the prototype and runs not&nbsp; just in the bugging mode as we saw earlier,&nbsp;&nbsp; so that attacker achieve remote code execution for&nbsp; a require call without additional requirements.&nbsp;&nbsp; The last question is how to exploit real&nbsp; application user or tools and detected gadgets.&nbsp;&nbsp; We call it the GitHub phone, no JS applications&nbsp; and took 15 most popular ones where an OR tool&nbsp;&nbsp; and got some prototype polluters cases. As you&nbsp; can see, the prototype pollution pattern is rare&nbsp;&nbsp; in practice and manual verification is applicable&nbsp; for the total number of detected cases. We&nbsp;&nbsp; confirm it eight detected cases as exploitable and&nbsp; reported them to maintainers. You can see we found&nbsp;&nbsp; two cases in NPM C line. Everyone I think use this&nbsp; up packet, par server and rocket chat messenger. &nbsp; The main problem with real exploitation of this&nbsp; vulnerability to achieve remote code execution,&nbsp;&nbsp; it's that code of some code of the application&nbsp; can breaks your application if you pollute&nbsp;&nbsp; object prototype. So the code does not&nbsp; expect any additional properties in the&nbsp;&nbsp; object prototype. It happened many times&nbsp; in this experiments and I want to show one&nbsp;&nbsp; technique that I used and that you can also&nbsp; apply for your research. Let's say case for&nbsp;&nbsp; par server. We have a par server. It's a servers&nbsp; that provide rest and use&nbsp;&nbsp; some database. For example one DB two store data&nbsp; from the request. It also use J S B zone. It's a&nbsp;&nbsp; library of the MongoDB that can serialize and&nbsp; sterilize the data to binary MongoDB format. &nbsp; So we detected prototype pollution case in par&nbsp; server. This example of the quote details is not&nbsp;&nbsp; interesting. What is not important? What's&nbsp; actually important for us that attacker&nbsp;&nbsp; can control name and the value of the polluted&nbsp; property. We also detected the gadget in JS bison&nbsp;&nbsp; package and this gadget can be triggered when&nbsp; JS bison digitalize some data from the MongoDB.&nbsp;&nbsp; So this gadget call evolve function for function&nbsp; string from the MongoDB that attacker can store&nbsp;&nbsp; to the MongoDB before. But the problem that&nbsp; this evolve call executed only if evolve&nbsp;&nbsp; function option is enabled. As you can guess,&nbsp; it's disabled by default in the configuration&nbsp;&nbsp; file but it can be polluted and the gadget reads&nbsp; from the polluted prototype and we can enable&nbsp;&nbsp; this feature in runtime. This is the main idea.&nbsp; So let's see on the 9th way we can exploit it.&nbsp;&nbsp; Attacker force sent a package that should trigger&nbsp; prototype pollution vulnerability the package,&nbsp;&nbsp; serialize it to the MongoDB response, de&nbsp; serialize it and trigger prototype pollution. &nbsp; Good. We pollute the option that we need to&nbsp; trigger a gadget after that the attacker sent&nbsp;&nbsp; a package that should trigger a gadget but&nbsp; serializer of JavaScript on library throws an&nbsp;&nbsp; exception and application crashed. It looks like&nbsp; un-exploitable keys. We cannot send any request&nbsp;&nbsp; after prototype pollution application crashed&nbsp; after the first request that we try to send.&nbsp;&nbsp; How we can bypass it. I found a way some kind of&nbsp; race condition. Let's see if the attacker sent the&nbsp;&nbsp; package that trigger remote code execution gadget&nbsp; first. This package serialize it and MongoDB start&nbsp;&nbsp; to handle it for the same long time. At this&nbsp; moment, the attacker sent second gadget that&nbsp;&nbsp; should trigger prototype pollution, vulnerability,&nbsp; it's serialize it and decent serialize it from&nbsp;&nbsp; the MongoDB and successfully trigger prototype&nbsp; pollution. After that DB decided to stop handling&nbsp;&nbsp; the guards the first request and pass to the&nbsp; application and we trigger remote code execution. &nbsp; This technique I used many times,&nbsp; usually you need very short time gap to&nbsp;&nbsp; trigger remote code execution. Gadget. One&nbsp; of the techniques that I use, I just sent&nbsp;&nbsp; like 2050 requests to trigger remote code&nbsp; execution and one request that trigger prototype&nbsp;&nbsp; pollution in the middle. Most likely we get some&nbsp; situation that we trigger remote code execution&nbsp;&nbsp; exactly after prototype pollution in some threat.&nbsp; Okay, let's see how it looks in practice. In&nbsp;&nbsp; short demo. We are on par server. We need to a&nbsp; script to exploit this vulnerability because the&nbsp;&nbsp; second request we need to send in hundred three&nbsp; hundreds milliseconds after the first one. So it's&nbsp;&nbsp; not possible to do manually. So this script&nbsp; prepared database to add some data to one GB&nbsp;&nbsp; to make it a little bit smaller to handle&nbsp; the first request for the long time and send&nbsp;&nbsp; these two requests. And you can see that we get&nbsp; calculator from the pars server so it works. &nbsp; In conclusion, we implemented and open&nbsp; source IT tools to detect prototype pollution&nbsp;&nbsp; vulnerabilities and their gadget detected 11 new&nbsp; gadget in not JS APIs. You also can find them in&nbsp;&nbsp; the GitHub repair. Some gadgets already fixed it,&nbsp; but not all combination of that and we reported&nbsp;&nbsp; a remote code execution vulnerabilities&nbsp; in the popular open source applications.&nbsp;&nbsp; So the interesting question for the future work,&nbsp;&nbsp; one of that, what is the more&nbsp; efficient way to detect gadget?&nbsp;&nbsp; So we have options like static analysis, dynamic&nbsp; analysis. We already use some HE approach. I think&nbsp;&nbsp; it's a good question for the future research,&nbsp; how many new gadget and API packages we did not&nbsp;&nbsp; consider in this research and API packages,&nbsp; but it's also a large source phase for the&nbsp;&nbsp; gadget. How many new gadget in no JS APIs source&nbsp; code. If we find some efficient way to detect it,&nbsp;&nbsp; to explore it, probably we can find some new&nbsp; ones. We continue researching this direction.&nbsp;&nbsp; If you're interested in this, just follow me on&nbsp; the Twitter and thank you for your attention.
