How is everybody? I need your energy, right? Yeah.&nbsp;&nbsp; Okay, I drank some. Welcome to our talk, Defender&nbsp;&nbsp; Pretender: When Windows Defender&nbsp; Updates Become a Security Risk.&nbsp;&nbsp; We'll introduce ourselves first. My name is&nbsp; Tomer Bar and I am the VP of security research&nbsp;&nbsp; at SafeBreach. Actually, we spoke 10 different&nbsp; talks at Black Hat USA and this is our ninth in&nbsp;&nbsp; Defcon. I've been around for the last 20 years.&nbsp; I presented in many global security conferences,&nbsp;&nbsp; and this year I was qualified to speak to&nbsp; talks at Black Hat and one at Defcon. &nbsp; Hey, guys. My name is Omer Attias. I'm&nbsp; a security researcher at SafeBreach.&nbsp;&nbsp; I have over six years of experience in&nbsp; cybersecurity, especially in low-level and&nbsp;&nbsp; vulnerability research. I'm also a technology&nbsp; and science enthusiast. Let's kick it off. &nbsp; We'll introduce our research motivation&nbsp; and goal, and then we'll describe in&nbsp;&nbsp; details Defender Pretender process, and the&nbsp; vulnerability we discovered. We will demo&nbsp;&nbsp; different super interesting attack vectors, and&nbsp; we'll finish our talk with important takeaways&nbsp;&nbsp; before leaving time for Q&A. Let's begin. In this session, we are going to walk you all&nbsp;&nbsp; through the process of how we turned&nbsp; Defender to Defender Pretender. Our&nbsp;&nbsp; motivation originated from the Flame malware&nbsp; used by probably state-sponsored threat actor,&nbsp;&nbsp; and exploited the Windows update process using a&nbsp; sophisticated man-in-the-middle attack. Flame was&nbsp;&nbsp; able to hijack the Windows update mechanism by&nbsp; posing as a legitimate Microsoft update server,&nbsp;&nbsp; allowing it to deliver malicious updates and&nbsp; maintain persistence on the target system. &nbsp; Our goal was to achieve similar capabilities&nbsp; but running without admin privileges,&nbsp;&nbsp; without possessing a forged certificate,&nbsp; and with no prior requirements of man in&nbsp;&nbsp; the middle and still achieve full&nbsp; takeover on Defender workflow. &nbsp; Let's start to analyze the Defender update process&nbsp; and understand what happens locally during an&nbsp;&nbsp; update. Defender check with the Microsoft Update&nbsp; Center for new updates. We found out that the&nbsp;&nbsp; updates are returned as a single executable.&nbsp; The file full name is pretty long. It's called&nbsp;&nbsp; Microsoft Protection Anti-Malware Front End,&nbsp; so we will refer to it as MPAM from now on. &nbsp; We downloaded this MPAM file, analyzed&nbsp; it, and found that it has a CAB resource,&nbsp;&nbsp; and then we manually extracted the resource&nbsp; and got six files: two executable files,&nbsp;&nbsp; MpEngine, and MpSigStub, and four files with&nbsp; unfamiliar VDM extension, at least for us.&nbsp;&nbsp; When we tried to execute this MPAM file,&nbsp; we observed that it executed the MpSigStub&nbsp;&nbsp; as a child process using the marked command&nbsp; line in the rail. After the execution, we saw&nbsp;&nbsp; the Defender was updated, so it seems like the&nbsp; Stub's role is to initiate the update process. &nbsp; The VDM files are loaded to Defender's&nbsp; main process as you can see here.&nbsp;&nbsp; MpEngine DLL was also used by the process and&nbsp; this DLL exports Defender's core functionality.&nbsp;&nbsp; Let's investigate a little bit&nbsp; more about this VDM files. &nbsp; The VDM files are actually portable executable&nbsp; files, meaning application. However, they cannot&nbsp;&nbsp; be executed as they have no code logic at all.&nbsp; We assume that they are special data files that&nbsp;&nbsp; contains the detection signatures. Furthermore,&nbsp; we observed that the two of them are labeled with&nbsp;&nbsp; the keyword Base and the other two are labeled&nbsp; with the keyword Delta with the main difference&nbsp;&nbsp; being their sizes. The Base file was significantly&nbsp; larger than the Delta files, and we speculate that&nbsp;&nbsp; this is due to incremental updating for efficiency&nbsp; reasons because you probably don't want to send&nbsp;&nbsp; the entire database each time you have to have&nbsp; a small update, right? It's not efficient. &nbsp; The Base file has a major version&nbsp; of 391 and O minor version,&nbsp;&nbsp; and the Delta file has the same major version&nbsp; and 3508 as the minor version. It's important,&nbsp;&nbsp; and I'm telling you that because the Delta version&nbsp; represent the current version of Defender security&nbsp;&nbsp; intelligence. It may indicate that the Delta&nbsp; files are related to the signature data,&nbsp;&nbsp; but a quick check reveals that all six&nbsp; files are digitally signed by Microsoft,&nbsp;&nbsp; which probably indicates that the update&nbsp; process was designed against tampering. &nbsp; However, let's continue to explore furthermore&nbsp; before reaching a definitive answer to this&nbsp;&nbsp; speculation. To summarize the update process to&nbsp; the ones that come in right now the MPAM file is&nbsp;&nbsp; fetched from the internet, and upon execution it&nbsp; triggers an update and the following three step&nbsp;&nbsp; occur: MPAM extract the VDM files, MpEngine and&nbsp; MpSigStub throw itself into the update payload&nbsp;&nbsp; folder located at the temporary directory,&nbsp; and MPAM then executes the SigStub in order&nbsp;&nbsp; to perform the update. Finally, SigStub takes the&nbsp; VDM files and MpEngine DLL and use them to update&nbsp;&nbsp; Windows Defender. So easy, right? Until now. Now that we have gained high level knowledge&nbsp;&nbsp; about the update process, we can start&nbsp; to play around with the files involved.&nbsp;&nbsp; If we will be able to modify MpEngine DLL to&nbsp; our own fake DLL, it'll be a game over for&nbsp;&nbsp; Defender resulting in local privilege escalation,&nbsp; right? We modified a valid update payload that we&nbsp;&nbsp; have downloaded from the internet and replaced&nbsp; only the original MpEngine DLL as you can see&nbsp;&nbsp; with our fake DLL, even though the DLL was&nbsp; digitally signed by Microsoft, the original one. &nbsp; And then we executed the Stub executable&nbsp; with the appropriate command line.&nbsp;&nbsp; Of course, this attempt failed. During a&nbsp; kernel debugging, we got this exception&nbsp;&nbsp; at the top that tells us that the DLL is not&nbsp; signed. This protection is implemented since&nbsp;&nbsp; Defender main process is a protected process,&nbsp; a PPL process which cannot load unsigned DLLs.&nbsp;&nbsp; In that point we thought, is this&nbsp; the end of our research lead? &nbsp; We decided not to give up and focus on the VDM&nbsp; files. What will happen if we'll update Defender&nbsp;&nbsp; using a modified older VDM file? We decided to&nbsp; modify only the file version which is embedded&nbsp;&nbsp; inside the file, so making Defender believe it's&nbsp; a newer version but without modifying the data&nbsp;&nbsp; itself. Of course, this modification will turn the&nbsp; VDM to be unsigned because it's embedded in the&nbsp;&nbsp; file. But we decided to give it a shot anyway. For our surprise, the update attempt succeeded.&nbsp;&nbsp; We actually update an older version,&nbsp; pretended to be a newer version,&nbsp;&nbsp; and most importantly using unsigned files with&nbsp; the low privilege user, unprivileged user.&nbsp;&nbsp; This was the first clue that something is fishy,&nbsp; but when we try to modify additional random byte&nbsp;&nbsp; in this VDM data itself and not just the file&nbsp; version and execute a second update, we got an&nbsp;&nbsp; error. We understood it won't be that simple, and&nbsp; we have to learn more about the update process. &nbsp; Let's summarize what we accomplished so far.&nbsp; We understood in eye level, Windows Defender&nbsp;&nbsp; update process and we have become familiar&nbsp; with the involved file, right? We attempted&nbsp;&nbsp; to modify MpEngine to use our own fake DLL,&nbsp; but we're unsuccessful in doing so. However,&nbsp;&nbsp; we did successfully modify an older update payload&nbsp; to appear as a new update payload by modifying the&nbsp;&nbsp; VDM files, but finally got an error when we&nbsp; updated using VDM file with modified data. &nbsp; We left with the question, how does a low&nbsp; privileged user can replace files managed&nbsp;&nbsp; by Defender protected process? We began to analyze&nbsp; MpSigStub. After a bit of reversing, we understood&nbsp;&nbsp; that MpSigStub communicate with MsMpEngine,&nbsp; which is the main process of Defender via RPC,&nbsp;&nbsp; remote procedural calls. But at that point in&nbsp; time, we were unable to find the specific RPC&nbsp;&nbsp; interface which was used, and the challenge&nbsp; was even more difficult since RPC tools that&nbsp;&nbsp; we all use like RPC View could not display the&nbsp; RPC data due to involved protected process. &nbsp; We did manual reverse engineering and revealed&nbsp; that the RPC_GUID which belongs to mpsvc.dll&nbsp;&nbsp; and that the function number parameter&nbsp; was 42 corresponding to a function name&nbsp;&nbsp; ServerMpUpdateEngineSignature,&nbsp; which by its name indeed seems&nbsp;&nbsp; like the function we were looking for. After the RPC is done, the execution will&nbsp;&nbsp; continue from Defender main process on the right&nbsp; and will reach innate engine context function&nbsp;&nbsp; which start the update process by calling&nbsp; our signal function in our MpEngine.dll. &nbsp; Now we are ready for dynamic kernel debugging&nbsp; to understand the rest of the execution flow.&nbsp;&nbsp; As you can see, the execution flow is pretty&nbsp; long, right? We will skip to the interesting&nbsp;&nbsp; part in the load database function&nbsp; which is called for each VDM file.&nbsp;&nbsp; Now we know for sure that the VDM is&nbsp; indeed contains the update database. &nbsp; Let's analyze the database format, so&nbsp; we will be able to modify the data in&nbsp;&nbsp; much smarter way than we did before, like in&nbsp; random data modification. As we mentioned,&nbsp;&nbsp; the VDM file is Windows portable executable&nbsp; files with no code logic, but it also includes&nbsp;&nbsp; a resource section. This resource section contains&nbsp; compressed data that start with RMDX magic bytes.&nbsp;&nbsp; The signature in both the Base and the Delta files&nbsp; are compressed with Zlib, but Zlib magic bytes are&nbsp;&nbsp; absent. By simply adding these two bytes at the&nbsp; bottom and running this simple command, it can be&nbsp;&nbsp; decompressed. We were very surprised to see that&nbsp; the signature are not even encrypted. In a brief&nbsp;&nbsp; look over the Base file, we could literally see&nbsp; where signatures begins and ends and probably the&nbsp;&nbsp; actual unique strings Defender searches in order&nbsp; to detect, for this example, the Conti ransomware.&nbsp;&nbsp; But the Delta file format appears to be a&nbsp; little bit more complex, and we have decided&nbsp;&nbsp; to prioritize understand it to a later time. Now we are going to focus on the Base file,&nbsp;&nbsp; and the Base file, as we assume, has the most&nbsp; of the signature logic because it's larger,&nbsp;&nbsp; and it seems easier to understand its&nbsp; format. Let's see what we discovered. &nbsp; Each signature has a four-byte signature&nbsp; header which contains signature type and size.&nbsp;&nbsp; The type is one byte long and the size is three&nbsp; byte long and the signature data begins just right&nbsp;&nbsp; after the signature header. So simple structure,&nbsp; and instead of reversing each signature type, we&nbsp;&nbsp; search on Google for some relevant information and&nbsp; found this comprehensive list of signature types. &nbsp; Each thread start with a signature type&nbsp; 5C. A threat is a collection of signature.&nbsp;&nbsp; These signatures are simply unique strings or&nbsp; byte sequences which belongs to a malware family.&nbsp;&nbsp; You can see several strings from the&nbsp; Conti threat, and the collection of&nbsp;&nbsp; the signatures always ends up with threat&nbsp; end signature, 5D type, at the bottom. &nbsp; The Base file is actually a sequence of threats.&nbsp; When one threat ends, the next threat starts and&nbsp;&nbsp; so on. Using this knowledge, we were able to&nbsp; extract more than 2.5 million signature from&nbsp;&nbsp; the Base file. We figure out most of the member&nbsp; of the signature, for example the threat name,&nbsp;&nbsp; its category, and severity of the threat. Now we&nbsp; can conduct an experiment in order to verify if&nbsp;&nbsp; we can modify it in a more smarter way than&nbsp; they did at the beginning of the research. &nbsp; We extracted the strings from the VDM files,&nbsp; copy them to a Visual Studio environment,&nbsp;&nbsp; and compiled a simple executable containing&nbsp; these strings. But as you can see,&nbsp;&nbsp; without any logic, as so you can see&nbsp; at line 16, the main function is empty.&nbsp;&nbsp; And then we copy this executable to a virtual&nbsp; machine with Defender and in a minute immediately&nbsp;&nbsp; it triggered Defender even though there is&nbsp; no malicious code at all. As expected the&nbsp;&nbsp; threat alert name is the name of the threat we&nbsp; investigated, Conti, as you can see at the top. &nbsp; Then we modified just the threat name associated&nbsp; with the signature from Conti to a different name,&nbsp;&nbsp; recompressed the VDM file, and tried to&nbsp; update Defender using this modified version.&nbsp;&nbsp; Unfortunately, this attempt was unsuccessful&nbsp; and resulted in unexplained error. We assume&nbsp;&nbsp; that the error is due to validation mechanism,&nbsp; and Omer will speak about those validations. &nbsp; Okay. We went back to the drawing board&nbsp; and reverse engineer the update process&nbsp;&nbsp; with the goal of identifying the point&nbsp; where the update failed and why it failed.&nbsp;&nbsp; A quick reminder, for each VDM&nbsp; file, LoadDatabase is called,&nbsp;&nbsp; and then checks the validity of the file, and&nbsp; then calls to ConsumeInputCompress function.&nbsp;&nbsp; ConsumeInputCompress function is a huge function&nbsp; that perform a lot of operations on the VDM files.&nbsp;&nbsp; Also we suspected the function to be the function&nbsp; that returned the error code we saw in the logs.&nbsp;&nbsp; We focused deeply on the internals of&nbsp; these two functions, and specifically&nbsp;&nbsp; on ConsumeInputCompress function. We did it by&nbsp; dynamically debugging the update execution. &nbsp; Let's see what we got from debugging.&nbsp;&nbsp; The debugging led us to figure out two important&nbsp; structures that contained in each VDM file.&nbsp;&nbsp; The first one is the RMDX header, which appears&nbsp; in the beginning of every VDM resource data.&nbsp;&nbsp; One of the important members of the RMDX&nbsp; header is the data offset which points to&nbsp;&nbsp; a second editor called Zlib header,&nbsp; which we can see marked in blue. &nbsp; The Zlib header actually contains&nbsp; the Zlib compressed data of the VDM&nbsp;&nbsp; and also consists of two VARs, and the second VARs&nbsp; suspected to be the CRC of decompressed data. We&nbsp;&nbsp; tried to calculate the CRC value of the compressed&nbsp; data, but we didn't get the expected value,&nbsp;&nbsp; so maybe this is not a CRC. We try to locate the&nbsp; point where this value is accessed and discovered&nbsp;&nbsp; that it's compared to a variable calculated&nbsp; using an algorithm similar to a CRC calculations.&nbsp;&nbsp; So after searching on Google, we found out that&nbsp; this is a specific CRC algorithm called JAMCRC,&nbsp;&nbsp; which is calculated by one minus the CRC value. We were confident that we could modify the VDM&nbsp;&nbsp; and fix the expected CRC value that appears&nbsp; in the headers, and by doing so the update&nbsp;&nbsp; would be validated successfully and we&nbsp; would be able to take down Defender.&nbsp;&nbsp; But the update was unsuccessful even though&nbsp; we fixed the CRC value of the compressed data.&nbsp;&nbsp; Likely there are additional validation checks,&nbsp;&nbsp; and we thought, where do this validation occurs&nbsp; and what they validate? So let's find them out. &nbsp; The modification was simple. We just open up the&nbsp; Base file and try to modify the signatures, and&nbsp;&nbsp; after the modification we wrapped up the Base file&nbsp; and try to do an update attempt. But it turned out&nbsp;&nbsp; to be quite more complicated than we thought. We&nbsp; completely ignored the Delta file, and we assumed&nbsp;&nbsp; that the error we got related to the relationship&nbsp; between the Base file and the Delta file. &nbsp; There are two pairs of VDMs. The first pair&nbsp; contain the antivirus definitions of Defender,&nbsp;&nbsp; and the second pair actually&nbsp; contains the spyware definitions.&nbsp;&nbsp; Each of these pairs is identical in terms&nbsp; of the form of the file and the signatures.&nbsp;&nbsp; All the findings and the conclusions we will&nbsp; show now apply to each of these VDM pairs. &nbsp; As we mentioned earlier, we ignore the Delta,&nbsp; but now it's time to focus on the purpose of&nbsp;&nbsp; the Delta file. During an update, both the&nbsp; Base and the Delta files are involved. The&nbsp;&nbsp; merged takes the Base file and the Delta simply&nbsp; defines the changes to be made to this Base file.&nbsp;&nbsp; The resulting output file will represent&nbsp; the updated version from the Delta,&nbsp;&nbsp; and this update process is&nbsp; referred to as incremental. &nbsp; To modify Base signatures, we need to supply&nbsp; a Delta file that precisely patches the Base&nbsp;&nbsp; file with the intended changes. But&nbsp; to do that, we need to understand the&nbsp;&nbsp; entire merge process internals and identify&nbsp; any other validations that we encountered. &nbsp; Now we will delve into the merge process.&nbsp; We have talked about the VDM file format and&nbsp;&nbsp; specifically the Base file format which&nbsp; contains threats and their signatures. &nbsp; Now let's look into the Delta format and how&nbsp; it combines with the Base file to create new&nbsp;&nbsp; file with new signatures. We went back to the&nbsp; decompressed Delta file, and we figured out&nbsp;&nbsp; that the Delta is a signature-based file too. We&nbsp; saw that the Delta always contain two signatures.&nbsp;&nbsp; The second signature is BLOB signature&nbsp; type, and we ignore the BLOB_RECINFO&nbsp;&nbsp; signature as we assume all the essential&nbsp; data contained in the BLOB signature.&nbsp;&nbsp; Right after the signature header, we have two&nbsp; numbers which we were not sure about their&nbsp;&nbsp; purpose, so we will call them unknown for&nbsp; now, and we'll come back to them later on. &nbsp; The rest of the data right after these numbers&nbsp; is actually the actions which define the actions&nbsp;&nbsp; done by the merge algorithm. How do they define&nbsp; the actions? Now we'll answer this question.&nbsp;&nbsp; We spotted the point where&nbsp; Defender parse the actions.&nbsp;&nbsp; This block of code shows where actually the&nbsp; action parse start. We reverse all the merge&nbsp;&nbsp; process and figured out merge how the merge&nbsp; works, so let's delve into the internals. &nbsp; We identify two action types, copy&nbsp; from Delta and copy from Base.&nbsp;&nbsp; Copy from Delta used to copy size bytes&nbsp; from the Delta file into the merge file,&nbsp;&nbsp; and copy from Base used to copy size bytes from an&nbsp; offset within the Base file into the merged file.&nbsp;&nbsp; Let's see exactly the format&nbsp; of each of these signatures. &nbsp; To parse the actions, we first need to read&nbsp; two bytes representing the action header.&nbsp;&nbsp; The first bit within the action header indicates&nbsp; the action type, 0 for copy from Delta and 1 for&nbsp;&nbsp; copy from Base. The rest of the action header is&nbsp; dependent on the action type. First let's take a&nbsp;&nbsp; look at copy from Delta action. Here we have&nbsp; an action example, and the first thing to do&nbsp;&nbsp; is to read the first two bytes and check the&nbsp; MSB to identify which type of action is it.&nbsp;&nbsp; The MSB here is 0, means this action is actually&nbsp; copied from Delta. Then it means that the next&nbsp;&nbsp; 15 bits represented the size variable&nbsp; which tells us how many bytes to read.&nbsp;&nbsp; The size variable is set to 1 in this case,&nbsp; so we read one byte right after the action&nbsp;&nbsp; header. In this case, we will read 5C&nbsp; and place it into the new merged file. &nbsp; Okay, now that we understand how to parse&nbsp; copy from Delta action, let's move on to copy&nbsp;&nbsp; from Base action. When we look at copy from&nbsp; Base action, we can see that the MSB is on.&nbsp;&nbsp; But in this case, after reverse engineer&nbsp; of ConsumeInputCompress function,&nbsp;&nbsp; we found out that in this case the size&nbsp; variable calculated in the following way: &nbsp; Firstly, we take the two bytes and turn&nbsp; off the MSB by end bitwise with 7FFF,&nbsp;&nbsp; and then add to this result 6 which result the&nbsp; size variable. We now know how many bytes to read,&nbsp;&nbsp; but the question is where we will read them from.&nbsp; Copy from Base action type consists also the&nbsp;&nbsp; offset variable. The next D word right after the&nbsp; action header indicates the offset, which in the&nbsp;&nbsp; Base file we should read the bites from. In this&nbsp; example, the size is equal to 8,005, and we should&nbsp;&nbsp; read them from the offset 1 of the Base file, and&nbsp; eventually place them into the new merged file. &nbsp; To summarize, the Delta file contains compressed&nbsp; data and by decompressing this data we can extract&nbsp;&nbsp; the BLOB signature. The BLOB contains actions,&nbsp; and the actions tells how to build the new&nbsp;&nbsp; updated file depending on the current version&nbsp; of the Base file. All this occurs in memory. &nbsp; Following our knowledge, we developed&nbsp; a script that merged between the Base&nbsp;&nbsp; file and the Delta file. We ran&nbsp; our code, and got a merged stream&nbsp;&nbsp; that actually seems like a base suite&nbsp; signatures but with some modifications. &nbsp; Here we can see a diff between Base signatures&nbsp; to merged signatures. The bytes that marked in&nbsp;&nbsp; yellow are the modified bytes, and the size of the&nbsp; data got bigger, probably the merged process added&nbsp;&nbsp; some new signatures. Here things are starting&nbsp; to reveal a clear image of what happening.&nbsp;&nbsp; As we said before, we will talk about&nbsp; the unknown numbers we mentioned before. &nbsp; We started to investigate the merged&nbsp; properties like the size of the merge&nbsp;&nbsp; and the CRC of the merge. We looked up for a&nbsp; correlation to the unknown numbers. Eureka,&nbsp;&nbsp; we got it. The first number represent the size of&nbsp; the merged data, and the second number is actually&nbsp;&nbsp; the CRC of the merged data. These two numbers are&nbsp; the expected values that Defenders compares with&nbsp;&nbsp; and the numbers is actually&nbsp; one more layer of validations. &nbsp; I want to recap the three validations we&nbsp; discovered. The first validation simply&nbsp;&nbsp; checks if the ZILB-compressed data is unchanged.&nbsp; How does Defender checks this? By comparing&nbsp;&nbsp; the expected CRC value within the ZILB data&nbsp; header to the calculated CRC of the ZILB data.&nbsp;&nbsp; The next validation are the ones we&nbsp; just mentioned. Merged size validation&nbsp;&nbsp; ensures the size of the merged data and merge CRC&nbsp; validation validates the CRC of the merge data. &nbsp; These checks confirm the&nbsp; success of the merged algorithm.&nbsp;&nbsp; We wondered if now we have all the knowledge we&nbsp; need in order to fake an update. We try to change&nbsp;&nbsp; the Delta actions in such a way that the merge&nbsp; will result with a difference, and it worked. We&nbsp;&nbsp; managed to update Defender with a fake Einstein&nbsp; database using unprivileged user. It's crazy. &nbsp; Let's see what we can do with this capability.&nbsp; We will explain three attack vectors.&nbsp;&nbsp; We have developed a fully automatic tool&nbsp; called WD Pretender which stands for Windows&nbsp;&nbsp; Defender Pretender. These tool supports all the&nbsp; attack vectors that we are going to represent,&nbsp;&nbsp; and we will share the GitHub&nbsp; link at the end of the talk. &nbsp; As we explained earlier, the signatures&nbsp; of Defender resulted from merging the&nbsp;&nbsp; Delta file and the Base file. The database&nbsp; files of Defender are composed of threats,&nbsp;&nbsp; and each threat has its name, and we can infer&nbsp; from the name, the purpose of the threat, right? &nbsp; Now, what will happen if we will delete all the&nbsp; threats that contain the keyword laZagne, for&nbsp;&nbsp; example, in their names? Let's see. We will show&nbsp; our deletion of threat resulting with bypass and&nbsp;&nbsp; with successful execute laZagne tool. Let's see. Now we are seeing that we are unprivileged user,&nbsp;&nbsp; and we will try to download laZagne tool,&nbsp; and it immediately triggered Defender,&nbsp;&nbsp; and Defender will delete this&nbsp; tool. Now we'll run WD pretender&nbsp;&nbsp; and delete all the threats that contain&nbsp; the laZagne name in their threat's name.&nbsp;&nbsp; The tool will explore the VDM files, and&nbsp; we'll run an update attempt with the Stub.&nbsp;&nbsp; We'll see in a sec that the&nbsp; update executed successfully,&nbsp;&nbsp; and now we'll try to download the laZagne&nbsp; tool again. Let's see what we get...&nbsp;&nbsp; Perfect. The tool downloaded, and we&nbsp; will execute successfully laZagne. Bingo.&nbsp;&nbsp; Thank you. Okay, now Tomer&nbsp; will present the next demos. &nbsp; One of the signature type seems very, very&nbsp; interesting. The name that Microsoft named&nbsp;&nbsp; it is friendly file signature and with&nbsp; hash algorithm, like 256 as you can see&nbsp;&nbsp; on the left on the red. We wonder, what&nbsp; do Microsoft means by friendly files? &nbsp; Parsing all the friendly file signatures from the&nbsp; VDM files revealed us a very, very long list of&nbsp;&nbsp; sorted list of hashes. The hash marked in green&nbsp; on the right belongs to Oracle VirtualBox Runtime&nbsp;&nbsp; library and the others belongs to other files.&nbsp; We assume that this signature is probably an&nbsp;&nbsp; allow list implementation probably to reduce false&nbsp; positives caused by, example, this Oracle file. &nbsp; What will happen if we'll replace&nbsp; the hash value of Oracle file with&nbsp;&nbsp; the mimikatz known hash value. We didn't&nbsp; change the mimikatz from the internet.&nbsp;&nbsp; Will it be allowed to run? Can we create a&nbsp; friendly mimikatz file? Let's see a demo. &nbsp; Okay, so we'll try to download mimikatz.&nbsp; It is detected as mimikatz on the right.&nbsp;&nbsp; Defender is working. Now we will execute&nbsp; on the left. Again Windows Defender&nbsp;&nbsp; Pretender, and with the hash of mimikatz, the&nbsp; known hash. Then we will update and you'll see&nbsp;&nbsp; on the right that the update was successfully&nbsp; from unprivileged user unsigned files,&nbsp;&nbsp; and you can also see that the logs are saying&nbsp; the same, that the update was successful.&nbsp;&nbsp; Now we will try to download mimikatz from the same&nbsp; place and let's see if it runs. Yeah, it runs. &nbsp; Let's see if we can extract all credentials from&nbsp; the memory of . Yeah. Right?&nbsp;&nbsp; Thank you. &nbsp; Now, for our final demo, we saved the best for&nbsp; last. This is going to be very, very cool. Trust&nbsp;&nbsp; me. We are going to force Defender to delete all&nbsp; the P files in the PC by modifying existing emotet&nbsp;&nbsp; signature to include the DOS mode stop string,&nbsp; which appears in all of the portable executable&nbsp;&nbsp; as a new malicious signature, and when Defender&nbsp; will find this string, this program cannot be&nbsp;&nbsp; run in DOS modes since the 80s, right? In OS files, it will automatically says,&nbsp;&nbsp; "I'm going to delete you," and&nbsp; causing permanent denial of service.&nbsp;&nbsp; We run our tool, generated a database,&nbsp; we are going to copy it to a machine,&nbsp;&nbsp; replace it, and we'll do an update after that.&nbsp; That's the user that is unprivileged user.&nbsp;&nbsp; We'll run the update, and on the&nbsp; left you'll see the update succeed. &nbsp; Okay, now we'll wait a few seconds. It's not&nbsp; edited at all, and Defenders started to say&nbsp;&nbsp; something about a lot of files detecting them as&nbsp; emotet and it keep going. If I had sound right&nbsp;&nbsp; now, you'll see a lot of concerts and Defender&nbsp; said, "What's going on? Restart the computer,"&nbsp;&nbsp; but we're not going to do it. We are going to scan&nbsp; the driver folder under system 32. Thank you. &nbsp; Let's see what happened. We'll show you that&nbsp; all the drivers and DLL files and processes&nbsp;&nbsp; and register keys and everything. Of course,&nbsp; we can do it also on user files that contain a&nbsp;&nbsp; specific word like Defcon. Any file that contains&nbsp; Defcon, we can delete it and so on. Trust me,&nbsp;&nbsp; Windows cannot load without drivers, so it&nbsp; won't work. We tried, we tried, we have a&nbsp;&nbsp; second restart attempt, but believe me it won't&nbsp; work, so it's a permanent denial of service. &nbsp; This demo was recorded on an older&nbsp; version of Defender. It was version 321,&nbsp;&nbsp; and the newest version at&nbsp; the time of the check, 381,&nbsp;&nbsp; implements few additional checks. For example,&nbsp; we had to remove all the signature type trusted&nbsp;&nbsp; publisher to make Defender delete benign&nbsp; drivers and OS executables, but it works. &nbsp; Let's speak a little bit about future work, and&nbsp; it's very interesting. We found out that the&nbsp;&nbsp; signature database file also include 30,000 LUA&nbsp; scripts. Microsoft used a little bit of modified&nbsp;&nbsp; LUA header for them, but we were able to overcome&nbsp; the modification and to decompile and extract the&nbsp;&nbsp; source code of 30,000 LUA script that control&nbsp; all of the remediation process of Defender.&nbsp;&nbsp; For example, a Lua rule to detect suspicious&nbsp; files masquerading as legit Windows files. The&nbsp;&nbsp; rules simply checks if a file has the same name&nbsp; of OS executable but not in the legit OS path. &nbsp; What will happen if we'll change this rule code&nbsp; to our own code and we try to use LUA libraries&nbsp;&nbsp; but failed since Microsoft probably limits the&nbsp; usage of Lua libraries? But we still think that&nbsp;&nbsp; we might be possible to achieve&nbsp; LPE, local privilege escalation,&nbsp;&nbsp; using Microsoft to remediation library, which&nbsp; we saw used in some of the other LUA scripts,&nbsp;&nbsp; but we had to leave it for future work because&nbsp; lack of time, but we encourage you to do it,&nbsp;&nbsp; and please update us if you can&nbsp; do it. We'll try to after Defcon. &nbsp; A little bit about takeaways. Of&nbsp; course, trust no one. We are Defcon,&nbsp;&nbsp; right? Even the most reliable security controls&nbsp; might be used as a loophole by adversaries, and&nbsp;&nbsp; their architecture and workforce should check and&nbsp; verify in continuous security validation process. &nbsp; Secondly, using digitally signed files does not&nbsp; always necessarily mean totally secure. Security&nbsp;&nbsp; vendors should always verify in any step of&nbsp; the process that the trust was not broken. &nbsp; Finally, signature object process of security&nbsp; control is probably a new attack vector,&nbsp;&nbsp; and we believe additional&nbsp; research is very important.&nbsp;&nbsp; We reported to Microsoft, which&nbsp; released a patch and assigned DCVAD.&nbsp;&nbsp; Please make sure that you are using the version&nbsp; of malware protection platform or above. &nbsp; Keep your cameras, this is the Windows&nbsp; Defender pretended GitHub QR code and link.&nbsp;&nbsp; I will wait a few seconds for those of&nbsp; you who would like to take a photo of it.&nbsp;&nbsp; This past research help us a lot, and we built our&nbsp; research based on their initial findings. Thank&nbsp;&nbsp; you and thank you all. Thank you&nbsp;&nbsp; all for joining.
