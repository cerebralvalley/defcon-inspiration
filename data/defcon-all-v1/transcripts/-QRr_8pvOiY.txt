- Welcome to track three. And our three o'clock is a new speaker. So first time speaking, what does that mean? (audience applauds and cheers) So let's start off with saying l'chaim to Ron and l'chaim! (Ron clears his throat) - Hello everyone. Today, I will talk with you about a new method to dump the memory of LSASS, (clears throat) which is called LSASS Shtinkering. In case you don't know shtinker is a Hebrew word for snitch and this is the reason behind the name of this method. (coughs) Sorry. (crowd laughs and cheers) We're going to snitch on LSASS. And we all know the phrase "snitches get stitches", but luckily, no one will know this is us. So a little bit about us. This method was discovered by Asaf Gilboa, a friend and a colleague of mine. Unfortunately, he couldn't join us today. And my name is Ron Ben-Yitzhak. I'm a security researcher at Deep Instinct. And a fun fact about me, last night I lost three-hundred dollars at the casino. (audience and Ron laugh) So this is the agenda for this talk. I'll start with an overview about credential lobbying attacks. I'll go over known memory dumping techniques and common tools. Then, I'll explain how this method was discovered by reverse engineering the client side of the Windows LLL reporting mechanism. (clears throat) Then I'll talk about reverse engineering the server side, and I'll finish with ways to detect and prevent this attack. So credential access is a huge subject. It's a whole tactic in the MITRE Matrix. And on this screenshot you can see all the techniques under this tactic. In this circle, focus on the LSASS memory sub-technique. Credentials are important for attackers. They use it to move across the network, spread their tools and usually their ransomware. And harvesting right credentials, is a simple way to log into other systems. It doesn't require exploiting vulnerabilities. And the holy grail for attackers, is the domain controller. Credential access is constantly used in actual attacks in the wild. And here you can see reports from multiple security vendors about some of the most infamous groups today that use credential dumping as part of their attacks. You can see reports about Conti, BlackCats, Lockbits and many more. But how does the credential dumping attack go? So, once a user has logged onto the system their credentials are stored in the memory of LSASS. This memory can be read and written to disk, then passed by its tools like Mimikatz. The credentials are extracted and used for lateral movement. So a little bit about LSASS. The Local Security Authority Subsystem Service. It is a system process that is responsible for authenticating users and verifying logons. And the problem with it is that it works as a single sign on. Like I said, the user logs in once and the credentials are stored in the memory of this process. And this memory can be dumped to disk by other processes with APIs like MiniDumpWriteDump. Another thing we should talk about is the Windows Error Reporting Service. It is a building component of the Windows operating system. It collects information about processes that are an unhandled exception and can create memory dumps of those processes. We already looked into the service as a part of a as part of our previous research that I'll talk about soon. And that the target of this research was to find a new way to perform a credential dumping attack. We wanted to force this service to dump the memory of LSASS for us, and that way we'll have a stealthy way to evade EDR solutions. Now, let's talk about some existing dumping techniques. The first one is using the tool ProcDump which is part of Sysinternals. It can dump the memory of LSASS with the following command line. Here is the example for that. And another way to do that is by using the DLL comsvcx. It doesn't expose this function which is called Minidump. You can supply the process ID of LSASS to this function and it will do the work for us. And here is an example for that as well. Another easy way is to just use the task manager. We can right click on the process and select create dump file. Now, another method we should talk about is Silent Process Exit which was also discovered by us, and it is a documented mechanism that was introduced in Windows 7. It is activated when a process terminates and it can do one of the following: show a message box, launch a new process, or create a dump file. You can configure this mechanism by using the registry. Simply create the keys and the values for the process this mechanism should operate on and trigger it by calling RtlReportSilentProcessExit. And this is the research I mentioned earlier. Behind the scenes it communicates with the Windows Error Reporting Service. And also for the rest of this talk, I'll refer to this service as the WERS Service. Now I have to show off a little bit. So, this method caught the attention of Benjamin Delpy, Florian Roth and also already two that have tested it against their security product and it completely bypassed it. But there are protections against dumping the memory of LSASS, such as protected process slides or PPL. This is a security mechanism in Windows that prevents tampering with sensitive processes. A flag is setting the kernel for the protected process and it restricts the access that the regular process can gain on it. And this can be set as a PPL with a single registered value and that will prevent other processes from gaining a handle with the permission of VM Wid. The permission that is required for reading the memory of another process. So you might be thinking that PPL is a perfect solution, but there is a problem with it especially for enterprises and large networks. It prevents third party DLL files from loading into LSASS. That means that benign authentication packages and plugins cannot be used. It can cause issues when there are many solutions and products that are installed in the network and that is why some organizations don't use it. So we talked about several dumping techniques and they all have issues. They're easy to identify, either by the command line or the APIs they use. And also some of the tools might be blocked by security products. So, now let's talk about LSASS Shtinkering. Like I said, it is a new method and it doesn't use a vulnerability. It abuses the service we talked about, We're going to manually report an error to this service that will make it think that an exception occurred inside LSASS. Then it will produce a memory dump for us without terminating it. This will bypass security products that allow processes like Werfaults to dump the memory of LSASS. This is an abstract of what is going to happen. We're going to create a message, then start the service, send the message to the service. It's going to process and validate it and hopefully perform the dump. What is required for the dumping procedure is an inheritable process handle with VMware and query limited information. Also an inheritable threat handle with query limited information and the final require final requirements is setting the register value dump type under the LocalDumps key to full dump. Now, let's see what happens when a process crashes. This process tries to read out of zero WerFault fault spawns the child process and produces a memory dump. But what happened behind the scenes? Every process has exception endos. And the last one is called C Specific Handler. It is implemented inside ntdll, and it is responsible for two things. The first one is reporting the exception details to the WER Service. It does that by calling NtAlpcSendWaitReceivePorts. And the second thing is terminating the process. So that raises a question, can we report an exception on our own process? And turns out their service won't terminate our process. The responsibility for terminating a process that crashes is on the process itself. So how do we do that? How do we report an exception on our own process? To understand that we need to reverse engineer the client side of WER. I'll start with explaining how to create a message. Then we'll see how to send a message and eventually we'll do that and see what happens. So the function WER peer report fault internal is responsible for creating a message. It starts with creating two unnamed events. These events will be used by the service. It will signal them to communicate with the clients. The next step is creating an unnamed file mapping with the protection of red lights. This mapping is then opened into a view. The step after that is duplicating the pseudo handlers for the process and the thread. Then all those values are written into the file mapping as an undocumented stocks. Those values are also sent to the FunctionReportExceptionInternal which stores them in another undocumented stocks and sends it to the function, send message to WER Service. But before we send a message let's understand how the communication between the client and the server work. So the communication is done with the advanced local procedure call or the LPC protocol. It is an undocumented IPC mechanism, which is used by RPC. And on the client's side, there are two important functions. The first function receives a string that represents the server and retains a port endo. The second function is called NtAlpcSendWaitReceivePorts and it uses the port end to send the message to the service and receive a message back. The next function we're going to talk about is send message to WER Service. Before the message is sent, we need to make sure the service is running. So this is done by calling anti updates WNF state data with the parameter WNF WER Service starts. Then we signal the service, it starts initializing, And once the event system error report's ready signaled, we know that the service is up and running. The next step will be using the string Windows Error Reporting Service ports to connect to the LPC ports. And finally, we can send a message. So let's try to do that. We're reporting an exception on our own process. The POC tool is launched WerFault spawns as a child process. We wait for it to finish and a memory dump is produced. And also the tool continues to execute until it exits. So, let's talk a little bit more about what happened in this demo. When the service receives the request it duplicates the file mapping handle to access the undocumented stock that was written into it. And also two instances of WerFault were spawned. The first instance is spawned under the service itself and the command line of this instance, specifies the file mapping handle the idea of the process that is being reported on, and the idea of the process that sent the request. The second instance of WerFault is spawned under the process that sent the request by calling create processes user. And this will be the process that eventually calls MiniDumpWriteDump and report the exception details to the event log. So, now we know how to send report an exception. So let's try to report on LSASS. We can just change the process ID and the handle. So let's see what happens. We launch our tool but it fails and it specifies an error code. So, what happened? The message back from the service specified an error code which means access denied. And to understand why we got that, we needed to reverse engineer the service side of WER. I'll start with an overview about the service, then we'll see where the error code came from and what we need to do to pass the validation checks performed by the service. The WER Service is implemented inside WerSvc.dll which means it is executed under svchost. This service is also set to manual starts which is why we needed to signal it first. The information about programs that crashed is sent to the service, and the logs that are produced will be used for diagnostics. The service initialization process happens inside the function start LPC server and we can see this function uses the string WER already saw, WindowsErrorReportingServicePorts. This string will be part of a stock called object attributes. And this stock will be sent to LPC, create ports. And also the next step in the initialization is creating a thread for the function static LPC server thread. This function will process incoming LPC messages. So now let's see why we got that error code. To find that out we search for references inside a code for this value. We place the break points on each reference and executed the service. The back one that hits is inside the function, check if system connecting to ports. So what is this function? The first thing it does is calling ImpersonateLoggedOnUser. The third that tenders our request is executed under the same user as the service, which is anti authority system. But after the impersonation, it will be executed as the process that sent the request. Then it tries to open the events, worse we see is system permission event but that call fails with the old code access denied which causes our request to fail. So what is so special about this event? Why can't we access it? To learn more about this event we search for other references to that string. It is referenced inside a function, start LPC server. The same function that initializes the service. And here we can see the creation of it. It is created with a security descriptor that specifies generic grid to the system user. but there is also another special thing about this event. It has a prefix. So now we need to understand what is the purpose of this prefix? So guess what we did. We searched for other references to that string. It is referenced inside a function create private name space. Now let's understand what are those. Private name spaces are used with objects called boundary descriptors to protect from a squatting attack. This is a denial of service attack where one program interface with the execution of another program by using shared synchronization objects, or in other words unprivileged application creates the objects before the privileged application thus denying access to the legitimate one. And here is an example for that. Let's say we have a malware and an EDR agent executing on the same system. The model executes first as a low privilege process, and then the EDR agent starts as a privileged one. The malware tries to create the Mutex service Mutex and it is the first one to try to do so, so it will be successful. And then it starts doing malicious stuff. Then the EDR agent starts, it tries to create the same Mutex but the error code will be already existing, and then it will exit. So how those objects protect from this attack? Private name spaces are like directory for kernel objects. Kernel objects that are created inside this directory are protected by boundary descriptors. And those descriptors contain the security IDs for the users in the groups that are allowed to create objects inside this directory. Another important thing is that name spaces are identified by both the name, the string, and the boundary descriptor. This means that different name spaces can have the same name, but different boundary descriptors. So let's look at the example again. But this time the mutex is granted under a name space that allows only the administrators group to create objects. The malware will try to create the exact same name spaces the EDR agent uses. To do that it needs to have the same name, which is easy, but also the same boundary descriptor. The malware can't create it because it is running as a low privilege process. So let's see what's happens. It will try to do it, but then it will fail and exit. Then the EDR agent starts. It starts as a privileged process, so it is able to create the exact name spaces it needs. And then it is the first process to create this mutex. So there will be our success and it will be able to protect the system. So basically namespaces protect named objects from unauthorized access and creating a private namespace enables applications and services to build a more secure environment. So let's see how we do that. The first step is calling create boundary descriptor. and then the security IDs will be added by calling add security ID to boundary descriptor. And the final step is calling create private namespace and send the boundary descriptor as a parameter. And this is exactly what the WER Service does. Let's look at the function, create private namespace. It creates a boundary descriptor, which is called WerSvcNameSpaceBoundary. Then it adds the security ID that represents the service to that boundary descriptor and creates the name space WerSvc. That means that events that are created under this name space were 100% created by WerSvc by no one else. Then the event is created and it is protected by two things. The first one is the name space, and the second one is the secure descriptor, which specifies the generic grid access only to the system user. So now we understand what is this event. So let's look at the validation checks that are The first one is comparing the idea of the process that send the request to the idea of the process that is being reported on. In our first example, we reported on ourselves, so those two values were the same. But now we're trying to report on LSASS. And the sender PID is set by the kernel, we have no control over that so those two values are different and this check fails. The second check is performed by the function, check if system connecting to port, the function we already talked about. And it basically checks the client security ID. Initially that check failed in our example but eventually we were able to pass this validation check. So that made the third one a bit irrelevant. And I won't go too into much details, but it compares the package name of the two processes and package names are related to Windows apps. So let's see how we pass the second validation check. Check if system connecting to ports, basically checks if the sender runs as NT Authority System. Initially this is what happened. Like I said, the thread started as NT Authority System then impersonated our user and the event failed opening the event failed. So all we needed to do is execute our tool Then after the impersonation, the thread stayed as NT Authority System and the event is opened. So to recap, this is that hack. We execute our tool system, it creates two undocumented stocks. The first one will be written into the file mapping, and the second one represents the LPC message. and it starts initializing. It creates the name space with the boundary descriptor, creates the event under the name space, and also the LPC port. Then we send a message and the service starts processing it. It compares the sender PID to the target PID that fails, and then it opens the event after the impersonation, that works. And the request is marked as valid and a dump is performed. So let's look at that. We'll launch our tool again, but this time no error code is specified. WerFault fault spawns as a child process and a memory dump of LSASS dot XC is produced. (crowd cheers and applauds) So now that we understand what is this attack, let's talk about detection and prevention. This attack leaves several artifacts on the system. Let's talk about each one. The first one is in the event log. The event ID an event ID with the idea of 1000 is generated under the application log. And it specifies that an exception occurred inside LSASS. But the most important thing is that our tool isn't mentioned anywhere in this event. This event can't be used to trace the attack back to us, back to our process. The second artifact is the dump file itself. It is created under the CrashDumps folder, and the existence of it is suspicious. But it is created by WerFault.exe and not our process. So the creation of the file can be traced directly back to us. Another log that is generated is under the WER Report Archive. It is a different type of log that specifies the details of the exception. And again, the most important point is that this process that sent the request isn't mentioned anywhere there. One thing that can't be used to detect this attack is the command line of WerFault.exe. It specifies the idea of the process that is being reported on and also the process that sent the request. So if the target process is LSASS and the source process isn't, it is a strong indication of this attack. But let's also talk about the advantages of this attack. WerFault is doing the dump, and this is great because it is a legitimate system process that exists on every version of Windows. The crash reports doesn't specify anything about our process and this tool can be compiled as both 32 or 64 bits. And the registry key we used the dump type is has legitimate usages. So these are the suggested actions for the defenders. Try to look for event ID 1000, which specifies an exception inside LSASS, but it isn't followed by a termination of LSASS. Also, like I said the command line of WerFaults can be used and also API monitoring. We can look for LPC messages that will be sent to Wer with the process ID of LSASS. And also depends on the network we can set LSASS as a PPL. Let's talk also about further research. And the first thing to do is look at other message types. The LPC message has the function that processes the LPC message yeah, has a lot of message types and the other message types might be exploited as well. And also we are using undocumented stocks, so that might change in the future. Specifically the stock that represents the LPC message. It has a variable that is called message size. And we already saw that in previous versions of Windows that's viable the value of this variable differentiates. So it might also be changed in the future. So these are the references for this research, and thank you very much. (audience applauds)
