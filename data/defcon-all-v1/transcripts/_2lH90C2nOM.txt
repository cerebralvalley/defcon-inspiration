Without further ado, boys. Thanks, man. &nbsp; Hi, DEFCON. Thanks for sticking with us&nbsp; today. Sunday is a bit more chill. Some&nbsp;&nbsp; of us are already flying home, some of us are&nbsp; recovering from hang over and the last of us&nbsp;&nbsp; are here. Thanks for sticking with us. There&nbsp; is also some zombies there. Okay, so today we&nbsp;&nbsp; are presenting the results of our research,&nbsp; which is and we will present a new approach&nbsp;&nbsp; for stack spoofing targeting Windows on&nbsp; specifically 64-bit architecture of Windows.&nbsp;&nbsp; Who we are. We are three friends. I am Alessandro&nbsp; Magnosi, principal security consultant in BSI&nbsp;&nbsp; where I do mostly red teaming, offensive tool&nbsp; development, and white box assessments. So&nbsp;&nbsp; source code review and stuff&nbsp; like that. In my free time I maintain some tools,&nbsp;&nbsp; some offensive tools in Porchetta Industry,&nbsp; which is an initiative by Marcello Salvati&nbsp;&nbsp; to support open source's developers. And in the&nbsp; rest of my time I'm a bug hunter for Synack team.&nbsp;&nbsp; Then there is my colleague. Hey, my name is Arash. I work at CyberArk. I have&nbsp;&nbsp; a dual role there. I'm very fortunate I get to do&nbsp; a lot of threat hunting, detection engineering,&nbsp;&nbsp; and I also get to do red teaming when time allows&nbsp; for it. And our friend Athanasios unfortunately&nbsp;&nbsp; couldn't be here today, he also contributed to&nbsp; this work. He's a red teamer at Nettitude. He&nbsp;&nbsp; was also great and he contributed a lot here&nbsp; so we want to make sure he's mentioned. &nbsp; So here we are. So a bit of story about&nbsp; this. All of this started back in 2022,&nbsp;&nbsp; when Namazso security researcher and&nbsp; developer released a book on Twitter,&nbsp;&nbsp; like an image, where it was showing a&nbsp; technique, which then we popularized and&nbsp;&nbsp; fix also a bit. And this, actually, we&nbsp; were very interested in this research,&nbsp;&nbsp; in his research, in his work. So we reached&nbsp; down to it, we started developing this in-house.&nbsp;&nbsp; Of course, we didn't know a lot about what was&nbsp; involved with the technique. So we started this&nbsp;&nbsp; research and development project. We developed&nbsp; our own version of the technique showed by&nbsp;&nbsp; Namazso. And after that, we continue researching&nbsp; on this technique. We will also present indeed,&nbsp;&nbsp; we'll also present the detection part for this&nbsp; technique and other additional research avenues&nbsp;&nbsp; that we researched. So just to give you a&nbsp; bit of background, let's define what stack&nbsp;&nbsp; spoofing is, what we mean with stack spoofing. So, of course, you know that when you start the&nbsp;&nbsp; process on Windows, when you execute a binary,&nbsp; you create a processing in Windows, which is a&nbsp;&nbsp; container, and then one or more threads are tasked&nbsp; with executing the real code of the program.&nbsp;&nbsp; Every thread maintains a call stack, which is an&nbsp; area of memory organized as a stack that maintains&nbsp;&nbsp; the information about all active sub-routines&nbsp; in a given time for a given thread. And what we&nbsp;&nbsp; do with spoofing is that we want to substitute&nbsp; the frames with fake ones. And this is usually&nbsp;&nbsp; never done for non-malicious activity. This is&nbsp; always done for malware to hide the execution&nbsp;&nbsp; flow or similar stuff. So you will see here&nbsp; four different frames, and why I put this&nbsp;&nbsp; here is just to let you see what a call stack is&nbsp; and you will see that there are certain things&nbsp;&nbsp; that appears that are common to a call stack. For example, we define these first two threads&nbsp;&nbsp; for this talk, we'll just talk about these&nbsp; two frames as thread initialization frames,&nbsp;&nbsp; which are usually at the bottom of the call&nbsp; stack, at the top actually of the call stack.&nbsp;&nbsp; Then you have internal frames of specific&nbsp; programs that we will refer to this during&nbsp;&nbsp; the talk as main modules frames. And then&nbsp; you have the frames related to an API call.&nbsp;&nbsp; So how this calculated by Windows? So the stack is&nbsp; evaluated by Windows using an unwinding algorithm.&nbsp;&nbsp; And this uses the information contained in&nbsp; the PE data section of a binary or related&nbsp;&nbsp; DLL. And in this PE data section, you usually&nbsp; have an exception directory, which contain all&nbsp;&nbsp; the information about the functions, specifically&nbsp; information about every runtime function entry. &nbsp; In this table contains information about the&nbsp; beginning address of a function, the end address,&nbsp;&nbsp; and a pointer to a structure, which is the&nbsp; unwinding infrastructure, that contains&nbsp;&nbsp; information about the size of the prologue,&nbsp; the count of codes and all the operations&nbsp;&nbsp; that were performed in a prologue of a specific&nbsp; function. Now why this is important? I mean,&nbsp;&nbsp; this is used by Windows to manage exceptions,&nbsp; but what we are interested in is the usage that&nbsp;&nbsp; this is done to calculate the call stack at&nbsp; one time. And to calculate that, what happens&nbsp;&nbsp; is that Windows evaluate the call frame size&nbsp; dynamically using, collecting the information,&nbsp;&nbsp; collecting the codes, so the operation in the&nbsp; prologue that impacted the frame sides and the&nbsp;&nbsp; operation are usually PUSH, of course. If you PUSH&nbsp; a register, you're subbing eight on the stack and&nbsp;&nbsp; if you allocate memory, you're subbing from RSP. So you allocate stake on the stack. So these two&nbsp;&nbsp; main operation are what modified the frame size&nbsp; and then there is the PUSH MACHFRAME, which is&nbsp;&nbsp; really not interesting for our talk. But yeah,&nbsp; it's another operation that impact the frame size.&nbsp;&nbsp; And this is important, this is needed in Windows&nbsp; because, if you remember 32-bit architecture&nbsp;&nbsp; in the product, you always have a PUSH EBP and&nbsp; then you save the EBP base pointer. So you have&nbsp;&nbsp; the base pointer pointed to the top of the frame&nbsp; and then you have the ESP pointing to the bottom&nbsp;&nbsp; of the stack. And this is useful, the contrary,&nbsp; but yeah, this is very useful to evaluate the&nbsp;&nbsp; call stack because you can just navigate back the&nbsp; chain of EBP that are pushed on the stack. But in&nbsp;&nbsp; 64-bit this is not possible because RSP is just&nbsp; bought as a base pointer and a stack pointer. &nbsp; So to evaluate the call stack, we need to&nbsp; recursively get the size of a stack frame,&nbsp;&nbsp; and then recover the return address, and then&nbsp; evaluate again the call stack frame and then the&nbsp;&nbsp; return address and traverse it back like this.&nbsp; So why we are interested in the call stack,&nbsp;&nbsp; we are interested in the call stack because when&nbsp; you execute malware on Windows, you can generate&nbsp;&nbsp; some anomalies that can help with detection. So&nbsp; we have in-memory execution that can be detected&nbsp;&nbsp; because when you inject code in-memory, if you're&nbsp; using especially to run a malware you do process&nbsp;&nbsp; injection, for example, or code injection, and&nbsp; you're using an executable section on the HIP,&nbsp;&nbsp; what happens is that that section will not have&nbsp; a module associated. So that executable memory is&nbsp;&nbsp; not backed by a file on disc. If there&nbsp; is no file, there is no PE data section. &nbsp; If there is no PE data section, there is no&nbsp; exception directory. If there is no exception&nbsp;&nbsp; directory, there is no way, there is no&nbsp; information that the unwinding aggregate&nbsp;&nbsp; can use to dynamically get the size of that&nbsp; specific code. So what will happen is that,&nbsp;&nbsp; too much, sorry, what will happen is that you will&nbsp; have something like this. So you will have a frame&nbsp;&nbsp; at a certain point that is not resolvable and this&nbsp; is associated with an unbacked memory region. The&nbsp;&nbsp; other anomaly that you can detect from the call&nbsp; stack is when you use direct or indirect syscalls&nbsp;&nbsp; and we are referring to direct system calls,&nbsp; actually direct anti-function execution like&nbsp;&nbsp; GetProcAddress, NTDLL, anti-function, and then&nbsp; execute the function pointer is calling directly&nbsp;&nbsp; the anti-function, and this will generate a call&nbsp; stack that misses the... Why? Sorry for that. &nbsp; That misses the frames associated with high-level&nbsp; API call. This stack frame is also generated if&nbsp;&nbsp; you're using an indirect system call. So if&nbsp; you're just locating the Cisco instruction&nbsp;&nbsp; in NTDLL and then you jump to it. So the frame,&nbsp; the stack is pretty much the same, and it misses&nbsp;&nbsp; the high-level API frames, of course. But these&nbsp; are the anomalies. When you want to detect this,&nbsp;&nbsp; well, you can do a periodic scan. So you have a&nbsp; tool that stop the frames, that stop the threads&nbsp;&nbsp; system-wide, get the context and analyze the stack&nbsp; frame. You can have a conditional scan, which is&nbsp;&nbsp; a periodic scan with another filter to filter for&nbsp; specific kind of threads that are in specific WAIT&nbsp;&nbsp; states, for example. Or you can use hooking or&nbsp; kernel tracing, which is instead done dynamically&nbsp;&nbsp; when system call is involved you can use something&nbsp; like DWTI or another kind of driver to get the&nbsp;&nbsp; user mode call stack armed for detections. So stack spoofing, previous research on the&nbsp;&nbsp; topic lead to the development of techniques&nbsp; to defend from specific scan periodic scan&nbsp;&nbsp; like stack truncation, stack cloning, and stack&nbsp; hiding, or to defend against specific rule-based&nbsp;&nbsp; detection on the call stack when executing a&nbsp; specific API. So stack truncation was probably&nbsp;&nbsp; the first one used and it's an evolution, it's&nbsp; a variation of normal return address spoofing&nbsp;&nbsp; where we replace the malicious frame with a&nbsp; trampoline, usually in a DLL or in an executable.&nbsp;&nbsp; And then we nullify the return address prior to&nbsp; the trampoline. This way we have a cut call stack&nbsp;&nbsp; like this, which is surely better than having&nbsp; garbage in the call stack, but it still has a&nbsp;&nbsp; defect, which is this stack is not unwinable. So&nbsp; we still can have a detection for this reason. &nbsp; Then we have stack crafting. Stack&nbsp; crafting instead is a technique that&nbsp;&nbsp; is usually asynchronous, like we have a system.&nbsp; There is a specific function that get called,&nbsp;&nbsp; and there are specific processes using that&nbsp; function in a legitimate way. So what we do&nbsp;&nbsp; is that we record that execution, that call&nbsp; stack, at that point. And when we need to call&nbsp;&nbsp; our target function, we just craft the same kind&nbsp; of call stack so that we can, in this way, we can&nbsp;&nbsp; bypass detection like for&nbsp; specific call stack trays or similar detection.&nbsp;&nbsp; Then we have stack cloning. This as well has been&nbsp; developed to defend from conditional scanning,&nbsp;&nbsp; and the idea is simple. Usually, our implants&nbsp; usually sleep most of the time. So when I want&nbsp;&nbsp; to sleep using a delay execution like rate delay&nbsp; execution using a sleep or using a WAIT, user&nbsp;&nbsp; requested WAIT state using WAIT for single object,&nbsp; what I need to do is just hunt for a thread that&nbsp;&nbsp; is already with that state that is sleeping&nbsp; with that state and then clone its call stack. &nbsp; This will make the call stack perfectly legitimate&nbsp; or appear apparently legitimate. So we will be&nbsp;&nbsp; defended by conditional scan that aren't for&nbsp; sleeping beacons. Stack hiding is similar&nbsp;&nbsp; but it uses another kind of approach. So the&nbsp; approaches that I will transform my thread in,&nbsp;&nbsp; I have a malicious code executing in memory.&nbsp; I transform my thread in a fiber. So I have a&nbsp;&nbsp; fiber thread now and then I create another fiber&nbsp; with a legitimate call stack that I use just&nbsp;&nbsp; for sleeping. So when I need to sleep, I just&nbsp; switch to the fiber thread to the other fiber&nbsp;&nbsp; that I created for sleeping. And then this process&nbsp; gets repeated when I need to go back from sleep.&nbsp;&nbsp; And this is what you have, this is the&nbsp; fiber with a legitimate call stack and&nbsp;&nbsp; this is the code in memory. So you can&nbsp; see that there are two different area to&nbsp;&nbsp; different memory area for the stacks&nbsp; and just one is active at a time. &nbsp; So what is our contribution today? We want to&nbsp; present two techniques, full moon and half moon,&nbsp;&nbsp; that are designed to obfuscate the stack at&nbsp; runtime to avoid detection, kind of based&nbsp;&nbsp; detection mostly or hooking detection. So stack&nbsp; moonwalking has some properties like it hides the&nbsp;&nbsp; caller from memory, it obfuscates the stack so&nbsp; it's difficult to understand which kind of call&nbsp;&nbsp; stack was actually taken, was actually generated&nbsp; when executing a specific API or system call. And&nbsp;&nbsp; then it has an auto-restore technique. So&nbsp; usually, we use ROP to restore the stack.&nbsp;&nbsp; We will see this is not always possible, but yeah,&nbsp; for the most part is use ROP to restore the stack.&nbsp;&nbsp; So full moonwalk is the first technique, and&nbsp; it operates like that. We have a function&nbsp;&nbsp; that we want to spoof an API or whatever. And what we do, of course, we are executing&nbsp;&nbsp; this from our main module. That can be in memory&nbsp; or can be like in this case it seems like it is&nbsp;&nbsp; a program, but it can be even injected in-memory.&nbsp; There is no problem. This is for main module. If&nbsp;&nbsp; you remember, I'm referring to all the frames&nbsp; generated by the main module. So it's like I&nbsp;&nbsp; just packed all the internal frames for the main&nbsp; module, and what I do is I select three frames,&nbsp;&nbsp; well, actually four to be a bit more stealthy&nbsp; in our intentions, but the needed are three, and&nbsp;&nbsp; these are as an SETFPREG frame, a PUSHREG&nbsp; frame, A DESYNC frame. And then yeah,&nbsp;&nbsp; we added the CONCEAL frame just to be a bit more&nbsp; stealthy. And what they do is just at a certain&nbsp;&nbsp; point in the SETFPREG frame, what we are doing&nbsp; is that we are restoring the base pointer again. &nbsp; And so this happens through this kind of call,&nbsp; which is MOV RBP, RSP, where we save our current&nbsp;&nbsp; RSP, which contains the address on the stack&nbsp; that contains the return address of this frame.&nbsp;&nbsp; And then with the second frame, the PUSHREG&nbsp; frame, we push RBP on the stack. Now what happens,&nbsp;&nbsp; this creates an opportunity for us to modify&nbsp; the address that was pushed in-memory because&nbsp;&nbsp; that RBP now is on the stack. If you modify&nbsp; it, this will be evaluated by the unwinding&nbsp;&nbsp; aggregate as the address that should be used to&nbsp; restore RSP. And this gives us an opportunity to&nbsp;&nbsp; tamper with this so that if this RSP points&nbsp; back to our thread initialization frame,&nbsp;&nbsp; practically what happens is that the main module&nbsp; will completely disappear from the call stack. &nbsp; Okay, this doesn't solve the memory problem.&nbsp; So if you have unbacked memory section,&nbsp;&nbsp; this will not resolve that, okay? But from&nbsp; the call stack is not visible anymore.&nbsp;&nbsp; Because doing just this will, of course,&nbsp; corrupt the execute the return flow of&nbsp;&nbsp; the program. We use the DESYNC gadget,&nbsp; which is a JOP gadget here, a JMP RBX,&nbsp;&nbsp; to eject the return flow so that we can execute a&nbsp; restore function which is under our control. And&nbsp;&nbsp; this restore function will reset the main module&nbsp; to what it was and just remove the fake frames.&nbsp;&nbsp; So this is the technique in a nutshell, and this&nbsp; is how the call stack is set up. And as you can&nbsp;&nbsp; see, there are the thread initialization&nbsp; frames, and then there is no executable,&nbsp;&nbsp; there is just kernel base and these frames are&nbsp; all generated by our algorithm. Now these frames&nbsp;&nbsp; have some logical inconsistencies that can be&nbsp; used for detection, and my colleague will- &nbsp; Sure. So I'll talk to you guys a little&nbsp; bit about the IOCs and why this doesn't&nbsp;&nbsp; necessarily create a perfect stack&nbsp; but does create a good-looking stack.&nbsp;&nbsp; So if we look at the slide we have&nbsp; currently, you can see the spoofed frame,&nbsp;&nbsp; the kernel-based DLL, open state explicit,&nbsp; and you actually see a location that +0x39d,&nbsp;&nbsp; that's an offset. So what's going to happen is&nbsp; that's the return address we would return to&nbsp;&nbsp; in order to continue execution within the stack.&nbsp; Now why is this relevant, and why is this an IOC?&nbsp;&nbsp; Because technically, well, let's talk about what a&nbsp; call stack is. Based on the name a call stack is,&nbsp;&nbsp; of course, a series of calls. So you will see an&nbsp; API call making another API call making another&nbsp;&nbsp; API call. And this is often so you can debug and&nbsp; you can see at what API you may have crashed,&nbsp;&nbsp; at what section you may have issues, et cetera. So it is supposed to show a series of calls&nbsp;&nbsp; to other functions. Now if we look at that&nbsp; spoofed frame, the kernel-based DLL or open&nbsp;&nbsp; state explicit, we go to the return address, but&nbsp; the return address isn't going to have a call&nbsp;&nbsp; because the return address is going to be after&nbsp; the call. That's where we continue execution,&nbsp;&nbsp; where we return. So what we do is we actually&nbsp; go before the return address and we try to look&nbsp;&nbsp; at that little move-by-pointer that we see&nbsp; there for the open state explicit function.&nbsp;&nbsp; We can see that that is not a call. And not&nbsp; only is that not a call to another function,&nbsp;&nbsp; it is certainly not a call to create private&nbsp; object security with multiple inheritance.&nbsp;&nbsp; So immediately, this becomes suspicious,&nbsp; and this isn't something a legitimate dev&nbsp;&nbsp; would ever do because it would just&nbsp; hinder their debugging process. &nbsp; It would impact them negatively. So this&nbsp; is something we would only see an attacker&nbsp;&nbsp; wanting to perform. Now while I say this is an&nbsp; IOC, it's really a difficult IOC, it's hard to&nbsp;&nbsp; deploy at scale. It's not really something you&nbsp; can hunt in your EDRs. And if you observe this on&nbsp;&nbsp; every single function, there would be very massive&nbsp; performance hits. So while this is an IOC, we're&nbsp;&nbsp; not necessarily sure there's a realistic detection&nbsp; method yet outside of the possibility of a utility&nbsp;&nbsp; such as intel pin tracer, which does it a little&nbsp; lower level than the operating system itself. &nbsp; So if we look here, we can tell you what we're&nbsp; looking for. These are the call op codes. This&nbsp;&nbsp; is basically the assembly that we're looking for&nbsp; in our Eclipse detector to observe full moonwalk.&nbsp;&nbsp; So we're looking for specific types of calls&nbsp; that MODR Rex.W basically determines if it's&nbsp;&nbsp; a long call or a short call, for example. And&nbsp; then we can see a relative 32 call, the call R64&nbsp;&nbsp; call as a register. And then, of course, the&nbsp; system call the NTDLL lowest level call that&nbsp;&nbsp; you will often see because NTDLL often makes the&nbsp; syscall for you. But if we were to observe there,&nbsp;&nbsp; we would see a syscall instruction. So this is&nbsp; the algorithm for all you PhD people out there.&nbsp;&nbsp; Basically, the idea is we're looking for&nbsp; very specific things in our algorithm,&nbsp;&nbsp; not just for stack spoofing, but the detector&nbsp; is looking for an invalid stack period and&nbsp;&nbsp; not just by unwinding because, as we know, we can&nbsp; actually fool the unwinding as previously shown. &nbsp; So what do we look for? We're looking for unbacked&nbsp; memory regions. That's the reflective injection&nbsp;&nbsp; detected. Unbacked memory means some injection was&nbsp; performed, a private RX commit memory region was&nbsp;&nbsp; created. This isn't backed to any files on disc,&nbsp; no DLLs, no excs. So we consider this malicious&nbsp;&nbsp; off and we consider some injection of some&nbsp; sort. There can be false positives with this&nbsp;&nbsp; to be clear, JITs, browsers, C#, you'll see these&nbsp; private commit memory regions getting created and&nbsp;&nbsp; executing code. So while this does help, it's&nbsp; not foolproof, to be clear. The next is we&nbsp;&nbsp; actually look for the DESYNC gadget because what&nbsp; full moon walk actually does is it DESYNCs the&nbsp;&nbsp; stack with basically a ROP gadget in order to&nbsp; be able to effectively craft its own stack for&nbsp;&nbsp; restoration as well. So we can just look for that&nbsp; gadget, and often that can be an IOC as well. &nbsp; Additionally, the call mismatch&nbsp; detected, as previously stated,&nbsp;&nbsp; message box A is going to make a very specific&nbsp; call to the next function. If we look at message&nbsp;&nbsp; box A, we look at the previous instruction, and&nbsp; it's not the right call to the next function,&nbsp;&nbsp; that's an IOC. And the wrong address detected&nbsp; as well. If the address is just not a call,&nbsp;&nbsp; if it's not strict, if it's not correct,&nbsp; it has to be a call, and it has to be a&nbsp;&nbsp; call in the correct order. I actually think I&nbsp; mixed up the mismatch and the wrong address,&nbsp;&nbsp; but the idea is the same. In one, we're basically&nbsp; looking for a call that's not a call or we're&nbsp;&nbsp; basically looking is it actually calling the&nbsp; next function, because that's what a call&nbsp;&nbsp; stack is supposed to do. This function calls that&nbsp; function, calls that function for investigation. &nbsp; So this is full moonwalk running. We have&nbsp; a successfully spoofed message box A,&nbsp;&nbsp; we're even telling you we're&nbsp; spoofing this message box A.&nbsp;&nbsp; Now we run our detector. On the left&nbsp; you can see our four spoofed frames,&nbsp;&nbsp; frames eight, nine, 10, and 11. Our detector&nbsp; first catches create file internal as a spoofs&nbsp;&nbsp; call because the previous instruction is&nbsp; not a call. And here's what's interesting,&nbsp;&nbsp; we also get PSS query snapshot. Now if you look&nbsp; at the addresses, as I said before, this does&nbsp;&nbsp; unwind. If you look at the PC address for PSS&nbsp; query snapshot, it matches the return address&nbsp;&nbsp; for the create file function. So an unwinder is&nbsp; going to think this is legitimate, this is in&nbsp;&nbsp; fact what was called, this is in fact where it&nbsp; needs to return but our detector knows better. &nbsp; Our detector goes there and says not only is&nbsp; there no call, there's absolutely no call to&nbsp;&nbsp; create file W. All we've done is fooled the&nbsp; Microsoft unwinder into thinking this looks&nbsp;&nbsp; legitimate. So if you actually use Microsoft's&nbsp; built-in unwinding, this is what we did here,&nbsp;&nbsp; it looks legitimate. We only caught this&nbsp; because our detector is looking for more.&nbsp;&nbsp; Now there are a few false positives, as mentioned&nbsp; before. We see this a lot with JITs specifically.&nbsp;&nbsp; We saw this in Wind DBG, it seemed to have&nbsp; a lot of jitting going on. We can see right&nbsp;&nbsp; here one of the processes for Wind DBG has just&nbsp; these two entirely unbacked memory regions. For&nbsp;&nbsp; reasons unknown to us it appears JITs don't&nbsp; always fully unwind and that these memory&nbsp;&nbsp; regions just always exist. And so there will&nbsp; be false-positives with JIT processes as usual.&nbsp;&nbsp; So we did testing to see what the&nbsp; false-positive rate actually was.&nbsp;&nbsp; We wanted to determine is this feasible? Is this something we can actually do without&nbsp;&nbsp; overloading a team? And we determined kind of,&nbsp; it's not too bad. So we'll see. We tested this&nbsp;&nbsp; on five different machines. Standard just means it&nbsp; was a standard Windows user machine. This is not&nbsp;&nbsp; a developer, this is just a person who logs on,&nbsp; watches video, does work on Word documents, and&nbsp;&nbsp; that's it. Dev is actually a developer. Somebody&nbsp; who installs Visual Studio, sets up postman,&nbsp;&nbsp; installs Wind DBG preview. And again, it makes&nbsp; sense the developer is going to have a higher&nbsp;&nbsp; false positive rate. They have more JITs, they're&nbsp; developing more tools that are going to have weird&nbsp;&nbsp; unbacked memory regions. Programming isn't really&nbsp; perfect and there's really no catchall for this.&nbsp;&nbsp; We can see on WIN 10 the false positive rate is&nbsp; actually a little lower than WIN 11 Enterprise. &nbsp; But we do see that for the dev machines, it&nbsp; actually decreased a bit. As far as Windows&nbsp;&nbsp; Server goes, we see a pretty high&nbsp; false-positive rate. That's likely&nbsp;&nbsp; associated with you're deploying your web servers&nbsp; on this, you're deploying your .NET applications,&nbsp;&nbsp; you're deploying all sorts of stuff that's going&nbsp; to sort of increase that rate of false positives.&nbsp;&nbsp; And just to end this, as far as detecting&nbsp; all this goes, it's like I said,&nbsp;&nbsp; for a hunter behind an EDR, this isn't really&nbsp; telemetry we get. Some EDR provide call stacks&nbsp;&nbsp; for very specific functions so as to not be too&nbsp; intensive, and we can sort of hunt on that and&nbsp;&nbsp; look for unbacked memory regions. But as far as&nbsp; it goes for full moonwalk, we just don't really&nbsp;&nbsp; have an effective way to check every single&nbsp; function call and ensure that this call is&nbsp;&nbsp; actually a valid call. So in a way, this unwind&nbsp; ability is pretty effective in today's age. &nbsp; So after developing the detection, we actually&nbsp; talked about other kind of avenues where we could&nbsp;&nbsp; actually make use of this kind of knowledge.&nbsp; And so as we've seen except for the in-memory&nbsp;&nbsp; execution, there was another problem which&nbsp; was the call stack frames allocated when we&nbsp;&nbsp; execute an high-level API, instead of going&nbsp; with the indirect Cisco. So we developed this&nbsp;&nbsp; additional technique which is an extension and&nbsp; it's half moonwalk. Half moonwalk is because we&nbsp;&nbsp; are just doing half of the technique. We are just&nbsp; considering what we are calling and not really&nbsp;&nbsp; the caller. And so what half moonwalk does is that&nbsp; we can always match an high-level API to a Cisco.&nbsp;&nbsp; So half moonwalk takes as input an high-level&nbsp; API name and its relevant Cisco instruction,&nbsp;&nbsp; and it will trace the execution&nbsp; flow from the high-level API&nbsp;&nbsp; until it finds the relevant anti-function. And what we are doing is that doing this tracing,&nbsp;&nbsp; we also collect all the information, all the&nbsp; exception information. So all the information&nbsp;&nbsp; in the PE data section related to the prologue.&nbsp; And what we do is that we recreate the frames&nbsp;&nbsp; of the high-level APIs before executing an&nbsp; indirect system call. So how we do that,&nbsp;&nbsp; we just need to literally emulate the prologue of&nbsp; the high-level API. And what happens doing this is&nbsp;&nbsp; that we can recreate the same kind of call stack&nbsp; perfectly emulated this time. So we also respect&nbsp;&nbsp; the location of the return address which is&nbsp; placed after the call and the call is actually&nbsp;&nbsp; matching what it's being called. But we never&nbsp; called the high-level API. So this is literally&nbsp;&nbsp; a SysWhisper type of indirect system call just&nbsp; with added spoofed frame for the high-level API. &nbsp; So now we have a little problem that we will&nbsp; solve afterward for the emulated system call,&nbsp;&nbsp; which of course is a function that will do this&nbsp; spoofing and it's visible as for now. So we'll&nbsp;&nbsp; solve this later. So we are concerned about the&nbsp; return flow, though. If we have these frames,&nbsp;&nbsp; they will return back to where they are supposed&nbsp; to return. But we didn't actually execute the&nbsp;&nbsp; high-level API, we just emulated the prologue.&nbsp; Would that be enough to actually ensure that&nbsp;&nbsp; the return flow is correct? Well, yes and no.&nbsp; So there are three strategies we can use. One&nbsp;&nbsp; is place the return on unwinding. So this is&nbsp; terribly worded. What I mean is that we place&nbsp;&nbsp; the return address on the epilogue, which, of&nbsp; course, is just locating the prologue. It's just&nbsp;&nbsp; literally rolling by the prologue. So that will&nbsp; work but we will be detected by Eclipse again&nbsp;&nbsp; then because we need to move the return address&nbsp; not after the call but on the epilogue itself. &nbsp; The other, and this is what it happens instead&nbsp; we have the call to NT protector to memory in&nbsp;&nbsp; this case, instead of placing the return here&nbsp; where it's supposed to do because there are all&nbsp;&nbsp; these tests, we just move it to the epilogue. And&nbsp; this works, of course, but as I said, we can be&nbsp;&nbsp; detected again by Eclipse with this because if&nbsp; we analyze the instruction before this return&nbsp;&nbsp; address, it won't be a call anymore. So we have&nbsp; a second strategy, which is the targeted setup,&nbsp;&nbsp; that works even better but it doesn't scale. And&nbsp; the targeted setup is just like I can analyze what&nbsp;&nbsp; are the post-call checks, what I call post-call&nbsp; checks are literally all these checks, all&nbsp;&nbsp; these if that are executed after the call. And to&nbsp; avoid them from branching, to avoid the branching&nbsp;&nbsp; I just see what I need to patch, and then I&nbsp; patch it before actually executing the call. &nbsp; Now it works, but you can't do it anytime, and&nbsp; honestly, it doesn't scale at all because you&nbsp;&nbsp; would need to know what thing to patch every&nbsp; time you execute an arbitrary function. This&nbsp;&nbsp; doesn't scale at all. So the last thing we can do&nbsp; is... I don't know what is happening, I'm sorry.&nbsp;&nbsp; Another thing I can do is using hardware&nbsp; breakpoints. So what happens is that I&nbsp;&nbsp; place a break point on return and we actually say&nbsp; sliding hardware breakpoints because we don't know&nbsp;&nbsp; how many frames we need to emulate. So if they&nbsp; are more than four, of course, I need a strategy&nbsp;&nbsp; to outdate the outer back points. Otherwise, I&nbsp; will miss some frame. Anyway, what it happens&nbsp;&nbsp; is that I place an hardware breakpoint at the&nbsp; return address. When the function returns back,&nbsp;&nbsp; I just execute an exception and alert that just&nbsp; execute the epilogue, emulate the epilogue,&nbsp;&nbsp; and then force the instruction pointers&nbsp; to return. And this scales well, but&nbsp;&nbsp; I always need executable memory because I always&nbsp; need to make the exception handler somewhere. I&nbsp;&nbsp; need to store the exception handler somewhere. So I always need some executable memory floating&nbsp;&nbsp; around. So as I said before, we also had a&nbsp; sort of a problem with the emulate system&nbsp;&nbsp; call because we said, well, there is&nbsp; no function if you remember before,&nbsp;&nbsp; this thing is actually pushing this frame and&nbsp; then jumping to the Cisco instruction here. So&nbsp;&nbsp; if someone is looking at this emulate system call,&nbsp; it can be problematic. So what we can create is an&nbsp;&nbsp; Opaque architecture; this is a HIF, but you can&nbsp; place a switch case, for example, that just will,&nbsp;&nbsp; when this is executed it will execute. So now we&nbsp; are stepping through here to emulate system called&nbsp;&nbsp; W. This is a frameless function. It will execute&nbsp; something on the stack, it will corrupt the stack&nbsp;&nbsp; doing this, but it will grab the position of this&nbsp; frame function, and it will just swap itself, swap&nbsp;&nbsp; the frame of the frameless function with the frame&nbsp; function. This way, it will restore the stack,&nbsp;&nbsp; the call stack that will appear legitimate. And here, what legitimizes this sequence of call&nbsp;&nbsp; is that we have a jump here to the restore&nbsp; proc. Of course, this is Opaque, it's not&nbsp;&nbsp; that great. But what is really important about&nbsp; this is that even if we have something with no&nbsp;&nbsp; frame information associated, as long as the&nbsp; stack size, so the modification on the stack&nbsp;&nbsp; are equal to something that we are creating&nbsp; and is framed, we can always replace it and&nbsp;&nbsp; fix the call stack. Why this is important&nbsp; because there are ways we can do that&nbsp;&nbsp; using standard Windows DLLs. So we selected this&nbsp; in particular because it was already abused in&nbsp;&nbsp; exploit development to bypass exploit mitigation.&nbsp; It's part of the exploit mitigation developed by&nbsp;&nbsp; Windows, and it's the call to NdrServerCall2.&nbsp; So we can use this kind of architecture&nbsp;&nbsp; here to completely hide our caller again. So how this may work. So if we can enforce&nbsp;&nbsp; in our implant maybe a stage zero if we can&nbsp; enforce in our implant this constraint. So&nbsp;&nbsp; we want our implant at any time the internal, the&nbsp; sides of the frames of the internal frames of our&nbsp;&nbsp; implant, they should always match the sides&nbsp; of the NdrStubCall2Stack. As you can see here,&nbsp;&nbsp; you have a lot of space allocated on the stack for&nbsp; this function. If we can enforce this constraint,&nbsp;&nbsp; we can start a thread pointing to our shell-coded&nbsp; memory, which is our implant. We can force these&nbsp;&nbsp; first two frames in the thread itself.&nbsp; Then we can start our implant. And when&nbsp;&nbsp; the implant needs to spoof a function, what we&nbsp; can do is call half moon, execute half moon,&nbsp;&nbsp; whereby the first frame spoofed is the Invoke&nbsp; function, which is an arbitrary call to anything,&nbsp;&nbsp; and then execute half moon with&nbsp; the rest of the high-level API&nbsp;&nbsp; frame and eventually call the spoof function. So what this creates is a completely legitimate&nbsp;&nbsp; call stack where we have the thread initialization&nbsp; frames, then we have the first two call to&nbsp;&nbsp; NdrStubCall, NdrServerCall2 and NdrStubCall2. Then&nbsp; we have the execution of our implant. We have the&nbsp;&nbsp; return address spoofed with NdrStubCall2Stack,&nbsp; which completely hides our implant frames. And&nbsp;&nbsp; then we have half moon executed with Invoke and&nbsp; all the high-level, all the remaining high-level&nbsp;&nbsp; API frames. And then we have the system call&nbsp; again executed. This will hide completely the&nbsp;&nbsp; called as well. So we are just going back and&nbsp; try to complete what half moon was not doing.&nbsp;&nbsp; So hiding the caller. Now the only drawback of&nbsp; this is that you always, in order to restore the&nbsp;&nbsp; functionality of the program on return, you always&nbsp; need an hardware breakpoint placed on Invoke. &nbsp; So you can handle these two frames as&nbsp; much as you want as we've shown before,&nbsp;&nbsp; so target that setup or return on epilogue or&nbsp; exception handler. But this, in Invoke function,&nbsp;&nbsp; you always need an exception handlers to&nbsp; actually restore the return address of the&nbsp;&nbsp; under NdrStubCall2Stack and return to our implant.&nbsp; Just a word about CET, we are studying a lot Intel&nbsp;&nbsp; CET with shadow stack mitigation and in direct&nbsp; branching tracing. And so far, we didn't find&nbsp;&nbsp; a way around CET. It's not surprising. So what&nbsp; happens with CET is that there is a shadow stack,&nbsp;&nbsp; of course, a clone. It maintains a list of&nbsp; all the return address that are placed just&nbsp;&nbsp; after a call and when on return, it will check&nbsp; if this return address matches the user mode,&nbsp;&nbsp; the writeable, the normal call stack. And if they&nbsp; don't match, there will be a #CP fault that will&nbsp;&nbsp; crash the problem, that will exit the problem. There is another thing that is concerning about&nbsp;&nbsp; Intel CET is that within indirect branch tracing&nbsp; there is no easy way to modify the instruction&nbsp;&nbsp; pointer as well. We can just use exception&nbsp; handler as much as we do now. But the good&nbsp;&nbsp; thing is that even in machine supporting CET so&nbsp; far, there are always processes that are not yet&nbsp;&nbsp; enabled, like browsers and stuff like that that,&nbsp; we can still abuse for executing this technique.&nbsp;&nbsp; So key takeaways. You always have the public&nbsp; POC available here. This will be announced&nbsp;&nbsp; with additional POCs after this talk in&nbsp; the coming days. This POC, of course, is,&nbsp;&nbsp; both of the techniques usually have been developed&nbsp; and designed to obfuscate the stack at runtime. So&nbsp;&nbsp; if you are planning to obfuscate the stack when&nbsp; you're going on sleep, probably I'd rather use&nbsp;&nbsp; the stack cloning or the stack hiding. And&nbsp; the reason is, well, as you see, there are&nbsp;&nbsp; certain logical inconsistency in this stack. So if you leave it there for a long period&nbsp;&nbsp; of time, you are increasing the chance of&nbsp; being detected by Eclipse, et cetera. The&nbsp;&nbsp; is just go there and see it&nbsp; and do a bit more analysis, manual analysis on&nbsp;&nbsp; the thread call stack. Of course, another point&nbsp; is that this technique can be used wherever you&nbsp;&nbsp; use return address spoofing as an arrangement.&nbsp; You can use sleep encryption, and release the&nbsp;&nbsp; POC not long ago about how you can integrate sleep&nbsp; encryption technique to this technique, to this&nbsp;&nbsp; stack spoofing technique, but it has a layer of&nbsp; complexity which is not always easy to manage. So&nbsp;&nbsp; a word of caution if you are trying to integrate&nbsp; this in your own implant. And yeah, I think pretty&nbsp;&nbsp; much it's everything. Thanks a lot for joining&nbsp; us and if you have any questions, thank you.&nbsp;&nbsp; If you want we are at the start, what? Sorry? Yeah, so if there's time we're happy to take&nbsp;&nbsp; questions, but I think he's also saying if you&nbsp; want to meet up with us after the talk, we'll be&nbsp;&nbsp; happy to talk to anyone who has any questions. Exactly. &nbsp; Yeah. Thanks, man. &nbsp; Thanks, guys. Thanks a lot.
