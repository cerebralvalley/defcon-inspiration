I have a question what's the question is this your first time speaking at Defcon it is oh [Music] [Applause] excellent so actually I wanted to say that we were we were experimenting with something new this year at Defcon could you set us up please we were looking for a uh a drone delivery system what is what is this what do you think will that deliver shots all right that's cool you all know how this works you've been here long enough okay wait slow down there man whoa well all [Applause] right cheers thank you cheers and now back to our regular scheduled [Music] talk so now I feel a bit warmer okay so I was saying uh let's see how the usual exploitation process takes place so if you're an exploiter you're trying to exploit something usually first of all you have to find a vulnerability and this vulnerability has to be useful and by useful means I mean that you have to be able to basically divert the control flow of the program to some address you you want and once you you you you're able to do this then you can perform your Des desired actions like I don't know invoking the system the system Library function and exact V or launching program copying wallets I mean doing the actual uh your actual final a and our this talk our focus is in this on this last part so we are assuming we are able to divert the contr flow and then we'll see what can we do afterward and in particular we'll see uh how can we do this last part of the exploitation process in presence of certain counter measures that I'll present because just being able to divert execution it's not enough because okay I can you know I have my I have control of the program counter but the question now is where I point it to the where it's the important thing and since it's 2015 we cannot you know upload a shell code in our Target and just jump there because op operating system I'm preventing this kind of stuff so as you all know um hackers and you know exploiters came up with the the concept of code reuse Tex so we cannot uh upload or you know inject new code and then what we do we reuse existing code so we we came up with the famous you know returning to libc at Tech and then all the return programming return oriented programming which I hope you are bit at least a bit familiar with the problem is that okay we are able still to perform our teack even in if we're not able to inject in a new Shell Code we're using Code but the problem is that um you know the operating system developers came up with a counter measure which is called aslr which basically means that it's other space layout randomization which means that uh basically the code that you want to reuse for instance the uh system Library functions and those kind of functions are not always in the same position their position is not deterministic so the question again is okay I can diver the control flow the code I want to reuse is there but where it is so the typical situation to uh get around aslr is um is use the functions that are already imported because so the main executable uh uses some let's suppose that the main executable uses some functions for for instance the C standard Library like the print F function which is very common to to be able to use this function it keeps in memory in the memory area which is the dedicated to the main binary a reference to this function which which basically holds the address of the of the print F function and so the typical uh way I was as I was saying to bypass aslr is okay let's try to read the address of this print F function which is actually imported by the main binary then if we have a copy of the C Library if you're targeting the C standard Library let's compute the distance between the print F function and another function we want to execute to do our to perform our malicious operations typically system or exact V we compute this distance and then if we are able to get from our Target the address of print F we can add the distance to you know to reach exact V and then we know the address of exact V and then we can you know uh code exactly and perform our our Tech this works but the problem is that first of all it requires you don't just have to be able to divert the control flow but you also have to be able to leak this piece of information the address of print F so you you need at least two vulnerabilities you need a a memory leak vulnerability typically and also you need the uh knowledge about the the layout of the library you're targeting so you basically need the exact copy not just the version you need like the exact copy so the exact build of the library which is not always the which is not always the case I mean there are some situation where maybe you don't have access to it and finally there's another point which is uh you know bit subtle is that you need to interact with the attacker okay so it's not a single stage attack it's not that you just launch your attack and it works you do your exploitation you need to communicate back to the attacker so basically you read the print F and then on the attacker side you compute the new address and you send another stage of the exploit so it's two stage and this is a problem because maybe if your final Target is um hair Gap machine for instance you can't really communicate maybe you have a JPG uh you are storing your exploiting a JPG or a pcap file or something which is you know open on an air gap machine you can really communicate so this is a problem so how can we solve this problem let's try to zoom out a bit actually our idea in um when we came up with this with these techniques is let's try okay let's try to zoom out a bit what are we trying to do basically what we're trying to do is to obtain the address of an arbitrary Library function so I have a name and I want its address so I'm able to call it but if you think well there is already a part of the operating system doing this job and it's the dynamic loader the dynamic loader basically the the role of the dynamic loader is to uh take the main binary or even a library and see what are the important functions for instance print F and from the name from the string print F obtain its address where where the print function actually resides in memory considering aslr and everything so let's try to get to know this guy the dynamic loader first of all uh I'm going to talk about Dynamic loading and everything in the in the context of Elf based platform so elf uh for those who do not that know that stands for executable and linking format and it's basically the most popular format for Unix based platforms so link or BSD uh like FreeBSD netbsd open BSD they all use elf which is a binary format as I said and so if we consider an elf file like an elf executable the first way we can see it we can split it in sections so it's a file and it has several parts with are which are called sections the most important sections that you usually deal with maybe if you've been doing a bit of uh reverse engineering are usually the do teex all the sections start with a DOT so the do text section which holds the executable code so the actual code binary code the the of the application then we have dot data which holds all the rable global data so let's say the global variables that are rable they're not just readon the read only ones stay in Arrow data which stands for read only data and then we have BSS which is a section which holds Global variable which are not initialized so for instance if you like in C you write a Global array that it's not initialized it will be zero initialized and will be will end up in BSS okay so uh what let's see what's the role of the dynamic loers basically the compiler hides a bit of the complexity of the dynamic loer because when we see this piece of code like a very simple um L Word program we see just print F but what actually the compiler meets it's not a to the print F in the C standard Library because the as I said the the the libraries are positioning in random position so you cannot know at compile time where the print F will end up what actually the compiler does is calling the print F at PLT which is a a trampoline uh so let's say a small piece of assembly code then we're going to see we're going to explore how it works this this small trampoline is store in another section which is called PLT the so-called Pro procedure linkage table which is a table composed by this small trampoline trampoline one for each imported function and basically uh let's see how it looks like uh so these trampoline are used to uh support lazy loading lazy loading is a feature of the several Dynamic loaders basically most of them which allows you when you start the program instead of resolving all the important function getting the real address at the startup of the program you basically obtain their address the first time you call them so if there's a function you never call it's not going to be resolved and you're saving time okay and also the start up of the program it's it's faster for the end user so it works a bit like this this is p code it's not actually it's an assembly optimized code what's the trampoline it's not doesn't look like this but let this is the high level stuff to to make you understand so if it's the first call that first time that the trampoline is it's being called the the main binder will let will pass the control flow to the dynamic loader calling this DL runtime resolve function which we'll explore in a bit otherwise it will just jump to the on the cached version of these this function for instance if this is the trampoline of print F there will be somewhere in memory we'll see where uh the the a place where this the address of print f is cached and the runtime resolve will take care of finding where print f is storing the address of the print F in at that cached print F address and then also call the the function so the L runtime resolve takes two parameters the first parameter is just a pointer to a data structure with describe which describe the current elf object we'll see more about that later and the one 123 parameter it's a uh the an index in a table it's an index representing a relocation so what is a relocation a relocation is basically a a directive for the dynamic loader telling him so take this symbol with a symbol it's a concept that represents like a function for instance in in a library or maybe a global variable in a in an external Library so a symbol take the symbol for instance print F in this case and write its address at this specific address in particular a offset represents where where the address of the symbol has to be written and R info represents the it's basically the identifier of the the symbol so reloc index which is the second parameter of DL runtime resolve is an index in the in a table of these relocation basically there is a section called R PLT section which uh is basically an array of relocation data structure which is where which are called L which are composed of exact by a offset and info so a info is I said it's an identifier but it's actually an index in another table which is called dine Sim which is an array of um of symbols structures which are actually lsim which have a lot of details we're not interested in what the only thing we're really interested in is that there's a field called St name which is an offset in another table and this is the last one I promise uh which is called dining which is basically just the concatenation of of or the name of all the symbols that are imported from a certain from a certain binary so to recap basically D runtime resolve takes reloc index which is an index in the real real PLT table which is a relocation from the relocation we we pass to the symbol table and from the symbol table we pass to the name which is in this case uh print F okay we we'll get back on these things so to recap again the time resol does three things finds the symbol and its associator location and Finds Its address so if you from the relocation go to the symbol and you go to the name and from the name the dynamic loader is able to get its address it write writes that address at the air offset and the address specified in a offset in the location and then it also transfer the execution to that function so it also calls it uh as you might be I might have understand at this point a offset in this case will point to the address of cach print F address so a offet tells okay right there the the address of this function so next time uh I'll call the trampoline I won have to resolve and get do all the trickery to get the uh to resolve the symbol again and just jump straight there it's sort of an optimization okay so uh okay where where where does this cached print F address actually stay is it's actually in another section which is called God PLT which is the it's more mostly known as the just the global officer table which basically holds an entry which is a pointer for each imported function so it's where these cached addresses actually are so just to recap we have PLT which contains all the trampoline which enable lazy loading we have got PLT where the cached address of the imported functions are stored where basically initially they are all uninitialized and they get initialized lazily from the first time you you call that function we have the table of relocation which is of relocations which is real PLT table of symbols which is D Sim we have the T and then we have the table of the symbol names which is D string just non-terminated strings conc needed okay so let's try to come up to with an attack to this to this system so first some assumptions our assumption as I said is that we are already able to divert the control flow and we are also able to run a a Rob chain and we also are able to write arbitrary memory location with this Rob chain so let's say we have a small very small Gadget which is able to say okay I have this address I have this value please write the value at that address so very simple stuff what can we do so if we look at this setting the idea is okay what if I'm able to replace the print F string for instance with exact V if I'm able to do so to replace that string and I invoke the DL run time resol function with the relocation index corresponding to print F what the loader will do it will go through relocations symbols but then in the end it will end in the dynamic string in the dynamic string table and it won't find print F but it will find exact V and so it will resolve the address of exact V and also invoke that function so we're basically in this way we will be able to uh invoke any function we want just by name if we are able to write this memory arbit this arbitrary memory location uh this sles is called naive approach because this approach actually does not work because there's no reason why the dynamic string table should be writable so the the dynamic string table is not writeable because it's like static information that you just write once and you never change so this attack will not will not work even if you have this gadget we'll try to write in a non writable memory location and we just get a sefall so let's try to work around this uh so we talk we've been talking about a lot of different sections but the dynamic loer actually does not go through does not consider sections by their name they it doesn't look up the I don't know PLT go Section by name it uses another section which is called the dynamic section which holds um basically key value pairs where the the key represent one one of the sections it needs and the value it is actual address for instance DT Sim tab keeps a pointer to the dynamic symbol table the tstr tab a pointer to Dynamic string table and and so on and so forth we're not really interested in details here but the point is that the dynamic loer uses this takes the information about the binary about the sections from this table so the nice thing is that dot Dynamic section is varable so what we can do it's instead of trying to write directly in the dynamic string table we can trick the loader into thinking that the dynamic string table it's actually somewhere else and where can we where can we make it point for instance we can we can build a fake string table in the BSS or in any other memory and any other section which is writable by us BSS it's typically vitable actually it's always writable so basically we go to the dynamic section and we change the D Val you a field of the structure making it Point instead of making it point to Dynamic string we make it point to BSS so when then what the dynamic loader will do is okay I'll go through relocations symbols and then when I'll get to the dynamic string table instead of going to Dy to do D string I go to BSS where I as the attacker I Forge a fake string table which is instead of print f as exact V and so basically we are able to perform from the attack in the sense of being able to AR call any function call any Library function sorry okay but this approach is still quite naive because actually we we we were not the first people thinking about this and so the developers of compilers and in particular of the linkers develop a protection called relocation real only more more shortly like railro which is a binary hardening technique which prevents exactly the attack we just described because why why is the dynamic section rable it's simply because there are some entries that we we did not describ yeah which have to be initialized at run time so it has to be rable when you start your binary but after you initialize them you can mark that section as read only and that's exactly what the railroad protection tells the dynamic cloer to do and so basically our preview attack doesn't work anymore but maybe we can do something else so uh we came up with another idea so so far we we played with the the the do Dynamics section but what can we do if we are able like to play a bit with the reloc index the second parameter of the the L run time resolve what happens if instead of making it point to an entry in the real PLT section in a making it point to an existing relocation we put an index which is big enough to go after the end of ra ra PLT section maybe we can trick the loader into going somewhere more interesting so let's take a look um in particular can we can we make the loader uh think that he has to resolve a relocation which is a in a memory area which is bright by us so not in every real PLT section but maybe in BSS so let's let's see the what section do we have after well PLT so let's suppose that we have we have some we are usually the indexes start from the the the beginning of the real PLT section and if we put an index big enough we can go through real PLT PLT do text Dynamic PLT and then we can end up in data and then in BSS and so if you are able to trick the loader in going there and we are able to forge a fake relocation there we can basically uh basically build our own relocation and resolve any Library function without H touching the dynamic section which is not right one so this is a an example of the thing so we put an reloc index which is big enough to not to go to real PLT but to get into BSS where we Forge a fake relocation which points to a fake uh symbol with the same trick so it's the same trick so if we start from D Sim we put an index which is big enough to end up in BSS again and then the same trick again with the St name we put an offset in the dynamic string table big enough to end up in BSS again so basically we are building all the data data structure which are needed by the dynamic loader to resolve an Library function in the BSS and using always the same trick of overflowing the uh like the the the section which are supposed to to be used we end up in BSS and we are able again to uh to call any arbit an arbitrary Library function bypassing the the railroad protection so here there are just some very simple formula how to compute the addresses that we have to to put there to for instance reloc index and the air info and then St fields to be able to perform this attack but this this is not that simple this is this will be quite too good to be true because actually re in the binaries round in particular like in yuntu and several other distribution have a feature which is called symbol versioning which basically allow you to depend not on any print F function but on a specific version of print f for instance I want print F version from a print F function from the I don't know GBC 2.22 okay this is just an elf Fe feature which is actually a new extension but we don't really care but since is popular we have to deal with it and what's the problem the problem is that if simpol versioning is enabled the info field it's not used just as an index in the symbol table but it's used also as an index in another table which is called the new version table which we will not talk about but the fact is that this symbol this index is used for two different things and so basically we have additional constraints so what we have to do in this case is either we make uh both both indexes go to the BSS section and so we can fake also the other data structures which are related to versioning which we not we not look into or we can make it point we can make the a the version thing point to a z 0 somewhere in a memory area where we cannot write like in if if we can find like a z0 in text it's fine so we just Bic because this just basically disable the versioning so is it doable yes and it's also it can also be automated the point is that there are certain situation with when this is really really hard for instance in particular with 64bit binaries using huge Pages which huge Pages it means that you have like memory Pages which can be large up to a megabyte and so basically the uh the the read only part of the binary where we have text relocation the relocation table the dynamic symbol table the dynamic string table and all these things which is the read only part it's very far away it's like one megabyte away from the writeable page okay and so this huge distance makes it really hard to satisfy the constraint we just saw and so in this case it's not doable the problem is that 64-bit binaries with huge pages and symbol versioning enabl are pretty popular so we found another solution for this the idea is in this case is not to to play around with reloc index but with current object info so let's look into current object info what do we have there uh it's basically a pointer to a link map structure and we have a this poin it's always stored in a reserved entry in the go table which is the second one go1 is the second entry of the go table and the nice thing about this data structure is that it has a field called Lino which it basically keeps a cache of pointers to the uh entries in the dot dynamic in the dot Dynamic section so the idea is if we can tamper with this internal data structure of the loader we can basically go back to the first attack when we were tampering with directly with the dot Dynamic section and this is this is actually what happens so we go to the PLT got section we go to the second entry which keeps a pointer to this link map structure we go to the L info field and to the in in the entry corresponding to DT Str strr tab which is a pointer which is the pointer to the dynamic entry keeping itself a pointer to the dynamic string table we can change this value and make it point to BSS and we can build a fake dynamic entry which tricks again the dynamic loader into thinking that the dynamic string table is a memory area that we control and so we're basically falling back to the to the first attack we saw so we are still tampering with the dot Dynamic table but indirectly like corrupting the internal data structure of the dynamic loader and we can do this reliaable and deterministically because the pointer to this data structure it's always in the go1 uh in in the go in the first second entry of the go okay then but still there there's an additional protection to this because railro kind it was it comes in two flavors partial railro which is the one we just saw but then there's full rro full rro basically it's um it has all the feature of partial rro but in addition it uh basically completely disables lazy loading this means that when you start a binary when you start your program all the uh go entries are initialized and the the the go it's Mark is G only so we cannot write in the go anymore and also since they are not used any longer the go1 um entry which was keeping the pointer to this link map structure is not initialized so we are we lost the pointer to this uh critical data structure which helped us to bypass partial rro and not not only this we also lose the pointer to DL run time resol which is something I skip over but we we were getting its address from the go2 address and the the go2 entry and also this this this entry is not initialized because simply lazy long is disabled so these three things we don't have a pointer to link map we don't have a pointer to De runtime resolve and we cannot brighten the go it seems like we're pretty far but the t debugs to the rescue so there is this feature which is like a the typical you know debug feature which is staying there and waiting for someone to abuse them and and here we are so DT thebug it's an an entry of the dot Dynamic table which is used by GDB and other debuggers to be able to break on on the on certain events related events related to Dynamic loading like uh okay we have a new library being loaded you want to break so so you can you know uh explore this library and see what new symbols you have so debugging stuff that we don't really care about but the nice thing is that this dynamic entry doesn't point to a section but points to another data structure which is rdb structure but this a debug structure keeps a pointer to link map so we basically are able just passing going through this uh debuging feature to get back to the previous attack but it's not that simple because as we said we we also so this is the last drawing I'm showing you I promise but this is really interesting because as we said we don't have a pointer we have we have found a pointer to link map going through the the the DTD buug dynamic entry but still we have to be able to find a pointer todl runtime resolve so let's do let's see how can we make this so let's start from this area of the drawing so in the dot Dynamic section we go to DTD buug we follow it value and we get to the air debug from Air debug we have this field which is air map which points to the link map structure that we saw before so first thing we do we corrupt the dtst strr tab entry which is the one keeping a pointer to the dynamic entry uh for the dynamic string okay and so we do the the thing we just did before we we we we build a fake dynamic entry and we build a fake string table and this is let's say usual for now but the point is that we what what will the dynamic loader do okay it will go through all this stuff that we craft and and it will resolve exact V exact V nice but then one of the feature of DL run time resolve it's also storing the address of exact V in the go but the go has been completely initialized and marked read only by the dynamic loader because of a full arrow and so we'll get a SE fold because we are trying to write in a memory area which is read only so we have to um to also to fake another entry of of the L info of the link map data structure which is JMP rail which is a pointer which was the the one holding a pointer to relocation table so we fake also the relocation table we create a fake relocation which points to an existing symbol but as air offset instead of pointing to the go it's pointing to like in this case a memory memory area just after it which is rable so we don't get the SE fold and so we solved also this problem now we have one last problem which is the address of DL runtime resolve so we don't have it in go to and so we have to get it somehow but how can we get it the trick here is that usually actually in our survey basically always only the main B is protected with full rro the libraries are not protected with full rro so if we are able to get to the Goot of libraries we can get we can still get the DL the address of d runtime resolve and the nice thing is that the link map structure it's not just a a data structure per se it's actually part of a a linked list of all the link map data structures representing all the loaded elf objects so basically what we can do is just we can D reference like the pointer to the next uh to the next entry in the dynamic in the in the link at list and get to the link map data structure of just any library for instance lib or whatever library is being loaded we go to the uh we get the address of the PLT go go and then we go to the third entry and we get again the address of DL runtime resolve and basically we are able to building building up all these thing together we're able to bypass also full railro which is pretty cool thing okay so but okay this is very all very interesting but how can we do that so we implemented leakless which is the them the the name of our tool which implements all these techniques you just give in the binary and it tells you which is the best uh actually the only the the approach that is most suitable for this particular binary based on the protection it has enabled and basically if it's 64 bit with huge pages and those kind of things and it can produce two different kind of of outputs it can either tell you okay do these things it outputs like a Json uh a Json file which basically instructs you okay if you are writing an exploit write this thing there write this thing there and then you'll be able and then just call the a runtime resolve and everything will work or if you provide leakless with the uh gadgets it needs and we'll see what kind of gadgets it needs it will also it can also produce the r chain for you directly so it actually produces the exploit to resolve one or more functions one after the other and call them with the appropriate parameter you can uh you can see the code there it's GitHub we just published it and just you can play around so the gadgets we talked about gadgets so basically we need at most four different gadgets depending on what of the four attacks I presented excluding the first one which was not working was just first naive approach depending on which attack we are dealing with we have different gadgets that we need so the first one the one which was basically uh just overwriting the dot Dynamic section that just needs a memory write Gadget so you give him it's you need a gadget you give him an address a value to write there and it writes there and so basically all the exploits all the techniques that we saw need this kind of gadgets so n stands for no rro P stands for partial rro H stands for when you have huge pages and all kind of stuff and f for full rro in the other case we have slightly more sophisticated gadgets for instance this one is if you look at it it's uh it's the one we use to tamper with data structure so you have a pointer which is usually a pointer to a data structure you add an Offset you do reference it and you write a value there and this is used uh for the for the huge Pages attack and for the full rro full rro has two addition requires two additional gadgets uh this one is basically a gadget that we need to store somewhere uh some data that we took from those like while while we were walking through all those data structures for instance the runtime resolve basically you choose a memory area where you want to save this address and you take it from this data structure at this offset and in this case so I I I initially skipped over this but for the first three attacks uh to call the L runtime result we were using a part of the PLT which if you remember it's where the trampoline were but in this case we cannot do that because the DL run time resolve it's not initial IED and so we also need to be able to write the address of the runtime resolve directly on the stack so we need an um a gadget which allows us to write at an offset on the stack so we need these four gadgets and this is also the reason why I'm here you know knowing you with four different techniques instead of just showing the last one which is the most powerful one because depending on the protection that are enabled you have different requirements and you need different gadgets which might be easier or harder maybe just take more time to to find and Implement so okay but after all these these things what what loaders are actually vulnerable to this so in our we we tested our leakless against the gipc so the new standard library and against the FreeBSD standard library but by manual inspection we think we deem vulnerable also diet lipy UC libc and new lib which is you know embedded uh there are C standard libraries for embedded systems and also open BSD and net SD because basically they all they all behave like the same if you think about it we're just relying on the standard elf features and except for minor modifications maybe you you they all for instance they all cach pointers to the dynamic entries somehow in their internal data structures they're maybe a bit different but there's not not a real big deal we we say that it's not vulnerable bionic which is the C standard library for Android because as far as I know it does support only pi binaries and musle which is an embedded um C standard library because simply it does not have La loading so it's like if you have full Arrow all over the place always so basically our Tech is worthless it's a feature not having a feature it's a feature actually and FreeBSD loader I'd say it's kind of not vulnerable because the the the the first the the first and the second the second attack I presented you um basically the one which was overflowing in the relocation table and symbol table and those kind of things does not work because these are the only guys that are actually checking the boundaries which is like basic two things to do but they are the only ones but still the the other two attacks they do work so it's kind of vulnerable and not vulnerable okay so let's recap what are the advantages of leakless so first of all it's single stage so you don't need a memory leak vulnerability and you don't require interaction with the victim we we are not sending back to the attacker anything we just do a single way single shot exploit which might work in in any situation and like air gap machine and those kind of things so like offline attacks which maybe before were not feasible are now feasible it's reliable and portable because it's it's deterministic there's nothing you know left to the left to Randomness or it's very deterministic and then you do not need a copy of the target Library so if you think about that in the in the beginning I told you that you have to compute in the typical situation when you have use a memory leak you have to compute the distance between exact V and print F but to do this you need a copy of the library which is not always the case in this case we do not care about the layout of the library we just uh we just abuse the dynamic loer which does it in the proper way and since it relies on just underl feature in most cases it's very portable and it's easy to port for instance to netbsd open BSD and so forth except it's l map layout of the the data structure but as I told you are really minor fixes to the exploit it's sure because if you if you want something with the same characteristic I just mentioned like reliability and deterministic and being deterministic you will need to basically reimplement what the dynamic loader is doing so Implement like in pu going through all the relocation symbols and those kind of things which is very complex and it's also it it's it gets very long because you it's not just say Okay dynamic en cler do this for me you have to do it by yourself and also the advantage of leakless is that once you set up all the data structures if you want to make multiple calls to different Library functions you just need to after you did the first one you just need to change the name of the function you want to call and just call again DL runtime resolve instead if you want to implement the dynamic loer you have to do again all the process and so it's it's a c that is not negligible and you know shorter shorter R chains mean higher higher feasibility you know sometimes buffer are small and you have limits and if you don't want to interact again you cannot read you know it's if you want to do it single stage being short is important uh we we also say that it's stealthy because basically we are not the typical criticism one could make to such an attack is that okay but why don't you just do this with CSS okay because first of all CIS might not be available you might not have them in the binary like even in mign but it's also more invasive because for instance you can always do if you can do CIS you can basically do everything you want but it might you might have intrusion detection system and also spoing a spoing a new process for instance if you want to do exactly it's kind of an invasive and here is an example of a thing we came up with it's a it's a c code for using uh P functions p is I am me I am client and so what we're doing here it's we're taking the uh the uh the pointer to the to the proxy object of pigon and we are setting so the attack we want to carry on is like okay set a proxy uh for so I can intercept the instant messaging traffic for this user so what we do is we take the global object for proxy we send a proxy which is our server the port the type and then we connect and disconnect the user sure you can do this also with Cisco somehow but reusing a program functionality it's much easier and that's less intrusive and it's also easier and also it's automated by and by this mean that our tool basically does almost everything except finding the gadgets but maybe in the future we can integrate it with some automated Gadget finding tool so counter measures well the most obvious counter measure is is enabling P so uh randomizing go for the position of the main binary okay this this is a is an actual solution but actually in our survey there are like very very it's Pi is not being deployed a lot it's they distribution basically use it on being binaries they deem critical I don't know why they don't they deploy it on all the binaries it doesn't make sense to me but this is a possible solution also DTD buug why should it be there just remove it or maybe uh use it enable it only if there is like an exported um V variable if you want like an environment variable if you want to do debug then you can you can enable it or you can make try to make the dynamic loader data structure read only so if you try to tumer with them you get a crash or just validate the input is also very good counter measure but the the key point is that this kind of core core components of the operating system should be reto with security in mind because basically the all the the libraries we inspected were just thrusting user input and since they are all over the place you are it's very it's very dangerous because if you if you're able to attack them you are able to obain something that is very portable and it's all over the place so also elf maybe should be redesigned a bit with security in mind from the beginning and the last thing I want to thank all the other people who work me with me in this like Amat which is here and Yan which is not here and Joan and Christopher at UCSB which allow me to be here and that's basically all maybe I have second to do a very short so just to show you some code so this is uh I'm just going we triy uh to implement to attack a real binary because you know we have a small TW test Suite in less but we also want to do something real so we took a CV in in wire shark just a very basic stack uh overflow I mean stack buffer overflow and we tried to generate an exploit with uh with leakless just providing with the gadgets oh just small demo we launch it this is just an error because I'm running wi shark as route don't do that it's a very bad idea we launch it passing passing a pickup which I generated and I got a shell it's it's pretty easy yeah K always good yeah and then we have the same with partial re but you know it's the same experience for you so just rust me so last thing this is the the part lless has been implemented in Python and this is the code to that you need to write if you want to provide gadgets to them to do to leakless and it's 200 lines of code but most of them it's just boiler plate that could be you know factorized out and it's basically just take the gadgets and mix them together so they can be reused by the let's say the part of lless which is agnostic from the binary and last thing is yeah I just wanted to show you the the Json output let me see so this this exploit Pi it's our it's basically the leakless script we pass it the W shark binary and we ask him to produce the exploit in Json format using the LD corrupt method which is uh the second one I presented and what it does it basically gives us some instructions like okay write these things at this address and so if you don't want the the RO chain to be generated automatically for it because maybe you have some complicated setting it can just give you the instruction to perform the Y and with this I'm done and thank you for your [Music] attention
