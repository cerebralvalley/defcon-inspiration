- DEF CON 30. Who's having a good time? - Yeah. (attendees clapping hands) - Whoa. You guys. Awesome. Awesome. That's great. So, this is Rex and Junyuan. So, let's give a big DEF CON 30 welcome. Come one. (attendees clapping hands) - Okay. Thank you, everyone, for coming to our talk about a bypassing system called Tracing. My name is Rex Guo. And this is my co-speaker, Junyuan Zeng. So, we have a lot to cover in this talk. So, I will just jump over this slide, you can look up us online. Okay, so imagine a sophisticated attacker compromised your Linux production environment. He launches a Log4Shell exploit and then a file reverse shell back to his machine. Then he discovered that machine is running a vulnerable version of sudo, so he elevated privilege on the box. And then he read the /etc/shadow file to see if there's any interesting hashes or cracks. He also discovered that he can do ssh hijacking to move into a second machine by reading into the process environment variable. So, he moved to the second machine. And as he is celebrating, he discovered that his access was gone. So, he fired the RC again, there's no luck. Quickly he discovered his access was completed blocked. Now, let's take a look at the other side of the story. While everything is happening, your security engineer received a sequence of alert from the cutting edge security monitoring software. And this software is able to monitor the system calls, and also process information for the application. So, for example, when the attacker executed a reverse shell, there will be a connect system call, and maybe others, depending on the reverse shell technique. When the attacker read the /etc/shadow file, there will be an open at system call. Essentially any non-trivial action that the application perform will need to involve a system call. So, how do you use the system call information to detect threats? So, here we show a really, really simple example. Let me explain what this is. Here's a rule that try to detect untrusted program reading the /etc/shadow file. And let me explain the rule. It's saying, "If the system call is open or open at, and has a read permission, and the file name points to /etc/shadow file, and the program is not in the allowlist of programs, that will fire an alert. Now, you can probably see that you can build much more complex rules, and even machine learning models, on top of this dataset with one system call or even multiple system calls. But they all rely on the fact that the system call monitoring software is able to extract the data correctly. So, in this talk, we are going to dive very deep into these system call tracing technologies. And then we'll talk about the vulnerabilities that we discover that allow us to bypass the tracing. And then we'll conclude with mitigations and takeaways. With that, I will hand over to Junyuan. - Thank you, Rex. So, as Rex mentioned, syscall tracing is very important to detect any threats. So, this diagram can give you an overview of syscall tracing. Basically, the syscall tracing include two parts. One is the hooks for syscall interception, and also the tracing programs. So, when the application issues a system call into the kernel, the system call code path is executed. If there are any hooks placed inside the code path, the attached tracing program will be triggered to collect system call data and send those information to the monitor agent to detect threats. So, the tracing program can be implemented in the kernel space, as shown in the left diagram, or implemented as a user space program as part of the monitor agent, which is show on the right side. So, the program used to collect syscall data, like syscall arguments, is called a tracing program. This program can be attached to different hooks, like tracepoint, kprobe, ptrace. So, we can directly leverage the Linux native mechanisms as tracing programs, or implement your own tracing programs, as kernel modules, eBPF probes or user space programs. So, the first kind of hooks for a syscall interception is called tracepoint, basically it's the kernel static hook for a syscall interception. The Linux kernel provides sys_enter and sys_exit tracepoint. So, if we attach the tracing program to the tracepoint, the function call of trace sys_enter and trace sys_exit will use the same parameters to trigger the tracing program. The first parameter is called regs, that's saving the syscall arguments. And the second is called ID, which saves the syscall number. Tracepoint provides a low overhead, but it only provides static system call interceptions. You can also use the dynamic approach, like kprobe, to intercept system calls. So, using kprobe, you can register the tracing program on almost any instructions in the kernel code path. Like, basically, it's a syscall code path. So, when the instruction's executed, the tracing program will be caught. Compared to tracepoint, kprobe is a dynamic approach, but you need to know exactly how data is placed on the stack or register in order to get useful information. So ptrace provides a user space solution for syscall interception. Similar to the tracepoint, it's a static hook in the syscall enter and exit. Using ptrace, you don't need to implement any kernel programs as a tracing program, but only user space program needed. Compared to the previous tool's approach, ptrace's performance overhead is high. But for optimization, you can combine it with the seccomp as syscall filtering for better performance. There other approaches for syscall interception, like LD_PRELOAD. This approach actually is easy to bypass if we use a seccomp call to trigger the system call. So, many of you have probably heard about the cloud workload protection products. This product usually provides advanced threat detection based on syscall tracing. There are different kinds of cloud workloads, such as virtual machines, containers on customer-managed VMs, serverless containers, and others. So, for serverless containers, it's usually allocated and maintained by the cloud providers on demand. So, they usually have no access to the host. This table summarizes how different syscall tracing techniques can be applied for different cloud workloads. For virtual machine, we can use any kinds of hooks and tracing programs. Tools we can leverage include Falco eBPF/kernel Module and Falco pdig We'll talk about this tools later in our talk. For containers that are on customer-managed VM, we can have the same options as virtual machines as long as we get enough capabilities. For serverless containers, as we mentioned, it has no access to the host. So, we can only use ptrace as hooking points and implement a tracing program in user space. Instead of Falco kernel Module and eBPF programs, we can only use the Falco pdig. Falco use similar techniques to a trace system call. It's an open source project in CNCF. It's very popular. And in kernel space, it supports kernel Module and eBPF probe using tracepoint. In user space, pdig is developed based on ptrace. So, Falco pdig is dedicated for system call tracing of service workloads. So, we did not evaluate other security monitor agents, but we believe the popularity of Falco represent an implementation that is widely accepted by the community. Unfortunately, this kind of implementation for syscall tracing is vulnerable to a TOCTOU issue. That is, Time_Of_Check, Time_Of_Use. Let's take the connect system call, for example. The second argument for connect system call is called user V address. That is a user pointer pointing to the user space buffer called socket address. So, during Time_Of_Check, the tracing program dereferences this user space pointer to get the socket address. And during Time_Of_Use, the kernel also dereferences the same user space pointer to also get the socket address. But between Time_Of_Check and Time_Of_Use, the user memory pointed by the user V address is vulnerable to be changed by the attacker from the user space. So, in this case, the socket address can be different causing a TOCTOU issue. So, let's dive into the connect system call, which can help you understand this TOCTOU issue. So, when the application issued a connect system call into the kernel, the system call handler will check if any tracing programs are attached to the static hook at the system call enter, like ptrace, seccomp, sys_enter and tracepoint. If this is true, And after that, the handler will look at the syscall table and jump to connect system call to create a connection on the socket. Before returning to the user space, the handler will again check if any tracing program attached to the static hook at syscall exit, like ptrace sys-exit tracepoint. Similarly, if this is true, So, as I mentioned earlier, the second argument for connect system call is user pointer pointing to the user space, or socket address in the user space. This pointer is propagated into the connect system code path and assigned to different kernel variables, which is highlighted in red. The kernel calls move_address_to_kernel function to make a copy of socket address from user space to the kernel buffer call address, that is highlighted in green. After that, the kernel will call the internal function, sys_connect_file function, to create the connection on the socket based on the kernel buffer address. And this is the Time_Of_use by the Linux kernel for connect system call arguments. So, before the memory copy function, the kernel buffer is not created. So, the user pointer is the only place we can get reference and also get the socket address In this case, during Time_Of_Check, to the static hook at system call enter, or to any places before the memory copy function using a dynamic approach like kprobe, the tracing programs have no other option, but need to dereference the user pointer After the memory copy function, the kernel buffer is created with copies of the socket address from user the space. However, in this case, this TOCTOU issue may still exist. So, think about if we attach a tracing program to the static hook system called exit, like sys_exit tracepoint or ptrace. The tracing program may still dereference the user pointer to get the socket address. Falco pdig use ptrace for syscall enter and exit, but it only use the seccomp syscall filtering as system call enter, since the seccomp is not available for syscall exit. For Falco version 0.31.1, the kernel module and eBPF implementation only use the sys_exit tracepoint. So, hopefully you get an idea about the TOCTOU issue for syscall tracing. Next, I'll hand over to Rex, to talk about the vulnerability. (Rex clears throat) - Okay, so the example that we see is on kernel version 5.7, and on connect system call. But really, the observation is true across all the kernel versions. Because we confirmed with the kernel developers these TOCTOU windows exist since the day that tracepoint and ptrace were introduced, because they were originally designed for performance and debugging purposes. So, in order to do secure tracing, what they recommend is the software needs to monitor the kernel memory instead. So, report this issue to Falco. Basically, the issue is they used sys_exit tracepoint, and they also used sys_exit ptrace. And there's a, you know, version older than 0.31.1, it's impacted. And if you happen to use the commercial version, you may want to check what version is impacted. So, we reported this issue in December, and the issues is mitigated in March. The mitigation that was deployed is for the Linux security module or the eBPF version. They check sys_enter and sys_exit system call arguments for a selected of system calls. And they also do the same for Falco pdig. In terms of how many system calls are impacted. So, we analyzed the open source rules in the Falco repo, and majority of them are impacted, with two exceptions. One is the execve system call. The reason for that is in their implementation, when they trace the execve, they actually read the kernel memory instead. The other one is the send to and send message We haven't found a reliable way to really block the system call. We'll talk about what blocking means in the next few slides. But keep in mind that it's very heavy to monitor send to and send message, and this typically limits its usage. Okay, so hopefully everybody understands the vulnerability at this point. It's fairly, relatively simple. But let's talk about how to exploit it. Now, in this... To exploit this tracing, we don't want to acquire any additional privilege or capabilities, right. Basically, the attacker should be able to evade detection, any user, any privilege or capabilities. We want to have some level of control on the time to inject the delay. And we'll talk more about what I mean by that later. We also need to inject enough delay, such that when we override the memory, the machine has enough time to propagate the overwritten data to the whole machine. The last thing is, we want this exploit to be a 100% reliable, because if the attacker being detected once, then the entire operation may be at risk. So, this leads us to two exploitation strategies. We'll just quickly briefly overview the exploit strategy number one, which is what we did in DEF CON last year. And from there, you will see how we reached exploit strategy number two. So, last year we discovered that you can actually inject a relatively small amount of delay by using cross core interrupt. And because the amount of delay we inject is relatively small, we have to precisely orchestrate the whole process. What I mean by that is have to inject at the precise time, precisely override it, and also synchronize using some special techniques. One of the parameters that we use in the whole exploit is Userfaultfd system call. Now, although this technique is powerful, it has some limitations. if you use docker container, and you enable the docker default seccomp profile, docker will actually block the system call. The other thing is, most cloud workloads don't use the system call. So, the usage of the system call itself indicate some kind of anomaly. And this is actually the mitigation deployed by Falco last year. They implemented a detection rule to detect the usage of the system call. Right, so last year we went back, and we think, you know, "Can we actually develop something that doesn't rely on using the system call?" So, we come up with the scenario, "What if you can actually inject a delay that is really, really long, then you don't have to orchestrate everything in the precise timing," right? It sounds very simple, but the question is, how to do this? And we actually find out there are two ways to do this. One is to use the blocking condition in system call. This can be used to attack sys_exit. The other one is to insert additional seccomp rules. This can be used to attack sys_enter. So, what do we mean by blocking the system call? If you think about the underlying mechanism of system call, it's basically the kernel interacting with resources on behalf of the user space program. Now, a lot of these resources are IOL devices, and the kernel will need to wait for the response from the device before you need to proceed. So, let's look at a concrete example. This is the connect system call. And in this diagram, there are two machines. There's a client, there's a server. Now, let's say the client is monitored by tracing software. The client wants to talk to the server, so he issue a connect system call. When the kernel start executing, it will notify the underlying networking stack, and they will send a SYN packet, the server return the SYN ACK and then the client return the ACK. Then the system call will return, tracepoint ptrace will read the arguments before the system call exit. Right? Now, we may be wondering if this was computer networking 101, what can go wrong here? Now, let's imagine this scenario. Many times when the attacker compromise a machine, or the workload, they will try to talk to the command and control server. This is a very practical setting. the attacker also controls the server side. So, let's look at the example in detail. Now, in the diagram, there's a client and a server. On the client, it's monitored by the tracing software. And then the attacker create a system call thread. The system call thread is first going to create an override thread. And then the system call thread call the connect system call with a memory page that points to the malicious IP it wants to talk to. Then the kernel will send the SYN packet over to the server. Now, remember the attacker control the server, so the attacker's gonna to say, "Hey, I wanna drop the SYN packet." Then what happens? The client will resend another SYN packet. And then the server can drop the packet again. Then the client will try again. So, every time the client retry, there's a delay controlled by the TCP congestion algorithm. Without going to too much detail, roughly you can think about it as an exponential delay for every retry. While this retry is happening, the override thread is going to override that memory page with the benign IP address. And then we have enough time for that data to propagate to all the memory copies on the system. And then finally the server will say, "Okay, here's your SYN ACK packet." And then tracepoint and ptrace will read out the benign IP address. And then the system call will exit. So, to show this works, we will show you two demos. First, we'll show a demo on VM container level. So, in this demo there will be... First, you are looking at the server side. Now, notice the server IP address ending with 176. Okay? And then we fire up this SCP program, looking for specific packet on a specific port. And then we also fire up a listening server on the server side. And then on the client side, we run the Falco software. We also run the Wireshark program. Then we run the exploit. It's basically a TCP client that try to connect to the server. You can see the connection is successfully established. And then it's sending some chat message. Now, you see the server received the chat message, it's sending something back. Everything is working, the connection is there. But now if you look at the IP address reported by Falco, it's actually saying 1.1.1.1. But if you look at the Wireshark, it's saying the first SYN pack, two SYN packets are dropped, and the IP address that is communicating is actually the server, the real server IP address. So, this indicates a successful bypass. Okay, now in the second demo, we're gonna show you the bypass on Fargate. So, similar here, first we are showing the server side. The server IP ending with 163. And we're going to run the XDP program that's going to drop the SYN packet. And then we're going to start a listening server. So, here's the Fargate container, it's going to run the pdig with our attack program. So, notice that pdig reports the connect system call is actually connecting to 1.1.1, which again indicates it's not able to identify the real IP address. You see the client server connected. They start chatting. But pdig reports, "Hey, we're connecting to 1.1.1." Okay, so at this point you're probably wondering, you know, this is for connect system call, what about the other system calls? So, we find out that the entire class of file system Also, if other system calls that relies on file system to perform some task are... They are also impacted. So, one example is the execve and the execveat system call, because when you execute a binary, first it's going to the disk to fetch the binary. Next, Junyuan will talk about how we bypass the file system system calls. - So, before I talk about how to bypass open access system call tracing, so let me introduce FUSE. So, FUSE stands for user space file system framework. It's usually included in the kernel Module, used as a library and also a remote utility. So, in cloud scenario, FUSE is used as the remote storage FUSE. So, with such FUSE we can mount the remote objects as local file system and also access the remote file as local file. Since it is the user space file system, so it provide faster involvement or development, and it usually do not panic the kernel. So, remote storage FUSE is widely used. So, here is the list of examples. From this example we can see the major cloud providers has their own FUSE. So, this is the general architecture for remote storage FUSE. If the user space application or container wants to open the remote file, usually FUSE, basically what it does is the same as opening a local file. It will issue an open or open at system call into the kernel. And the request will be routed from the VFS layer the FUSE kernel driver, and then to the user space file system Daemon, like gcsfuse. The Daemon will send the request to the remote storage, like GC storage or AWS s3. Once the response is back, it will send back to the user space application through the original path. So, one thing I need to mention here is the delay between the client and remote server is much longer than the syscall is in the client. So, basically you can leverage the long delay to bypass the open at source tracing. So, let's see how it works. So, we have the malicious client that is monitored by the tracing program. The syscall thread is going to open a remote file called malicious file. Basically, it will issue a open at system call into the kernel with the path name pointing to the malicious file in user space. Since the file is stored remotely, so the open at request will be routed from the kernel space to the user space and then reach the remote server. Before the response is back, the override thread can jump in and override the path name pointed... Override the memory pointed by path name from malicious file name to benign file name. And again, because the delay is much longer than the syscall itself, so the CPU have enough time to propagate a change to different copies of memory and registers. So, after the response back, and before returning to the user space, the tracing program can use the sys_exit tracepoint to get the arguments from the system call. Unfortunately, this argument has been changed In this case, we can successfully bypass the open at system call tracing. Let me show you how it works in a demo. (mouse clicking) So, we have a console on the left. So, for the Google Cloud storage bucket. And we deployed a Falco agent in GKE cluster. And we log into one of the pods. It shows the process inside the container, so we have a Falco agent running, We have a GCS FUSE Daemon running, which mount the Google Cloud storage bucket into the local folder, MNT. And then we check the logs for the Falco. So there are one event generated, because we just log into the pod. And then we check the folder, MNT, so it's empty, which mean Google storage bucket is empty. And then we're trying to open the file called malicious file in MNT folder. Yeah, then we check the folder, so the malicious file is created in the remote storage in Google cloud. We check the Falco logs again. So, there are one event generated, because we're using an open at system call to open the malicious file in the MNT folder. We remove the malicious file, which will be removed from the remote server. So, you run the attack, as just mentioned, and you check the MNT folder. So, the malicious file is created by the attack code. And then you check your log for the Falco. So, there are no new events generated, which means our bypass succeeded. Next, I hand over to Rex, to talk more about the exploits, and also conclude. - Okay, so, you know, the previous gave you an example on networking and file system. Basically, these are examples to demonstrate that how you can trigger Now, you know, this doesn't exhaust all the blocking condition. There are many other ways that you can trigger blocking conditions on differences system calls. But we're going to talk about how to use seccomp to bypass syscall tracing at sys_enter. So, if you recall the slide earlier, when the system called execute, it's first going to hit ptrace, and then going to hit seccomp, and then a bunch of other stuff. Then the real system call is going to dereference the user space memory. Now, the question is, can we use seccomp to bypass the ptrace tracing at sys_enter? Before I dive into the exploit, just a brief recap about what seccomp is. So, seccomp is a kernel mechanism to filter system calls. You can build, you know, modern sandboxes on top of it, and it allow the developer to filter system calls, and even based on their arguments. Now, if you have complex seccomp rules, this quickly become harder to compute, and takes more time to compute. Also, one caveat to keep in mind, which will be useful later, is the first inserted seccomp rule will get evaluated the last. So, it's evaluated in a reverse order. Okay, so here's how to bypass the ptrace sys_enter. The idea is very simple. Now, what you're seeing here is just a single machine. The attacker runs the system call thread. It's going to insert a bunch of seccomp rules. And then it creates the override thread. And then it's going to call a system call. So, in this diagram we use create at, but really this works for any system call. So, in the create system call I patch in the path name that points to malicious file, sorry, benign file. And ptrace happily read the benign file argument. Then the seccomp check starts. And then the override thread. At the same time, it's overriding the memory with the malicious file. Because the seccomp take quite a bit of time to compute, due to the number of rules that we inserted, then the system has enough time to propagate this benign file to all the memory copies. And then finally, seccomp compute finish, the kernel reads the argument, and kernel actually create a malicious file for us. Okay, so there is one interesting caveat related to ptrace. It is, you can use ptrace in plain mode. You can also use ptrace in the mode called ptrace plus seccomp redirect. What that means is, when the tracer... If the tracer start the application, then the tracer can say, any system call that the application calls, please redirect back to the tracer. So, the tracer sometimes insert a seccomp rule first and that rule says as long as you hit the seccomp rule, you redirect execution back to the tracer. So, if the tracer is able to insert the first seccomp rule, then no matter how many seccomp rules that the application insert, it's not going to impact the timing window between when the tracer reads the system call argument and when the kernel reads it. So, in this case, it's not vulnerable to the attack. But if you are attacking to run the application, then you have no control on how many seccomp rules has been inserted before the tracer attached. Okay, so we have covered a lot of different issues, and also how to exploit them. So, just to summarize these issues, and also how to mitigate different scenarios. So, first thing is ptrace at sys_enter. Now, in order to mitigate this, you can use ptrace plus seccomp redirect, but you need to start the application as well. Now, if you need to attach to a running application, you may want to inspect what seccomp filter has already been inserted to see if there's any anomaly. Otherwise, you can also insert a seccomp redirect, but then that means all the previous seccomp rules inserted by the application are ignored. So, it won't be useful. The second thing is tracepoint at sys_enter. Although the TOCTOU issue exists, but we haven't found a reliable way to exploit it. The next one is tracepoint at sys_exit. Now, this is a vulnerable, to both the attack in this talk, and also our research last year. In order to mitigate this, one option is to compare the sys_enter and sys_exit tracepoint data to see if the argument has been tampered. Of course, this will incur some performance overhead. And the other option is to use kprobe, which I'll soon talk about in the last row. So, next one is ptrace at sys_exit. Now, it's vulnerable to the same attack as the previous one. one will need to deploy all the mitigations at ptrace at sys_enter and then compare the ptrace at sys_enter and sys_exit results. kprobe and other internal kernel functions. Whether this is vulnerable really depends on which kernel function is being probed. But in general, if you want to avoid the TOCTOU issues, specific on system call monitoring, you can take the Linux security module interface. Some newer kernels also allow you to do BPF-LSM, but this depends on how wide range of kernel you need to support. Because the Linux security module interface have different support across different kernel versions. And if the, you know, specific kernel interface you happen to inspect is not in RSM, then you quickly get into the complexity of figuring out whether that exists in all the kernel versions you want to support. Okay, so to conclude. First, if we zoom into this research issue, the first thing is we showed that the kernel tracing can be bypassed reliably in many different ways. So, if you happen to use a tool that does similar things, you may want to check if your tool is vulnerable to this. Because we've only evaluated the open source solution, we didn't evaluate any proprietary software. The second thing to keep in mind is the mitigation is complicated, it depends on what, you know, form factors that you use, and also what kernel versions that the software needs to support. So, you want to double check that the mitigation was actually implemented in the way that you want it to be. And then, if I zoom out from this particular issue a little bit, if we think about the bigger picture, if your security team is able to correlate different data sources and have a comprehensive view about your environment, then it suddenly make the evasion complexity much higher. Last, but not least, this is something that we do lot at Lacework, is that we understand, you know, we... You should understand that your environment, what is normal, what is intended to be wrong in your environment. If you know the baseline of your environment, then even if the attacker is able to override these arguments, they're basically being constrained to the value that is fit into the normal expectation. Okay, before we take the Q&A, I just wanna give a quick shout out to the following folks. So, Joe helped with the kernel and security discussion. A lot of my Lacework labs colleagues give great feedback on this work. Also, I wanna thank the Falco open source team for a very collaborative disclosure process. And also John Dixon for helping the presentation. So, before finally we take the Q&A, if you have any other question about cloud security in your environment, or if you want to advance your career in cloud security, I'm very happy to talk to you guys offline. With that, we're ready for Q&A. So, anyone who want to ask a question, there's a mic here. And Steve will help to walk to you guys. Hi. - Can you hear me? - Yeah. - Okay. In some of your screen captures, you had, kind of, your exploits from last year, they were Phantom V1. These are Phantom V3. What was Phantom V2? - Oh yeah. Thank you. Thank you, for that. So, certainly there's too many attacks that we publish. So, Phantom V2, at the time we called it semantic confusion. So, for the folks who's not familiar with, that's also a bypass on the system call tracing. The idea is this, when you trace file search an object, for example etc/shadow, if it is an open at, then the rule is basically detecting whether the path is open at. But what if you create a link that is the linking to the file object, and then you open the link? Then the tracing software will not be able to interpret it, but the kernel will be able to interpret it. So, there's a confusion between the tracing software and the kernel. All right. Thank you, everyone, for coming.
