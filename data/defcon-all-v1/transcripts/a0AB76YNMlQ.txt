hello everyone I'm joining from Tinson security sham lab and today I'm going to talk about Windows security medications so in in recent years Microsoft Windows has already released five with five major releases and each one of them Microsoft has decided for some new security features in the release and in 2017 they decided to put many of the security features under Windows Defender brand and some of the some of the names are code integrity guard exploit guard arbitrary code guard and application guard so today we all go through these security features Microsoft edge has become the new default browser on Windows 10 it is also the most targeted browser in recent Pantone competitions in fact it has already surpassed its famous predecessor internet explorer so normally attacker compromises browser by first gaining code execution in a renderer process that's the process that parses the web page and executes JavaScript code etc and then attacker needs to exit to use an unavailability to escape from the renderer sandbox this work at the attackers normal user privilege on the target system in order to do some real harm the attacker can optionally use another about to escalate used kernel or system privilege and that will gain attackers full compromise of the target system the first one I'm going to talk about is code integrity guard so after compromise the process attacker can have to compromise the process use a piece of shell code attacker can load a deal into the compromised process or it can create a evil process to execute a larger amount of code because this way it's easier to write than shellcode so Windows 10 intro to introduce to medication policies to prevent these from happening the first one I'm going to talk about is child process policy when the child process policy is enabled for a certain process it prevents the process from a prevents process from creating a sub process as the child process of this process so how does it work well actually at the time Windows did not have a uniform way of recording mitigation policies for certain process so this sort this particular policy is recorded in the token token flag field of the kernel token token kernel object so when the process is created it first needs to derive is token from its parent process that is done in the security subsystem or se in the NT OS kernel and the security substance check checks the token flag field to see if there is a child process restricted bit in that token flag field and it found it denies the process creation and also the earth attacker can also choose to load a deal into a compromise process so the process signature policy is is checked during procuring a section creation whenever a deal is loaded into a process it first needs to create an image section into that process so before the section is created the memory subsystem or mmm calls the calls the code integrity subsystem or CI to check if the executable deal backing this back in this section is as has a digital signature or is digitally signed if not it prevents the section from create created so there's three levels for process signature policy the first one is Microsoft signed only this is the most restrictive one as it only allows loading of certain Microsoft core system files the second one is store sign darling the store sign only allows loading additional files from Windows Store all the universal Windows apps has this level of protection enabled so they cannot load unsigned deals by default and the last one is medication opting this not only allow load Microsoft or store files but also wql files which is normally part of a device driver package so CIT has some limitations attackers can choose to implement their own loader such as a loader that map's the Theo file into a block of executable memory since it's only your executable memory and not a major section it cannot figure the code integrity policy so it's little bit harder but still trivial to bypass next thing I'm going to talk about is Windows Defender exploit guard Windows Defender exploit guard is used to protect memory vulnerabilities the memory vulnerabilities are the most common way to remotely compromise and applications such as web browser we do have DP or a SLR on modern operating systems but this is not enough a TPS SAR has too many bypasses so we do need what we do need is a built-in protection as at the system level the eme T or enhanced medication experience toolkit by Microsoft is quite powerful but it's not reinstalled the exploit card is basically a system-level reimplemented m80 it is internally called payload restrictions because it restricts attackers ability to deliver a malicious payload after attacker against control over the execution flow so exploit card can be enabled those dynamically or statically for dynamic enablement the source process the process that starts the starts the exploit card process a pass a payload restriction policy structure to the NTS kernel for the static enablement there's image file execution options actually Microsoft has introduced a new feature for Ivo called a field filter traditionally IFE you can only set options by the image base name now by using Ivo filter we can specify a for pass for the for the executable to be to be filtered to be enabled as certain medication policies so exploit card is enabled via via our own document feature undocumented feature called verifier hook the verifier hooks starts at the earliest time possible in NT do NT do cause the verifier hook and loads the payload restrictions deal from the system's 32 directory although the exploit car is completely implemented in the user mode there are also kernel facilities to help implement the exploit card this this structure is unsigned loans 32-bit it records all the features that explored God has and entity o before the process initialization will read this read the information from this structure and pass it on to the payload restrictions deal so the exploit car also registered for Theo notification this is an undocumented feature in anti do after this notification is registered whenever a deal loaded into or unloaded from a process the entity or first calls the notification callback so by by registering this notification exploit guard can patch the newly loaded deal into into the process exploit card like eme T also use user mount hook to implement its features in this in in all these hooks it checks for potentially illegal operations but exploit card also comes with its own limitations just like EMA T its user mode hook can be bypassed of course and it definitely increased the difficulty of code execution but it's really not preventing it because already already the attacker already get to get control over the execution flow next thing I'm going to talk about is called arbitrary code guard a difficult car is introduced in Windows 10 restaurant to today most exploits needs to either allocate or modify executive or memory by by limiting that behavior exploit arbitrary code gar actually breaks most of exploits available in the world so arbitrary code guard limits three kinds of behavior first is creating executable memory and second is modifying the executable memory and third is mapping a writable Oreca and executable section so there are four policies for dynamic code policy this is called prohibited tonight code allows read opt-out allow remote downgrade and audit prohibit dynamic code the first one turns down the arbitrary guard and the second one allows dynamic disable of the arbitrary code guard on a per thread basis and cert 1 allows disable arbitrary code car from another process and first one is enabling the arbitrary code guard in a log online mode this is quite useful when you are developing an application that uses the arbitrary code guard so this this policy is actually checked in 3 in 3 3 different API calls the first one is mapping a view of section this mapping a executable section second is allocating a executable memory and third is changing the protection of the existing memory blocks all these are going to my arbitrary code block function and this function actually checks for the pro process a CG switch and also the pro pro pro thread a CG disable switch if but if the process is opt into the a CG and the thread is not opted out of the a CG well this action is blocked by the a CG so for by enabling the a CG the most most important problem is how do we deal with the legitimate applications that actually use dynamic code generation the most obvious example being the web browsers web browser has JavaScript engines JavaScript engine nowadays always use just-in-time compiler to generate native code for JavaScript and this will need to allocate executable memory in the render process and this will of course speed is this will of course be blocked by the ACG so to solve that problem the Microsoft edge has choose to re-architect is JavaScript engine to move the jet part out of process so it turns out a CG only checked the source process not the target which means that if you are a cg process you cannot allocate executable executable memory in the normal process but if you were a normal process you can allocate executable memory in a CG process so by sending a JavaScript code to a JIT server and the juicer will compiles the JavaScript code and send backs to send blacks through writing an executable memory in the render process this way we implemented an autocross at jet compilation so ACG has its own set of limitations first it increased design complexity dramatically because it requires out of process compilation for any dynamic code also attackers can also generate limited native code via out of process jet because because the the jet server cannot distinguish between legitimate or illegal requests for the jet code compilation also now also not all GPU drivers are compatible with a CG Windows 10 use a black list to detect any incompatibility and if detected uses allows read opt out switch to disable the ACG also ACG does not protect against return oriented programming or ROP so ROP does not generate any new code or modifying any existing code so it does not trigger any ACG policies and yet another layer of protection is is introduced in Windows 10 rs3 is called Windows Defender application guard so the application guard is basically a virtual machine running on the local computer running only but only Microsoft edging inside the virtual machine so powered by the hyper-v virtualization technology it is not enabled by default and can be enabled via group policy so when a user opens the webpage in application guard it automatically renders the webpage in a virtual machine like any other virtual machines application gar shares the same hardware with the host operating system but it does have its own operating system kernel services and applications so by enabling a PG we see several process popped up on the host operating system there is hyper-v service VM compute and also the hyper-v manager process so the hyper-v service actually create a unique identifier for the new container and the VM compute process actually starts the container for a 4d for the application guard and finally after the continuous started after the container is started it uses remote desktop protocol to connect to the local computer so there's the RDP client running on local computer it's actually a local desktop protocol if you will so this this RDP client relays all the UI clipboard and printer through the RDP protocol and this is send boss by default with all the medications and labeled and also this is a simplified overview of the application guard on the left hand side on left-hand side is the is the host OS and on the right hand side the child partition is the guest OS these two operating systems can only communicate via VM bus it's a communication mechanism implemented via shared memory so there's a set of vs CS or virtual servers clients that relays all the device requests five operations to the VSPs on the host operating system also there's the DNS client that actively detects if sis running application guard if so a redirect is TS request to the host WTH a proxy so there's a virtual hard disk for the 4440 virtual machine the first one the system template base it's a virtual disk for the container OS image this is generated from the system files of the host OS and that for the file writes operations in the virtual machine it's use another virtual disk called sandbox virtual disk this capture all the write operations and it it is discarded after each rung of the virtual machine there's storage virtual service provider handles all the container storage access so you see the sandbox virtual disk is mounted on C and there's another persistent disk another tooth persistent disk for storing logs and user files so when you trying to run anything apart from Microsoft edge in the container it gives you this message says this app cannot be wrong in Windows Defender application guards please contact your administrator for help well I am the administrator right so how can I get around this actually use another guard called device guard to implement this - to restrict certain application from running in the container this device guard is uses a undocumented binary format for its voice roles this is converted from the XML format and comes preloaded with virtual machine after a bit of reverse engineering we were able to recover its five format and it has a fixed lens header and also multiple blocks of variable length data it has many information stored designers signing scenarios and ek use and secure boot settings but the most important part is the fire rules the fire rules allows the allows the device guard to deny certain applications from running by checking its file name or version information or signatures so how do we run our own tools in the application guard well remember we say it's a binary file turns out if you remove the file physically from the container image and the device card policy will be disabled so the most most simple way to do this to get the open source wim Lib tool it's a editor for the windows imaging format so use the use the vim delete command to remove the policy file from the windows defender application guard image and restart the application guard then use a comment dialog to open the Explorer window in the application guard then you can run anything inside it so what's actually inside the virtual machine well actually it's a pure Windows 10 professional edition it has certain virtual machine agents running inside for process operations this virtual machine has a random password set by the hyper-v service it is used for remote desktop connections things like clipboard printers is not enabled by default but can be configured by a group policy so there's two virtual machine' agents running inside first ones called hyper-v guests compute service this this service is SD service to communicate with the host via the hyper-v socket it controls the compact container state and modified container settings and also it redirects the standard output and input for front for any application to the hyper-v socket the second is called container execution agent or exe EC service is a RPC server running inside the virtual machine for creating users and shutdown systems but actually there's no process for Microsoft edge controller actually there's no need for controlling Microsoft edge the user is directly controlling it from outside the virtual machine through the remote desktop protocol so in conclusion Windows is more secure with many more newly added medications but these medications are not several poets attackers and always find ways around them virtualization as the additional layer of protection can significantly increase the difficulty of exploitation but at the cost of performance actually the runtime performance penalty is too high for everyday use that would be all for today thank you
