Hello, hackers. This is a talk about back in&nbsp; MikroTik. I'm NiNi Chen, a security researcher&nbsp;&nbsp; at Devcore. We're from Taiwan. I also a member&nbsp; of Balsn CTF Team and a member of Undefined.&nbsp;&nbsp; Okay, why? Why the RouterOS? Because we&nbsp; want to participate in the Pwn2Own Toronto&nbsp;&nbsp; in 2022. As one of the targets the router&nbsp; board of MikroTik hasn't been explored in&nbsp;&nbsp; the previous event. More directly no one tried to&nbsp; demo exploit against list target in the contest.&nbsp;&nbsp; You may already know the rules of the event.&nbsp; Participants must find a pre-auth RCE with no&nbsp;&nbsp; user interaction to acquire the points of&nbsp; the target. In the SOHO Smashup a special&nbsp;&nbsp; category was introduced in 2022. Participants&nbsp; have to chain the router WAN site exploit and&nbsp;&nbsp; other devices it poise to earn the points and&nbsp; bounty. The bounty and point in this category&nbsp;&nbsp; are relatively high, so it's a good target&nbsp; for us to earn more to win the contest. &nbsp; Let's recap some best thing quickly. The&nbsp; router board of MikroTik is quite popular&nbsp;&nbsp; and the RouterOS is the operating system of&nbsp; router board hardware. By scanning the MikroTik,&nbsp;&nbsp; we can observe about 3 million device online.&nbsp; RouterOS is a standalone operating system&nbsp;&nbsp; based on the Linux kernel and MikroTik also&nbsp; offers ISO images so we can run it as a VM.&nbsp;&nbsp; It's closed sourced and also closed&nbsp; ecosystem. According to the download&nbsp;&nbsp; terms on MikroTik's website, you pay 45 bucks&nbsp; to get a GPL source code. What a bargain. &nbsp; Currently, there are two versions of RouterOS,&nbsp; the v7 and the v6. They should be considered&nbsp;&nbsp; two branches because their implementation&nbsp; are slightly different and the cloud hosted&nbsp;&nbsp; router is the RouterOS image for VM but&nbsp; only the AMD 64 version is available. &nbsp; Most service on RouterOS are implemented&nbsp; by MikroTik themself and they share a&nbsp;&nbsp; similar architecture so we name them Nova&nbsp; binary afterward and they use Nova message&nbsp;&nbsp; to talk to each other. Though the Router OS&nbsp; is based on Linus kernel, users are trapped&nbsp;&nbsp; inside a customized UI and there is no official&nbsp; method to access the underlying Linux system.&nbsp;&nbsp; Only developers can install a backdoor package&nbsp; to assess the Linux system for debugging purpose.&nbsp;&nbsp; An administrator can manage their routers via&nbsp; a web panel or Winbox, which is a Windows GUI&nbsp;&nbsp; application. It's also a popular attack service&nbsp; because the Winbox interface on the router can&nbsp;&nbsp; be used to talk to all the Nova binary. Some of&nbsp; them can be accessed without authentication. &nbsp; To understand how people research the RouterOS,&nbsp; let's have a look at past CVEs of the RouterOS.&nbsp;&nbsp; There are a total of 80 CVEs and 28 of them are&nbsp; pre-auth exploitable and the victim is the router&nbsp;&nbsp; itself. 16 out of the 28 are those in various&nbsp; components. Eight out of 28 are DNS poisoning&nbsp;&nbsp; allowing the attacker to MITM or firewall&nbsp; invasion. Only four pre-auth vulnerability&nbsp;&nbsp; in history can lead to a shell or log in&nbsp; as admin directly without user interaction.&nbsp;&nbsp; These are what we care about. Three out&nbsp; of four were found between 2017 and 2019&nbsp;&nbsp; and three out of four are in the wild. Let's have a close look. CVE-2017-20149&nbsp;&nbsp; aka Chimay-Red is one of the exploits leaked&nbsp; from CIA in 2017 by sending an HTTP request&nbsp;&nbsp; with corrupt content length so we can corrupt&nbsp; stack to execute arbitrary code by stack clash.&nbsp;&nbsp; CVE-2018-7445 is a buffer overflow in SMB found&nbsp; by blackbox fuzzing. CVE-2018-14847 is also one&nbsp;&nbsp; of the exploits leaked from the CIA in 2017. This bug allows unauthenticated attackers to read&nbsp;&nbsp; arbitrary file on RouterOS. It sounds not a big&nbsp; deal in the first place but since the password&nbsp;&nbsp; store on RouterOS was from simply repeating&nbsp; xor password in plain text with the hash of the&nbsp;&nbsp; username and the fixed string at that time, so the&nbsp; password can be recovered easily and the bug with&nbsp;&nbsp; arbitrary file reading is sufficient to log in&nbsp; the mean. CVE-2021-41987 is a heap buffer overflow&nbsp;&nbsp; in the decode of the SCEP service. This was&nbsp; discovered on an APT's C2 server in 2021. As we&nbsp;&nbsp; can see, these vulnerabilities are mostly in the&nbsp; wild. We can learn too much from them directly. &nbsp; Let's take a look at some talks and articles about&nbsp; the RouterOS. In 2017, there are three talks and&nbsp;&nbsp; they are about a packaged format adopted by the&nbsp; RouterOS, the developer inspect how the RouterOS&nbsp;&nbsp; store the password, et cetera. Talks are&nbsp; interesting and helpful to have a big picture&nbsp;&nbsp; of the RouterOS. In 2018, the talk Bug Hunting in&nbsp; RouterOS analyze the Nova message in binary and&nbsp;&nbsp; JSON formats, introduced the how Winbox client&nbsp; talk to the Winbox interface by Nova message&nbsp;&nbsp; and found the post-auth stack overflow. In 2019, the first one is the same as the previous&nbsp;&nbsp; one so the next one is MikroTik Firewall and NAT&nbsp; Bypass. It's a talk about the vulnerability in the&nbsp;&nbsp; Winbox that can be used to scan the intranet&nbsp; if the Winbox is exposed to the internet.&nbsp;&nbsp; The next Finding an Exploiting CVE found a&nbsp; stack buffer overflow in SMB by blackbox fuzzing&nbsp;&nbsp; and explained how they got the shell&nbsp; with their exploit on the RouterOS VM.&nbsp;&nbsp; And the Deep-Dive: MikroTik Exploits is a security&nbsp; analysis about the CIA exploits and the massive&nbsp;&nbsp; exploit event based on the leaked payload. The next one, Help Me Vulnerability,&nbsp;&nbsp; You're My Only Hope released a tool to help&nbsp; users to jailbreak their device to check if&nbsp;&nbsp; leaked exploit was already compromising them.&nbsp; The last one, RouterOS: Chain To Root is about&nbsp;&nbsp; chaining DNS poisoning vulnerability&nbsp; and a downgrade attack to get a shell.&nbsp;&nbsp; Finally, in 2022 Pulling MicroTik into Limelight&nbsp; talk more about Nova message and disclosed&nbsp;&nbsp; post-auth vulnerability in Triple W that can be&nbsp; used to jailbreak. They also released a tool to&nbsp;&nbsp; help researchers quickly get a shield on the AMD64&nbsp; version of RouterOS with the vulnerability. &nbsp; It seems like the whole world is talking about&nbsp; the Nova message in the IPC of the RouterOS. So&nbsp;&nbsp; we also try to understand what it is. Let's talk&nbsp; about the IPC of RouterOS with an example. While&nbsp;&nbsp; we use the login through the telnet to access the&nbsp; console of RouterOS, the telnet will immediately&nbsp;&nbsp; call the execl to execute the login process to&nbsp; ask the user for a use surname and password.&nbsp;&nbsp; Then the login process will send a Nova&nbsp; message with the username and password&nbsp;&nbsp; to the user process to check if they are correct.&nbsp; The user process also responds in Nova message. &nbsp; The Nova message consists of key value&nbsp; pairs and the key can only be numbers.&nbsp;&nbsp; The Sys_To and Sys_From arrays containing the&nbsp; ID of non-binary and the ID of the handler.&nbsp;&nbsp; The communication between two process is&nbsp; something like leaks. Every message is sent to&nbsp;&nbsp; the loader via socket and the loader dispatch&nbsp; the message to the corresponding binary. &nbsp; Login process send a request with a username&nbsp; and password to the user process. The value of&nbsp;&nbsp; Sys_2 means the socket is the handler 4 and the&nbsp; binary ID is 13 and the process itself doesn't&nbsp;&nbsp; have to fill the Sys-From with its process&nbsp; ID because the loader we do that based on&nbsp;&nbsp; which socket it received the message from.&nbsp; Next, the loader remove the target binary ID&nbsp;&nbsp; in Sys-2 as the source ID to Sys-From and then&nbsp; forward the message to the target process. &nbsp; When the user process receive the message&nbsp; from the socket, it also remove the handler&nbsp;&nbsp; from the Sys_2 and pass it to the corresponding&nbsp; handler. Then the handler does the rest of thing.&nbsp;&nbsp; We mentioned that the Winbox interface on&nbsp; the RouterOS is a popular attack service&nbsp;&nbsp; since it can be used to access all the process.&nbsp; That's because the protocol inbox client used&nbsp;&nbsp; to talk to the interface on the router is&nbsp; basically a Nova message in the TCP packet.&nbsp;&nbsp; So if the target handler failed to check the&nbsp; center's privilege, that can cause problems. &nbsp; Nova message is typed value mapping&nbsp; and can contain various types and&nbsp;&nbsp; also Nova message itself. Because the JSON&nbsp; format of the Nova message is deprecated,&nbsp;&nbsp; we only talk about the binary forms today.&nbsp; Okay, the M2 at the beginning is the magic&nbsp;&nbsp; bytes for the Nova message. Then they use four&nbsp; bytes to describe a key. The first three bytes&nbsp;&nbsp; of key are ID and the last byte is type and&nbsp; bytes following are the value for that key. &nbsp; So these messages ID types and values&nbsp; look like this. Types like pool can&nbsp;&nbsp; be expressed with one bit directly&nbsp; using the lower bit of the type bytes.&nbsp;&nbsp; To understand which binary corresponding to the&nbsp; ID in the Sys_From and Sys_2 we need to parse .x3&nbsp;&nbsp; file under the loader directory. It's kind of XML&nbsp; but in binary form. If the binary was introduced&nbsp;&nbsp; by installing a package, its ID is under the&nbsp; loader directory of that package. Some IDs can&nbsp;&nbsp; be found in the .x3 file because the process is&nbsp; not persistent. The ID is just a serial number&nbsp;&nbsp; like the login process we just mentioned. Other binaries also have their .x3 files&nbsp;&nbsp; for a different purpose. For example, the&nbsp; .x3 of Triple W describe which URI should&nbsp;&nbsp; be handled by which . It&nbsp; seems like it's a good target for fuzzing,&nbsp;&nbsp; right? But we can't just fuzz it and expect&nbsp; we get something after two months. &nbsp; Now it's a spoiler. While we were writing our&nbsp; exploit with the vulnerability that I'll introduce&nbsp;&nbsp; later, a talk at PLC 2022, MicroTik RouterOS&nbsp; Security: The Forgotten IPC Message though I&nbsp;&nbsp; think it's far from forgotten but anyway, the&nbsp; talk is about fuzzing and the Nova message,&nbsp;&nbsp; and he found lots of post-auth CVEs. By the number&nbsp; of CVEs, it looks like the fuzz has been running&nbsp;&nbsp; for a long time. A bit of an afterthought,&nbsp; I don't think we can get a better result if&nbsp;&nbsp; we do the same thing at the time. Not to&nbsp; mention that we only have two months. &nbsp; Is this still possible to pawn the RouterOS?&nbsp; Review this CVEs Again, most of the vulnerability&nbsp;&nbsp; we think interesting were discovered many years&nbsp; ago. Researchers also found lots of vulnerability&nbsp;&nbsp; but as we can see the MicroTik becomes safer&nbsp; and safer. But in 2021, a vulnerability in the&nbsp;&nbsp; SCEP server we just mentioned was discovered&nbsp; by analyzing in a wild exploit on the APT C2&nbsp;&nbsp; server. Was it undiscovered until then because it&nbsp; was not a default feature or is it just because&nbsp;&nbsp; people have overlooked something? Most talks and&nbsp; articles focus on jailbreaking, analyzing the&nbsp;&nbsp; in-the-wild exploits and Nova message in IPC. I have a summary after reading them and starting&nbsp;&nbsp; to reverse by ourself. That is no one will&nbsp; sanity would like to dive into the details&nbsp;&nbsp; of Nova binaries. I mean expect for the exploits&nbsp; from the CIA and APT. Most of the research about&nbsp;&nbsp; bug hunting is fuzzing the protocol playing&nbsp; with Nova message or fuzzing Nova message.&nbsp;&nbsp; By the outcome, it seems like attackers&nbsp; are familiar with the Router OS than us.&nbsp;&nbsp; We need more details about the Nova message and&nbsp; don't get me wrong, I'm not against fuzzing but&nbsp;&nbsp; we have to ensure we check everything&nbsp; to take advantage of the contest. &nbsp; I don't think the system is perfect now. There's&nbsp; a gap between researchers and attackers. So what&nbsp;&nbsp; was missed if we want to find some pre-auth RCE&nbsp; bugs in RouterOS? The first question is where are&nbsp;&nbsp; the entry point to the customized IPC? Most of&nbsp; them can only assess by authenticated user. We&nbsp;&nbsp; can foresee that playing with Nova message in IPC&nbsp; will result in lots of post-auth bugs. IPC just a&nbsp;&nbsp; part of implementation of the most functionality.&nbsp; We want to review the crucial part directly and&nbsp;&nbsp; carefully. For example, how does the DHCP process&nbsp; pass a DHCP packet to extract the info in it? &nbsp; Then we have to mention the&nbsp; architecture of the Nova binary.&nbsp;&nbsp; Every Nova binary has a looper. The looper use&nbsp; poll to check the status of socket and process&nbsp;&nbsp; event in each loop just like a regular event loop&nbsp; you've seen everywhere. Looper is responsible for&nbsp;&nbsp; the communication between the current process and&nbsp; the loader. The looper register a spatial callback&nbsp;&nbsp; for the unit socket which is used to communicate&nbsp; with loader. The spatial callback on MSGSock will&nbsp;&nbsp; dispatch the message to the corresponding handler&nbsp; once it receive Nova message from the socket. &nbsp; So how does the handler work? While&nbsp; the looper receive a Nova message,&nbsp;&nbsp; you'll dispatch it to the corresponding handler.&nbsp; Look at the right-hand side and let's assume that&nbsp;&nbsp; Handler 1 is responsible for the Nova message&nbsp; and then your code function NV table with&nbsp;&nbsp; the CMD prefix like corresponding to&nbsp; the command specified by the CMD. &nbsp; There are many function here but&nbsp; because the handler is the best class&nbsp;&nbsp; commands relative to objects are unimplemented.&nbsp; The CMD unknown is often overridden and used to&nbsp;&nbsp; extend the customized command. Sometimes the&nbsp; developers override the handle CMD instead of&nbsp;&nbsp; the CMD unknown. You may have already read&nbsp; some of these in a past research, but most&nbsp;&nbsp; of the time the Nova binary use the derived&nbsp; classes rather than the best handler class. &nbsp; The derived classes can store a collection&nbsp; of object of a specific type. For example,&nbsp;&nbsp; while the user is setting up a DHCP server, they&nbsp; will send a message to the Handler 0 of DHCP&nbsp;&nbsp; process to create a DHCP server. The Handler 0,&nbsp; which is an instance of AMap, calls the function&nbsp;&nbsp; on its V table which offset x7c to create a&nbsp; DHCP server object and insert it into a tree.&nbsp;&nbsp; The 7c is the offset to the constructor&nbsp; of the inner object and you can find the&nbsp;&nbsp; offset of such function in other derived&nbsp; classes by reversing the CMD object. &nbsp; In the previous example, the web panel&nbsp; sends a Nova message to DHCP to create&nbsp;&nbsp; a DHCP server. But what if I told you that some&nbsp; functionality don't even use the Nova message.&nbsp;&nbsp; Check the CDP and the LLDP for example. They&nbsp; are implemented in discover. The looper register&nbsp;&nbsp; two callback functions. The looper keeps&nbsp; looping and check the status of sockets.&nbsp;&nbsp; If there are incoming packets, the&nbsp; looper call the callback function.&nbsp;&nbsp; The callback of CDP will check if the&nbsp; interface that receive the packet is&nbsp;&nbsp; not filtered by firewall rule, then extract&nbsp; the information from a packet and store them&nbsp;&nbsp; directly into another ASecMap without sending&nbsp; Nova message. So there's no Nova message here. &nbsp; We started to review the RouterOS by enumerating&nbsp; functions and reversing them. Then we found&nbsp;&nbsp; something strange. One day while plugging and&nbsp; unplugging some cable connect to the router.&nbsp;&nbsp; Suddenly the log file show that radvd crashed&nbsp; several times. So we tried to reproduce the&nbsp;&nbsp; crash by manually plugging and unplugging cables&nbsp; about a thousand times but couldn't figure out&nbsp;&nbsp; the crash condition. The condition is unknown and&nbsp; the crash appears randomly. Instead of mindlessly&nbsp;&nbsp; unplugging and plugging the cables, we reverse the&nbsp; radvd to see if we can find something interesting.&nbsp;&nbsp; We didn't find the root cause of the&nbsp; crash but we got something better. &nbsp; So what is radvd? Radvd is a service about&nbsp; SLAAC of IPv6. Let's say a computer or a node&nbsp;&nbsp; wants to get an IPv6 address. It'll first&nbsp; multicast a router solicitation to old router&nbsp;&nbsp; asking for a network prefix. Then if a router&nbsp; receive the RS, it will multicast a router&nbsp;&nbsp; advertisement with network prefix to all nodes.&nbsp; After that, a node can calculate its IPv6 address&nbsp;&nbsp; by combining the network prefix and EUI-64,&nbsp; which is calculated from the MAC address. &nbsp; Another scenario is that even upstream&nbsp; router, Router A may be it's an ISP. They&nbsp;&nbsp; give you a prefix with /48 which assigned in&nbsp; advance by email or something. Then you can&nbsp;&nbsp; use them whatever you want in your LAN. Then&nbsp; here comes the link prefix and routed prefix.&nbsp;&nbsp; First, the Router B act as a node on the interface&nbsp; that link to the Router A. Then Router B ask&nbsp;&nbsp; Router A for a prefix. The Router A send a /64&nbsp; prefix for Router B to calculate its IPv6 address.&nbsp;&nbsp; Router B calculate an IPv6 address. Let's&nbsp; say it is 2001:db8::1 and use it on the&nbsp;&nbsp; interface that links with Router A. So now the&nbsp; Router A can route all traffic to the address&nbsp;&nbsp; on the /48 to Router B. Now when nodes want to get IPv6&nbsp;&nbsp; and send RS to the Router B, the Router B just&nbsp; multicast the /48 prefix to nodes, then the notes&nbsp;&nbsp; act as usual. So the functionality of radvd in&nbsp; RouterOS should have function two functionality.&nbsp;&nbsp; First, it can act as a node to receive RA&nbsp; and it can also multicast RA to the LAN. &nbsp; Let's follow the execution flow of radvd to find&nbsp; the bug. The radvd have a similar design pattern&nbsp;&nbsp; to the discover process we mentioned earlier. It&nbsp; registers a callback, the looper pull the socket&nbsp;&nbsp; in every loop and if there is an incoming ICMPv6&nbsp; packet, the looper call a registered callback.&nbsp;&nbsp; What callback do is roughly like this. It check&nbsp; if the packet is a valid RA or a valid RS. If it&nbsp;&nbsp; is RA, then extract information and store them in&nbsp; the Handler 1, which is Amap, and if it is an RA,&nbsp;&nbsp; it will multicast the RA immediately. Sorry, if&nbsp; it is RS, it will multicast RA immediately. &nbsp; There are three cases the RouterOS will multicast&nbsp; the prefix to nodes. The first one is when you&nbsp;&nbsp; receive the RS from nodes. The second is when you&nbsp; receive an RA from upstream router. The last one&nbsp;&nbsp; is the router will multicast the prefix in a&nbsp; default period which is in 200 to 600 seconds.&nbsp;&nbsp; And yes, the router will multicast, the RA to&nbsp; nodes once you receive a RA. But it's hard to&nbsp;&nbsp; trace this behavior directly because this is&nbsp; about a subscription. I'll talk about it later.&nbsp;&nbsp; We didn't know that then. But the other two&nbsp; cases can be traced directly. The radvd code&nbsp;&nbsp; send RA right after it receive an RS. The Handler&nbsp; 1 registers a timer to send RA periodically. &nbsp; So we can find the send RA function by tracing&nbsp; these two methods. Following the send RA function,&nbsp;&nbsp; we found something interesting while&nbsp; the radvd deal with DNS advisory.&nbsp;&nbsp; It will advise DNS to nodes based on&nbsp; the DNS list it received from upstream.&nbsp;&nbsp; The Add DNS function is used to flatten&nbsp; the DNS tree and add it to the ICMPv6.&nbsp;&nbsp; The RA row is the ICMPv6 payload, which is&nbsp; the local variable with the size of 496 bytes.&nbsp;&nbsp; Step into the Add DNS and we can immediately find&nbsp; out there is probably a stack buffer overflow. &nbsp; The function Add DNS to packets by memory copy&nbsp; without any boundary check. That causes stack&nbsp;&nbsp; bubble overflow if the DNS is big enough. The DNS&nbsp; addresses are from the RDNSS of the RA but the&nbsp;&nbsp; mixed length of the RDNSS of an RA is 8 bit which&nbsp; is insufficient to override the return address.&nbsp;&nbsp; But if this is not the first time, the&nbsp; RouterOS receive an RA. The radvd have to&nbsp;&nbsp; expire all addDNS. They mix the radvd, flatten&nbsp; another tree to override more bytes for us.&nbsp;&nbsp; So what we have to do is we have to act as a bay&nbsp; neighbor that send a router advertisement with a&nbsp;&nbsp; big RDNSS list to the router. Then send another&nbsp; router advertisement with the big RDNSS list. &nbsp; Now the return address is overridden with an&nbsp; IPv6 address in the RDNSS of the first RA.&nbsp;&nbsp; It is time to construct the exploits and it looks&nbsp; easy because there is no protection enabled.&nbsp;&nbsp; So let's find a way to jump on the stack and&nbsp; run our shortcut. That should be easy. &nbsp; Thank you. The overflow IPv6 addresses are&nbsp; sorted since they were stored in a tree. To&nbsp;&nbsp; make the order of the IP addresses&nbsp; stable to ensure the shellcode is good,&nbsp;&nbsp; we can use part of the IPv6 prefix as a&nbsp; serial number and put a jump and at the&nbsp;&nbsp; end of each address to jump over the prefix&nbsp; because they are not valid instructions.&nbsp;&nbsp; But because of the delay slots of MIPS,&nbsp; which means the instruction after the&nbsp;&nbsp; branch instruction will always be executed&nbsp; before the branch instruction itself. &nbsp; We have to move the jump instruction forward.&nbsp; But the other problem is that you cannot do a&nbsp;&nbsp; Syscall in the delay slot. So basically this is&nbsp; a bad idea. It's actually a CTF101 problem. Just&nbsp;&nbsp; make a prefix valid instruction. They won't affect&nbsp; our gadget or shellcode, then everything is fine.&nbsp;&nbsp; And because we didn't leak any stack address,&nbsp; plus we didn't find any gadget that can be used&nbsp;&nbsp; to move stack pointer to register T9 for jump&nbsp; instruction in the first place, so what we did&nbsp;&nbsp; here is first write our gadget on memory, then&nbsp; jump to that gadget. Finally, our shellcode will&nbsp;&nbsp; be executed. Sounds good but there is I-Cache. The MIPS CPU has two cache: the instruction cache&nbsp;&nbsp; and the data cache. While writing our gadget&nbsp; on memory, we are actually writing on D-cache.&nbsp;&nbsp; Now when we try to control the execution&nbsp; flow to execute the gadget for running our&nbsp;&nbsp; shellcode, the processor first check if&nbsp; the instruction are already in the I-cache.&nbsp;&nbsp; Because we wrote our gadget on the&nbsp; data section, the cache always miss it.&nbsp;&nbsp; So the memory content will be loaded into I-cache.&nbsp;&nbsp; That cause the processor to execute a bunch&nbsp; of node until the program crash. To flush the&nbsp;&nbsp; D-cache, we need to make the processor context&nbsp; switch or exhaust the D-cache whose size is&nbsp;&nbsp; 32 kilobytes. There is no sleep in radvd. Other functions we also trap into the kernel&nbsp;&nbsp; but they don't guarantee the context switch will&nbsp; always happen. To make our exploit consistently&nbsp;&nbsp; successful in maximizing the chance to win the&nbsp; game, we have to flush the 32 kilobyte D-cache.&nbsp;&nbsp; Luckily, the value in randomized VA space of&nbsp; RouterOS is one which means the heap is now&nbsp;&nbsp; randomized. The only things we need to do&nbsp; is do some memory allocation to extend the&nbsp;&nbsp; heap and write some gadget on it. We may run&nbsp; out of bytes for shellcode because of that,&nbsp;&nbsp; but the DNS addresses was stored in a tree. They&nbsp; cost lots of memory space. The heap is already&nbsp;&nbsp; larger than 32 kilobytes during the processing of&nbsp; a request. So we can call the memory copy by GOT&nbsp;&nbsp; hijack to write 32 kilobytes garbage on the heap&nbsp; to flush the D-cache for our shellcode directly&nbsp;&nbsp; and the profit. The vulnerability was assigned CVE-2023-32154&nbsp;&nbsp; and it's already been fixed. After that&nbsp; we found the vulnerable code had existed&nbsp;&nbsp; at least since the RouterOS v6. This means&nbsp; the vulnerability has existed since 2013,&nbsp;&nbsp; nine years until we found it and 10 years&nbsp; until they fixed it. So no one with sanity&nbsp;&nbsp; would like to dive into the details of&nbsp; Nova binary. Q.E.D. You're welcome. &nbsp; Compiler exploit with our exploit&nbsp; from a Canon printer for the Pwn2Own.&nbsp;&nbsp; The attacking scenario is that an attacker is&nbsp; a bad neighbor of the MikroTik router sending&nbsp;&nbsp; crafty ICMPv6 to attacking the router and if&nbsp; we compromise the router successfully, we can&nbsp;&nbsp; forward our payload to attack the printer behind&nbsp; the router. The whole setup can be simplified as&nbsp;&nbsp; such in the contest, just let our laptop act as&nbsp; the upstream router of the attacker router. &nbsp; We can almost see our 100k bounty but another&nbsp; but our exploit only worked on MacOS and failed&nbsp;&nbsp; on Ubuntu, whether it is a VM or not. We try to&nbsp; debug the exploit by running the exploit on VM&nbsp;&nbsp; and recording the traffic, dump those traffic&nbsp; and replay it. It just failed as we expected.&nbsp;&nbsp; We also dump the traffic from Ubuntu and replay&nbsp; it on Mac. However, it somehow succeed. &nbsp; So we guess there is an OS that will reorder&nbsp; the packet. We put a sniffer on router to&nbsp;&nbsp; monitor the traffic from Mac and Ubuntu and&nbsp; because the AF packet is not affected by&nbsp;&nbsp; any firewall chains, the result should be&nbsp; reliable but they'll look the same. The last&nbsp;&nbsp; resort could be that I have to go to Toronto&nbsp; in person, but we can't leave it like that&nbsp;&nbsp; because who knows if the bug will suddenly happen&nbsp; again during the contest even on the MacOS. &nbsp; Eventually, we find the problem. It's&nbsp; all about speed. The gaps look tiny&nbsp;&nbsp; and you may never notice in one shot but the&nbsp; difference between them is actually 390 times.&nbsp;&nbsp; The problem is not on Ubuntu, it's just because&nbsp; the Mac sends two packet too quickly causing race&nbsp;&nbsp; condition. Plus we were lazy and did not&nbsp; actually calculate how many bytes we need&nbsp;&nbsp; to override the written address. We just send a&nbsp; bunch of garbage and do the pattern matching. &nbsp; So the solution is put a slip before sending a&nbsp; second RA and fixing the offset of our payload.&nbsp;&nbsp; Then it's very stable. The probability&nbsp; of success is almost a hundred percent.&nbsp;&nbsp; But how? The radvd is only one thread,&nbsp; how could there be a race condition?&nbsp;&nbsp; I didn't mention that while the radvd policy&nbsp; in the incoming packet, the DNS address are&nbsp;&nbsp; stored in a vector but somehow the handler&nbsp; passed a DNS tree to the Add DNS function.&nbsp;&nbsp; What's the relationship between these two&nbsp; members? It's actually about why we can't&nbsp;&nbsp; find where a callback function multicast&nbsp; the RA when you receive an RA packet. &nbsp; Let's look closely at the callback function.&nbsp; There is an array responsible for storing&nbsp;&nbsp; something called a remote object. Then it looks&nbsp; straightforward. Just the&nbsp;&nbsp; new DNS vector then do RO DNS on each of them and&nbsp; append result to the DNS remote object vector. So&nbsp;&nbsp; what is the remote object? The remote object is a&nbsp; mechanism to store common resources in the process&nbsp;&nbsp; and other process can assess them via ID. For&nbsp; example, a DNS remote object is actually stored in&nbsp;&nbsp; the Handler 2 of the resolver and the radvd only&nbsp; preserve the ID of the object in Handler 1. &nbsp; Now let's talk about the subscription in Nova&nbsp; Binary. The binary IP Pool 6 is responsible for&nbsp;&nbsp; managing the IPv6 address pool. The DHCP subscribe&nbsp; to it so the DHCP can react to the change.&nbsp;&nbsp; It's done by sending a subscribe Nova message&nbsp; and the Sys_Notify CMD can specify which command&nbsp;&nbsp; you want to subscribe to. The FE000B means notification.&nbsp;&nbsp; So if there is another binary try to add&nbsp; an object to the Handler 1 of IP Pool 6. By&nbsp;&nbsp; sending a Nova message the Add Object of&nbsp; Handler 1 will be executed. In most cases&nbsp;&nbsp; the Add Object will cause sendNotifies to notify&nbsp; subscribers who subscribe to FE000B to inform&nbsp;&nbsp; them that the IP Pool 6 has changed. So when&nbsp; radvd receive a RA from the upstream router,&nbsp;&nbsp; it call roDNS to create DNS remote object for each&nbsp; IPv6 address in the RDNSS of RA. The command Add&nbsp;&nbsp; Object of Handler 4 in the resolver is responsible&nbsp; for such request and will add the IPv6 object into&nbsp;&nbsp; the Handler 2. Also, because the Handler 1 of&nbsp; radvd subscribed to the resolver's Handler 2,&nbsp;&nbsp; the Handler 2 will notify it with every DNS&nbsp; addresses it has now. So the Handler 1 construct&nbsp;&nbsp; the RA for LAN which list DNS addresses. The problem is in the implementation of roDNS&nbsp;&nbsp; that sends Nova message by post&nbsp; message which is non-blocking.&nbsp;&nbsp; It means the remote object won't get the object&nbsp; ID immediately. So if we send requests too fast,&nbsp;&nbsp; the second RA arrives before the radvd gets the&nbsp; object ID of the old remote object. The radvd&nbsp;&nbsp; can only soft delete those old objects rather than&nbsp; really delete them that cause the race condition.&nbsp;&nbsp; This is what happen if we follow them step by&nbsp; step. Because both process are single thread,&nbsp;&nbsp; that's assuming they are in the first loop of&nbsp; the looper while the router radvd received the&nbsp;&nbsp; first RA from when with one DNS address,&nbsp; the radvd creates a remote object in the&nbsp;&nbsp; resolver by sending a Nova message and&nbsp; the resolver will set up a timer after&nbsp;&nbsp; receiving the first request. Because it cannot&nbsp; tell how many 18 operation should be grouped,&nbsp;&nbsp; it just set a timer and send only one&nbsp; notification when the time's up. &nbsp; Apart from notification, the resolver should&nbsp; send a response back to tell radvd the object&nbsp;&nbsp; ID of the remote object it just asked for. But if&nbsp; we send the second RA quickly before the resolver&nbsp;&nbsp; sends the object ID back, the radvd can only&nbsp; mark the old DNS remote object as destroyed.&nbsp;&nbsp; Then keep going to create the DNS remote&nbsp; object for the RDNSS in the second RA.&nbsp;&nbsp; They will send a request to the resolver, but&nbsp; as I say, we send the second RA too quickly. The&nbsp;&nbsp; resolver is still in the first loop which try&nbsp; to send the object ID it created in the first&nbsp;&nbsp; loop. So our request will stay in the socket.&nbsp; Now the resolver send the object ID back. The&nbsp;&nbsp; response handler is a callback registered by&nbsp; the first request will handle the response. &nbsp; The handler finds all the object has already been&nbsp; marked as soft delete. So it won't update the&nbsp;&nbsp; object ID. Instead, it will post another request&nbsp; to the resolver to delete that object. The request&nbsp;&nbsp; will be processed in the third loop of the&nbsp; resolver after the request of creating the DNS&nbsp;&nbsp; remote object for the second RA. In the second&nbsp; loop of the resolver it processes the creation&nbsp;&nbsp; request with the DNS address from the second RA&nbsp; and the response with object ID. Now time's up.&nbsp;&nbsp; The timer of the resolver think a group of 18&nbsp; operations should have been done and it's time&nbsp;&nbsp; to notify everyone what DNS addresses it&nbsp; has. And because the handler one of radvd&nbsp;&nbsp; subscribes to the handler two of the resolvers,&nbsp; he received the DNS addresses in the Nova message,&nbsp;&nbsp; then store them as a tree and use them to&nbsp; construct the RA for LAN in the RA routine. &nbsp; At that moment the result of two&nbsp; requests is missing. That's why&nbsp;&nbsp; our first version of exploit can only work on&nbsp; MacOS because we trigger the race condition.&nbsp;&nbsp; It seems hard to achieve a race condition&nbsp; because there is a timer but the mechanism is&nbsp;&nbsp; actually more complex than I just explained. So&nbsp; the rest would always happen as long as the two&nbsp;&nbsp; RAs arrive at the router in the short period. The pattern of the race condition in the remote&nbsp;&nbsp; object is as follows, use non-blocking method&nbsp; to create delete remote object and the target&nbsp;&nbsp; process subscribes to the remote object. Then&nbsp; we can mix the result of two requests into one.&nbsp;&nbsp; They make us bypass some checks in the binary.&nbsp; We tried to find other vulnerability with this&nbsp;&nbsp; pattern so that we could register the&nbsp; LAN category of the router in Pwn2Own.&nbsp;&nbsp; But we ran out of time and did not manage it to&nbsp; find other vulnerability to explore the LAN side&nbsp;&nbsp; of RouterOS. But at least we are the first team&nbsp; to explore the SOHO Smashup category successfully&nbsp;&nbsp; and also won the title of Master of Pawn. Okay. In summary, MikroTik reimplements everything&nbsp;&nbsp; with its own designed IPC. The business logic is&nbsp; scattered all over. We have to have an abstract&nbsp;&nbsp; concept of Nova binary to find the most critical&nbsp; code to reversing. And there is a pre-auth RCE&nbsp;&nbsp; on WAN that has existed for nine years. Also,&nbsp; we introduced a pattern of race condition in&nbsp;&nbsp; remote object due to non-blocking method to&nbsp; you. And the last thing, we'll drop tools we&nbsp;&nbsp; use in the Pwn2Own event to ease the reversing of&nbsp; RouterOS at GitHub. That's it. Any question?
