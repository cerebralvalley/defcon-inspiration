So, we have a uh pre-recorded video pre-recording from the speakers and we're going to get started right now. Thanks. Thanks. Thanks. Thanks. >> Hello everyone. Thanks for coming and I'm really honored and excited to be here to give a presentation. So today my topic is safe harbor or house of waters. I'm building the hidden parts of the to engine. engine. So here let me briefly introduce ourselves and we are all security engineers from Alaba cloud and I'm Gandjo and I completed this work together with my colleague Lson and we were really looking forward to presenting in person but unfortunately due to visa application issue we are unable to attend the invite on site and we sincerely apologize for this and as a result we have to share our presentations through a pre-recorded video instead and if you have any questions please feel free to contain us. us. So here this is the agenda. So first is the introduction section and after that we will share where the story begin and then I willize how it only works and introduce to script engine and finally I will share the impact of this vulnerability and share some defense recommendations and summary. So first introduction and pytor is a machine learning framework based on torch machine learning library and it was developed by Facebook and now it is an open-source project and it is very easy to use and has some really great features features and ptor is widely used in the field of deep learning research natural language processing or cord and computer vision and but to be honest in the field of machine learning there are many many popular frameworks and the well-known projects like tensflow and kuras and but however according to the paper with colite and you can see that pytorch has undoubtedly been the most popular machine learning framework. So if someone can find some bugs in such a famous AF framework and that will be a really cool thing and next come to the second part where it all begin and in the early days PyTorch use Pico to load and save model for convenience. However, Pico is one of the most well known security issues in Python and in fact the official Python document was that using Pico can be unsafe especially when ny and trusted data. So in 2021 and someone raised an exception on GitHub and called for a solution to address the PC issue in Py torch and finally in 2022 Pytor released a notable update introducing Wing parameter and let's briefly analyze the implementation of Winging as you can see that by default Winging is set to false and it will use PL to load the model and if we set B equal true and you can see that it will use something called V only and picola and you can consider it as a restricted ple. So now let's try it out. So first since by default the wing is forced and we will use ple to load the model. So here we can easily create create a evil model and use this reduce magic method and you can see that after we save the model and we load this model and you can see that we successfully execute the system command and we get the output route route and however if we set equal true and you can see that it jumps through an exception and it seems that the vulnerability is effectively mitigate And also on the official PTO website and you can see that they consider to load with only code true is also secure and they also warn that loading and trusted checkpoint with only force must never be done and additionally when similar security issues are discovered in other PyTorch based components and they often fix it by using equal true and here this an example. So initially VM use to the low with always equal true and which introduce a security risk and they fix it by simply add the honing equal true parameter parameter and so not only VM and in general whenever a software is found to be using to load without only equal true and the fix is also the same way just adding the parameter but here comes the question can we blend in trust and security of owning So next part we will d deeper into it how vony actually works and earlier we point out that vhing is something like a restricted ple. So before we analize how is implemented and we need to understand how ple works and pico is something like an interpreter and it reads and interpreters each op code in sequence and executing the corresponding operation. So for example here we first encounter the global op code and it will use use the load global function and you can see that it will call fun class and it will import this model and so here after we execute this command and you can see that it will get the system function and push it onto the stack stack and since now we get the function and we need to get the arguments and so here we can use the uni code of code to read the string and so Here we will get the ami and since arguments must be in turbo format and we need to wrap this ring into a turbo. So we use this turbo one op code and you can see that it will replace it with the turbo and so here after execute the load turbo and you can see that we wrap this string into a turbo and now we get the function and its arguments. So now we can use the reduce op code to execute this function. So here we use the reduce and it will execute the node reduce function and you can see that it will get the arguments get the function and call it. So finally we can get a command execution. So how does this holding address this issue and through the above example and we can roughly sense that the most dangerous part of the ple dialization is load global and load reduce. So the von and pod just add some restriction to the dangerous op code. Looking at the code, you can see that the local global function has some black list and white list list and also load reduce in a similar way and it has a white list and so in general withholding is implemented using a white list and blacklist. So how can they bypass and a common approach is to analyze the white list and check whether there are any potential dangerous function that could be exploited and I did the same but I didn't find anything useful in the end and we are also limited by fixed ways of thinking. So for example here in this case most of us may only see the obvious white list and blacklist mechanism and stop here. But however in reality the loading process of to load with is honing equal true is quite complex. So is it possible that there might be some issues elsewhere in the loading process. So I try to conduct a complete analise of the withholding workflow to ensure nothing is missed. So I started analyze entire to download process and I steing stabbing to the code and debugging the code line by line. And during one of my exercise I noted something very strange and tortog download. So what is that? And I had no idea this. So I just search it on the Google and the document that says that torchg download.load is something to use to load models in the tocript format. So now you can understand that why we name this topic. Yes, we will analyze and exploit the to script engine. Hello everyone. And this part will cover the basics of touchcript and its code implementation. implementation. Touch script is used for deploy model in environments without passing interpreter such as CPP and mobile. It can be compiled from passing code through scraping or tracing and save as PD field which can be load and run in CPP and mobile. The save and load process involve serialization. I will explain how Python code is compared to touch script the serialization and des serialization process the structure of PD fields and finally how it executes. Fourth we can use touch JT decorator to subcribe the one Python function to touch. The steps to compare are uh first press source code into a Python. Second, convert the py to a g. Third, transform the j into an air graph. graph. The air graph initially contains many unnecessary nodes which are eliminated through opt optimization to make it more more concerned. more concerned. Let's take a deep look about the compare process. Before we start I need to show what is function and model in this context. You can see that one is a function uh the other is a class. The scraping process for these two are somewhat different. So we will introduce them separately. them separately. Let's look at the function first. Uh the compare process is handled within the script msh function. There are two main functions get JT dev and JTs compare. The get JD defe function performs two main tasks. First it use the as library to convert a passing code into a passing a and second it calls build dep to transform that pass into a surf implemented gst. implemented gst. JIT secret compel is a function bond from CBP to pass. Uh with it implementation in the CP layer, it further converts the GT2 AR within the CPB code. CPB code. Uh it eventually invokes uh the two function which finish the transformation from JT to AR. Additionally to IR also cause some optimization functions to simplify the AR graph. Now the function compare process is done. Uh a high level understanding of the process is enough for this sharing. If you are interested in the compare source code details you can use the cost stack I have provided to navigate through the s code. Then let's look at the model. Uh the overall process is similar to that of functions. Expect that there is an additional step at the beginning retrieving all functions from the model. It need retrieve functions and compare functions by one. Uh the stop fction is responsible for handling this. Stops f is analyzed for infer method to compel in this connect. Uh let's see what the infer methods to compare function does. Uh in in the infer methods method to compare function is force use d to get all attributes and methods of the model. After fing it uh saves the methods that need to be exported uh thereby collecting all functions in the model that require completion. completion. Uh mixed sub for method converts the collected functions into gtxt using the get jity dev function which is the same when used during function comp complet compilation. compilation. Uh back to uh back to create a secret model. The method stops variable stores the titst we just converted and the create a methods and properties from stops function takes to measured the stops as input and is resp is responsible for converting it to uh we can see that uh this cost stack is similar to the cost stack for function compilation. compilation. uh both eventually finish the conversion into IR function. Now the model completion completion now the model compare process is done. Overall some key steps are the same as for function. Both use the get gab to convert a pattern to gd then convert it to I to the serialization future can save models and deploy them to other environments. Use touch save to save model as a as a pdf view. Use touch load to load a pdf as a model. The passing code here is converted from IR. You will notice is slightly different than the original code. code. Let's see how touch how subri function model is serialized. Uh subscribed function has a additional step compared to sub model. It needs to be converted to secret model first after that both call serialized for serialization. serialization. uh during the serialization process some information is stored in the data pair and the constant PR in pico format. Additionally, it saves the passing code converted from AR and the debug info to the code folder. Uh to summarize the save process for touch function for converted to touch model. uh the remaining process is the same. Uh first uh the value corresponding to the model is serialized in pico format as a data p. Second optin code and debug info via pass print and write to code directory. Uh third uh save tensor constants to constance pkr. Now the serialization is done. Uh let's look at the detail of the PT field. Uh the PT field is compressed achieved using uh zip compression. Uh the field tree after decompression is show above and we can use ple tools to dis de disassemble it. Above is the deassemse output of data p. The meaning of this ple code is create an object named the touch placeholder model and set it training attribute to true and the touch placeholder model object is a secret model. So when interpreting data PKR subscribed model object is created the constants PKR contains ple code which can create a value object with tensor the touch py field contains python code strings is the same as the content of the code attribute in circ model because they are obtained in the same way uh Okay, now we understand the three most important parts saved in the serialized field. Uh data be care and the source code in the code directory. Uh let's see how these parts are restored into a secret model. We can see that in the des serialized function the three main parts of the of the serialized field are converted into a subscribed model. Uh let's look at the function implementation. function implementation. This serializer will call reader achieve to read the constants and the data. Then we'll get one model in CPP which is secret model in python. Uh during the deserialization process first I'm picking constants be to obtain tensor constants then saved in constants table. Uh second I'm pickling datab during the process of pickling datab uh uh relevant source code is read from the code directory and press as needed and the related constants are also read from the constants table. The coder pressing process includes converting Python code to custom code tokens and then transforming tokens into gitst and finally into IR. This process differs uh significantly from the earlier pass to touch completion. To summarize the load process, uh first reach main logic via d serial des serialize. Uh second call read achieve to read the constants pkr convert constants to n values by mping and save them to constants table. Third call reader chip to read the datab restore corresponding values by amity. Fourth during uh during datab uh source importer reads code fields and constants table to restore through press tab to find named tab to import named the type. the type. Now the deser d serialization is done. Uh let's uh let's take a look at how touch circuit is running. The air graph con uh contains of many nodes uh and every nodes contain three parts y output and op code. OB code is the key to execution. Uh the process of executing tre is iter uh is to iterator through the AI and convert it into instructions. The interpreter the instructions. Every instructions has one op code. There is a switch table here that finds the corresponding process uh process logic through the option and the execute set. set. Now we have a basic understanding of the uh of the tc scrap workflow. We reading the code I discovered something interesting. There is one OB code names OP and it can do a call on operator table and operator table contain many functions corresponding to operators. I'm curious about what operators they are. Uh some built-in functions register themselves as operators requiring op instructions to call corresponding functions and the radius operators class managers the operation operators and register them by radius operator function. I page the pyouch code and add one function to output all operators. You can see there are over 2,000 operators here. All these operators save. Uh during our code audit of these operators, we discovered two interesting operators operators uh save and from field. Here we can see the corresponding implements of these two functions. The save function will receive specif value as a model to specify the field. Uh this can lead to this can lead to arbitrary field rest. Uh but the save the field contains a lot of dirty data which model information. The from field function will read the uh field which I specified the uh and convert the field container to tensor then return the tensor. Uh we can get the fuel content from the tensor. tensor. Now we know these two operators have vulnerabilities but there is still a problem. How to call them from touch script? To get the answer we need to first understand the operator registering process. The get built-in table function registers some object member functions to builtin ops and during this process it also adds prefix to the member functions for rename. Uh finally the address of these uh functions and their modified names will be placed into built-in table uh during the touch script completion process to sugar value will be triggered uh where it checks whether the Python object exist in built-in table. If they if it exists it return a built-in function. function. Uh finally this built-in function will be inserted into the air graph through emit built-in call. Uh back to the built-in registering process. We now understand that save may come come from one of these objects in models containing buins. By testing each object each object individually uh for the princess of for the prince of save function we will find that the save function exists in the touch object item from field can also be obtained in the same way. same way. So we just need to call touch safe or touch from field to get arbitrary field read write ability in touch script. Uh now we can try writing a field to get but since there are some dirty charact characters only specif uh special fields will work. the Ubuntu contact field because of permission mismatch. Our secret use uh touch save which red field with uh 644 permissions but the column diamond requires 600 permission. Here is one poke video. Okay, you can see we get and the vulnerabilities we have discussed so far might be more similar to logic vulnerabilities. Uh on the other hand, some hackers may prefer memory related vulnerabilities. We have also discovered a few overflow issues. issues. Here you can see that uh uh when we load this model PT field, it triggers a hip overflow. As a result, we can observe that hip address and the loaded address of the library are leaks. Uh, this is one poke video. Okay, you can see we get as by memory bug, bug, but due to time limited, we won't go into them in greater detail here. Uh we hope to share them in the future. Uh this is the full details of this CVE. Uh the fix is also relative simple. It prevents the use of touch corruptor when west only is enabled. So what is the impact of this vulnerability? vulnerability? The entire air ecosystem is just like this house and ptor is one of the fundamental components and if there is an issue within PTO just like we have removed one of the bottom level components and the entire ecosystem is then compromised and we did a simple search on GitHub for project using goal true and we can see that in fact many projects using it in this way and here we select two well-known projects as an example to illustrate the impact of this vulnerability and one is VM and the other is transformance and VLM is a high performance library for efficient large language model inference and optimized for speed and memory usage and so why did I choose it and because it had a C number and you can see that it used post load function and resing parameter is default value force so it has an RC vulnerability and the fix way is very simple just adding parameter and this seems to solve the problem. But because of the PyTorch issue, all mitigation ends up being ineffectively and the SE hub you want solo waters and at the same time I found a very interesting phenomenon and you can see that when I check it requirements CPU txt and you can see that it just hardcoded the torch version and also in the GPU version and you can see that it also hardcoded the torch version. So what does this mean? It means that even if pyology released a new patch version and users of VM cannot update timely because VM has hardcoded the version. So as a result the users will remain exposed to this vulnerability for a long period period and so now let's set up the environment since the latest VM has already patched this issue. So we need need to install old version and exactly 0.73 and you can see that when you install VM and it will install the exact version of torch torch and so here we will use this function as an example to illustrate the existence of the vulnerability and at first I didn't think too much about that I just want to take the poke from the previous vulnerability and just hoping for one should succeed and you can see And finally we will definitely enter the logic we want and it will load our evil model. So we will proceed but field and you can see that we don't create this field. So why and let's take a look at the previous pen. You can see that after we use post to load this model and we need to call the model to invoke the model and in order to trigger this forward logic. But here you can see after we load this evil model and it didn't invoke the model, it just fetch the items and then delete the model. So this model was not invoked and uh is it really end? And at that point I noticed the error message and it says that the model don't have the attribute at this error message give me some inspiration and atoms is essentially a keyword in Python and it used to iterate the dictionary type object. So you can see that in fact items is a function. So here can we spoof the function name and so here I just try for the first time and you can see that I change the function name from forward to item and just run the code and you can see that why it still see that our model don't have the it atoms attribute and but if we write it in this way and you can see that we just call the items function in our forward logic and we run a code and you can see that we successfully create a field And this means we succeed. And this is very strange. And the reason is that when retrieving models functions, it determines whether these functions need to be exported for functions not decorated as exports and a lazy export strategy is adopted meaning that they are only exported if called within the exported function. So here you can see that in the left picture we just decorated our items function with exported. So here it will export the item function and we can get this H attribute and in the right picture and you can see that since we call it in our exported function forward and our object we also have this atoms function and so finally we report vulnerability to the VM official team and who highly acknowledge our work and you can see that it is very interesting since PyTorch document claims it is is and the patch is very simple just change this version version and next transformers transformers is a popular opensource library by hugging face and provided pre-trained models and tools for natural language processing task use transformer based architecture and transformers also implementing some security hardening measures for example here you can see that it just used with only equal true to load the And for so now we set our environment and you can see that since the latest version of transformers also fix this issue and we need to install our old version 4.51.3 version 4.51.3 and here this is the official document example from transformers and you can see that we need to load the model from our packing phase and create a pipeline and then we can create our conversation. So here for example we just ask a question the secret to baking a really good cake is and you can see that just give me some answer and here this is my demo project and you can see that we only have three field uh readme config JSON and a test pytor field and don't contain any evil code and if we run the code and you can see that it will use to load to load our remote pytor model and let Let me introduce this code and when lo when we load a remote hugging face repo and it will load this model and by default fields in safe tensors or pettor torch is allowed is allowed and just like the previous VRM vulnerability and it will call the keys function. So so we can just change the function from items to keys and we save the project in our local report. And of course if we run this code and you can see that we we can exploit and we can create this field and after that we report this vulnerability to hugging face and you can see that it just patch it and the patch is also very simple. You can see that check torch load is safe. It check the torch version greater or equals 2.6 and finally is defense and summary. So now if you are still use old version and pytor please update and here is also some recommendations. So for example for from the model format perspective we should use more secure format like safe tensors and also from the model community perspective and we should scan and flag malicious models and hugging face has already done the relevant work and also from the user perspective we should not load untrusted models and it is better to load them in the sandbox. the sandbox. So any questions and thanks again for listening and if you have any questions please feel free to contact us at any time.
