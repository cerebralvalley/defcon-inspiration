hi i'm alexander a security researcher at your secure and today i'm with my colleague olivier to showcase our rdp interception tool pyrdp pyrodp is a tool that can be used to perform real-time rdp interception which were designed to support both honeypots for malware research and offensive capabilities offensive capabilities for intrusion test engagement today we'll be highlighting core features for both use cases in this section i'm going to introduce you to prior dp's core feature set which can be used in both offensive and defensive contexts defensive contexts in the environment for this demo we have the pyrdp system deployed in front of a windows server 2016 system prior dp is running on a linux system and deployed using our docker image the client will be a windows 10 system running microsoft terminal services also known as mstsc in its default mode of operation prior dp will collect a lot of information but only output significant events in its logs its logs the player component which has more granular information granular information will be demoed later let's take a look at the default flags to use prior dp in this example we run it via docker and we map a volume here to make sure that we have access to the artifacts generated with pi rdp after our demo after the interactions the dash p here is to give the host access to the port so rdp being on 3389 this will ensure that any connection to 389 on the host is redirected to the docker image this is the docker image name name of the latest pi rdp the monster in the middle executable that is used here used here and this is the target server so whenever whenever something connects on 3389 on this system system it will then forward the connection to this ip address and do its monster in the middle capabilities let's start the the rdp man in the middle you can see that ssl information or private key and certificate are generated upon the start because we didn't give them in the docker image docker image and the volume so they don't exist and they are created on the fly this should probably be customized for most users now let's take a look at what we have if a user connects and plays around i'm going to start the microsoft client here you can see the certificate error that is being generated the the priority p system by default will create a certificate for hostname called www.example.com called www.example.com this is probably not the best you should generate a more certificate more adapted to the target or the user that you're trying to fool we provide a tool called pi rdp clone cert cert to do that clonecert will connect to a specific server specific server and copy the information from the certificate in a certificate key pair that you can then use with pyrdp this is the reason why we built this tool is to train user to avoid clicking through certificate certificate since if there is an attack tool that is efficient efficient then user will be enticed to be more careful careful just like what firesheep does did with the the ssl attacks it changes the mentality of a lot of people and made tls enabled by default everywhere everywhere well with pi rdp we hope that system and administrator will be more careful about those certificate errors and deploy pki internally which is very rarely the case in our engagements that we see so we're going to click on yes now we can see several interesting things things happened in the on the log section of the screen the screen you can see that the client random is logged logged which is very interesting if you ever take packet capture you can then decrypt and do very granular protocol level analysis of the rdp connection on the the system we get a password prompt prompt which is different than the usual password prompt uh because we downgrade the authentication and it's now interactive it's now part of the the session so i'm gonna write the password made a little mistake here okay i'm in so there's a secret file on the desktop but you can see in the logs that channel information is logged username since it was not typed and not sent by the client is not there you can see that clipboard data was instantly intercepted instantly intercepted and you can see that there is credential candidates from heuristic here scary demo gods this is being done with a heuristic since it doesn't happen at the protocol level level we are interactively entering credentials credentials inside the started rdp sessions that's because we are downgrading the client's request request for nla since nla will resist two monster in the middle attacks this attack resistance is rather complex and has been partially circumvented in specific cases which we will cover in the honeypot section later by default the clipboard content is intercepted intercepted and we can see it we'll demo it right now now so if we open this many secret file and we put it in the clipboard you can see right away away instantly that it was intercepted on the server side on in the logs what's interesting that you might not have noticed here is that um i didn't interact with the the mstsc client when the the output showed in the logs so let me just give you an example to highlight it again so i copied in notepad notepad not even touching the mstsc client screen screen however the information was sent to the the the server so the clipboard content is being is being very shared very enthusiastically with mstsc mstsc and mo and also this is what happens with most with most rdp clients let's copy a few more time just to see you should be aware that this goes beyond this beyond this so this windows machine is in the virtualbox virtualbox system running the mstsc client uh the virtualbox will also notify the the guest windows 10 of clipboard events that happen on my host so this is very interesting because for example if i would paste a log line here on my clipboard what we would expect is expect is the information to flow from my host to my guest my guest from the guest os clipboard to the mstsc client and then to the intercepted system and the expected happened you can see that the clipboard data is visible here visible here and it's it happened pretty fast so this is a very interesting capability that will that will probably allow some information to be leaked if we ever intercept bad actors or in a penetration testing context testing context you can gather a lot of information if an administrator leaves an rdp window open in the background for a long time you'll be able to get clipboard content clipboard content with most likely sensitive information including including passwords if the user uses a password manager like keepass the what happens here is that pirrdp just receives a message about new clipboard content and will send to the client a message saying i need it need it the content is then never forwarded to the server the server so it's just standard operation there's no update of the clipboard on the server side so next up in the demo is the file collector collector rdp allows clients to share a drive upon connection let's configure it so let's disconnect we go in local resources and then we can share whole map drives that's interesting because the share is not at a very granular level so this gives a lot of opportunity for attacker a new warning is is given to the user saying that it's potentially dangerous and it can harm the computer so that's very interesting interesting and it's new it was not there on older mstsc mstsc clients same certificate error let's take a look at the file share in action so we can see that the the c drive on engineering 13 which is my client my client is visible on the on through the rdp session session what happens if we click on it is we this we get access to the information on it it and we see at the same time information being collected being collected by the pirdp system if we open the secret.txt file on the root of the c drive we also notice that the the information was saved by prior dp prior dp in the output folder to finalize this part of the demo we're gonna stop prior dp and look at the artifacts generated the pi rdp output folder has several items in it replays which are recordings of the exact sessions including keystrokes and screen capture which can be replayed later there is there are logs and there are files if we look at the logs we can see that the ssl log is also here available here so this is very useful useful again if you do packet capture in order to decrypt them with wireshark or other tools there is a json logs which will be covered later covered later and there is amanda middle log which is the exact the exact output that was available from inside the that you could see on the command line and if we look at the files then you can see all the files that were collected by the system so we can take a look at the the secret file that we saw earlier loaded from the c drive is here with the content the mapping of the files to the events is available through the log files this concludes the prior dp demo of the monster in the middle component if you need fine-grained content like keystrokes keystrokes and see the screen during an active attack you need to combine the use of the pi rdp mitm tool with the pyrdp player let's launch both tools right now here i'm bringing up the previous command that you use to run just the monster in the middle component and now i'm going to add dash i and an ip address to this execution execution the dash i flags tells pyrdp to send recorded rdp events to a prior dp player over the network this allows a player that is decoupled from the attacker's machine like the setup we have here now we are going to launch the player on my local system you can see that it is a qt interface and that it has tabs for live connections and replays so live activity activity and replay or recorded previously recorded activities let's connect the client client to the monster in the middle and see what the player sees in order to for the demonstration to be complete we'll complete we'll ensure that we have a map drive here so the c drive is shared upon an rdp connection then let's connect you can see that there is some activity already happening in the player interface now you see that a tab open called engineering 13 which is the host name that is sent by the client the screen is visible including the pointer position pointer position which is hard to see right now but if we see here with the yellow dot now if we enter the credential you can see on the below interface exactly the key strokes uh being inputted including special characters like space and shift or control so this is very interesting and important important to have an actual an accurate representation representation of the connection which could be used in many contexts many contexts and you can see that you have also a pixel perfect pixel perfect representation of the interactions this can really demonstrate the impact of an attack on rdp to executives for instance additionally if the client mapped a drive you can explore his drive without his knowledge so we can browse here and um we can double we can right click and download a file on on our local system part demo download this here you can also browse and explore the user's user's system file system so here for example we are loading into dave's files and looking at his documents and we can download the many secrets files again on our system rdp demo here we go this is a pretty interesting demonstration of the dangers of mapping a drive you basically allow the server to query anything using file i o on your map drive normally normally that is fine as you can but as you can see see with pyrdp it can be abused and it could also be abused with a malicious rdp server another feature of the player is the take control button that you can see right here right here this will demonstrated later will be demonstrated later by alex in the offensive section of the demo lastly is the ability to replay previous captures this is called a replay in prior dp's context with the player i'm gonna demonstrate to you by loading a previous capture for our previous demo so this is susan's interactions so what we see right now is um the state initial state you can see that you can change the speed play and once you play you do have the clipboard clipboard events another clipboard but the keyboard and clipboard events at the bottom of the screen you can also seek seek in the video we see the clipboard data also here um you can scale the window so that it's easier to see globally especially with 4k environments and you can increase the speed of the of the replay so this is very good if you're hunting for a specific session for specific interaction this concludes our demonstration of the player's features pirrdp also comes with a converter tool that can be used to convert pre-recorded replay files replay files into videos the converter tool also accept pcaps and can output to replay files replay files let's take a look at its option here we will convert a previously recorded session into a video that we will play will play priority p convert mp4 as the format of output the input file rdp replay and let's have a default output file now let's look at the resulting file in this section we will describe pi rdps feature that are useful in the context of running honeypots part of pi rdp's honeypot features is a headless mode headless mode this enables server-side deployments running it running it on non-x86 systems like a raspberry pi and allowed us to significantly reduce the size of our docker image for example this is a cloud system where we want to deploy pi rdp and we are going to run the headless image this is the command line from pi rdp this pulls in the latest uh master branch branch image and we are going to use the slim tag tag so that we are going to use the smaller version of our image without the graphical interface features and the con video conversion features now let's see how long it takes the new image is 70 megabytes which is pretty good for a python docker image with native code down from 600 megabytes for our latest release and you can see that the source service is running is running right now headless mode also means that we needed a headless player otherwise keystroke data is unavailable for command line enthusiasts let's see the headless player in action we can remove the port it's no longer required for the player but we still need the volume mount if we look at the options we can see a dash dash headless at the bottom so usually the player is the graphical interface with the qt but the new headless feature allow us to look at replay files without the gui we're gonna look at the lena file the the dump is one shot deal you get all of the output at once but uh you can go back and see everything everything that was collected including clicks and stuff that was uh sent in the connect during the connection so as is common with the beginning of rdp sessions rdp sessions you see a lot of control character released or and pressed in order to reset everything then you can see that i entered credentials so so demo so the the password is scary demo gods but you uh and i made a mistake here so this is a lowercase o lowercase o um you can see also the backspace that that are pressed so i started with demo instead of scary and so d e backspace backspace scary but then i made a mistake so i authenticated again authenticated again um you can see that as soon as i was authenticated authenticated the clipboard was shared and this was my notes was sent to the server then you can see some clicks and then during that demo i pasted several secrets to show how easy it was to leak data to the server system and easy for our pi rdp to correct and so uh and then i disconnected which ends the the stream so with the headless player you have access to all of this uh from the command line instead of having to load the graphical interface another feature specific to honeypot use cases is credential replacement this allows your honeypot to accept any credentials entered by the client and relay to the server the correct set of credentials for login for login for instance although the user for this vm is pi rdp i will be able to log in using admin and a garbage password so let's do the demonstration for this i'm going to go back to using the man in the middle as you can see below below i'm gonna input the correct credentials for the vm and start the man in the middle now using the ms tsc client i'm going to put a different username here admin lulz to make sure that it doesn't exist exist and i'm gonna attempt to i'm gonna try to connect to the honeypot i have the warning because i have a map drive and i have an error let's try again okay transient error so we have the certificate error and we got connected and if you look at the log the log you can see that in the client info the admin loss was supplied pi rdp has the ability to intercept files and transit and to allow client file system exploration from the interactive player last year these capabilities were extended to allow active file querying from the monster in the middle component when a client connects connects pi rdp will list mapped drives and download all the files which match a specific list of allowed files patterns patterns let's see this in action so we issue the same mitm command as during normal operation but we add the crawl flag let's run it now we connect from the windows system and its files using a map drive and its files will be explored by the man in the middle component let's connect so we have the certificate error yes now what we see here oh we need to authenticate before the file file system is exposed to the server by the client we identicated and now what we see here is a begin crawling disk c saving secret to pi rdp output files cloudia secret.txt so this file was at at the root of the c drive and so this is why it was the first to be collected and it was quite fast you'll also be able to notice that the structure uh the directory structure is respected respected so as the crawling happens in the background background the files are gonna gonna be recreated locally on the pi rdp system oh we can see it starting now so it matched a directory in the user's folder and documents stuff.ssh and then stuff passwords that keep pass database a flag and so on and you'll see also the temporary files and executable under the user directory are going to be saved by default so it can take a while to gather all these files based on network conditions the list of files to collect or to ignore ignore can be tweaked on the command line so with this feature we hope that we'll be able to gather very interesting information information especially on honeypot system deployed in the wild pi rdp can also be used as an offensive security tool to regain a foothold or pivot within a network let's look at some attack techniques session hijacking can be used to take over an intercepted rdp connection first irdp mitm is launched with the target server and the player address to connect to connect to in this case localhost next the player is launched and waits for intercepted sessions the player can handle multiple sessions at the same time and will automatically display the live feed of the connected session clients who connect to the malicious server will receive a certificate prompt the certificate can be customized to spoof the common name of the server the likelihood of the certificate being accepted is considerably high since most companies do not deploy fully signed private key infrastructures for rdp rdp after a successful connection is intercepted the player can be used to monitor the screen and input of the session when the attacker clicks the take control button prior dp takes over the session and redirects all player inputs to the server and all server output to the player the real rdp client appears frozen during the hijack once the attacker is done the session is returned to the client which unfreezes this technique is noisy but looks like a network error to the untrained eye after the real client recovers control there may be artifacts on the screen because the server and client will be out of sync in order to avoid that the screen should be left as closely as possible to its original arrangement when control is released hiring a specific server does not scale well during engagements because it will most likely redirect intercepted sessions to a server which isn't the one that the client tried to connect to prioritp supports transparent proxy with the help of a few firewall rules note that this technique only works on linux and requires running pyridp as root to use ip transparent sockets transparent sockets use the firewall rule templates from the documentation directory and modify them to reflect your environment here we use the address range that we will arbsmooth will arbsmooth because we are spoofing the sub that we are in we exclude our own ip address to prevent packet production loops in ip tables tables next create a custom routing table for priority ps intercepted connections table id is a number identifying the table ensure that ip480 is enabled and create a pre-routing rule that will mark all traffic distance to the server range for transparent proxy by priority the last firewall rule is used to mark traffic coming back from the servers to the clients for transparent proxy the last step is to ensure that marked packets use the pyrdp routing table created earlier created earlier which will be responsible for delivering the marked packets to priority b through the loopback interface with the firewall configured make sure to launch fire dps root and provide the transparent switch to enable transparent proxy start vendor cab on your lan facing interface and arms move the subnet to intercept here we set our spoof internal to true because we are spoofing our own subnet this is not required when spoofing an adjacent subnet for the demonstration we are only spoofing one client when the victim attempts to connect to their destination server they will be intercepted by power adp which will then establish a connection with the intended server this technique scales well for internal engagements and wars in combination with other attacks the last technique that we will cover is command injection which automates session hijacking to seamlessly run a payload for an intercepted connection using the same transparent proxy configuration as the previous technique start pyradp with a powershell payload that acts as a stager the stager will query a web server which hosts the real malicious payload and executed in the context of the intercepted session intercepted session the payload delay and payload duration parameters are used to control when the payload should be executed and how long to hijack the session for the malicious payload consists of a simple partial reverse shell for demonstration purposes this shell will connect back to the pi rdp workstation on port 888. start metasploit's multi-handler to catch a reverse shell once it is established established next use python's built-in web server to host the payload that the stager will retrieve make sure to disable exit on session so that multiple sessions can be lastly start arm spoofy to intercept rdp sessions sessions as they are established once a session is intercepted everything will proceed as normal from the client's point of view until the payload delay is reached after the delay has elapsed the client session will appear to freeze while the payload is being delivered and executed executed the stager retrieves the code from the server and executes it causing the reverse shell to connect to the metasploit listener and giving the attacker covert access to the target server even if the client later disconnects from the server the reverse shell persists persists this technique is powerful and can easily result in multiple sessions when using less detectable payloads this concludes our brief tour of prior dp's features dp's features if you'd like to see the full feature set or get the source code make sure to check out the pyrdp github lastly if you have any war stories to share we'd be happy to hear about them thanks for watching
