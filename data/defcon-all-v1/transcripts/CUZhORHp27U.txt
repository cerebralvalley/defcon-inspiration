Hi. Hello everyone. H welcome to the missing link dry new RCS complete the chain. chain. I am Gastonas Narees and my partner here is Octavo. We are security researchers at at Faraday. We mostly focus on firmware and embedded and low-level stuff. Okay. what we are going to see today. First, we are going to do an introduction and a recapitulation about our previous work and what we are doing now. We are we are going to show new vulnerabilities vulnerabilities and with the vulnerabilities that we found now and the old ones we are going to show the full chain exploit from the internet to persistence. We also are going to check the the wave of routers reboots that we saw we saw on March and we are going to give some conclusions and takeaways. Okay, so let's start with the introduction. introduction. Yes, it's not our first time with the routers here in Devcon. Last year we gave a talk about this showing some vulnerabilities and focusing on detecting persistent uh and focusing on persistence on these routers. Quick recapitulation Quick recapitulation uh we have a small business router uh with a MIP 32 CPU and running an RTOS. The name of the RTOS is dry OS, but Drytech also has mediumsiz enterprise routers that has an ARM CPU and they run Linux. But an important thing of this router is that in the Linux they emulate using Kimu the same operating system as the small business routers. So if we could find something in the small business routers, it it should be also in the medium size enterprise. Okay, let's talk about the attack surface. We found over 5,00 exposes device mostly in the UK and we found previous vulnerabilities that affect these routers from in information disclosure to remote code execution and our previous vulnerabilities. Okay, last year we released a tool for helping all the people to start reverse engineering and finding vulnerabilities on these rout routers. The name of this tool is write the arsenal. You will find it in GitHub and now it supports new firmware versions. Okay, vulnerabilities. We found a bunch of things. uh we found a missing SSL certific certificate validation and non-constantine password comparison that let us make a side channel attack. We found secure pass password storage, predictable two factor authentication code generation and the most important three are first the DHCP server ner reference that let us reboot and maybe make the name of service attack and the last two allow us to upload arbitrary modules uh kernel modules to the device. Okay. So what we have now we have to use a password re use or a password default and with the two factor authentication we could authenticate we have authenticated code execution and then we have kernel level persistence but that's not enough. We wanted more more more what we wanted is a pre-authentication vulnerability to gain code execution and then have kernel level persistent and in that way we complete the chain from internet to the persistence. Okay, we ended up our last talk uh saying that there are probably more vulnerabilities in these routers because uh the complexity of this operating system is really high and we only looked a part of it and that was happened. So what got our attention to come back to to to see that that routers was dry break a research presented by the people of forscout and the blackhat Europe 2024 and the most important thing that caught our attention was a a bus buffer overflow in a function with the name of get CGI get CGI so we reverse engineer all the fware finding vulnerabilities brought persistent malware but we didn't look at the CGI request parser and then we learned something about this. We we learned that there is a new vulner vulnerable pattern and a new attack surface. So we come back and start again to see the firmware on this attack surface and we found two more vulnerabilities and now we can complete the attack chain from the internet to the persistence. Okay, let's talk something about the new vulnerities vulnerities and exploitation. and exploitation. The first vulnerity that we found was a buffer overflow in TR69 stand parsing and it's really related to one CV that force code released that CV from force code was a stack westflow when processing query string parameters on the get CGI function. So what does the CGI function or or how it works? Okay, so first is going to have two important arguments. The first one first one is a buffer and the second one is a query string. So uh it will receive that parameters and will parse the string to get the parameters on the on the heap. uh we will see that has a loop for each to to process each argument. Uh as we know the arguments of a query are separated by amp percent. So make word is going to grab the the the current argument and we'll copy on the heap that and then it's going to change the plus sign for spaces and escape the URL and then it's going to f to look for the equal sign because the arguments has a key and a value and in that sign it's going to change change that equal sign for a null character and then it's going to store the the address of the key and the address of the value. We are going to see an example to to understand it better. Okay. So for example, we have on the top right a URL with the arguments and we assume that CGI handler is going to call this get CGI with a buffer in the stack. We could see that at the at the left the color stack and the get CGI stack. So as we said the first thing is going to be that it's going to store the first argument on the heap and then it's going to clean it and find the equal sign and change it for a n pointer. So we have two strings and then it's going to store in the first index of the buffer the key and the in the second index of the buffer the value and it's going to do the same for each argument. But the important things here is that it's not going to check the amount of parame parameters that is going to parse. So eventually if we put enough arguments we are going to uh modify the the return address of of the stack of the function the function and then we will have a remote co execution with the buffer overflow. Okay, that's was how get CGI works. But this is the function that handles connections to the TR69 stand. And we can see that it's really similar to get CGI. So it has the same pattern, the same vulnerable pattern at the vulnerabilities here. We think that the compiler has in line the get CGI function in this one. And for that for for that the force code group couldn't find it. Okay. So we have to enable TR69 stand and the router is going to start listening on a UDP port that port and all the packets received are going to be uh parsed by that function that we saw in a really simple way. Erh, how TR69 works. The rout router is going to send a bind request to the stand server and the stand server is going to answer to the binding response. a funny fact that the router is listening on a non-default port stone port but we as an attacker could send data or queries to the router without having the binding request or binding response. The router is always listening. So we made a proof of concept uh that crashed the router and it can be converted to a remote go execution for following the force code exploitation strategy strategy but we didn't go in that way be because of the restrictions that the this interface has and we found a more interesting bug that now Octavio is going to show you. Hi. So the second bug is an integral overflow in the CGA parsing function. Its relationship with the forcout CV is that it's located in the same get CGI function. And this function is very important because every CGI handler calls it. Every query you do to the router um to a CGI endpoint ends up calling this function. So we decided to take a better look at it. This function starts by um checking the request method from the header of the request and in this case the part we are interested in if is if it is a post request. So when a post request um arrives the router then the content length is extracted from the header and it's used to allocate some memory on the hip to copy the contents of the post request. Yes, the the body of the request. So all this is is done using signed integers. So if we set the content length header to the maximum 32-bit integer positive, then when we when the router calls the maloc and adds one to this, we'll get the uh minimum 32-bit integer uh which is negative. So if we take a look at how the malog works in this router, it's it also uses signed comparisons. So if we send this number then we will get our heap being allocated in the smallest cache and this cache uh it has only 24 bytes. So now we transformed our integral overflow into a hip overflow. We can see here an example. So we set the counter length to this number and then the router doesn't crash but we get a message a log that says slab panic the object checksum there was an error while checking the object checksum and this is because the slab allocator that this router uses has some meta data in its objects. So we are landing in a cache with uh for 25 24 byt objects but these objects are really represented using 32 bytes. After the user data there is the line that made the location the time and the offset but there is also a checkum and this is the metadata that we are correcting with our overrun. When these objects are free or not in use, the first part of the data or where the user would put the data is used to store a pointer to the next free object. And so this this internal structure is really a linked list. And what what is stored is a pointer to the first free block on the list. Yeah. So how can we transform this into an arbitrary right? Suppose we get this first chunk and now some someone makes an allocation. an allocation. We can start to write past our boundaries and eventually we will end up overwriting a pointer to the next free chunk. chunk. So if we made two more allocations then the second one will be an arbitrary address that we can define and we can arbitrary write uh data at this direction. But the downside of this that is that that now we have a broken allocator. allocator. Uh luckily this is not a problem for for exploitation. exploitation. So how can we use this to execute code? Well as Gaston said these routers run MEIPS and in MIPS all exceptions trigger a jump to a general exception handler. This handler will look for the reason of the exception in the cost register co-processor register and it will it will use this value to index a table that has pointers to handlers for each x specific exception code. So what we can do is use this arbitrary write to override an exception handler pointer in this table to some place we control. Then we have to trigger the exception to make the code flow shamp there and we will get an arbitrary code execution. Specifically for attacking this router, we will send a shell code in a request and this will get stored in a low level network reception buffer which is almost every time at a fixed offset in memory. Then we will use the arbitrary write to point the illegal address load exception handler pointer from this table to the buffer that we control at that that should have the the shell code. We will use the last year talk bug the reference that Gon talked about to trigger this exception and then the shell code will be executed in place of the exception handler. handler. So now a proof of concept of of this This is kind of quick. So I will pause it. So now you see all the slap panics. You are overrunning this metadata in the heap. heap. And this attack is just a proof of concept that executes a a payload that only prints success repeatedly. So now we have to chain this with all the previous vulnerabilities to make the full chain exploit. A recap of uh last year talk. The persistence strategy was based on DLMs. DMs are dynamically loadable modules. These modules are kernel modules that provide extra functionality to the real-time operating system and are used for example to implement the SSH demon. There are updated independently from the firmware and this is what makes them persistent and are dynamically loaded every time the router boat boots. So if you modifi modify one of these kernel modules when the router boots it will load them first. they will have priority over the ones that are default that come with the firmware image and we it will prevent the loading. So this module will persist reboots, will persist factory resets and more importantly will persist even after firmware updates because they are stored in a separate partition on the flash. So these modules though are encrypted and are also compressed. We reverse engineered both algorithms and it turned out that the module recryption was used was using a modifi modified version of the XDA algorithm and this is a symmetric encryption scheme. So this means that the keys are present on the firmware. So we can uh encrypt and decrypt modules with that. And we also found out that the module compression is the same as the firmware image which is a modified version of the LC4 algorithm. So we wrote the decryptor encryptor and we also wrote the the compressor compressor. And when we took a DLM and we decrypt it and then the compress it what we got was a relocatable elf. And well this is a very known format. So we could patch it patch it to make a back door SSHD mode for example that got an that allowed us to login with our credentials. So to chain everything we have to send a request including a shell shell code and we will use a shell code that changes the admin password from one from from the default one to one we know. This request will also trigger the heat buffer overflow and will replace the interrupt handler for illegal access instru illegal access to memory with a pointer to this same shell code that we send in the request. Then we will use the null reference to trigger this exception and our shell code will execute. Now with the new password that we have set, we can upload a malicious model and use it to access the router through SSH. So here's the demo. First we show that we cannot login. Now we have successfully changed the password. We have upload uploaded our module and now the router is rebooting. So now we added a a new user to the router with our preferred credentials and this user will coexist with the existing user and the real users of the router won't notice. So now Gaston will talk about how this relates to the router reboots that were seen in the wild at the beginning of this year. Okay. So in early in March we start uh receiving some news and some messages about some routters devices that were rebooting itself without cows. Um it was like all the routters were revoling and we start thinking about this on what what it could be and after after that we read the security advisory of of Rekch and they say this issue appears to be linked to previous disclosure vulnerability from early March and this match with or to vulnerabilities that we show to you today. So we start we start thinking that it possibly it could be or vulnerabilities and we had an an hypothesis uh because a successful exploitations depends on the landing address of the payload and that has to be correct. Uh if it is not correct the the router is going to crash and it's going to reboot. But these offsets vary with the firmware version and the router model. So So uh maybe the the reboots are caused by the file exploitation attempts and if there was failure exploitation attempts there also could be successes on exploitation but the successes are silent so we don't know about it. So we we start investigating we set up four honeypotss in different in different countries where the routters are more popular. We use one physical router and we emulate three others. But we couldn't detect any reboots neither neither exploitation attempts. So we think that we we don't know if we were late or we need more honeybots. But the questions remains open. So is going to continue with some conclusions. Well, some of these bugs, the first ones were fixed by Try from August to October of last year, and the last two ones, Try Techch was very, very quick to fix them were fixed in November of 2024. So, we learned some lessons about this research. The first one is that bugs are like cockroaches, a phrase from Ivan Arce. uh the surface that that um for scout team started to look had some bugs. We look at it, it had some more and there are probably more bugs and well from our research perspective in our initial research we focused on synctosource analysis and this led us to a deep understanding of the inner workings of the router but source to sync approach also favors completeness and exploring all the surface would have led us to found the CCA paring functions for example and this router has a a lot of surface. It has different VPN services and other things. So there will be more bugs here and both strategies are complimentary. So we have learned that and for users always the same. Update your firmware do not unnecessary surface. surface. So there were more vulnerabilities affecting these routers and there will be more. We are sure of it. Uh but we are retiring from DC for now. So now it's up to you to keep looking on for bones and we have this tool to extract the firmware images that you can use to start reverse engineering. And now we are moving into car hacking. So we invite you to check evil dogy which is an open-source modular canvas research and penetration testing tool that we are developing. So that's it. Thank you for your time and if you have any questions just ask.
