Okay, thank you everyone. So, let's start the talk. Um, hi, I'm Suha from Taiwan and this is my first time to present at Defcon and also my first time at Defcon and also my first time at least least and not my side in US. Okay, so thank you all for joining my session. So I'm excited excited to share some practical red team networking techniques with you today. So I guess we can get start. Okay. Okay. So now let me take you into a typical day in my IT life seeing my internet laps server logs. And there's a login from Frank. from Frank. And there's a login from Bob. And then wow, there's a invalid login from a public IP 999.99. And how how and why this this can happen? This is a intranet server without no destination NAT. So, okay, I say it. I'll bend it that bad IP. But a second later, there's another IP attacking my server again. So, how is that possible that someone can use a public IP address to attack our intranet server? This is a key point that we are going to discuss in this talk. This presentation will explore ways of lower level network penetration through virus of internal protocoling. This technique can be used for advanced red team exercise or can be blocked before they are abused by malicious attackers in your cooperate network. Before we dive into the details, let me briefly introduce myself. introduce myself. Hi, I'm Su Hao uh from Taiwan and I now is a red timmer trade researcher at Tich and I graduated from Chinhai University in Taiwan and a former hacker president. I love to play something with networks and it's and network protocols. I start this research for fun and it turned out that it is great importancy to general network security. So I'm sharing my GitHub account here where you can find all the tools in and material of this research. research. So this is today agenda. First I will share some new rating techniques using IP spoofing and how we can use that for initial access. land. I'll reveal the nightmare RVX l including tonal hijacking and how buggy routing protocols can lead to IP hijacking and even domain compromise. And finally, I'll wrap up with some key takeaways before we move on to the Q&A. So first I want to talk about some spoofing source IP in the public network also know as when we all know that even in recent years package spoofing is still possible on public networks. Please note that all the IP address are example IP address not any common public IPs. An attacker with the IP address 2.2.2.2 2 can send a DNS request with a source IP of 3.33.3 that does not belong to itself and 1.1.1 and 1.1.1 will receive the packet and it had no way to verify whether the packet is a spoofed packet or a valid one and it will send a DNS response to the 3.33.3. So the 3.33-3 will receive the DNS response with our making making a request. This is a typical DDOS DNS amplification attack and it still work nowadays. nowadays. Next I want to dive into the company network infrastructure how it will do block a computer from accessing the public network for security purpose. So for the best practice when a packet is sent from a crucial server to a public IP public IP it will arrive to the firewall and the firewall will drop the packet. However, we see some it just disabled the NAT mechanism on the router. So the packet will still forward out to the Y area network area network and the remote server will see and and and receive that packet and see that source IP is not valid. So it will weather drop that package or respond to a unknown address and the client will not receive the message. So when your code is completed a mess but it still work because the client still not receive any response. So the client is not connected to the internet. So if we consider both situation together, what would happen if we spoof the source IP address on the internet? Imagine a rating scenario. The attacker hack a company's device like one three. It can create a tunnel between the C2 server and the compromised device. After that, it can create a DNS request through the terminal which source IP is the attacker public IP which is 99.99. When the packet arrives the compromised device, it will forward the packet to the company intranet router. After the router will receive the packet, it will look up its routing table and forward the package to the second victim. As we know, if there is no fire for the second victim 1.1.2, the response package will send back to the attacker through the public internet. internet. Then the attacker will receive the packet with there is no evidence that the packet is come from 1.3. The entire TCP or UDB string only shows the IP source IP is 99.9 and IP stores 192.168.1.2. So why IR is hard? In typical later movement attacks, the attacker used the IP address for previous compromised machine to target to the next system. This means when the alarm being triggered, an IR team could simply use network log to trace the attacker path from one compromised machine to the previous one. previous one. The IR team could find a reent log on the previous compromised machine and identify the initial assess point and then the IR team will shut down the entire attack chain step by step. However, when spoofing technique are involved, the source IP in the log will not be the previous compromise machine. So the IR team will not be able to find the attacker path. They could only see a public IP is attacking their domain controller. Thus the previous compromised machine will not be caught even if the alarm had has triggered and it can change to another public source IP to continue the attack chain. If the source IP is bended overlooking the attack path, the malicious package source and destination IPs only shows 99.9 and private IP.2. Thus, no one will know the previous compromised machine is.13. On the other hand, the turnout IP is a HTTP traffic with a source IP of 7.77.7. So if the 99.9 is bended, the attacker can simply switch the another public IP and attack the same target or different target. The IR team would need to check every router for layer 2 port locks in order to identify the compromised machine. Also the source MAC address can also be forged at the first hop. So what if the ISP filter package with a private source IP address? We can use H323 NAT pass through to enable a temporary destination NAT. The attacker can send a special packet to 99.99 to 99.99 which is its C2 server. The NAT router will then create a temporary destination NAT rule for a private IP3 port 445 and open to its public IP port 445. This allows attacker to assess private IP 1.3 on port 445 via 999.9. So here's a quick demo. We can see that 241 has a web server at80. Initially the attacker 142 could not access the internet in internet web server via the public IP. After we send create a server and send a H323 packet from the compromised machine, we receive a packet from the victim routers and we receive and we know that the router give me a DNT port to connect to the intranet west server. Also, we can see that the web log source IP is from a public IP rather than a private IP. So let's create a break point for the attack chain. Another method discovered by Jimmy Sai involves spoofing a fake TCP sync packet from the from the compromised device using the next target IP as the source IP address which is 1.3 for now and sending it to the tiger attacker server. The SNAT mechanism can be abused as a destination NAT to connect a internet service. So this is a second quick demo. We can see that the device at 35 has a web server running at 8080. Initially the attacker at 131 could not access the internet web server via the public IP. After we spoof a sync sync package from the compromised device using the next target IP and the pro source and send it to the attacker server. The router will create a source destination NAT but we can abuse this road as a uh destination NAT. NAT. So we can the SNAT mechanism will allows us to connect to the internet west server. Additionally, we can see that the west server log shows the source IP as a public IP rather than the private one. So this also create a break point in the attack trend. self. Next, can we replace the tunnel with uh official VPN? The answer is yes. In some cases, there are many commercial SSLVPN solution that can spoof the source IP from the client. As shown in the diagram, commercial SSLVPN lacks those discussed as cyber sec 2025 may allow list behavior. list behavior. On the other hand, whether open-source VPN solution like WG Guard or OpenVPN are affected depends on their configuration. So where's the initial access? Do we have a chance to do this without an initial foothold in internet? Can we use any existing turn? The answer is yes. On the internet exchange, everyone is on the send layer 2 network. Attacker can set a private network range as the next hub to the router of the company you want to attack. Also the sec the second method we can abuse a 16 kernels such as GRE IP or SIT terminal. But again a good firewall configuration could cause this fail. fail. So in the internet exchange if we compromise a router at an internet exchange or we can just um rent a XP VM to assess a internet exchange. We can force a private subnet as the next hub to the victim company router and then we can create a connection using our owned public IP as the source IP and a private subnet as a destination. The victim router will forward the packet to its intranet via its routing table and the the intranet target will respond to the attacker to its public IP because the package source IP is a public one. public one. On the other hand, we can abuse existing internal like GRE. So first, what is a GRE? GRE is a stateless layer three terminal. This is widely used because it is easy to set up by only setting its protocol GRE public IP and GE interface IP and its routing table which is next hub. Nowadays there are still a lot of company that use GRE like cloudflare matrix transcend and its customer they can choose between IPSC or GRE and IPSC is safer and AWS transit gateway also support Jernal but sadly it's used for internal work networking only also security week reports that AP groups like Typhoon create J internals to collect private from compromised device less a lot of company also use J ernal but we don't know so how does J works when a package want to go through a J ernal the kernel will pack the packet with a J header and send it over the pugly internet at the receiver receiver side the GI packet is unpacked and the inner packet is forward according to its routing table J is status and doesn't provide any encryption which means it's possible for everyone can spoof a G package via public network for example a private IP2 want to send a packet to another site 2.2 to the packet will send to its default gateway and because of the it is a G terminal the gateway router will add a J header to the packet and then send it through the public internet internet after the G packet arrive at 22.2 too. It will ref remove the GI header and forward the inner package according its routing table and then the 2.2.2 to receive the packet and if it want to respondse to the packet the vice versa the packet will send back to its default gateway and then add the GI header and send back to the 1.1.1 router and remove the GI header and send it back to 1.2. how do we find a dry eternal? We can use all techniques. For example, we can search for net floor dashboard like a a photoodo on Google and filter for G traffic. This way we can obtain the IP address on both ends of the tunnel. Other oinking technique can also help too. Also we can use GI spoofing techniques to send to scan for existing G terminal. First we can create a G tunnel use using the command provide above. Then we can craft a G packet with a spoofit source IP address once one not belong to us and send it over the public network to the victim. If the victim has a GI pure config with the GI packet source IP for example 1.1.1.1 it will decapsulate the inner packet and process it according to its routing table and forward to the inner packet. So we can create and send many packet with different source IP address to br the correct victim pier. If the victim does not recognize the source IP as a nom, it will drop the packet. On the other hand, if the source IP matches one of the victim nos, the victim will accept the GI packet and forward the inner packet according to its routing table. For scanning purpose, we set the destination of the inner packet to the victim itself. victim itself. So the victim will immediately reply the inner packet with with an ICMP response and send it back to the attacker. Then we can identify the vict address by address by by its ID finder and sequence. We can encode the information into ICMP at the final and sequence field which together can represent all 250 55 to the power of four possible IPv4 address. We also create a scanner script on GitHub. As you can see, when the J source address matches the remote J peer address, we can get a ICMP reply. Then we can put everything together to get initial access. Let's imagine a scenario. The victim 1.1.1 has a GI tunnel with 2.2.2 two. An attacker can forge a G packet that appear to be sent from 2.2.2. The inner packet is a DNS request sent from the provider IP3.3 to an internal network IP. When the packet arrive, the victim will trust and use the GRE packet because it can claims that to come from 2.2.2.2. Then the victim will unpack the GRE packet and discover that that contains a DNS request which destination is a private network IP. The victim will then forward the packet to its init company intranet based on its routing table. Then when the in internal DNS server received the DNS packet, it will respond and send the reply through the public internet to the attacker server. Thus the attacker can interact with service on the victim internet including loads using the TCP protocol. We have created a demo lab. The architecture is illustrated in the diagram. We have a target router which I pier is 1.1.1 and there's a intranet way server whose public network is.1.2. So the web server is host on 1.2 and the server can assess the public network via sat. On the other hand, the router has a dry eternal and it can directly assess the internet with server and also he has a publicly IP and uh and then we can see that there's a jar eternal the pier is.1.1. So initially the attacker IP address 142 cannot access the private network 1 192.168.1.2 then the attacker can create a spid general via the command shown at above to the victim router and directly send traffic to the private address 1.2. too through the fake tunnel then the attacker can direct access and interact with the in internal west server. Similarly, if the layer 2 terminal like G tab also if we can lick the victim MAC address we can also exploit this in the same way. It is common to use the SNMP protocol to obtain this. For short L TLDDR summary when a company does not config its firewall and use dies on encrypted turn off even it's a legacy configuration an attacker can expound this turn off for an initial asset to the internet. So then we'll reveal a nightmare of default configuration of VXL. So what is a VXLAN? VXLAN is the ST and unencrypted layer 2 terminal. It packs layer 2 Ethernet frame into a layer 4 UDP packet. Each subnet is uniquely identified by a name called VNI. We can configure VXL internal similar to the GRE by giving the remote IP local IP and destination port and VNI. However, this configuration is vulnerable based on the previous configuration. Save about a ST standard VXL pier is easy as usual. How about hijacking a VXL internal? Yeah. Yes. Here's the only difference. The only thing you have to change is your local IP argument. So why does this happen? Does the kernel kernel the Linux kernel doesn't check the source IP for VXL packets? How does it accept VXL packet with a valid VNI import even if the source IP is not configured? After looking at the Linux memo, you will see that this is a feature but not a bug. a bug. But this feature is insecure and it turn on by default. Before reporting, router OS couldn't turn this feature down. Now you can change the setting to off, but it's still on by default. So what's happen when learning is enabled in VXLAN? Normally when a configured pier send a VXL packet, the kernel will added its MAC address to the FDB also known as forwarding database table shown at the bottom on the slide. The next time a packet needed to be sent to the destination MAC address that is list in the FDB table, the kernel pack and send a packet to the remote location using the information from the FDB table. Similarly, when learning more is enabled, any VXL packet with a valid VN port will be added to the FTB table. Also, the rem remote IP could be any IP on the internet. Does an attacker 99.99 can create a VXL packet with a MAC address which is broadcast address FF ff and the Linux kernel will addited this MAC address to the list. Then when the kernel want to send a broadcast package on the VXL interface, it will look up the FTB table and then send it to the attack server which is 99.9. So we know the how VXL work. How do we get the information like we can hijack a VXL? We know our own IP but we don't know the victim's IP victim's IP or VNI or VN VXLAN inner subnet. However, all this information can be obtained by a simple scan. An attacker can discover the victim IP port and VNI by sending numerous packets. packets. So let's focus on how to determinate the VX and inner subnet range. We can gather information by sending a single VX L packet whether where both the source and destination MAC address are set to the broadcast address. Then when a ARP request sent from the VXL interface, they will also send a copy to the attacker. Another method involves sending a neighbor discover protocol packet. When a router OS receive a broadcast neighbor discover protocol NDP message, it will respond to the broadcasting MAC address with its own IP and MAC address. Thus, we can send numerous VXL packet with different VNI and port configuration. Each con contains an inner NDP packet where both the source and destination MAC address are set to When the VNIM port matches the victim configuration, the victim device will add the attacker's IP to its FDB table. len the victim unpack the vxlan package and see that it is a NDP request the victim router reply to the NDP packet and try to send the respond to the bam MAC address it checks to check the FTP table and find like the destination is 99.99 99.99 which is the attacker IP so the router will send a packet to the attacker. Then the attacker has everything to hijack the turtle. We create a scanner. We can discover the victim IP port and VNI by sending numerous packet. VXL has default port 4789 and 8472 and VNI usually smaller than 100. Here's a scanner link. We have also created a lab for VXL scanner demonstration which include a router OS router and a west server. So for demo we can see that the scanner aim to scan the target at 200. The scanner send numerous packet with different default port and different VNIS. Later we can receive an NDP reply with VNI42 and port A472. The inner submit is 10.0.0.1. Then by simplify create we can simplify create a VXL with the information above. We can direct access the VXLAN intranet server. We also scan for insecure VX10 configuration worldwide. Usually VNI using VNI equals one and default ports. We found like more than 900 VXL endpoint respond to the scanner. Additionally, there are 4,000 IPs inside the VXL subnet. Some of this are public IP, which means we can hijack public IPs. Additionally, some endpoint replied the numerous BCAT packet combined with the IP spoofing. This can lead to DDoS attack. attack. Lastly, we see some VXL packets have source IP that are private address. This rais a question. Why are the private address being used as a source IP on the VXL packet? So if I use VXLAN in a incrival I am safe. safe. The answer is no. VX then still accept pack traffic in different interface. different interface. So if you have a public IP interface and also you create a VXL interface, it can be scan and hijack. For LT TLDDR, we can hijack a VXL internal using only three pieces of concept. The V Victton IP address, the VXL port and the VNI. There's no need to know the peer IP and internal IP address. Furthermore, if your network setup including a public IP address and VXA on any interface is highly vulnerable. So what can hacker do after hijacking a VX internal? Not only can attacker gaining initial assets to the company in internet but also they can hijack IP communication or perform a man in the middle attack between the two site. Additionally attacker can target layer 2 network service such as exping RVD to rce vulnerabilities. Incident respond is challenging because the source IP cannot be trust. Moreover, there are turns often these terminals often run routing protocols like BGP or OPF. OPF. Attackers can hijack IPs that are even not transmissioning through that terminal terminal such as those of a domain controller or exxi server. exxi server. So what is a routing protocol? It is a protocol like routing. Router can exchange the routes and network information with other routers. Routing protocols help router to directly learn about the networks around them and to determine the best path for forwarding the packet. For example, router A has a subnet 121 192.168.1.0/24 0/24 and the router C to learn the routes from router B by routing protocols. protocols. We often see company use VX lander to connect connect two site with routing protocols. protocols. But when we combine combine this with the running feature, we can hijack routers a IP. Then we can announce we can connect to the routing protocols and announce domain controllers IP in the routing protocols with SL2. Other routers will receive and trust the routing prefix and redirect the domain controller traffic to the attacker because the route is more sus speific more slow smaller than the land subnet. We have summarized the potential impact of hijacking different service IP. If an attacker hijack the IP of domain controller and until end is possible meaning SMB signing is disable or ADCS EC ESC88 is present they can take over the entire doment if a attacker hijacks the IPvir proxim or any HTTPS service and the original SSL SSL certificate certificate certificate is on site or not violate user may not be noticed and the attacker can then take over the account or the full server. In short, hacking this service IP can lead to account take over deny our service DNS hijack or even for domain compromise or server compromise. And here's a bonus bonus. A bad configuration in the company's OPF lead to IP hijacking. This attack factor has been published for years and we actually observe it during our rating scenario yet very few people discuss it. Do you check and use a TCB dump after you get into a internet? If you see this on the victim internet, it might be vulnerable. Just like the exportation method described in the previous slide. If you see a hello package, there is a highly possible that you can direct establish an OPF connection with the router and hijack the routes. So in shorts we can if we connect to the OPF we can hijack the domain controller again and get previous escalation. Last there are some key takeaways. Here's the key key takeaways for blue team. First check all unencrypted turn in your company's network. If you find any don't use them. This include protocol like GRE, ip SIT, GR tab and VXL. These turn are not secure and can be abused by attackers. Next, make sure you have secure firewall in place. Your firewall should filter outbound in intranet traffic especially as y-ac as y-ac packet with a response from server. Also Also check for IP spoofing in your internet. Ideally, all ISP should not should fill out spoofed IP address, but in real in reality, this is rarely possible. Check if OPF is not enabled on ports between routers. It's only enabled on ports between routers. OP should not open on unnecessary port. Monitor your routing prefix for any anomalies. anomalies. Uncept change in router protocol can indicate an ongoing attack. Finally, step out minimum accept prefix size in your routing protocol. For example, /24. example, /24. In summary, avoid encrypted turnout, secure your firewall, filter for IP spoofing, spoofing, restrict OPF moning routing, and enforce minimum prefix size. And here's the takeaway for writing. First, scan or use all techniques to find victims on encrypted turnons. This can be entry point in into the network. Once you are inside the internet, check the victim's network setup for spoofing. Use source IP sping techniques during high-risk scanning to avoid detection. Look for OPF low message to identify active routing protocols and potential attack path. attack path. Scan for misconfiguration vx tunnel. If you find a vulnerable tunnel, hack it to get a initial assess abuse routing protocols and hack IP for lateral movement and pre privilege escalation. Rating can continue looking for more vulnerable protocol in future. Remember scan, find hacked. And here are some key takeaway for tool makers. makers. First implement an intranet IP spooling command control tools. Develop the automated tools to test possible of IP spoofing within a target internet. Automation save times. Create automation Create automation automated correction mechanism for mismatch between IP destination and source address when in the same TCP session. Some routers still perform SNAT even the packet is the server response. Automate the process of sending H323 package or new TCP packet to trigger a routing mechanism especially for ISP lab filter private IP as a source IP address. address. Develop tools for automation OPF IP hijacking attacks. finally implements a more efficient GI scanner for global scanning similar to what Max Scan does for TCP. for TCP. So, thank you everyone.
